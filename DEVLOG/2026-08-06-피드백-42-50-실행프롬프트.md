# 피드백 42~50 실행 프롬프트 (구현 에이전트 핸드오프용)

아래 프롬프트를 구현 에이전트(Codex·Opus·다른 Claude 세션)에게 그대로 전달한다. PR 단위로 나눠 전달해도 된다(프롬프트의 "이번 회차" 지정만 바꿀 것).

````text
C:\Bflow-BGonly 저장소에서 피드백 수정 작업을 수행해줘.

**유일한 사양 문서: `C:\Bflow-BGonly\DEVLOG\2026-08-06-피드백-42-50-수정계획.md`**

이 문서는 자기완결적이며 독립 리뷰 2패스를 거쳤다. 문서를 처음부터 끝까지 읽고, 명시된 작업 순서대로 구현할 것.
이번 회차: **PR 1 (작업 1~5)** 부터 시작한다. PR 2~4 는 각각 앞 PR 이 main 에 머지된 뒤 별도 회차로 진행한다 (PR 2 는 부록 A-2 라이브 DB 마이그레이션 게이트가 머지 전에 선행돼야 한다).

규칙:
1. 계획서의 **절대 규칙**을 따른다 — 문서에 명시되지 않은 리팩터링·포맷 변경·기능 추가 금지. 수정 범위는 명시된 파일·블록으로 한정. 슬랙 웹훅 URL 코드 변경 금지.
2. 라인 번호가 어긋나면 병기된 **앵커(고유 코드 조각)** 검색으로 위치를 재확정한다. 앵커도 못 찾으면 임의 진행하지 말고 해당 작업을 건너뛰고 마지막에 보고한다.
3. 각 작업의 **before 블록과 실제 코드가 다르면** 그 작업을 중단하고 차이를 보고한다 (스냅샷 이후 코드가 변경된 신호다).
4. 작업당 커밋 1개, 커밋 메시지는 한글로 변경 내용을 명확히. 계획서가 지정한 테스트 갱신·추가(신규 테스트 파일의 package.json 등록 포함)를 코드 수정과 같은 커밋에 포함한다.
5. 각 작업 완료 시 해당 작업의 '검증 방법'을, PR 완료 시 부록 B 전체 검증을 실행하고 결과 수치를 기록한다. 버전은 착수 시점 origin/main 기준 마이너 +1 로 재확인한다(공통 규칙 4절).
6. `DEVLOG/update-notes.json` 최신 항목을 계획서의 비개발자 톤 규칙대로 작성한다.
7. 완료 후 `DEVLOG/2026-08-06-피드백-42-50-구현.md` 를 작성(또는 회차분 추가)한다. 계획서와 실제 코드가 달랐던 지점이 있으면 반드시 기록.
8. 작업 브랜치: 계획서 공통 규칙 4절의 브랜치명(PR 1: `fix/feedback-45-44-46-43-50`). 완료 후 PR 을 생성하되 **머지는 하지 않는다**(한솔 확인 후 진행).

완료 보고에는 작업별 커밋 해시, 테스트 결과 수치(기준선 대비 증감), 건너뛰거나 계획과 달라진 부분을 포함할 것.
````

## 구현 후 체인 (사용자 안내용)

1. 구현 완료·PR 생성 확인
2. `codex-review-loop` 스킬로 PR 리뷰 루프 (명시적 완료 신호까지 재트리거)
3. 한솔 머지 지시 → PR 2 는 머지 **전** 부록 A-2 라이브 DB 마이그레이션 적용
4. `bflow-release-deploy` 스킬로 빌드·배포 (manifest.json 마지막 갱신)
5. PR 1 배포 직후 부록 A-1 (한솔: 슬랙 템플릿 비고 줄 삭제 + Moho 아이콘 교체 + 실기 3종 검증)
6. 배포 타임스탬프를 메모리에 기록 — 다음 피드백 라운드의 상태 대조 기준
