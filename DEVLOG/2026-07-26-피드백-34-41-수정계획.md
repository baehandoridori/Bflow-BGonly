# B flow 피드백 34~41 수정 계획서 (에이전트 실행용) — 37번 제외

> **이 문서는 자기완결적이다.** 원본 피드백 문서(구글 독스)·이전 대화 없이 이 문서만으로 구현을 완료할 수 있도록 작성되었다.
> **작성 기준 스냅샷:** 2026-07-26, `main` HEAD `0978dcf163d2f746df423879fa2f8c4dbec0c121` ([v1.91.1] 리깅 공지 폴더 링크를 실사용 오토핫키 포맷으로 정렬), 작업 트리 미커밋 변경 없음. 본 문서의 모든 라인 번호·코드 인용은 이 스냅샷 기준이며, 인용 코드는 전부 실제 파일과 대조 검증되었다.
> **테스트 기준선 (이 스냅샷, 수정 전):** `npm run typecheck` 통과 / `test:playground` 461 pass · `test:auto-update` 29 pass · `test:entity` 239 pass · `test:notifications` 142 pass · `test:presence` 33 pass · `test:character` 116 pass · `test:ui` 7 pass — 전부 fail 0. 작업 중 실패가 나면 그 작업의 수정이 원인이다.
> **리뷰 이력:** 본 문서는 독립 서브에이전트 검증 2패스를 거쳤다 — ① 사실 검증(before 인용 22건 글자 단위 대조, 라인 번호 ~45건, 앵커 ~20건, 기술 전제 ~18건: 전부 실파일 일치 확인), ② 구현 시뮬레이션(전 작업 연쇄 추적 + 관련 테스트 17파일 앵커 전수 대조 + 신규·갱신 정규식을 after 코드에 Node 로 실제 실행). 발견·반영 완료: [블로커] applyTabUpdate 를 모듈 레벨 (set,get) 주입·비함수형 set 으로 재작성(실제 applyCharacterUpdate 구조 미러) / persist 테스트 정규식을 `from '@\/` 로 좁힘(주석 리터럴 오탐). [결함] CharacterCard 휠 effect deps 에 compact 추가(컴팩트 왕복 후 휠 사망 방지) / 팝업의 CharacterBoardView 를 lazy+Suspense 로(초기 번들 회귀 방지) / 렌더러 쪽 Realtime union 2곳(supabaseService·types) 추가. [개선] 보기 토글 disabled 방식(툴팁 표시 가능) / 리스트 썸네일 fit 미적용 / 미로드 분기 mt-4 / 그룹 빈 상태 검색 문구 분기. 신규·갱신 테스트 정규식은 전부 after 코드 시뮬레이션으로 통과 확인된 것만 실었다.
> **라인 번호는 ±수십 줄 어긋날 수 있다.** 모든 위치는 라인 번호와 함께 **함수명/고유 코드 조각(앵커)** 을 병기했으므로, 라인이 어긋나면 반드시 코드 조각 검색으로 위치를 재확정할 것.
> **절대 규칙:** 이 문서에 명시되지 않은 리팩터링·포맷 변경·기능 추가를 하지 말 것. 수정 범위는 명시된 파일·블록으로 한정한다.
> **선행 문서와의 관계:** 직전 계획서 `DEVLOG/2026-07-15-피드백-30-33-수정계획.md` 는 v1.91.0(PR #232)·v1.91.1(PR #233) 로 **구현·배포 완료** 상태다. 같은 범위의 미실행 계획서는 없다. 이 계획서 자체는 문서만 추가하는 커밋으로 저장소에 들어가며 src/electron 코드는 스냅샷과 동일하다.

---

# 공통 섹션

## 0. 한눈에 보는 상태 요약 (34~41)

피드백 문서는 "비플로우 관련 요청사항"(구글 독스, 작성자: 리깅 담당 팀원 윤성원 — 캐릭터 현황판의 주 사용자). 문서는 `업데이트 완료 내역`(누적 1~33번)과 `업데이트 요청 사항`(누적 34번~) 두 섹션으로 나뉜다. **번호는 문서 전체 누적 카운트**다(섹션 내 번호는 1부터 재시작 — 요청 사항 섹션의 1~8번 = 누적 34~41번). 이번 범위는 **34·35·36·38·39·40·41번**이다.

- **37번(공지 비고 생략 시 빈 항목 제거 + 알리미 아이콘 교체)은 한솔 지시로 이번 범위에서 제외한다. 어떤 작업도 37번을 건드리지 않는다.**
- 누적 42~47번 자리는 자리표시자("ㅁㄴㅇ", "ㅁㄴ" 등 작성 중 표시)라 범위 외다.
- 모든 항목은 배포된 v1.91.1 기준으로 재현되는 **미해결** 상태다 (34번의 "링크가 percent 인코딩으로 보이는" 부분만 v1.91.1 에서 이미 수정 — 4절 참고).

| # | 피드백 요약 | 근본 원인 (코드로 확정) | 이번 작업 |
|---|---|---|---|
| **34** | 작업 완료 공지에서 (a) 제목에 `[모호 리깅 현황]` 머리말이 두 번 찍힘, (b) 내용 줄이 `"캐릭터" - "복장" 리깅완료` 로 자동 기입되길 원함 | (a) 앱의 `TITLE_TEMPLATES` 가 머리말을 포함한 제목을 보내는데, 슬랙 워크플로 메시지 서식도 제목 앞에 같은 머리말을 붙임 → 중복. (b) `CH_name` 은 `"캐릭터" - "복장"` 까지만 조립되고 `리깅완료` 접미사가 없음 | ★ **작업 2** |
| **35** | 사이드바 캐릭터 메뉴 옆 `+` 버튼 삭제 ("잘못 들어간 것 같다") | B6 구현(`Sidebar.tsx` 의 `isCharacterAddItem` 블록 + `useAppStore.pendingCharacterAddRequest` 배선)이 의도적으로 추가한 버튼 — 사용자가 불필요 판정 | ★ **작업 1** |
| **36** | 캐릭터 현황판을 별도 팝업 창으로 열기 | 기능 부재. 단, 앱에 위젯 팝업 시스템(`openWidgetPopup` + `#widget-popup/{id}` 해시 + 세션 브로드캐스트)이 완비되어 있어 등록만 하면 됨 | ★ **작업 6** |
| **37** | ~~공지 비고 생략 시 빈 항목 제거 + 알리미 아이콘~~ | — | **범위 외 (한솔 지시로 제외)** |
| **38** | 현황판 상단 메뉴(검색·필터·버튼)가 스크롤해도 따라오기 | 헤더가 스크롤 컨테이너(`h-full overflow-y-auto`)의 일반 자식이라 함께 밀려 올라감 | ★ **작업 4** |
| **39** | 담당자 입력을 씬 작업자 지정처럼 목록 선택으로 | `AssigneeNamePicker` 가 자유 텍스트 입력만 제공 — 팀원 목록(`useAuthStore.users`)은 색상 구분에만 쓰고 후보 제시는 안 함 | ★ **작업 3** |
| **40** | 보기 방식 3종(리스트 / 카드 / 이미지 없는 카드) | 기능 부재. 카드 그리드 단일 렌더 | ★ **작업 5** |
| **41** | 캐릭터 정리 탭·그룹 + 검색 색인 | 기능 부재. 그룹/카테고리 개념·컬럼·테이블 전무 | ★ **작업 8·9** (별도 PR) |
| 공통 | 버전·업데이트 노트 | — | ★ **작업 7** (PR A) · **작업 10** (PR B) |

## 1. 피드백 원문 요약 (첨부 이미지 내용 포함)

실행 에이전트는 원본 문서의 이미지를 볼 수 없으므로, 이미지가 담은 정보를 전부 글로 남긴다.

### 항목 34 — "작업 완료 공지 관련"

> "아핫 요렇게 뜨는 현상이 있네요!!
> **[제목]** [모호 리깅 현황] - 드라마 톤 특수리깅 완료 공지
> **[내용]** "캐릭터 이름" - "캐릭터 복장 이름" 리깅완료
> **[비고]** 추가할 설명 내용들 (생략 가능)
> **[링크]** "캐릭터 작업 폴더 링크"
> 별다른 내용을 작성하지 않아도 알아서 캐릭터 이름까지 탁 기입되어서 한방에 완료공지 깔금하게 들어가면 참 좋겠네요"

첨부 이미지(슬랙 스크린샷): "리깅 알리미" 워크플로 봇 메시지. 굵은 제목 줄이 **`[모호 리깅 현황] - [모호 리깅 현황] - 드라마 톤 특수리깅 완료 공지`** 로 머리말이 두 번 찍혀 있다. 그 아래 불릿 하나가 **빈 채로** 있고, 다음 불릿 "파일 경로 링크"에 percent 인코딩된 jbbj 링크 원문이 노출되어 있으며, 그 아래 Supabase 이미지 URL + 이미지 미리보기가 붙어 있다.

**상태 대조:** 스크린샷의 "링크 percent 인코딩 노출"은 v1.91.1(`buildJbbjOpenLink` 한글 원문 유지) 배포로 이미 해소된 과거 증상이다. 이번 작업 대상은 (a) 제목 머리말 중복, (b) `[내용]` 줄 자동 기입(`리깅완료` 접미사 포함) 두 가지다. 스크린샷의 "빈 불릿"이 슬랙 워크플로 서식의 `[내용]` 줄이 비어 보인 것일 가능성이 있는데, 앱은 `CH_name` 변수를 항상 비어 있지 않게 보내므로(3절 전제) 앱 수정 후에도 빈 줄이 재현되면 슬랙 워크플로 쪽 변수 바인딩 문제다 — 부록 C 체크리스트로 한솔에게 전달한다.

### 항목 35 — "사이드 메뉴에서 + 버튼은 삭제"

> "이거는 이전에 잘못 들어간 것 같아서 삭제해도 좋을듯합니다~!!"

첨부 이미지: 좌측 사이드바가 펼쳐진 상태에서 "캐릭터" 메뉴 항목 오른쪽 끝의 작은 `+` 버튼에 빨간 동그라미. (이 버튼은 B6 구현으로 추가된 "사이드바에서 캐릭터 추가" 버튼이다. 상세 모달 좌측 목록 하단의 "+ 캐릭터 추가" 버튼(F27)은 **별개이며 유지 대상**.)

### 항목 36 — "캐릭터 현황판은 팝업 메뉴로 새로 열기 가능할지"

> "캐릭터 현황판은 따로 열어두고 에피소드란을 확인하고 싶은 경우가 많아서, 혹시 캐릭터 현황판은 따로 여는게 가능할까요~~!"

첨부 이미지 없음.

### 항목 38 — "상단 메뉴는 스크롤해도 따라오면 편할것같습니다잇~!! 핫핫"

첨부 이미지: 캐릭터 현황판 상단 영역 전체 — "캐릭터 현황판" 제목 + "캐릭터 현황판/에피소드 에셋" 탭 + "이름으로 검색" 인풋 + 해시태그 필터 칩 행 + 우측 "키 비교 보기 / 보관 3 / 캐릭터 추가" 버튼 — 에 빨간 테두리. 스크롤 시 이 영역이 계속 보이길 원한다.

### 항목 39 — "담당자 이름 매치"

> "요것도 이제 직접 기입 말고 작업자 지정하는 것 처럼 목록이 나오면 참 편할듯합니다~!! 이렇게 씬별 작업자 추가하는 것 처럼 말이지요~~"

첨부 이미지 2장: ① 캐릭터 상세의 담당자 알약(pill) 버튼("이혜민", "윤성원" — `AssigneeNamePicker` 트리거). ② 씬 상세 "속성" 패널의 담당자 행 — 선택된 칩("강선영" + x) 아래로 팀원 목록 드롭다운(류성철/류이레/박정인/배한솔/안류천/원동우/윤성원/이다은, 각각 색 점 뱃지)이 펼쳐진 모습(= `AssigneeMultiSelect`). 캐릭터 담당자도 ② 같은 목록 선택이 되길 원한다.

### 항목 40 — "캐릭터 보기 방식 추가"

> "리스트 / 카드 (이미지 미리보기) / 카드 (이미지는 없음) 형태로 보는 방식을 여러 형태로 볼 수 있으면 좋겠습니다~!! 점점 캐릭터가 늘어남에 따라서 더 많은 리스트를 한번에 볼 수 있게 하면 좋겠다 싶네요~!"

첨부 이미지 없음.

### 항목 41 — "캐릭터 정리 폴더 방식에 대한 아이디어"

> "여러 탭을 만들 수 있게 하고, 그 탭에서 나름의 규칙을 정해서 만들어둘 수 있게 하면 좋겠습니다!!
> 1. 탭 추가
> 2. 탭 이름 설정 (그룹별로 보기 / 나이대 별 분류 / 주요 캐릭터 정리 / 남여 구분 / 전신이 없는 캐릭터 등등.. 유저가 자유롭게 분류)
> 3. 그 탭 내에서 폴더(그룹) 을 자유롭게 만들고, 그룹별로 배치할 수 있게 하기
> 4. 그리고 그 모든 탭들, 그 내에 분류된 그룹의 이름들을 자동으로 분류해서, 캐릭터 검색 색인으로 활용하기"

첨부 이미지 없음.

## 2. 아키텍처 전제 (전 작업 공통 배경 — 전부 코드로 확인됨)

- **프로젝트:** B flow — Electron + React 18 + TypeScript + Tailwind + Zustand + Supabase(PostgreSQL/Realtime/Storage). 저장소 `C:\Bflow-BGonly` (레포 이름은 역사적 이유로 Bflow-BGonly, 정식 명칭 B flow).
- **데이터 흐름 (절대 규칙):** 렌더러는 Supabase를 직접 호출하지 않는다. `renderer store → src/services/supabaseService.ts(window.electronAPI.* 호출) → electron/preload.ts(ipcRenderer.invoke) → electron/main.ts(ipcMain.handle + wrapIpc) → electron/supabase.ts(supabase-js)` 단방향. 새 IPC 채널 추가 시 이 4곳 + `src/types/index.ts`(ElectronAPI 인터페이스) + `src/mocks/devElectronAPI.ts`(프리뷰 mock) 를 전부 맞춘다.
- **낙관적 업데이트:** 데이터 변경은 로컬 즉시 반영 → 서버 저장 → 실패 시 "이 업데이트가 바꾼 필드만, 아직 낙관값 그대로일 때만" 롤백 + `toast.error`. 캐릭터 보드 표준 헬퍼: `useCharacterBoardStore` 의 `applyCharacterUpdate`/`applyCostumeUpdate`(1021~1137행 부근) + pending 버킷(`trackPendingFields`/`mergeIncomingWithPending`, TTL 15초).
- **테스트:** vitest 아님 — Node 22 내장 `node --test`. 테스트 파일은 레포 루트 `tests/` 폴더의 camelCase `*.test.ts`, import 는 **상대경로 + 명시적 `.ts` 확장자**(`@/` alias 를 런타임 import 하면 `ERR_MODULE_NOT_FOUND`). **새 테스트 파일은 package.json 의 해당 `test:*` 스크립트에 명시적으로 나열해야만 실행된다.** 기존 캐릭터 테스트 다수가 소스 문자열 정규식 앵커 방식이라, 소스 수정 시 이 문서가 지정한 테스트 갱신을 반드시 동반한다.
- **검증 커맨드:** 코드 변경 후 `npm run typecheck` + 관련 `npm run test:*` + `npm run build:vite` 통과 필수 (CLAUDE.md 규칙).
- **디자인 토큰:** 배경 `bg-bg-primary` / 카드 `bg-bg-card` / 보더 `border-bg-border` / 텍스트 `text-text-primary`·`text-text-secondary` / 액센트 `accent`. 전부 CSS 변수 기반(`rgb(var(--color-…) / <alpha-value>)`)이라 라이트/다크 모드 모두 자동 대응 — **hex 하드코딩 금지.**
- **캐릭터 현황판 레이어:** `src/constants/characterLayers.ts` 의 `CHARACTER_LAYER_CLASS = { modal: 'z-50', lightbox: 'z-[70]', menu: 'z-[80]', popover: 'z-[85]', editor: 'z-[90]' }`. 이번에 추가하는 sticky 헤더는 `z-20`(모든 오버레이보다 아래).
- **미리보기(수동 검증) 실행:** `npm run dev:renderer` → 브라우저에서 `http://localhost:5190/?preview=1` → 스플래시 후 로그인 화면에서 mock 사용자 `배한솔` / 비밀번호 `1234`. Electron API 는 `src/mocks/devElectronAPI.ts` 가 대체한다. **Electron 전용 기능(팝업 창 등)은 mock 에 없으므로 실기 확인 항목으로 분리한다** (부록 A).
- **버전/문서 규칙:** 기능 추가 = 마이너 버전. `DEVLOG/update-notes.json` 최상단에 항목 추가 — **비개발자 톤 필수**(기술 용어·파일명·컴포넌트명·IPC 채널명 금지, "상황+영향+결과" 시나리오 문장). 커밋 메시지는 한글.
- **슬랙 리깅 공지 구조:** 앱은 메시지를 조립하지 않는다. `RiggingAnnounceModal → sendRiggingAnnounce(src/services/slackWebhookService.ts) → IPC 'slack:send-rigging-webhook' → Slack Workflow 트리거 URL` 로 평면 변수 `{title, CH_name, Path, bigo, image}` 만 JSON POST 하고, 메시지 레이아웃(제목 머리말·불릿 구성·하이퍼링크 서식)은 Slack Workflow Builder 쪽 설정이다. **슬랙 워크플로 서식이 제목 앞에 `[모호 리깅 현황] - ` 를 붙인다는 것이 피드백 34 스크린샷으로 확인**되었다(앱이 머리말 포함 제목을 보내면 두 번 찍힘). 웹훅 URL 하드코딩(`main.ts` 의 `SLACK_RIGGING_WEBHOOK_URL`)은 의도된 기존 패턴 — 리뷰에서 P1 지적이 나와도 코드 변경 없이 근거만 남기고 진행한다 (한솔 결정).
- **위젯 팝업 시스템 (작업 6의 기반):** 메인 프로세스 `openWidgetPopup(widgetId, title, extra?)`(electron/main.ts:4032, 앵커 `function openWidgetPopup`)이 메인과 같은 `index.html` 을 `#widget-popup/{id}` 해시로 로드하는 frameless BrowserWindow 를 만든다. 렌더러 엔트리(`src/main.tsx` `bootstrap()`)가 해시를 감지해 `WidgetPopup`(src/views/WidgetPopup.tsx) 을 렌더하고, `WIDGET_REGISTRY`(WidgetPopup.tsx:51) 에서 위젯 컴포넌트를 찾는다. 세션은 메인 프로세스가 canonical 로 소유하고 모든 창에 `session:changed` 브로드캐스트하므로 **두 번째 창도 로그인 상태를 자동 공유**한다. 캐릭터 보드 Realtime 이벤트도 `broadcastToAllWindows` 로 위젯 창까지 전달된다(main.ts:3120 부근). 위치/크기/열림 상태는 `widgetPositionCache` 에 영속화되어 앱 재시작 시 `isOpen=true` 위젯 팝업이 자동 복원된다.
- **캐릭터 현황판 접근 게이팅:** `src/hooks/useCharacterBoardAccess.ts` 의 `useCharacterBoardAccessState()` — metadata(`feature-access`/`character-board`) 기반 fail-closed. 사이드바 메뉴 노출과 App.tsx 뷰 렌더 양쪽이 이 플래그로 분기. 팝업 창도 같은 훅으로 게이트한다(작업 6).

## 3. 코드베이스 지도 (이번 작업 관련만)

| 파일 | 역할 | 이번 작업 |
|---|---|---|
| `src/components/layout/Sidebar.tsx` | 좌측 내비 — `isCharacterAddItem` `+` 버튼(418~537행 부근) | 작업 1 수정 |
| `src/stores/useAppStore.ts` | 앱 전역 store — `pendingCharacterAddRequest` 배선(100~105·274·309·323~324행) | 작업 1 수정 |
| `src/views/CharacterBoardView.tsx` | 캐릭터 현황판 뷰(396줄) — `CharacterGrid`(40~327) + `CharacterBoardView`(329~393) | 작업 1·4·5·6·9 수정 |
| `src/components/characters/RiggingAnnounceModal.tsx` | 리깅 완성 공지 모달 — `TITLE_TEMPLATES`(25~30행)·`onSend`(193~215행) | 작업 2 수정 |
| `src/components/characters/AssigneeNamePicker.tsx` | 담당자 편집 모달(자유 텍스트, 169줄) | 작업 3 수정 |
| `src/components/characters/CharacterCard.tsx` | 카드 컴포넌트(179줄, memo) | 작업 5 수정 |
| `src/components/characters/CharacterListRow.tsx` | **신규** — 리스트 보기 행 | 작업 5 생성 |
| `src/utils/characterViewPersist.ts` | **신규** — 보기 방식 localStorage 영속화(alias-free) | 작업 5 생성 |
| `src/views/WidgetPopup.tsx` | 위젯 팝업 렌더러 — `WIDGET_REGISTRY`(51~70행) | 작업 6 수정 |
| `electron/main.ts` | 메인 프로세스(5,048줄) — `openWidgetPopup`(4032행) | 작업 6·8 수정 |
| `src/utils/characterTabGroups.ts` | **신규** — 탭 그룹 순수 헬퍼(alias-free) | 작업 8 생성 |
| `DEVLOG/migrations/2026-07-26-character-board-tabs.sql` | **신규** — 탭 테이블 마이그레이션 | 작업 8 생성 |
| `electron/supabase.ts` | 메인 DB 함수(4,593줄+) — 캐릭터 realtime(4498행) | 작업 8 수정 |
| `electron/preload.ts` | IPC 브리지 — 캐릭터 섹션(705~761행) | 작업 8 수정 |
| `src/services/supabaseService.ts` | 렌더러 서비스 — 캐릭터 섹션(826~1107행) | 작업 8 수정 |
| `src/stores/useCharacterBoardStore.ts` | 캐릭터 보드 store(1,150줄 부근) | 작업 8 수정 |
| `src/types/index.ts` | 도메인 타입 + ElectronAPI 시그니처 집중 | 작업 8 수정 |
| `src/mocks/devElectronAPI.ts` | 프리뷰 mock — 캐릭터 섹션(1481행 부근) | 작업 8 수정 |
| `tests/characterFeedbackBatch.test.ts` | B3~B10 앵커 — **B6 테스트가 작업 1 로 깨짐 → 갱신 필수**(42~52행) | 작업 1 수정 |
| `tests/riggingAnnounce.test.ts` | B11+31 공지 앵커 — 작업 2 로 2건 깨짐 → 갱신 필수(60·79행) | 작업 2 수정 |
| `tests/characterFeedback3.test.ts` | F25~F29 앵커 — `cardDragEnabled` 원문 고정(79행)이 작업 5 로 깨짐 → 갱신 필수 | 작업 5 수정 |
| `tests/characterBoardAssetWorkflow.test.ts` | 자산 워크플로 앵커 — `AssigneeNamePicker` 앵커(252~253행)는 작업 3 이후에도 **그대로 통과**(유지 확인만) | 무수정 (확인만) |
| `tests/characterFeedback4.test.ts` | **신규** — 항목 36·38·39·40 앵커 + persist 유닛 테스트 | 작업 3·4·5·6 생성 |
| `tests/characterBoardTabs.test.ts` | **신규** — 탭 그룹 헬퍼 유닛 + 배선 앵커 | 작업 8·9 생성 |
| `package.json` | 테스트 스크립트 등록 + 버전 | 작업 5·7·8·10 수정 |
| `DEVLOG/update-notes.json` | 업데이트 노트(배열 최신이 첫 요소) | 작업 7·10 수정 |

**참조 전용(수정 금지):** `src/components/common/AssigneeMultiSelect.tsx`(씬 작업자 콤보 — 패턴 참고), `src/components/scenes/SceneDetailModal.tsx`, `src/components/characters/CharacterDetailModal.tsx`(F27 `+ 캐릭터 추가` — **유지**), `src/components/characters/CostumeDetail.tsx`(담당자 피커 호출부 — 무수정), `src/utils/scenesViewPersist.ts`(persist 패턴 원본), `src/hooks/useCharacterBoardAccess.ts`(훅 재사용, 무수정), `src/services/slackWebhookService.ts`(작업 2 는 모달만 수정 — 서비스 무수정), `src/components/widgets/Widget.tsx`(`IsPopupContext` 수입처), `src/App.tsx`, `src/views/EpisodeAssetBoard.tsx`.

## 4. 공통 규칙

- **PR 분할:** 이번 범위는 **PR 2개**다.
  - **PR A** = 작업 1~7 (항목 34·35·36·38·39·40) — DB 변경 없음. 브랜치 `fix/feedback-34-40`, 버전 `1.92.0` 가정.
  - **PR B** = 작업 8~10 (항목 41 탭·그룹) — DB 마이그레이션 포함. **PR A 머지 후 main 에서 분기**, 브랜치 `feat/feedback-41-tabs`, 버전 `1.93.0` 가정. **마이그레이션을 라이브 DB 에 먼저 적용한 뒤 머지·배포한다** (부록 B-2 게이트).
- **버전 표기 정책:** 구현 착수 시점에 origin/main 의 `package.json` 버전을 다시 확인하고, 1.91.1 보다 올라가 있으면 그 기준 마이너 +1 로 조정한다(update-notes·커밋 메시지·PR 제목 동일 적용). 계획 작성과 구현 사이에 main 이 움직일 수 있다.
- **커밋:** 작업당 1커밋, 메시지는 한글로 변경 내용을 명확히. 테스트 갱신·추가는 해당 코드 수정과 같은 커밋에 포함.
- **금지:** 이 문서에 없는 리팩터링·의존성 추가·파일 이동. **37번 관련 수정 금지**(공지 비고 처리·알리미 아이콘은 건드리지 않는다). `slackWebhookService.ts`·`CostumeDetail.tsx`·`AssigneeMultiSelect.tsx` 수정 금지. 웹훅 URL 관련 코드 변경 금지.
- **아이콘:** `lucide-react` 만 사용 (이미 의존성에 있음).

## 5. 작업 순서와 이유

**PR A: 1 → 2 → 3 → 4 → 5 → 6 → 7.** / **PR B: 8 → 9 → 10.**

- 작업 1(버튼 제거)·작업 2(공지 문구)는 서로 독립이고 파급이 가장 작다 — 먼저 끝내 리듬을 잡는다.
- 작업 3(담당자 목록)은 `AssigneeNamePicker.tsx` 단일 파일 수정 — 독립.
- 작업 4(sticky)가 `CharacterBoardView` 의 뷰 골격을 바꾸고, 작업 5(보기 방식)와 작업 6(새 창 버튼)은 그 골격 위에 얹는다 — **4 → 5 → 6 순서 필수** (5·6 의 before 블록은 4 완료 상태 기준).
- 작업 8(탭 데이터 계층)이 타입·store·IPC 를 만들고 작업 9(탭 UI)가 소비한다 — 8 → 9 순서 필수. 작업 9 의 before 블록은 작업 5 완료 상태의 `CharacterBoardView` 기준이다(PR A 머지 후 분기하므로 자동 충족).

**작업 간 의존성 요약:** 5→(4), 6→(4), 9→(8, PR A 전체). 나머지 독립.

## 6. 전역 리스크 & 주의점

1. **소스 앵커 테스트 동반 갱신:** `tests/characterFeedbackBatch.test.ts`(B6), `tests/riggingAnnounce.test.ts`(60·79행), `tests/characterFeedback3.test.ts`(79행 `cardDragEnabled` 원문)는 이번 수정으로 반드시 깨진다. 각 작업이 지정한 갱신을 같은 커밋에서 수행할 것. 지정된 갱신 외에 임의로 앵커를 지우지 말 것.
2. **새 테스트 파일 등록:** `tests/characterFeedback4.test.ts`·`tests/characterBoardTabs.test.ts` 는 package.json `test:character` 스크립트 끝에 **명시 등록**해야 실행된다(작업 5·8 에 정확한 diff 있음).
3. **살아남아야 하는 기존 앵커:** `CharacterBoardView` 재구조화(작업 4·5·9) 중 다음 원문 줄은 글자 하나 바꾸지 말 것 — `tests/characterBoardAssetWorkflow.test.ts`·`characterTier2.test.ts`·`characterFeedbackBatch.test.ts` 가 고정한다:
   - `onCreated={(c) => { setTab('board'); setPendingOpenId(c.id); }}`
   - `if (!pendingCharacterBoardRequest || !loaded) return;` 블록 일체
   - `costumes={byCharacter.get(c.id) ?? EMPTY_COSTUMES}` / `onOpen={openCharacterDetail}` / `onContextMenu={openCardContextMenu}`
   - `보관 {archivedCharacters.length}` / `검색·필터 초기화` / `키 비교 보기` / `const maxReferenceHeight = useMemo` / `const displayCharacters = useMemo` / `displayCharacters.map` / `imageHeightPx={heightCompareMode`
   - `void reorderCharacters(moveCostumeInOrder(allIds, dragId, targetId))`
   - CharacterCard 쪽: `aspect-[3/4] bg-bg-border/30 flex items-center justify-center overflow-hidden` (이미지 박스 className), `el.addEventListener('wheel', onWheel, { passive: false })`, `onOpen(character.id, shown?.id)`, `onContextMenu(character.id, event, shown?.id)`, `imaged.length > 1`
   - AssigneeNamePicker 쪽: `setModalOpen`, `placeholder="이름 입력"`
   - 전역: `aspect-[4/3]` 문자열을 캐릭터 보드 소스 어디에도 새로 넣지 말 것(`doesNotMatch` 앵커 존재).
4. **마이그레이션 적용 전 배포 금지(PR B):** `character_board_tabs` 테이블이 라이브 DB 에 없으면 v1.93.0 앱의 현황판 로드가 실패한다(load 가 Promise.all 로 묶여 있어 탭 로드 실패 = 보드 전체 실패). **부록 B-2 게이트를 머지 전에 반드시 통과**할 것. 테이블은 추가 전용이라 구버전 앱(1.92.x)과 호환된다.
5. **`@/` alias 금지(테스트 대상 유틸):** `src/utils/characterViewPersist.ts`·`src/utils/characterTabGroups.ts` 는 node --test 가 직접 import 하므로 어떤 `@/` import 도 넣지 말 것(간접 import 포함). `characterTabGroups.ts` 가 쓰는 `createUuid` 는 `./createUuid`(상대 경로) 로 import 한다 — 이 유틸은 이미 alias-free 로 검증되어 있다(`tests/createUuid.test.ts`).
6. **다중 창 일관성:** 팝업 창(작업 6)은 자체적으로 `ensureLoadedAndRealtime()` 을 돌리는 독립 렌더러다. 메인 창과 팝업 창의 캐릭터 데이터는 각자 Realtime 으로 동기화된다 — 창 간 직접 상태 공유를 시도하지 말 것. localStorage(보기 방식)는 같은 파티션이라 창끼리 공유되지만 실시간 동기 이벤트는 없다(허용 — 각 창이 다음 렌더 시 반영).
7. **대형 파일 주의:** `electron/main.ts`(5,048줄)·`electron/supabase.ts`(4,593줄+)·`src/services/supabaseService.ts`·`src/types/index.ts` 는 크다. 반드시 앵커 검색으로 위치를 잡고, 지정 블록 외를 건드리지 말 것.

---

# PR A (버전 1.92.0 가정, 브랜치 `fix/feedback-34-40`)

# 작업 1 — 항목 35: 사이드바 캐릭터 `+` 버튼 제거

## 문제/요청 요약

사이드바 "캐릭터" 메뉴 항목 오른쪽의 `+`(캐릭터 추가) 버튼을 삭제한다. 이 버튼만을 위해 존재하던 `useAppStore.pendingCharacterAddRequest` 배선(설정·소비·리셋)도 함께 제거해 죽은 코드를 남기지 않는다. **상세 모달 좌측 목록의 "+ 캐릭터 추가"(F27)와 현황판 헤더의 "캐릭터 추가" 버튼은 유지한다.**

## 근거 (전부 스냅샷 실파일 대조)

`src/components/layout/Sidebar.tsx:423-424` (앵커 `isCharacterAddItem`):

```tsx
          // B6: 권한 있는 캐릭터 항목엔 펼침/hover 시 '+' 로 바로 추가 창을 연다.
          const isCharacterAddItem = item.id === 'character-board' && characterAccess.allowed && !characterAccessFailed;
```

`src/components/layout/Sidebar.tsx:518-536` (앵커 `if (isCharacterAddItem)`):

```tsx
          if (isCharacterAddItem) {
            return (
              <div key={item.id} className="relative">
                {navButton}
                {isVisuallyExpanded && (
                  <button
                    type="button"
                    aria-label="캐릭터 추가"
                    title="캐릭터 추가"
                    onClick={(e) => { e.stopPropagation(); setView('character-board'); setPendingCharacterAddRequest(true); }}
                    className="absolute right-3 top-1/2 -translate-y-1/2 flex h-6 w-6 items-center justify-center rounded-md text-text-secondary hover:text-accent hover:bg-accent/15 transition-colors cursor-pointer"
                  >
                    <Plus size={15} />
                  </button>
                )}
              </div>
            );
          }
          return <div key={item.id} className="contents">{navButton}</div>;
```

`pendingCharacterAddRequest` 의 전체 사용처는 grep 으로 확정했다 — `useAppStore.ts`(선언 100~105·setView 리셋 274·goBackNavigation 리셋 309·초기값/구현 323~324), `Sidebar.tsx`(구독 + 버튼 onClick), `CharacterBoardView.tsx`(구독 334~335 + 소비 effect 353~359), `tests/characterFeedbackBatch.test.ts`(B6). 그 외 참조 없음(문서 `docs/superpowers/plans/…` 는 기록물이라 무수정).

## 수정 내용

### 1-1. `src/components/layout/Sidebar.tsx`

(a) `isCharacterAddItem` 선언 2줄(주석 포함, 423~424행)을 삭제한다. 바로 위의 `isAccessRetryItem` 선언(421~422행)은 유지.

(b) 518~536행의 `if (isCharacterAddItem) { … }` 블록 전체를 삭제하고 마지막 줄만 남긴다:

```tsx
          return <div key={item.id} className="contents">{navButton}</div>;
```

(c) 파일 상단 lucide import(3행)에서 `Plus` 를 제거한다 — 실제 import 는 `…, PanelLeft, Plus } from 'lucide-react'` 로 `Plus` 가 마지막이라 **`, Plus` 토큰**을 제거한다. 이 파일에서 `Plus` 사용처는 삭제된 버튼뿐 — 삭제 후 `grep -n "Plus" src/components/layout/Sidebar.tsx` 로 0건 확인.

(d) `setPendingCharacterAddRequest` 구독 줄(260행 부근, 앵커 `useAppStore((s) => s.setPendingCharacterAddRequest)`)을 삭제한다.

### 1-2. `src/stores/useAppStore.ts`

(a) 인터페이스 선언(100~105행) — before:

```ts
  /**
   * 사이드바 '캐릭터' 항목의 '+' 등 외부 표면에서 캐릭터 추가 창 열기를 요청할 때 사용.
   * 뷰 전환 race 를 피해 store 기반으로 소비한다 (pendingCharacterBoardRequest 와 동일 패턴).
   */
  pendingCharacterAddRequest: boolean;
  setPendingCharacterAddRequest: (v: boolean) => void;
```

→ 블록 전체 삭제.

(b) `setView`(268~275행) — before:

```ts
  setView: (view) => set((s) => ({
    currentView: view,
    previousView: s.currentView,
    // 캐릭터 딥링크 요청은 현황판이 아닌 다른 뷰로 이동하는 순간 폐기 —
    //   로드 지연 중 사이드바로 이탈한 미소비 요청이 나중에 예기치 않은 모달을 열지 않게 한다.
    pendingCharacterBoardRequest: view === 'character-board' ? s.pendingCharacterBoardRequest : null,
    pendingCharacterAddRequest: view === 'character-board' ? s.pendingCharacterAddRequest : false,
  })),
```

→ after (마지막 줄만 삭제):

```ts
  setView: (view) => set((s) => ({
    currentView: view,
    previousView: s.currentView,
    // 캐릭터 딥링크 요청은 현황판이 아닌 다른 뷰로 이동하는 순간 폐기 —
    //   로드 지연 중 사이드바로 이탈한 미소비 요청이 나중에 예기치 않은 모달을 열지 않게 한다.
    pendingCharacterBoardRequest: view === 'character-board' ? s.pendingCharacterBoardRequest : null,
  })),
```

(c) `goBackNavigation` 반환 객체(309행, 앵커 `pendingCharacterAddRequest: false,` — `pendingSceneModalRequest: null,` 와 `pendingCharacterBoardRequest: null,` 사이) 줄 삭제.

(d) 구현부(323~324행) — before:

```ts
  pendingCharacterAddRequest: false,
  setPendingCharacterAddRequest: (v) => set({ pendingCharacterAddRequest: v }),
```

→ 2줄 삭제.

### 1-3. `src/views/CharacterBoardView.tsx`

(a) 구독 2줄(334~335행) 삭제:

```tsx
  const pendingCharacterAddRequest = useAppStore((s) => s.pendingCharacterAddRequest);
  const setPendingCharacterAddRequest = useAppStore((s) => s.setPendingCharacterAddRequest);
```

(b) 소비 effect(353~359행, 주석 포함) 삭제:

```tsx
  // 사이드바 '+' 등에서 캐릭터 추가 창 열기 요청 — 데이터 로드와 무관(모달은 데이터 불필요).
  useEffect(() => {
    if (!pendingCharacterAddRequest) return;
    setTab('board');
    setAddOpen(true);
    setPendingCharacterAddRequest(false);
  }, [pendingCharacterAddRequest, setPendingCharacterAddRequest]);
```

361~362행의 주석("미소비 딥링크 요청 청소는 …")은 `pendingCharacterBoardRequest`(딥링크) 에도 해당하므로 유지한다.

### 1-4. `tests/characterFeedbackBatch.test.ts` — B6 테스트 교체

before (42~52행):

```ts
test('B6: 사이드바에서 캐릭터 추가', () => {
  assert.match(appStore, /pendingCharacterAddRequest: boolean/);
  assert.match(appStore, /setPendingCharacterAddRequest: \(v: boolean\) => void/);
  assert.match(appStore, /pendingCharacterAddRequest: view === 'character-board' \? s\.pendingCharacterAddRequest : false/);
  assert.match(sidebar, /const isCharacterAddItem = item\.id === 'character-board' && characterAccess\.allowed && !characterAccessFailed/);
  assert.match(sidebar, /setPendingCharacterAddRequest\(true\)/);
  assert.match(sidebar, /aria-label="캐릭터 추가"/);
  assert.match(boardView, /if \(!pendingCharacterAddRequest\) return;/);
  assert.match(boardView, /setAddOpen\(true\);/);
  assert.match(boardView, /setPendingCharacterAddRequest\(false\);/);
});
```

after (같은 자리에 교체 — B6 이 "제거 확인" 테스트로 바뀐다):

```ts
test('B6→피드백 35: 사이드바 캐릭터 + 버튼 제거 — 배선 잔재 없음', () => {
  // 사용자 요청(피드백 35)으로 사이드바 '+' 는 삭제됐다. 죽은 배선이 되살아나지 않게 고정.
  assert.doesNotMatch(sidebar, /isCharacterAddItem/);
  assert.doesNotMatch(sidebar, /aria-label="캐릭터 추가"/);
  assert.doesNotMatch(sidebar, /PendingCharacterAddRequest/);
  assert.doesNotMatch(appStore, /endingCharacterAddRequest/);
  assert.doesNotMatch(boardView, /endingCharacterAddRequest/);
  // 캐릭터 추가 진입점은 현황판 헤더 버튼과 상세 모달(F27)로 유지된다.
  assert.match(boardView, /<Plus size=\{16\} \/> 캐릭터 추가/);
  assert.match(detailModal, /<Plus size=\{15\} \/> 캐릭터 추가/);
});
```

주의: `/endingCharacterAddRequest/` 는 `pendingCharacterAddRequest`(소문자 p)와 `setPendingCharacterAddRequest`(대문자 P) 를 한 번에 잡기 위한 의도적 부분 문자열이다. `detailModal` 변수는 이 파일 17행에서 이미 로드되어 있다(`CharacterDetailModal.tsx`).

## 엣지 케이스

- 사이드바가 접힌 상태(hover 만 펼침)에서도 `+` 가 렌더되던 조건(`isVisuallyExpanded`)까지 통째로 사라지므로 잔여 상태 없음.
- `goBackNavigation` 은 이제 캐릭터 추가 요청을 알지 못한다 — 요청 개념 자체가 사라졌으므로 안전.
- Spotlight/위젯 딥링크(`pendingCharacterBoardRequest`)는 별개 배선 — 건드리지 않았는지 diff 로 확인할 것.

## 검증 방법 / 완료 기준

- [ ] `npm run typecheck` 통과 (미사용 import/변수 에러 없음 — `Plus`·구독 제거 확인)
- [ ] `npm run test:character` 116 pass 유지(개수 동일 — B6 은 교체이지 삭제가 아님)
- [ ] 프리뷰: 사이드바 펼침 + 캐릭터 항목 hover 시 `+` 버튼이 더는 없음 / 캐릭터 메뉴 클릭·현황판 "캐릭터 추가" 버튼·상세 모달 "+ 캐릭터 추가" 는 정상 동작
- [ ] 커밋 예: `[v1.92.0] 사이드바 캐릭터 + 버튼 제거 (피드백 35)`

---

# 작업 2 — 항목 34: 리깅 공지 제목 머리말 중복 제거 + 내용 자동 기입

## 문제/요청 요약

(a) 슬랙 공지 제목이 `[모호 리깅 현황] - [모호 리깅 현황] - …` 로 머리말이 두 번 찍힌다 — 슬랙 워크플로 서식이 머리말을 붙이므로 **앱 템플릿에서 머리말을 뺀다**. (b) `[내용]` 줄이 `"캐릭터 이름" - "복장 이름" 리깅완료` 포맷이 되도록 **`리깅완료` 접미사를 추가**한다. 사용자가 아무것도 입력하지 않아도 템플릿 기본값만으로 완성된 공지가 나가야 한다.

## 근거

`src/components/characters/RiggingAnnounceModal.tsx:25-30` (앵커 `TITLE_TEMPLATES`):

```ts
/** 제목 템플릿 — 버튼을 누르면 제목(title)만 채운다(비고는 직접 작성). '[모호 리깅 현황] - …' 포맷 (피드백 31a). */
const TITLE_TEMPLATES: { key: string; label: string; title: string }[] = [
  { key: 'general', label: '일반 리깅 완료', title: '[모호 리깅 현황] - 리깅 완료 공지' },
  { key: 'drama', label: '드라마 톤 특수리깅 완료', title: '[모호 리깅 현황] - 드라마 톤 특수리깅 완료 공지' },
  { key: 'fix', label: '수정·보완 완료', title: '[모호 리깅 현황] - 리깅 수정·보완 완료 공지' },
];
```

`src/components/characters/RiggingAnnounceModal.tsx:196-204` (앵커 `await sendRiggingAnnounce({`):

```ts
      await sendRiggingAnnounce({
        // 피드백 31(a): [내용] 줄 포맷 — '"캐릭터 이름" - "복장 이름"'. 복장이 여럿이어도 어떤 복장인지 구분된다.
        characterName: `"${character.name}" - "${costume.name}"`,
        title: title.trim(),
        folderPath: character.workFolderPath,
        notes,
        imageUrl: selectedImage?.url ?? null,
      });
```

머리말 문자열이 소스에 존재하는 곳은 이 `TITLE_TEMPLATES` 와 테스트 앵커뿐이다(서비스·메인 프로세스는 머리말을 붙이지 않음). 슬랙 워크플로 서식이 제목 앞에 `[모호 리깅 현황] - ` 를 붙인다는 사실은 피드백 34 스크린샷(머리말 2회)과 31 시절 스크린샷(구버전 제목에도 머리말 선행)으로 교차 확인했다.

**이력 주의:** v1.91.1(PR #233) 때 "워크플로 템플릿 구버전 잔존"이 별도 진단되어 템플릿 갱신 가이드가 PR 본문으로 한솔에게 전달된 바 있다(적용 여부 미확인). **이 계획서의 확정 결정은 "머리말은 슬랙 워크플로 서식이 소유한다"** 이다 — 한솔이 템플릿을 갱신할 때도 머리말은 워크플로 서식 쪽에 유지해야 하며, 앱은 어떤 경우에도 머리말 없이 보낸다. 배포 후 실공지 1회로 최종 확인한다(부록 C).

## 수정 내용 (`src/components/characters/RiggingAnnounceModal.tsx` 단일 파일)

### 2-1. `TITLE_TEMPLATES` 교체 (25~30행) — after:

```ts
/** 제목 템플릿 — 버튼을 누르면 제목(title)만 채운다(비고는 직접 작성).
 *  슬랙 워크플로 서식이 제목 앞에 '[모호 리깅 현황] - ' 머리말을 자동으로 붙이므로,
 *  앱은 머리말 없이 보낸다 — 포함해 보내면 공지에 머리말이 두 번 찍힌다 (피드백 34a). */
const TITLE_TEMPLATES: { key: string; label: string; title: string }[] = [
  { key: 'general', label: '일반 리깅 완료', title: '리깅 완료 공지' },
  { key: 'drama', label: '드라마 톤 특수리깅 완료', title: '드라마 톤 특수리깅 완료 공지' },
  { key: 'fix', label: '수정·보완 완료', title: '리깅 수정·보완 완료 공지' },
];
```

### 2-2. `onSend` 의 `characterName` 교체 (197~199행) — after:

```ts
      await sendRiggingAnnounce({
        // 피드백 34(b): [내용] 줄 포맷 — '"캐릭터 이름" - "복장 이름" 리깅완료'. 입력 없이도 완성 공지가 나간다.
        characterName: `"${character.name}" - "${costume.name}" 리깅완료`,
```

(`title:` 이하 나머지 필드는 무변경.)

### 2-3. 제목 입력 아래 안내 캡션 추가 (260~268행 제목 블록)

before:

```tsx
        {/* 제목 */}
        <div className="flex flex-col gap-1.5">
          <span className="text-xs text-text-secondary">제목</span>
          <input
            value={title}
            onChange={(e) => { setTitle(e.target.value); setActiveTpl(null); }}
            placeholder="예) 드라마 톤 특수리깅 완료 공지"
            className="bg-transparent border border-bg-border rounded-md px-3 py-2 text-sm text-text-primary outline-none focus:border-accent/50"
          />
        </div>
```

after (input 아래 캡션 한 줄 추가 — 나머지 동일):

```tsx
        {/* 제목 */}
        <div className="flex flex-col gap-1.5">
          <span className="text-xs text-text-secondary">제목</span>
          <input
            value={title}
            onChange={(e) => { setTitle(e.target.value); setActiveTpl(null); }}
            placeholder="예) 드라마 톤 특수리깅 완료 공지"
            className="bg-transparent border border-bg-border rounded-md px-3 py-2 text-sm text-text-primary outline-none focus:border-accent/50"
          />
          <span className="text-[11px] text-text-secondary/80">슬랙 공지 제목 앞에는 [모호 리깅 현황] 머리말이 자동으로 붙어요 — 제목에 또 쓰지 않아도 돼요.</span>
        </div>
```

### 2-4. 파일 상단 모달 doc 주석 갱신 (32~38행)

38행의 `- 워크플로 변수 CH_name="캐릭터" - "복장" / …` 줄을 다음으로 교체:

```
 * - 워크플로 변수 CH_name='"캐릭터" - "복장" 리깅완료' / Path=폴더 jbbj 링크 / bigo=비고(줄바꿈) / image=고른 이미지 공개 URL.
 *   제목 머리말 '[모호 리깅 현황] - ' 은 슬랙 워크플로 서식이 붙인다(피드백 34a — 앱은 머리말 없이 전송).
```

### 2-5. `tests/riggingAnnounce.test.ts` 갱신 (2곳)

(a) 60행 — before:

```ts
  assert.match(modal, /characterName: `"\$\{character\.name\}" - "\$\{costume\.name\}"`/);
```

after (주석 줄 59행도 함께 교체):

```ts
  // 피드백 34(b): [내용] 줄은 '"캐릭터" - "복장" 리깅완료' 포맷.
  assert.match(modal, /characterName: `"\$\{character\.name\}" - "\$\{costume\.name\}" 리깅완료`/);
```

(b) 79행 — before:

```ts
  assert.match(modal, /\[모호 리깅 현황\] - 리깅 완료 공지/);
```

after (머리말 없는 새 템플릿 + 중복 재발 방지 고정):

```ts
  // 피드백 34(a): 머리말 '[모호 리깅 현황]' 은 슬랙 워크플로가 붙인다 — 템플릿 제목에 다시 넣으면 두 번 찍힌다.
  assert.match(modal, /title: '리깅 완료 공지'/);
  assert.match(modal, /title: '드라마 톤 특수리깅 완료 공지'/);
  assert.doesNotMatch(modal, /title: '\[모호 리깅 현황\]/);
  assert.match(modal, /머리말이 자동으로 붙어요/);
```

나머지 단언(76~84행의 `useState(TITLE_TEMPLATES[0].title)`, `doesNotMatch(비고를 한 줄 이상)`, `hasNote`, `disabled={sending || pasting}`)은 after 코드에서도 그대로 통과한다 — 수정 금지.

## 엣지 케이스

- **사용자가 제목에 머리말을 직접 타이핑하면** 여전히 두 번 찍힌다 — 캡션(2-3)이 그 실수를 예방하는 안내다. 앱에서 강제로 머리말을 제거(strip)하지는 않는다(사용자 입력 존중, 최소 수정).
- **수정·보완 템플릿에도 `리깅완료` 접미사가 붙는가?** `CH_name` 은 템플릿과 무관하게 항상 `… 리깅완료` 다. 피드백 34 의 요청 포맷이 접미사를 명시했고, 수정·보완 공지도 "리깅(작업)이 완료됐다"는 의미라 허용 — 템플릿별 접미사 분기는 과잉 설계로 판단해 하지 않는다.
- 캐릭터·복장 이름에 따옴표가 들어가는 경우: 기존 포맷도 동일하게 감쌌으므로 회귀 없음.

## 검증 방법 / 완료 기준

- [ ] `npm run typecheck` + `npm run test:character` 통과 — pass 수 116 유지(단언 수만 변동, test 블록 수 불변)
- [ ] 프리뷰: 공지 모달 제목 기본값이 `리깅 완료 공지`(머리말 없음), 템플릿 칩 3종 모두 머리말 없는 제목 채움, 제목 아래 안내 캡션 표시
- [ ] 실기(선택): 슬랙 전송 시 제목 `[모호 리깅 현황] - 리깅 완료 공지` 한 번만, 내용 줄 `"캐릭터" - "복장" 리깅완료` — 부록 C 의 워크플로 확인과 함께
- [ ] 커밋 예: `[v1.92.0] 리깅 공지 제목 머리말 중복 제거 + 내용 자동 기입 (피드백 34)`

---

# 작업 3 — 항목 39: 담당자 피커에 팀원 제안 목록 추가

## 문제/요청 요약

캐릭터 상세(복장 패널)의 디자인/리깅 담당자 편집 모달(`AssigneeNamePicker`)이 자유 텍스트 입력만 제공한다. 씬 상세의 작업자 지정(`AssigneeMultiSelect`)처럼 **팀원 목록이 후보로 나오고 클릭/키보드로 고를 수 있게** 한다. 자유 입력(외부 인력 이름)은 그대로 유지한다.

## 근거

- `src/components/characters/AssigneeNamePicker.tsx`(169줄) — 팀원 목록(`useAuthStore.users`)을 `userNameSet`(37행)으로만 사용: 입력된 이름이 목록에 있는지에 따라 칩 테두리 알파만 다르게 함. 후보 제시 없음. 모달 input(138~155행)은 Enter=`addDraftName()`(추가 후 모달 닫힘), Escape=닫기.
- 씬 쪽 `src/components/common/AssigneeMultiSelect.tsx` 는 fuzzy 필터 후보 + 색 점 뱃지 + 키보드 내비를 이미 구현(224~254행 부근) — **패턴 참고만 하고 컴포넌트 교체는 하지 않는다**(인라인 콤보라 StageRail 헤더 레이아웃과 맞지 않음. 모달 유지가 최소 변경).
- 저장 데이터는 콤마 구분 문자열(`character_costumes.design_assignee`/`rigging_assignee`) — 변경 없음. 호출부 `CostumeDetail.tsx:326-346` 무수정.
- 색상: `getUserColor(name)`(이름 해시) — 씬 드롭다운과 동일 함수라 색이 자동으로 일치한다.
- 살릴 테스트 앵커(`tests/characterBoardAssetWorkflow.test.ts:252-253`): `assert.match(assigneeNamePickerSource, /setModalOpen/);` / `assert.match(assigneeNamePickerSource, /placeholder="이름 입력"/);` — after 코드에 둘 다 남는다.

## 수정 내용 — `src/components/characters/AssigneeNamePicker.tsx` 전체 교체

파일 전체를 아래로 교체한다 (기존 169줄 → 약 210줄). 기존과 달라지는 부분: ① `highlightIdx` 상태 + `suggestions` 계산 추가, ② `addName`(목록 선택 — 모달 유지) 추가, ③ input `onKeyDown` 에 ArrowUp/Down/Enter 하이라이트 처리, ④ input 아래 제안 목록 렌더, ⑤ 부제 문구 변경. 트리거 버튼·칩 목록·`addDraftName`(직접 입력 — 추가 후 닫힘)·하단 버튼은 원문 유지.

```tsx
import { useEffect, useMemo, useRef, useState, type KeyboardEvent as ReactKeyboardEvent } from 'react';
import { User, X } from 'lucide-react';
import { useAuthStore } from '@/stores/useAuthStore';
import { useModalFocus } from '@/hooks/useModalFocus';
import { cn } from '@/utils/cn';
import { parseAssigneeNames } from '@/utils/characterStageMeta';
import { getUserColor } from '@/utils/userColor';
import { CHARACTER_LAYER_CLASS } from '@/constants/characterLayers';

function claimReactKey(event: ReactKeyboardEvent<HTMLElement>) {
  event.preventDefault();
  event.stopPropagation();
  event.nativeEvent.stopImmediatePropagation?.();
}

function formatAssigneeNames(names: string[]): string | null {
  const unique = Array.from(new Set(names.map((name) => name.trim()).filter(Boolean)));
  return unique.length > 0 ? unique.join(', ') : null;
}

export function AssigneeNamePicker({
  label,
  value,
  onChange,
}: {
  label: string;
  value: string | null;
  onChange: (value: string | null) => void;
}) {
  const users = useAuthStore((s) => s.users);
  const [draftName, setDraftName] = useState('');
  const [modalOpen, setModalOpen] = useState(false);
  // 피드백 39: 제안 목록 키보드 하이라이트 인덱스 (-1 = 하이라이트 없음 → Enter 는 직접 입력 추가).
  const [highlightIdx, setHighlightIdx] = useState(-1);
  const inputRef = useRef<HTMLInputElement>(null);
  const dialogRef = useRef<HTMLDivElement>(null);
  const modalFocus = useModalFocus(dialogRef, { active: modalOpen, autoFocus: false });
  const selected = parseAssigneeNames(value);
  const userNameSet = useMemo(() => new Set(users.map((user) => user.name)), [users]);

  // 피드백 39: 씬 작업자 지정처럼 팀원 목록을 후보로 제시 — 이미 선택된 이름 제외 + 입력어 포함 필터.
  const draftQuery = draftName.trim().toLowerCase();
  const suggestions = users
    .filter((u) => !selected.includes(u.name))
    .filter((u) => !draftQuery || u.name.toLowerCase().includes(draftQuery));

  const closeModal = () => {
    setDraftName('');
    setHighlightIdx(-1);
    setModalOpen(false);
  };

  const remove = (name: string) => {
    const next = selected.filter((item) => item !== name);
    onChange(formatAssigneeNames(next));
  };

  useEffect(() => {
    if (!modalOpen) return;
    const id = window.setTimeout(() => inputRef.current?.focus(), 0);
    return () => window.clearTimeout(id);
  }, [modalOpen]);

  // 입력어가 바뀌면 후보 목록이 달라지므로 하이라이트를 리셋한다.
  useEffect(() => {
    setHighlightIdx(-1);
  }, [draftName]);

  // 목록에서 한 명 추가 — 모달을 닫지 않아 연속 선택이 가능하다 (피드백 39).
  const addName = (name: string) => {
    if (!selected.includes(name)) onChange(formatAssigneeNames([...selected, name]));
    setDraftName('');
    setHighlightIdx(-1);
    inputRef.current?.focus();
  };

  const addDraftName = () => {
    const names = draftName
      .split(/[,\n]/)
      .map((name) => name.trim())
      .filter(Boolean);
    if (names.length === 0) return;
    const next = [...selected];
    for (const name of names) {
      if (!next.includes(name)) next.push(name);
    }
    onChange(formatAssigneeNames(next));
    setDraftName('');
    setModalOpen(false);
  };

  const firstName = selected[0] ?? null;
  const color = firstName ? getUserColor(firstName) : 'rgb(var(--color-text-secondary))';
  const display = firstName
    ? selected.length > 1
      ? `${firstName} 외 ${selected.length - 1}`
      : firstName
    : '담당자 추가';

  return (
    <div className="flex items-center">
      <button
        type="button"
        onClick={() => setModalOpen(true)}
        title={label}
        className={cn(
          'flex min-h-8 max-w-[160px] items-center gap-1.5 rounded-full px-2.5 text-[11px] transition-[background-color,box-shadow,transform] active:scale-[0.96]',
          selected.length > 0
            ? 'bg-bg-border/20 text-text-primary shadow-[0_0_0_1px_rgba(255,255,255,0.08)] hover:bg-bg-border/35'
            : 'bg-transparent text-text-secondary shadow-[0_0_0_1px_rgba(255,255,255,0.08)] hover:bg-bg-border/20 hover:text-text-primary',
        )}
        aria-label={`${label} 편집`}
      >
        <User size={12} className="shrink-0" style={{ color }} />
        <span className="truncate">{display}</span>
      </button>
      {modalOpen && (
        <div className={`fixed inset-0 ${CHARACTER_LAYER_CLASS.popover} flex items-center justify-center bg-overlay/55 p-4`} onMouseDown={closeModal}>
          <div
            ref={dialogRef}
            role="dialog"
            aria-modal="true"
            aria-label={`${label} 추가`}
            tabIndex={-1}
            onKeyDown={modalFocus.onKeyDown}
            className="w-full max-w-sm rounded-2xl bg-bg-card p-4 shadow-2xl ring-1 ring-white/10"
            onMouseDown={(event) => event.stopPropagation()}
          >
            <div className="mb-3 flex items-start justify-between gap-3">
              <div>
                <div className="text-sm font-semibold text-text-primary">{label} 추가</div>
                <div className="text-xs text-text-secondary">목록에서 고르거나, 쉼표로 여러 이름을 직접 입력할 수 있어요</div>
              </div>
              <button type="button" aria-label="닫기" onClick={closeModal} className="rounded-lg p-2 text-text-secondary hover:bg-bg-border/30 hover:text-text-primary">
                <X size={18} />
              </button>
            </div>
            {selected.length > 0 && (
              <div className="mb-3 flex flex-wrap gap-1.5">
                {selected.map((name) => {
                  const itemColor = getUserColor(name);
                  const isListedUser = userNameSet.has(name);
                  return (
                    <button
                      key={name}
                      type="button"
                      title={isListedUser ? `${name} 제거` : `${name} 제거 (사용자 목록에 없는 이름)`}
                      onClick={() => remove(name)}
                      className="group flex min-h-8 items-center gap-1.5 rounded-full px-2.5 py-1 text-xs transition-[background-color,box-shadow] hover:bg-bg-border/25"
                      style={{ color: itemColor, boxShadow: `inset 0 0 0 1px ${itemColor}${isListedUser ? '66' : '99'}` }}
                    >
                      <span className="h-1.5 w-1.5 rounded-full" style={{ background: itemColor }} />
                      {name}
                      <X size={12} className="text-text-secondary opacity-60 transition-opacity group-hover:opacity-100" />
                    </button>
                  );
                })}
              </div>
            )}
            <input
              ref={inputRef}
              type="text"
              value={draftName}
              onChange={(event) => setDraftName(event.target.value)}
              onKeyDown={(event) => {
                if (event.key === 'ArrowDown') {
                  event.preventDefault();
                  setHighlightIdx((i) => (suggestions.length === 0 ? -1 : (i + 1) % suggestions.length));
                  return;
                }
                if (event.key === 'ArrowUp') {
                  event.preventDefault();
                  setHighlightIdx((i) => (suggestions.length === 0 ? -1 : i <= 0 ? suggestions.length - 1 : i - 1));
                  return;
                }
                if (event.key === 'Enter') {
                  event.preventDefault();
                  if (highlightIdx >= 0 && highlightIdx < suggestions.length) {
                    addName(suggestions[highlightIdx].name);
                    return;
                  }
                  addDraftName();
                }
                if (event.key === 'Escape') {
                  claimReactKey(event);
                  closeModal();
                }
              }}
              placeholder="이름 입력"
              className="w-full rounded-xl border border-bg-border bg-bg-border/10 px-3 py-2 text-sm text-text-primary placeholder:text-text-secondary/50 outline-none focus:border-accent/70"
            />
            {suggestions.length > 0 && (
              <div className="mt-2 max-h-44 overflow-y-auto rounded-xl border border-bg-border/70 bg-bg-border/10" role="listbox" aria-label="팀원 목록">
                {suggestions.map((u, i) => {
                  const itemColor = getUserColor(u.name);
                  return (
                    <button
                      key={u.id}
                      type="button"
                      role="option"
                      aria-selected={i === highlightIdx}
                      onClick={() => addName(u.name)}
                      onMouseEnter={() => setHighlightIdx(i)}
                      className={cn(
                        'flex w-full items-center gap-2 px-3 py-2 text-left text-sm text-text-primary transition-colors cursor-pointer',
                        i === highlightIdx ? 'bg-accent/15' : 'hover:bg-bg-border/25',
                      )}
                    >
                      <span className="h-2 w-2 rounded-full shrink-0" style={{ background: itemColor }} />
                      {u.name}
                    </button>
                  );
                })}
              </div>
            )}
            <div className="mt-4 flex justify-end gap-2">
              <button type="button" onClick={closeModal} className="rounded-lg px-3 py-2 text-sm text-text-secondary hover:bg-bg-border/25 hover:text-text-primary">
                취소
              </button>
              <button type="button" disabled={!draftName.trim()} onClick={addDraftName} className="rounded-lg bg-accent px-3 py-2 text-sm font-medium text-white hover:opacity-90 disabled:opacity-40">
                추가
              </button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
```

## 테스트 — **이 작업에서 `tests/characterFeedback4.test.ts` 를 신규 생성하고 등록한다**

(a) 신규 파일 `tests/characterFeedback4.test.ts` 전체 내용 (이후 작업 4·5·6 이 이 파일에 블록을 추가한다):

```ts
/**
 * 2026-07-26 캐릭터 현황판 피드백 배치(항목 36·38·39·40) 배선 고정 테스트.
 * 소스-문자열 검사 — 리팩터 시 앵커가 깨지면 함께 갱신할 것.
 */
import { readFileSync } from 'node:fs';
import test from 'node:test';
import assert from 'node:assert/strict';

test('피드백 39: 담당자 피커 — 팀원 제안 목록 + 키보드 선택', () => {
  const picker = readFileSync('src/components/characters/AssigneeNamePicker.tsx', 'utf8');
  assert.match(picker, /const suggestions = users/);
  assert.match(picker, /aria-label="팀원 목록"/);
  assert.match(picker, /onClick=\{\(\) => addName\(u\.name\)\}/);
  assert.match(picker, /if \(event\.key === 'ArrowDown'\)/);
  assert.match(picker, /addName\(suggestions\[highlightIdx\]\.name\)/);
  // 자유 입력(외부 인력)과 기존 앵커는 유지된다.
  assert.match(picker, /placeholder="이름 입력"/);
  assert.match(picker, /setModalOpen/);
});
```

(b) `package.json` 의 `test:character` 스크립트 끝에 등록 — before 는 `… ./tests/characterHeightKey.test.ts` 로 끝난다. after 는 그 뒤에 ` ./tests/characterFeedback4.test.ts` 를 덧붙인다(공백 구분).

## 엣지 케이스

- **팀원 전원이 이미 선택된 경우:** `suggestions` 가 비어 목록이 렌더되지 않는다 — 직접 입력만 가능(기존과 동일).
- **목록 클릭 시 저장 실패:** `onChange` → `updateCostumeField` 낙관적 업데이트가 store 차원에서 롤백+토스트 처리(기존 경로 그대로) — 피커는 관여하지 않는다.
- **하이라이트 상태에서 목록이 줄어드는 경우:** `draftName` 변경 effect 가 -1 로 리셋. Enter 가드(`highlightIdx < suggestions.length`)로 stale 인덱스 클릭 방지.
- **mock 프리뷰:** `useAuthStore.users` 는 프리뷰 mock 로그인 시 시드 사용자 목록으로 채워진다 — 목록 노출 검증 가능.

## 검증 방법 / 완료 기준

- [ ] `npm run typecheck` 통과, `npm run test:character` 117 pass (신규 테스트 1블록 추가, `characterBoardAssetWorkflow` 의 기존 피커 앵커 2건 그대로 통과 확인)
- [ ] 프리뷰: 캐릭터 상세 → 복장 → "디자인 담당자" 클릭 → 모달에 팀원 목록(색 점 포함) 표시 / 입력어로 필터 / 클릭 시 즉시 칩 추가 + 모달 유지 / ↑↓+Enter 선택 / 목록에 없는 이름 직접 입력도 기존대로 동작
- [ ] 커밋 예: `[v1.92.0] 담당자 피커에 팀원 목록 선택 추가 (피드백 39)`

---

# 작업 4 — 항목 38: 현황판 상단 메뉴 스크롤 고정

## 문제/요청 요약

캐릭터 현황판을 스크롤하면 제목·탭·검색·필터·버튼이 전부 밀려 올라간다. **제목+탭은 스크롤 영역 밖 고정**으로 빼고, **검색·버튼·필터 칩 행은 스크롤 컨테이너 안 sticky** 로 만들어 스크롤 중에도 항상 보이게 한다.

## 근거

`src/views/CharacterBoardView.tsx:364-392` (앵커 `return (` 아래 `h-full overflow-y-auto p-6`): 뷰 루트가 스크롤 컨테이너이고 제목 블록(366~377)·`CharacterGrid`(380) 가 그 일반 자식이다. `CharacterGrid` 의 헤더 래퍼는 203행 `<div className="flex flex-col gap-2.5">` — 배경 없음(투명). 앱 상단 `Header` 의 제목 매핑에 `character-board` 키가 없어 "캐릭터 현황판" 텍스트는 이 뷰 내부 `<h1>` 이 유일하다. `MainLayout` 의 `<main className="flex-1 overflow-auto p-4">` 가 바깥 컨테이너지만 뷰 루트가 `h-full` 로 채워 실질 스크롤은 뷰 내부에서 일어난다.

## 수정 내용

### 4-1. `src/views/CharacterBoardView.tsx` — `CharacterBoardView` return 블록 교체 (364~392행)

before 는 위 근거의 현행 코드(364행 `<div className="h-full overflow-y-auto p-6">` 루트). after:

```tsx
  return (
    <div className="h-full flex flex-col">
      {/* 제목·탭은 스크롤 밖 고정 영역 — 상단 메뉴가 항상 보인다 (피드백 38). */}
      <div className="px-6 pt-6 flex flex-col gap-3">
        <div>
          <h1 className="text-xl font-bold text-text-primary">캐릭터 현황판</h1>
          <p className="text-sm text-text-secondary mt-0.5">
            {tab === 'board' ? '캐릭터별 복장 디자인·리깅 진행 상황' : '에피소드별 등장 캐릭터·이 편 주의점·복장'}
          </p>
        </div>
        <div className="flex items-center gap-1.5 border-b border-bg-border pb-2">
          <TabButton active={tab === 'board'} onClick={() => setTab('board')}>캐릭터 현황판</TabButton>
          <TabButton active={tab === 'episode-assets'} onClick={() => setTab('episode-assets')}>에피소드 에셋</TabButton>
        </div>
      </div>

      <div className="flex-1 min-h-0 overflow-y-auto px-6 pb-6">
        {tab === 'board' ? (
          <CharacterGrid onAdd={() => setAddOpen(true)} pendingOpenId={pendingOpenId} onConsumeOpen={() => setPendingOpenId(null)} />
        ) : (
          <div className="pt-4">
            <EpisodeAssetBoard onOpenCharacter={(id) => { setTab('board'); setPendingOpenId(id); }} />
          </div>
        )}
      </div>

      {addOpen && (
        <AddCharacterModal
          onClose={() => setAddOpen(false)}
          onCreated={(c) => { setTab('board'); setPendingOpenId(c.id); }}
        />
      )}
    </div>
  );
```

바뀐 것: 루트 `h-full flex flex-col` / 제목+탭 블록이 `px-6 pt-6` 고정 영역으로 / 콘텐츠가 `flex-1 min-h-0 overflow-y-auto px-6 pb-6` 스크롤 컨테이너로(상단 패딩 없음 — 아래 sticky 헤더가 자체 `pt-4` 를 가짐) / 에피소드 에셋 탭은 `pt-4` 래퍼로 기존 여백 보존. `onCreated={…}` 줄은 **원문 그대로**(테스트 앵커).

**미로드 분기 여백 보정:** `CharacterGrid` 의 조기 return 분기 2개(오류 화면 + 로딩 스켈레톤, 170~199행 부근)는 sticky 헤더 없이 렌더되므로 컨테이너 상단 패딩 제거로 여백이 사라진다 — 각 분기의 최상위 요소 className 에 `mt-4` 를 추가한다(스켈레톤은 183행 `grid grid-cols-…` 루트, 오류 분기는 그 위의 루트 요소. 내부 원문은 무수정).

### 4-2. `CharacterGrid` 헤더 래퍼를 sticky 로 (203행)

before:

```tsx
      <div className="flex flex-col gap-2.5">
```

after:

```tsx
      {/* 검색·버튼·필터 칩 — 스크롤해도 상단에 붙는다 (피드백 38). 배경은 토큰 기반이라 라이트/다크 자동 대응. */}
      <div className="sticky top-0 z-20 -mx-6 bg-bg-primary/85 px-6 pt-4 pb-3 backdrop-blur-md flex flex-col gap-2.5">
```

`-mx-6`+`px-6` 는 스크롤 컨테이너의 좌우 패딩(px-6)을 상쇄해 sticky 배경이 콘텐츠 영역 전체 폭을 덮게 한다(카드가 헤더 옆 틈으로 비쳐 보이지 않게). `z-20` 은 카드·삽입선(z-[3])보다 위, 모달(z-50)·컨텍스트 메뉴(z-[80])보다 아래.

### 4-3. 테스트 — `tests/characterFeedback4.test.ts` (작업 3 에서 생성한 파일)에 추가

파일 상단 import 아래에 소스 로드 상수를 추가하고:

```ts
const boardView = readFileSync('src/views/CharacterBoardView.tsx', 'utf8');
```

테스트 블록을 추가한다:

```ts
test('피드백 38: 현황판 상단 메뉴 고정 — 제목·탭 고정 영역 + sticky 헤더', () => {
  assert.match(boardView, /className="h-full flex flex-col"/);
  assert.match(boardView, /flex-1 min-h-0 overflow-y-auto px-6 pb-6/);
  assert.match(boardView, /sticky top-0 z-20 -mx-6 bg-bg-primary\/85 px-6 pt-4 pb-3 backdrop-blur-md flex flex-col gap-2\.5/);
  assert.doesNotMatch(boardView, /"h-full overflow-y-auto p-6"/);
});
```

## 엣지 케이스

- **로딩 스켈레톤/오류 화면:** `CharacterGrid` 의 미로드 분기(177~199행 부근)는 헤더 없이 그리드 자리만 그린다 — sticky 와 무관, 무수정.
- **빈 목록/검색 무결과 화면:** sticky 헤더 아래 그대로 표시 — 검색어 지우기 버튼 접근성 유지.
- **키 비교 보기 모드:** flex 나열도 같은 스크롤 컨테이너 안 — sticky 동일 적용.
- **필터 칩이 없는 경우(`allTags.length === 0`):** 칩 행이 렌더되지 않아 헤더 높이만 줄어든다 — sticky 정상.
- **라이트 모드:** `bg-bg-primary` 는 CSS 변수라 자동 대응. hex 하드코딩 금지.
- **위젯 팝업 창(작업 6 이후):** 같은 컴포넌트가 팝업에서도 렌더 — 팝업 배경(아크릴) 위에서 `bg-bg-primary/85`+블러 는 반투명 헤더로 자연스럽게 겹친다. 실기 확인 항목.

## 검증 방법 / 완료 기준

- [ ] `npm run typecheck` + `npm run test:character` 통과 — 특히 `characterBoardAssetWorkflow`·`characterTier2`·`characterFeedbackBatch`(B4) 의 boardView 앵커 전부 통과(6절 리스크 3 의 보존 줄 확인)
- [ ] 프리뷰: 캐릭터를 스크롤해도 ① 제목+탭 ② 검색·버튼·칩 행이 계속 보임 / 카드가 헤더 뒤로 지나갈 때 비침 없음(블러 배경) / 에피소드 에셋 탭 전환 정상 / 상세 모달·컨텍스트 메뉴가 헤더 위에 뜸
- [ ] 커밋 예: `[v1.92.0] 캐릭터 현황판 상단 메뉴 스크롤 고정 (피드백 38)`

---

# 작업 5 — 항목 40: 보기 방식 3종 (카드 / 이미지 없는 카드 / 리스트)

## 문제/요청 요약

카드 그리드 단일 보기뿐이라 캐릭터가 늘수록 한눈에 보기 어렵다. 헤더에 보기 방식 토글(카드 / 이미지 없는 카드 / 리스트)을 추가하고, 마지막 선택을 localStorage 에 영속화한다(씬 뷰의 `scenesViewPersist` 패턴).

## 근거

- 그리드: `CharacterBoardView.tsx:277`(작업 4 이후에도 동일 줄 존재) — `heightCompareMode ? 'flex flex-wrap items-end gap-4' : 'grid grid-cols-[repeat(auto-fill,minmax(180px,1fr))] gap-4'`.
- 보기 상태 영속 선례: `src/utils/scenesViewPersist.ts`(localStorage 단일 키 + try/catch, 인용 검증됨), `MyTasksWidget.tsx` 의 리스트⇄카드 인라인 토글. 캐릭터 보드에는 영속 키 전무.
- 드래그 재배치 게이트: `CharacterBoardView.tsx:140` `const cardDragEnabled = !showArchived && !heightCompareMode && !query.trim() && activeTags.length === 0;` — 이 원문을 `tests/characterFeedback3.test.ts:79` 가 정규식으로 고정한다(수정 시 동반 갱신 필수).
- 카드: `CharacterCard.tsx` — 이미지 박스(115행 `aspect-[3/4] …`)와 정보부(142~176행)가 분리돼 있어 이미지 박스만 조건부로 빼면 "이미지 없는 카드"가 된다.

## 수정 내용

### 5-1. 신규 `src/utils/characterViewPersist.ts` (전체)

```ts
/**
 * 캐릭터 현황판 보기 방식 영속화 (피드백 40) — scenesViewPersist 와 동일 패턴.
 * localStorage 단일 키. 단일 사용자 PC 환경이라 사용자별 분리 불필요.
 * try/catch 로 localStorage 비활성 환경(테스트·개인정보보호 모드) 무시.
 * 주의: node --test 가 직접 import 한다 — '@/' alias import 금지.
 */

const KEY = 'bflow_character_board_view_mode';

export type CharacterBoardViewMode = 'card' | 'compact' | 'list';

export function loadPersistedCharacterViewMode(): CharacterBoardViewMode | null {
  try {
    const v = localStorage.getItem(KEY);
    return v === 'card' || v === 'compact' || v === 'list' ? v : null;
  } catch {
    return null;
  }
}

export function savePersistedCharacterViewMode(mode: CharacterBoardViewMode): void {
  try { localStorage.setItem(KEY, mode); } catch { /* ignore */ }
}
```

### 5-2. 신규 `src/components/characters/CharacterListRow.tsx` (전체)

```tsx
import { memo, type MouseEvent as ReactMouseEvent } from 'react';
import { Image as ImageIcon } from 'lucide-react';
import type { Character, CharacterCostume } from '@/types';
import { CharacterImageFrame } from '@/components/characters/CharacterImageFrame';
import { DESIGN_STAGE_META, RIGGING_STAGE_META, characterStageColor } from '@/constants/characterStages';

/** 리스트 보기 행 (피드백 40) — 한 줄에 썸네일·이름·복장/EP 수·단계 요약. 클릭/우클릭 동작은 카드와 동일. */
export const CharacterListRow = memo(function CharacterListRow({
  character,
  costumes,
  onOpen,
  onContextMenu,
}: {
  character: Character;
  costumes: CharacterCostume[];
  onOpen: (characterId: string, costumeId?: string) => void;
  onContextMenu: (characterId: string, event: ReactMouseEvent<HTMLButtonElement>, costumeId?: string) => void;
}) {
  const designDone = costumes.filter((c) => c.designStage === 'done').length;
  const riggingDone = costumes.filter((c) => c.riggingStage === 'done').length;
  const shown = costumes.find((c) => c.featuredImageUrl) ?? null;

  return (
    <button
      type="button"
      onClick={() => onOpen(character.id, shown?.id)}
      onContextMenu={(event) => onContextMenu(character.id, event, shown?.id)}
      className="flex w-full items-center gap-3 rounded-lg border border-bg-border bg-bg-card px-3 py-2 text-left transition-colors hover:border-accent/50 cursor-pointer"
    >
      <div className="h-10 w-10 shrink-0 overflow-hidden rounded-md bg-bg-border/30 flex items-center justify-center">
        {shown ? (
          /* fit 은 3:4 크롭 프레임 기준으로 저작된 값이라(CharacterImageFrame 헤더 주석) 1:1 썸네일에는 적용하지 않는다. */
          <CharacterImageFrame
            url={shown.featuredImageUrl}
            alt={character.name}
            background={shown.imageBackground}
            className="w-full h-full"
          />
        ) : (
          <ImageIcon size={16} className="text-text-secondary/40" />
        )}
      </div>
      <div className="min-w-0 flex-1">
        <div className="truncate font-medium text-text-primary">{character.name}</div>
        <div className="text-xs text-text-secondary">
          복장 {costumes.length}
          {character.episodeIds.length > 0 ? ` · EP ${character.episodeIds.length}` : ''}
        </div>
      </div>
      {costumes.length > 0 && (
        <div className="flex shrink-0 items-center gap-1.5 text-[11px]">
          <span
            className="px-1.5 py-0.5 rounded-md"
            style={{
              backgroundColor: characterStageColor(DESIGN_STAGE_META.done, 0.14),
              color: characterStageColor(DESIGN_STAGE_META.done),
            }}
          >
            디자인 {designDone}/{costumes.length}
          </span>
          <span
            className="px-1.5 py-0.5 rounded-md"
            style={{
              backgroundColor: characterStageColor(RIGGING_STAGE_META.done, 0.14),
              color: characterStageColor(RIGGING_STAGE_META.done),
            }}
          >
            리깅 {riggingDone}/{costumes.length}
          </span>
        </div>
      )}
    </button>
  );
});
```

주의: `CharacterImageFrame`·`characterStageColor`·스테이지 메타의 정확한 시그니처는 `CharacterCard.tsx` 원문(115~170행)과 동일하게 썼다 — 컴파일 에러가 나면 `CharacterCard.tsx` 의 실제 호출 형태에 맞출 것. `fit` 은 의도적으로 뺐다(3:4 크롭 기준 값이라 1:1 썸네일에 부적합) — prop 이 필수 타입이면 `fit={undefined}` 가 아니라 optional 여부를 파일에서 확인해 optional 이면 생략 유지.

### 5-3. `src/views/CharacterBoardView.tsx` — `CharacterGrid` 수정 (작업 4 완료 상태 기준)

(a) import 갱신 (13~24행 부근):
- lucide 줄에 아이콘 3종 추가: `import { Archive, Plus, Image as ImageIcon, Search, Ruler, LayoutGrid, Grid3x3, List } from 'lucide-react';`
- 아래 두 줄 추가:

```tsx
import { loadPersistedCharacterViewMode, savePersistedCharacterViewMode, type CharacterBoardViewMode } from '@/utils/characterViewPersist';
import { CharacterListRow } from '@/components/characters/CharacterListRow';
```

(b) 모듈 레벨 상수 추가 — `type BoardTab = …`(26행) 바로 아래:

```tsx
/** 보기 방식 토글 옵션 (피드백 40). */
const VIEW_MODE_OPTIONS: { mode: CharacterBoardViewMode; label: string; Icon: typeof LayoutGrid }[] = [
  { mode: 'card', label: '카드 보기', Icon: LayoutGrid },
  { mode: 'compact', label: '이미지 없는 카드 보기', Icon: Grid3x3 },
  { mode: 'list', label: '리스트 보기', Icon: List },
];
```

(c) 상태 추가 — `const [heightCompareMode, setHeightCompareMode] = useState(false); // '키 비교 보기'(T2-3)` (52행) 바로 아래:

```tsx
  // 보기 방식 (피드백 40) — 마지막 선택을 localStorage 에 영속화(씬 뷰 scenesViewPersist 패턴).
  const [viewMode, setViewMode] = useState<CharacterBoardViewMode>(() => loadPersistedCharacterViewMode() ?? 'card');
  const changeViewMode = (mode: CharacterBoardViewMode) => {
    setViewMode(mode);
    savePersistedCharacterViewMode(mode);
  };
```

(d) 드래그 게이트 교체 (140행) — before:

```tsx
  const cardDragEnabled = !showArchived && !heightCompareMode && !query.trim() && activeTags.length === 0;
```

after (리스트 보기에서는 행 드래그를 지원하지 않는다 — 카드/컴팩트만):

```tsx
  const cardDragEnabled = viewMode !== 'list' && !showArchived && !heightCompareMode && !query.trim() && activeTags.length === 0;
```

(e) 보기 방식 토글 렌더 — 우측 버튼 그룹 시작(209행 `<div className="flex items-center gap-1.5">`) 바로 다음, "키 비교 보기" 버튼 **앞**에 삽입:

```tsx
            {/* 보기 방식 토글 (피드백 40) — 카드 / 이미지 없는 카드 / 리스트. 키 비교 중에는 카드형 고정이라 비활성.
                pointer-events-none 을 쓰면 hover 가 막혀 안내 title 이 영영 안 뜬다 — disabled 로 클릭만 막는다. */}
            <div
              role="group"
              aria-label="보기 방식"
              title={heightCompareMode ? '키 비교 보기 중에는 카드 보기로 고정돼요' : undefined}
              className={cn('flex items-center rounded-lg border border-bg-border p-0.5 shrink-0', heightCompareMode && 'opacity-45')}
            >
              {VIEW_MODE_OPTIONS.map(({ mode, label, Icon }) => (
                <button
                  key={mode}
                  type="button"
                  aria-label={label}
                  title={heightCompareMode ? undefined : label}
                  disabled={heightCompareMode}
                  onClick={() => changeViewMode(mode)}
                  className={cn(
                    'flex h-7 w-7 items-center justify-center rounded-md transition-colors cursor-pointer disabled:cursor-default',
                    viewMode === mode && !heightCompareMode ? 'bg-accent/20 text-accent' : 'text-text-secondary hover:text-text-primary',
                  )}
                >
                  <Icon size={14} />
                </button>
              ))}
            </div>
```

(f) 그리드 렌더 분기 교체 (277~299행) — before 는 6절 리스크 3 의 보존 줄들을 포함한 현행 `displayCharacters.map` 블록. after:

```tsx
        <div className={heightCompareMode
          ? 'flex flex-wrap items-end gap-4'
          : viewMode === 'list'
            ? 'flex flex-col gap-1.5'
            : viewMode === 'compact'
              ? 'grid grid-cols-[repeat(auto-fill,minmax(150px,1fr))] gap-3'
              : 'grid grid-cols-[repeat(auto-fill,minmax(180px,1fr))] gap-4'}>
          {displayCharacters.map((c) => (
            !heightCompareMode && viewMode === 'list' ? (
              <CharacterListRow
                key={c.id}
                character={c}
                costumes={byCharacter.get(c.id) ?? EMPTY_COSTUMES}
                onOpen={openCharacterDetail}
                onContextMenu={openCardContextMenu}
              />
            ) : (
              <CharacterCard
                key={c.id}
                character={c}
                costumes={byCharacter.get(c.id) ?? EMPTY_COSTUMES}
                onOpen={openCharacterDetail}
                onContextMenu={openCardContextMenu}
                compact={!heightCompareMode && viewMode === 'compact'}
                imageHeightPx={heightCompareMode
                  ? (c.referenceHeightPx && maxReferenceHeight
                      ? Math.max(90, Math.min(300, Math.round((300 * c.referenceHeightPx) / maxReferenceHeight)))
                      : 170)
                  : undefined}
                referenceUnset={heightCompareMode && !c.referenceHeightPx}
                onDragStartCard={cardDragEnabled ? handleCardDragStart : undefined}
                onDragOverCard={cardDragEnabled ? handleCardDragOver : undefined}
                onDropCard={cardDragEnabled ? handleCardDrop : undefined}
                onDragEndCard={cardDragEnabled ? handleCardDragEnd : undefined}
                dragging={draggingCardId === c.id}
                dropTarget={dropTargetId === c.id && draggingCardId !== c.id}
                dropEdge={cardDragEnabled && dropTargetId === c.id ? dropEdgeFor(cardOrderIds, draggingCardId, c.id) : null}
              />
            )
          ))}
```

(닫는 `</div>` 이하 기존 구조 유지. `CharacterCard` 의 기존 props 줄들은 원문 그대로 — `compact` 한 줄만 추가된 형태.)

### 5-4. `src/components/characters/CharacterCard.tsx` — `compact` prop

(a) props 타입에 추가 — `referenceUnset?: boolean;` (36행) 아래:

```ts
  /** 이미지 없는 카드 보기 (피드백 40) — 이미지 박스를 생략하고 텍스트 정보만 그린다. */
  compact?: boolean;
```

(b) 구조 분해(14~27행)에 `compact,` 추가 (`referenceUnset,` 다음 줄).

(c) 이미지 박스 조건부 — before(115행 시작):

```tsx
      <div ref={imageRef} style={imageHeightPx ? { height: imageHeightPx } : undefined} className="relative aspect-[3/4] bg-bg-border/30 flex items-center justify-center overflow-hidden rounded-t-xl">
```

after: 이 `<div ref={imageRef} …>` 블록 전체(115~141행, 키 미설정 배지 포함)를 `{!compact && ( … )}` 로 감싼다. **내부 원문(className 문자열 포함)은 한 글자도 바꾸지 말 것** — 테스트 앵커. 들여쓰기만 한 단계 추가:

```tsx
      {!compact && (
        <div ref={imageRef} style={imageHeightPx ? { height: imageHeightPx } : undefined} className="relative aspect-[3/4] bg-bg-border/30 flex items-center justify-center overflow-hidden rounded-t-xl">
          …(원문 그대로)…
        </div>
      )}
```

(d) 휠 순환 effect 의 deps 확장 (61~80행) — compact 에서는 `imageRef` 가 미부착이라 `el = null` 로 무시되는 것까지는 좋지만, **compact → card 로 되돌아올 때 이미지 박스 DOM 이 새로 마운트되는데 effect 는 재실행되지 않아 wheel 리스너가 새 노드에 붙지 않는다**(카드 key 불변이라 리마운트 없음). deps 한 줄만 고친다 — before(80행):

```tsx
  }, [imaged.length]);
```

after:

```tsx
  }, [imaged.length, compact]);
```

(effect 본문·`el.addEventListener('wheel', onWheel, { passive: false })` 앵커는 무수정 — B8 테스트 생존.)

### 5-5. `tests/characterFeedback3.test.ts` 갱신 (79행)

before:

```ts
  assert.match(boardView, /const cardDragEnabled = !showArchived && !heightCompareMode && !query\.trim\(\) && activeTags\.length === 0/);
```

after:

```ts
  // 피드백 40: 리스트 보기에서는 카드 드래그 재배치를 끈다(행 드래그 미지원).
  assert.match(boardView, /const cardDragEnabled = viewMode !== 'list' && !showArchived && !heightCompareMode && !query\.trim\(\) && activeTags\.length === 0/);
```

### 5-6. `tests/characterFeedback4.test.ts` 에 블록 2개 추가

파일 상단 import 에 추가:

```ts
import { loadPersistedCharacterViewMode, savePersistedCharacterViewMode } from '../src/utils/characterViewPersist.ts';
```

블록:

```ts
test('피드백 40: 보기 방식 3종 — 영속화 + 렌더 분기', () => {
  const persist = readFileSync('src/utils/characterViewPersist.ts', 'utf8');
  assert.match(persist, /bflow_character_board_view_mode/);
  // node --test 직접 import 대상 — alias import 금지. (파일 주석의 '@/' 리터럴에 걸리지 않게 import 구문만 잡는다.)
  assert.doesNotMatch(persist, /from '@\//);
  assert.match(boardView, /loadPersistedCharacterViewMode\(\) \?\? 'card'/);
  assert.match(boardView, /savePersistedCharacterViewMode\(mode\)/);
  assert.match(boardView, /aria-label="보기 방식"/);
  assert.match(boardView, /grid-cols-\[repeat\(auto-fill,minmax\(150px,1fr\)\)\]/);
  const row = readFileSync('src/components/characters/CharacterListRow.tsx', 'utf8');
  assert.match(row, /onOpen\(character\.id, shown\?\.id\)/);
  assert.match(row, /onContextMenu\(character\.id, event, shown\?\.id\)/);
  const card = readFileSync('src/components/characters/CharacterCard.tsx', 'utf8');
  assert.match(card, /compact\?: boolean/);
  assert.match(card, /\{!compact && \(/);
});

test('characterViewPersist: node 환경 방어 + 스텁 왕복', () => {
  // node 환경에는 localStorage 가 없다 — load 는 null, save 는 무예외가 계약이다.
  assert.equal(loadPersistedCharacterViewMode(), null);
  assert.doesNotThrow(() => savePersistedCharacterViewMode('list'));
  // localStorage 스텁 주입 후 왕복 + 알 수 없는 값 방어.
  const store = new Map<string, string>();
  (globalThis as Record<string, unknown>).localStorage = {
    getItem: (k: string) => store.get(k) ?? null,
    setItem: (k: string, v: string) => { store.set(k, v); },
  };
  savePersistedCharacterViewMode('compact');
  assert.equal(loadPersistedCharacterViewMode(), 'compact');
  store.set('bflow_character_board_view_mode', '이상한값');
  assert.equal(loadPersistedCharacterViewMode(), null);
  delete (globalThis as Record<string, unknown>).localStorage;
});
```

## 엣지 케이스

- **키 비교 보기와의 상호작용:** `heightCompareMode` 분기가 항상 우선 — 키 비교 중에는 어떤 보기 방식이어도 카드형 나열이며, 토글은 흐려지고 클릭이 막힌다(`pointer-events-none`). 키 비교를 끄면 저장된 보기 방식으로 복귀.
- **드래그 재배치:** 카드/컴팩트에서는 기존대로(필터 없음 조건 하) 동작, 리스트에서는 비활성. 리스트 행 드래그는 범위 외(부록 D).
- **보관 목록/검색 결과:** 보기 방식은 목록 종류와 무관하게 적용된다.
- **localStorage 손상 값:** `'이상한값'` → null → 'card' 폴백 (유닛 테스트로 고정).
- **컴팩트 카드에서 휠 복장 전환:** 이미지가 없으므로 휠 전환 무의미 — `imageRef` 미부착으로 자연 비활성(코드 분기 불필요).
- **B8 카드 휠 앵커:** `characterFeedbackBatch` B8 의 카드 앵커들(`el.addEventListener('wheel'…` 등)은 원문 유지로 통과.

## 검증 방법 / 완료 기준

- [ ] `npm run typecheck` 통과, `npm run test:character` 120 pass (블록 3개 추가: 40 앵커 + persist 유닛 + 38 은 작업 4 에서 이미 +1), fail 0
- [ ] 프리뷰: 토글 3종 전환 — 카드(기존)·컴팩트(이미지 없음, 좁은 그리드)·리스트(행) / 새로고침 후 마지막 보기 방식 유지 / 키 비교 켜면 토글 비활성+카드형 / 리스트 행 클릭·우클릭이 카드와 동일하게 상세·메뉴 오픈
- [ ] 커밋 예: `[v1.92.0] 캐릭터 보기 방식 3종 추가 (피드백 40)`

---

# 작업 6 — 항목 36: 캐릭터 현황판 별도 창(팝업)으로 열기

## 문제/요청 요약

캐릭터 현황판을 별도 창으로 띄워 두고 메인 창에서는 에피소드 화면을 보고 싶다. 기존 **위젯 팝업 시스템을 재사용**한다: 현황판 제목 옆 "새 창으로" 버튼 → `widgetOpenPopup('character-board', …)` IPC → 프레임리스 팝업 창에 현황판 렌더. 세션·Realtime·위치 영속화·재시작 자동 복원은 위젯 팝업 인프라가 이미 제공한다(2절 전제).

## 근거

- `electron/main.ts:4032` `function openWidgetPopup(widgetId, widgetTitle, extra?)` — 첫 오픈 기본 크기 420×360, AOT 기본 켜짐(4042~4044행). 현황판에는 너무 작다/성가시다 → 위젯별 프리셋 필요.
- `src/views/WidgetPopup.tsx:51` `WIDGET_REGISTRY` — 위젯 id → `{ label, component }`. 여기 등록만 하면 해시 라우팅·세션 수신·Toaster·테마가 전부 따라온다. 렌더 지점(852행 부근) `{ready ? widgetMeta.component : …}`.
- `CharacterBoardView` 는 마운트 시 `ensureLoadedAndRealtime()` 로 스스로 데이터를 로드·구독한다(341~344행) — 팝업 쪽 초기화 수정 불필요. 캐릭터 Realtime 브로드캐스트는 `broadcastToAllWindows` 로 위젯 창에도 이미 전달된다.
- 접근 게이팅: 메인 창은 사이드바+App.tsx 가 게이트하지만 팝업 렌더러에는 게이트가 없다 → `useCharacterBoardAccessState()`(fail-closed) 래퍼로 동일 게이트를 건다. (팝업은 재시작 시 자동 복원되므로, 권한이 회수된 사용자의 PC 에 창이 되살아나는 경로까지 막는다.)
- `IsPopupContext`(src/components/widgets/Widget.tsx:9, `createContext(false)`) — `WidgetPopup` 이 value=true 로 감싼다(849행) → 뷰가 "팝업 안" 을 감지해 "새 창으로" 버튼을 숨길 수 있다.
- 프리뷰 mock(`devElectronAPI.ts`)에는 `widgetOpenPopup` 이 **없다**(grep 0건) — 버튼은 `typeof … === 'function'` 가드로 브라우저 프리뷰에서 자동 숨김. 팝업 검증은 실기 항목.
- `window.electronAPI.widgetOpenPopup` 타입은 `src/types/index.ts:1088` 에 optional 로 이미 선언되어 있다 — 타입 수정 불필요.

## 수정 내용

### 6-1. `electron/main.ts` — 위젯별 첫 오픈 프리셋

(a) `function openWidgetPopup(` (4032행) 바로 **위**에 상수 추가:

```ts
// 위젯별 첫 오픈 기본값 — 캐릭터 현황판처럼 화면형 팝업은 위젯 기본(420×360·AOT)이 맞지 않는다 (피드백 36).
const WIDGET_POPUP_DEFAULTS: Record<string, { width: number; height: number; alwaysOnTop?: boolean }> = {
  'character-board': { width: 1160, height: 780, alwaysOnTop: false },
};
```

(b) 기본값 3줄 교체 (4041~4044행) — before:

```ts
  const savedPos = widgetPositionCache.get(widgetId);
  const initWidth = savedPos ? Math.max(280, savedPos.width) : 420;
  const initHeight = savedPos ? Math.max(200, savedPos.height) : 360;
  const initAOT = savedPos ? savedPos.alwaysOnTop : true;
```

after:

```ts
  const savedPos = widgetPositionCache.get(widgetId);
  const preset = WIDGET_POPUP_DEFAULTS[widgetId];
  const initWidth = savedPos ? Math.max(280, savedPos.width) : preset?.width ?? 420;
  const initHeight = savedPos ? Math.max(200, savedPos.height) : preset?.height ?? 360;
  const initAOT = savedPos ? savedPos.alwaysOnTop : preset?.alwaysOnTop ?? true;
```

(저장된 위치가 있으면 항상 저장값 우선 — 프리셋은 최초 1회만 적용. 이하 로직 무수정.)

### 6-2. `src/views/WidgetPopup.tsx` — 게이트 래퍼 + 레지스트리 등록

(a) import 추가 (기존 위젯 import 들 아래) — **정적 import 금지.** `App.tsx:23` 이 `CharacterBoardView` 를 lazy 로 초기 번들에서 제외하고 있으므로, 팝업에서도 같은 패턴을 쓴다(정적 import 는 모든 창의 엔트리 청크에 현황판 의존 트리를 끌어들이고 Vite dynamic/static 혼용 경고를 낸다):

```tsx
import { useCharacterBoardAccessState } from '@/hooks/useCharacterBoardAccess';
```

그리고 react import 줄에 `lazy`, `Suspense` 를 추가하고, 모듈 레벨(WIDGET_REGISTRY 위)에:

```tsx
// 현황판은 App.tsx 와 동일하게 lazy — 팝업 엔트리 청크를 무겁게 하지 않는다 (피드백 36).
const CharacterBoardView = lazy(() => import('@/views/CharacterBoardView'));
```

(b) `WIDGET_REGISTRY` 선언(51행) 바로 **위**에 게이트 컴포넌트 추가:

```tsx
/** 캐릭터 현황판 팝업 본문 (피드백 36) — 메인 창과 동일한 접근 게이팅(fail-closed)을 거친다. */
function CharacterBoardPopupBody() {
  const access = useCharacterBoardAccessState();
  if (!access.allowed) {
    return (
      <div className="flex h-full flex-col items-center justify-center gap-2 p-6 text-center text-sm text-text-secondary">
        {access.loading ? '권한 확인 중...' : '캐릭터 현황판에 접근 권한이 없어요.'}
        {!access.loading && access.error && (
          <button
            type="button"
            onClick={access.retry}
            className="rounded-lg border border-bg-border px-3 py-1.5 text-xs text-text-primary hover:border-accent/50"
          >
            다시 확인
          </button>
        )}
      </div>
    );
  }
  return (
    <Suspense fallback={<div className="flex h-full items-center justify-center text-sm text-text-secondary/50">불러오는 중...</div>}>
      <CharacterBoardView />
    </Suspense>
  );
}
```

(c) 레지스트리에 등록 — `'ep-full-act-progress': …` 줄(69행) 다음에 추가:

```tsx
  'character-board': { label: '캐릭터 현황판', component: <CharacterBoardPopupBody /> },
```

### 6-3. `src/views/CharacterBoardView.tsx` — "새 창으로" 버튼 (작업 4·5 완료 상태 기준)

(a) import 갱신: react import 에 `useContext` 추가, lucide import 에 `ExternalLink` 추가, 그리고:

```tsx
import { IsPopupContext } from '@/components/widgets/Widget';
```

(b) `CharacterBoardView` 컴포넌트 본문 — `const [pendingOpenId, …]`(339행 부근) 아래에 추가:

```tsx
  // 팝업 창 안에서는 "새 창으로" 버튼을 숨긴다 (피드백 36 — 팝업이 팝업을 또 열지 않게).
  const isPopup = useContext(IsPopupContext);
```

(c) 작업 4 에서 만든 제목 블록을 다음으로 교체 (제목·부제 원문 유지, 우측 버튼 추가):

```tsx
      <div className="px-6 pt-6 flex flex-col gap-3">
        <div className="flex items-start justify-between gap-3">
          <div>
            <h1 className="text-xl font-bold text-text-primary">캐릭터 현황판</h1>
            <p className="text-sm text-text-secondary mt-0.5">
              {tab === 'board' ? '캐릭터별 복장 디자인·리깅 진행 상황' : '에피소드별 등장 캐릭터·이 편 주의점·복장'}
            </p>
          </div>
          {!isPopup && typeof window.electronAPI?.widgetOpenPopup === 'function' && (
            <button
              type="button"
              onClick={() => { void window.electronAPI?.widgetOpenPopup?.('character-board', '캐릭터 현황판'); }}
              title="캐릭터 현황판을 별도 창으로 열어요 — 다른 화면을 보면서 같이 쓸 수 있어요"
              className="flex items-center gap-1.5 rounded-lg border border-bg-border px-3 py-2 text-sm text-text-secondary hover:border-text-secondary/40 hover:text-text-primary shrink-0 cursor-pointer"
            >
              <ExternalLink size={15} /> 새 창으로
            </button>
          )}
        </div>
        <div className="flex items-center gap-1.5 border-b border-bg-border pb-2">
          <TabButton active={tab === 'board'} onClick={() => setTab('board')}>캐릭터 현황판</TabButton>
          <TabButton active={tab === 'episode-assets'} onClick={() => setTab('episode-assets')}>에피소드 에셋</TabButton>
        </div>
      </div>
```

### 6-4. `tests/characterFeedback4.test.ts` 에 블록 추가

```ts
test('피드백 36: 캐릭터 현황판 팝업 창 — 프리셋 + 게이트 + 버튼', () => {
  const main = readFileSync('electron/main.ts', 'utf8');
  assert.match(main, /const WIDGET_POPUP_DEFAULTS/);
  assert.match(main, /'character-board': \{ width: 1160, height: 780, alwaysOnTop: false \}/);
  assert.match(main, /preset\?\.width \?\? 420/);
  assert.match(main, /preset\?\.alwaysOnTop \?\? true/);
  const popup = readFileSync('src/views/WidgetPopup.tsx', 'utf8');
  assert.match(popup, /function CharacterBoardPopupBody/);
  assert.match(popup, /useCharacterBoardAccessState/);
  assert.match(popup, /'character-board': \{ label: '캐릭터 현황판', component: <CharacterBoardPopupBody \/> \}/);
  assert.match(boardView, /widgetOpenPopup\?\.\('character-board', '캐릭터 현황판'\)/);
  assert.match(boardView, /새 창으로/);
  assert.match(boardView, /useContext\(IsPopupContext\)/);
});
```

## 엣지 케이스

- **이미 열린 팝업에서 다시 열기:** `openWidgetPopup` 이 기존 창 focus 로 처리(4034~4038행, 무수정).
- **팝업 안에서 버튼 숨김:** `IsPopupContext` 로 처리 — 팝업이 팝업을 못 연다.
- **브라우저 프리뷰:** mock 에 `widgetOpenPopup` 이 없어 버튼 자체가 숨는다 — 나머지 현황판 기능 프리뷰 검증에는 지장 없음.
- **권한 회수 후 재시작 자동 복원:** 게이트 래퍼가 fail-closed 로 차단 화면을 그린다. 창 자체를 강제로 닫지는 않는다(사용자가 닫으면 `isOpen=false` 로 기록되어 다음 재시작에 안 살아난다) — 과잉 설계 방지.
- **팝업에서 상세 모달·라이트박스:** `fixed inset-0` 오버레이는 팝업 창 기준으로 뜬다 — 정상. 실기 확인 항목.
- **팝업과 메인 창 동시 편집:** 각 창이 독립 store + Realtime 수신이라 기존 다중 사용자 동기화와 동일 경로로 수렴(LWW).

## 검증 방법 / 완료 기준

- [ ] `npm run typecheck` + `npm run test:character` 121 pass + `npm run test:ui` 통과
- [ ] `npm run build:vite` 통과
- [ ] 실기(Electron): "새 창으로" 클릭 → 1160×780 팝업, AOT 꺼짐 / 팝업에서 카드·상세·검색·보기 방식 동작 / 메인 창에서 단계 토글 시 팝업에 ~수초 내 반영(Realtime) / 팝업 안에 "새 창으로" 버튼 없음 / 앱 재시작 시 팝업 자동 복원
- [ ] 커밋 예: `[v1.92.0] 캐릭터 현황판 별도 창으로 열기 (피드백 36)`

---

# 작업 7 — PR A 버전·업데이트 노트

## 수정 내용

1. `package.json` `"version"` 을 `1.91.1` → `1.92.0` (구현 착수 시점 origin/main 버전 재확인 정책 — 4절).
2. `DEVLOG/update-notes.json` 배열 **맨 앞**에 추가 (비개발자 톤 — 기술 용어·식별자·파일명 금지):

```json
{
  "version": "1.92.0",
  "title": "캐릭터 현황판이 새 창으로 열리고, 보기 방식도 골라요",
  "items": [
    {
      "category": "feature",
      "summary": "캐릭터 현황판을 별도 창으로 띄울 수 있어요",
      "description": "현황판 제목 옆 '새 창으로' 버튼을 누르면 캐릭터 현황판이 따로 창으로 열려요. 본 화면에서는 에피소드를 보면서, 옆 창으로 캐릭터를 같이 확인할 수 있어요. 창 위치와 크기는 기억되고, 앱을 다시 켜면 열어뒀던 창도 그대로 돌아와요."
    },
    {
      "category": "feature",
      "summary": "캐릭터를 카드·간단 카드·리스트로 바꿔 볼 수 있어요",
      "description": "캐릭터가 많아지면 큰 카드로는 한눈에 보기 어려웠는데, 이제 이미지 없는 간단 카드나 한 줄 리스트로 바꿔 볼 수 있어요. 마지막으로 고른 보기 방식은 다음에 열 때도 유지돼요."
    },
    {
      "category": "ux",
      "summary": "현황판을 스크롤해도 검색과 버튼이 따라와요",
      "description": "아래로 한참 내려가면 검색창이나 필터를 쓰려고 다시 맨 위로 올라가야 했는데, 이제 상단 메뉴가 화면 위에 붙어 있어서 언제든 바로 쓸 수 있어요."
    },
    {
      "category": "ux",
      "summary": "담당자를 목록에서 골라 지정할 수 있어요",
      "description": "디자인·리깅 담당자를 매번 이름으로 직접 입력해야 했는데, 이제 씬 작업자 지정처럼 팀원 목록이 떠서 클릭 한 번으로 고를 수 있어요. 목록에 없는 이름은 예전처럼 직접 입력도 돼요."
    },
    {
      "category": "bugfix",
      "summary": "리깅 완료 공지의 제목이 두 번 겹쳐 나오던 문제를 잡았어요",
      "description": "슬랙 공지 제목에 같은 머리말이 두 번 붙어 나오던 현상을 고쳤어요. 이제 내용을 따로 적지 않아도 캐릭터 이름과 복장 이름, 리깅완료 문구까지 자동으로 채워져서 버튼 한 번으로 깔끔한 공지가 나가요."
    },
    {
      "category": "ux",
      "summary": "사이드 메뉴의 캐릭터 + 버튼을 정리했어요",
      "description": "쓰임이 겹쳐 헷갈리던 사이드 메뉴의 작은 + 버튼을 없앴어요. 캐릭터 추가는 현황판의 '캐릭터 추가' 버튼과 상세 화면의 '+ 캐릭터 추가'에서 그대로 할 수 있어요."
    }
  ]
}
```

각 항목은 `category`/`summary`/`description` 3키만 갖는다(기존 스키마 준수).

3. PR A 생성: 제목 `[v1.92.0] 캐릭터 현황판 개편 — 새 창·보기 방식·상단 고정·담당자 목록 + 공지 문구 (피드백 34·35·36·38·39·40)`. PR 본문의 `📋 업데이트 요약` 섹션은 update-notes 와 같은 비개발자 톤, 상세 기술 설명 섹션은 개발자 톤.

## 완료 기준

- [ ] `npm run build:vite` 전체 통과 (typecheck + 전 테스트 스위트 + vite build)
- [ ] update-notes 항목이 비개발자 톤 규칙(2절) 준수 — 식별자·파일명·기술용어 0건
- [ ] 커밋 예: `[v1.92.0] 버전·업데이트 노트 (피드백 34~40 배치)`

---

# PR B (버전 1.93.0 가정, 브랜치 `feat/feedback-41-tabs`, **PR A 머지 후 main 에서 분기**)

# 작업 8 — 항목 41 (1/2): 탭·그룹 데이터 계층

## 설계 결정 (근거 포함 — 실행 에이전트는 이 결정을 따를 것)

- **모델: `character_board_tabs` 단일 테이블 + `groups` JSONB.** 탭 1 row, 그룹은 row 안의 배열 `[{ id, name, characterIds[] }]`. 정규화(그룹/멤버 별도 테이블 3개) 대안을 검토했으나 기각 — 이 기능의 편집 주체는 사실상 리깅 담당 1인("대부분 제가 관리하게 되겠지만")이고, 앱의 전역 충돌 전략이 이미 LWW 다. 탭 단위 LWW 는 그 전제 안에서 충분하며, IPC·Realtime·롤백 표면적을 1/3로 줄여 구현 리스크를 크게 낮춘다.
- **그룹 배열 순서 = 표시 순서, `characterIds` 배열 순서 = 그룹 내 카드 순서.** 별도 sort 컬럼 불필요.
- **캐릭터 삭제와의 정합:** 그룹의 `characterIds` 에 남은 죽은 id 는 **렌더 시점에 존재하는 캐릭터만 그리는 방식으로 무해화**한다(작업 9). DB 측 cascade 정리는 후속(부록 D).
- **탭이 없는 팀원에게는 아무 변화도 없다** — '전체' 탭이 기존 화면 그대로다.

## 수정 내용

### 8-1. 신규 마이그레이션 `DEVLOG/migrations/2026-07-26-character-board-tabs.sql` (전체)

기존 캐릭터 테이블 마이그레이션(`2026-07-08-character-costume-images.sql`)과 동일한 보일러플레이트 패턴(RLS allow_all DO 가드 + publication 가드 + `set_updated_at()` 트리거 + `created_by TEXT REFERENCES users(id)` — **users.id 는 TEXT 타입**이다):

```sql
-- 캐릭터 현황판 사용자 정의 탭·그룹 (피드백 41).
-- 탭 1 row = 사용자 정의 탭 1개. 그룹은 groups JSONB 배열([{id,name,characterIds}])로 탭 단위 LWW 편집.
-- 추가 전용(DROP 없음). 라이브 DB(mpqifkpxalwxgcrddchv)에 코드 PR 머지 전 먼저 적용.

CREATE TABLE IF NOT EXISTS character_board_tabs (
  id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name       TEXT NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  -- 그룹 배열: [{ "id": "uuid", "name": "그룹명", "characterIds": ["캐릭터 uuid", ...] }]
  groups     JSONB NOT NULL DEFAULT '[]',
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  created_by TEXT REFERENCES users(id) ON DELETE SET NULL
);

CREATE INDEX IF NOT EXISTS idx_character_board_tabs_order ON character_board_tabs(sort_order);

DROP TRIGGER IF EXISTS trg_character_board_tabs_updated_at ON character_board_tabs;
CREATE TRIGGER trg_character_board_tabs_updated_at
  BEFORE UPDATE ON character_board_tabs
  FOR EACH ROW EXECUTE FUNCTION set_updated_at();

ALTER TABLE character_board_tabs ENABLE ROW LEVEL SECURITY;
DO $$ BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_policies WHERE tablename='character_board_tabs' AND policyname='allow_all') THEN
    CREATE POLICY "allow_all" ON character_board_tabs FOR ALL USING (true) WITH CHECK (true);
  END IF;
END $$;

DO $$ BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_publication_tables WHERE pubname='supabase_realtime' AND tablename='character_board_tabs') THEN
    ALTER PUBLICATION supabase_realtime ADD TABLE character_board_tabs;
  END IF;
END $$;

NOTIFY pgrst, 'reload schema';
```

### 8-2. 신규 `src/utils/characterTabGroups.ts` (전체, alias-free)

`createUuid` 는 `src/utils/createUuid.ts` 에 이미 존재하며 alias-free 로 검증됨(`tests/createUuid.test.ts`) — **상대 경로**로 import 한다. (export 명이 `createUuid` 인지 파일에서 확인 후 사용.)

```ts
/**
 * 캐릭터 현황판 탭·그룹 순수 헬퍼 (피드백 41).
 * character_board_tabs.groups JSONB 배열을 다루는 불변 갱신 함수 모음.
 * 주의: node --test 가 직접 import 한다 — '@/' alias import 금지(상대 경로만).
 */
import { createUuid } from './createUuid';

export interface TabGroupLike {
  id: string;
  name: string;
  /** 배열 순서 = 그룹 내 카드 표시 순서. */
  characterIds: string[];
}

/** JSONB 원시값 → 안전한 그룹 배열. 형식이 어긋난 원소는 버린다(수동 편집·구버전 방어). 중복 id 는 첫 등장만 유지. */
export function sanitizeTabGroups(raw: unknown): TabGroupLike[] {
  if (!Array.isArray(raw)) return [];
  const out: TabGroupLike[] = [];
  for (const item of raw) {
    if (!item || typeof item !== 'object') continue;
    const rec = item as Record<string, unknown>;
    if (typeof rec.id !== 'string' || typeof rec.name !== 'string') continue;
    const ids = Array.isArray(rec.characterIds)
      ? rec.characterIds.filter((v): v is string => typeof v === 'string')
      : [];
    out.push({ id: rec.id, name: rec.name, characterIds: Array.from(new Set(ids)) });
  }
  return out;
}

/** 그룹 추가 — 새 그룹은 맨 뒤. */
export function addGroup(groups: TabGroupLike[], name: string): TabGroupLike[] {
  return [...groups, { id: createUuid(), name, characterIds: [] }];
}

export function renameGroup(groups: TabGroupLike[], groupId: string, name: string): TabGroupLike[] {
  return groups.map((g) => (g.id === groupId ? { ...g, name } : g));
}

/** 그룹 삭제 — 소속 캐릭터는 미분류로 돌아간다(멤버십만 사라짐). */
export function removeGroup(groups: TabGroupLike[], groupId: string): TabGroupLike[] {
  return groups.filter((g) => g.id !== groupId);
}

/**
 * 캐릭터를 그룹으로 이동. targetGroupId=null 이면 모든 그룹에서 제거(미분류로).
 * 항상 기존 그룹들에서 먼저 제거한 뒤 대상 그룹 맨 뒤에 추가한다.
 */
export function moveCharacterToGroup(groups: TabGroupLike[], characterId: string, targetGroupId: string | null): TabGroupLike[] {
  const stripped = groups.map((g) =>
    g.characterIds.includes(characterId)
      ? { ...g, characterIds: g.characterIds.filter((id) => id !== characterId) }
      : g,
  );
  if (targetGroupId === null) return stripped;
  return stripped.map((g) => (g.id === targetGroupId ? { ...g, characterIds: [...g.characterIds, characterId] } : g));
}

/** 같은 그룹 안에서 characterId 를 targetCharacterId 앞으로 이동 — 카드 드래그 순서 조정. 둘 중 하나라도 없으면 무변경. */
export function reorderWithinGroup(groups: TabGroupLike[], groupId: string, characterId: string, targetCharacterId: string): TabGroupLike[] {
  return groups.map((g) => {
    if (g.id !== groupId) return g;
    if (characterId === targetCharacterId) return g;
    if (!g.characterIds.includes(characterId) || !g.characterIds.includes(targetCharacterId)) return g;
    const without = g.characterIds.filter((id) => id !== characterId);
    const idx = without.indexOf(targetCharacterId);
    return { ...g, characterIds: [...without.slice(0, idx), characterId, ...without.slice(idx)] };
  });
}

/** 그룹에 배치된 캐릭터 id 전체 집합 — 미분류 섹션 계산용. */
export function groupedCharacterIdSet(groups: TabGroupLike[]): Set<string> {
  const set = new Set<string>();
  for (const g of groups) for (const id of g.characterIds) set.add(id);
  return set;
}
```

### 8-3. `src/types/index.ts` — 도메인/row 타입 + ElectronAPI 시그니처

(a) `Character` 인터페이스(182~195행) 아래에 추가:

```ts
/** 캐릭터 현황판 사용자 정의 탭의 그룹 1개 — 탭 row 의 groups JSONB 배열 원소 (피드백 41). */
export interface CharacterBoardTabGroup {
  id: string;
  name: string;
  /** 배열 순서 = 그룹 내 카드 표시 순서. */
  characterIds: string[];
}

/** 캐릭터 현황판 사용자 정의 탭 (피드백 41). 그룹 편집은 탭 row 단위 LWW. */
export interface CharacterBoardTab {
  id: string;
  name: string;
  sortOrder: number;
  groups: CharacterBoardTabGroup[];
  createdAt: string;
  updatedAt: string;
}
```

(b) DB row 타입 — `CharacterCostumeRow`(342행 부근) 근처에 추가:

```ts
/** character_board_tabs DB row (피드백 41). */
export interface CharacterBoardTabRow {
  id: string;
  name: string;
  sort_order: number | null;
  groups: unknown;
  created_at: string;
  updated_at: string;
  created_by?: string | null;
}
```

(c) `ElectronAPI` 인터페이스 — `supabaseSetPrimaryCostumeImage` 시그니처(앵커 검색) 근처에 추가:

```ts
  // ─── 캐릭터 현황판 탭·그룹 (피드백 41) ───
  supabaseLoadCharacterBoardTabs: () => Promise<any[]>;
  supabaseAddCharacterBoardTab: (input: { name: string; sortOrder: number; createdBy?: string | null }) => Promise<any>;
  supabaseUpdateCharacterBoardTab: (id: string, updates: Record<string, unknown>) => Promise<any>;
  supabaseDeleteCharacterBoardTab: (id: string) => Promise<{ ok: boolean }>;
```

(기존 캐릭터 IPC 시그니처들(1542~1546행 부근 `supabaseLoadCharacters` 등)이 `Promise<any[]>`/`Promise<any>` 표기를 쓰므로 그에 맞췄다 — 서비스 계층에서 `as CharacterBoardTabRow[]` 로 좁힌다.)

### 8-4. `electron/supabase.ts` — DB 함수 4개 + Realtime 테이블 추가

(a) 캐릭터 함수 구역(앵커 `sbLoadCharacters` 또는 `loadCharacters` — 실제 export 명은 파일에서 확인, main.ts 는 `sbLoadCharacters` 별칭으로 import)에 **인접 함수와 동일한 스타일**로 4개 추가. 의미 명세(스타일은 인접 함수를 그대로 따를 것):

```ts
/** 캐릭터 현황판 탭 전체 로드 — sort_order 오름차순. (탭은 소수라 페이지네이션 불필요.) */
export async function loadCharacterBoardTabs(): Promise<Record<string, unknown>[]> {
  const { data, error } = await supabase
    .from('character_board_tabs')
    .select('*')
    .order('sort_order', { ascending: true })
    .order('created_at', { ascending: true });
  if (error) throw error;
  return (data ?? []) as Record<string, unknown>[];
}

/** 탭 추가 — 생성된 row 반환. */
export async function addCharacterBoardTab(input: { name: string; sortOrder: number; createdBy?: string | null }): Promise<Record<string, unknown>> {
  const { data, error } = await supabase
    .from('character_board_tabs')
    .insert({ name: input.name, sort_order: input.sortOrder, created_by: input.createdBy ?? null })
    .select()
    .single();
  if (error) throw error;
  return data as Record<string, unknown>;
}

/** 탭 부분 수정 (name / sort_order / groups). */
export async function updateCharacterBoardTab(id: string, updates: Record<string, unknown>): Promise<Record<string, unknown> | null> {
  const { data, error } = await supabase
    .from('character_board_tabs')
    .update(updates)
    .eq('id', id)
    .select()
    .maybeSingle();
  if (error) throw error;
  return (data ?? null) as Record<string, unknown> | null;
}

export async function deleteCharacterBoardTab(id: string): Promise<{ ok: boolean }> {
  const { error } = await supabase.from('character_board_tabs').delete().eq('id', id);
  if (error) throw error;
  return { ok: true };
}
```

(b) `startCharacterBoardRealtime`(4498행) — 3곳에 `'character_board_tabs'` 추가:

- broadcast 파라미터 타입 union(4500행): `'characters' | 'character_costumes' | 'character_costume_images' | 'episode_character_mapping' | 'character_board_tabs'`
- tables 배열(4508행) — before: `const tables = ['characters', 'character_costumes', 'character_costume_images', 'episode_character_mapping'] as const;` → after: `const tables = ['characters', 'character_costumes', 'character_costume_images', 'episode_character_mapping', 'character_board_tabs'] as const;`
- `enrichEpisodeCharacterMappingPayload` 의 `table` 파라미터 union(4575행)에도 동일 추가(이 함수는 mapping 외 테이블은 그대로 통과시키므로 로직 무수정).

(c) **렌더러 쪽 같은 union 2곳도 함께 확장한다** (빠뜨리면 컴파일은 되지만 탭 이벤트가 "타입상 불가능한 값"으로 store 에 도달한다):

- `src/services/supabaseService.ts:1096-1101` — `subscribeCharacterBoardRealtime` 의 payload `table` union 에 `| 'character_board_tabs'` 추가.
- `src/types/index.ts:1576-1581` — `onCharacterBoardRealtime` 콜백 payload 의 `table` union 에 `| 'character_board_tabs'` 추가.

### 8-5. `electron/main.ts` — IPC 핸들러 4개

`supabase:set-primary-costume-image` 핸들러 블록(앵커 검색) 바로 뒤에 추가하고, 파일 상단 `./supabase` import 목록에 4개 함수(별칭 규칙은 기존 `sbLoadCharacters` 스타일)를 추가:

```ts
// ─── IPC 핸들러: 캐릭터 현황판 탭·그룹 (피드백 41) ──────────────

ipcMain.handle('supabase:load-character-board-tabs', wrapIpc(async () => {
  return sbLoadCharacterBoardTabs();
}));

ipcMain.handle('supabase:add-character-board-tab', wrapIpc(async (
  _e: unknown,
  input: { name: string; sortOrder: number; createdBy?: string | null },
) => {
  return sbAddCharacterBoardTab(input);
}));

ipcMain.handle('supabase:update-character-board-tab', wrapIpc(async (
  _e: unknown,
  id: string,
  updates: Record<string, unknown>,
) => {
  return sbUpdateCharacterBoardTab(id, updates);
}));

ipcMain.handle('supabase:delete-character-board-tab', wrapIpc(async (
  _e: unknown,
  id: string,
) => {
  return sbDeleteCharacterBoardTab(id);
}));
```

### 8-6. `electron/preload.ts` — expose 4개

`supabaseSetPrimaryCostumeImage`(750~751행) 바로 뒤에 추가:

```ts
  // ─── 캐릭터 현황판 탭·그룹 (피드백 41) ───
  supabaseLoadCharacterBoardTabs: () => ipcRenderer.invoke('supabase:load-character-board-tabs'),
  supabaseAddCharacterBoardTab: (input: { name: string; sortOrder: number; createdBy?: string | null }) =>
    ipcRenderer.invoke('supabase:add-character-board-tab', input),
  supabaseUpdateCharacterBoardTab: (id: string, updates: Record<string, unknown>) =>
    ipcRenderer.invoke('supabase:update-character-board-tab', id, updates),
  supabaseDeleteCharacterBoardTab: (id: string) =>
    ipcRenderer.invoke('supabase:delete-character-board-tab', id),
```

### 8-7. `src/services/supabaseService.ts` — row 변환 + 래퍼 4개

캐릭터 섹션(`rowToCostumeImage`/`loadCharacterCostumeImages` 부근, 991~1060행)에 추가. `sanitizeTabGroups` 는 `@/utils/characterTabGroups` 에서 import:

```ts
// ─── 캐릭터 현황판 탭·그룹 (피드백 41) ───

export function rowToCharacterBoardTab(row: CharacterBoardTabRow): CharacterBoardTab {
  return {
    id: row.id,
    name: row.name,
    sortOrder: row.sort_order ?? 0,
    groups: sanitizeTabGroups(row.groups),
    createdAt: row.created_at,
    updatedAt: row.updated_at,
  };
}

export async function loadCharacterBoardTabs(): Promise<CharacterBoardTab[]> {
  const rows = (await window.electronAPI.supabaseLoadCharacterBoardTabs()) as CharacterBoardTabRow[];
  return rows.map(rowToCharacterBoardTab);
}

export async function addCharacterBoardTab(input: { name: string; sortOrder: number; createdBy?: string | null }): Promise<CharacterBoardTab> {
  const row = (await window.electronAPI.supabaseAddCharacterBoardTab(input)) as CharacterBoardTabRow;
  return rowToCharacterBoardTab(row);
}

export async function updateCharacterBoardTab(
  id: string,
  updates: Partial<Pick<CharacterBoardTab, 'name' | 'sortOrder' | 'groups'>>,
): Promise<void> {
  const snake: Record<string, unknown> = {};
  if (updates.name !== undefined) snake.name = updates.name;
  if (updates.sortOrder !== undefined) snake.sort_order = updates.sortOrder;
  if (updates.groups !== undefined) snake.groups = updates.groups;
  await window.electronAPI.supabaseUpdateCharacterBoardTab(id, snake);
}

export async function deleteCharacterBoardTab(id: string): Promise<void> {
  await window.electronAPI.supabaseDeleteCharacterBoardTab(id);
}
```

(타입 import 줄에 `CharacterBoardTab`, `CharacterBoardTabRow` 추가.)

### 8-8. `src/mocks/devElectronAPI.ts` — mock 4개

캐릭터 mock 구역(1481행 `// ─── 캐릭터 현황판 (mock) ───` 이하)에 인접 스타일대로 추가. `createUuid` 는 이 파일에서 이미 사용 중:

```ts
    // ─── 캐릭터 현황판 탭·그룹 (mock, 피드백 41) ───
    supabaseLoadCharacterBoardTabs: async () => {
      const store = (localStore.__characterTabs as Record<string, unknown>[] | undefined)
        ?? (localStore.__characterTabs = [] as Record<string, unknown>[]);
      return store as unknown[];
    },
    supabaseAddCharacterBoardTab: async (input: { name: string; sortOrder: number; createdBy?: string | null }) => {
      const store = (localStore.__characterTabs as Record<string, unknown>[] | undefined)
        ?? (localStore.__characterTabs = [] as Record<string, unknown>[]);
      const now = new Date().toISOString();
      const row = {
        id: createUuid(), name: input.name, sort_order: input.sortOrder, groups: [],
        created_by: input.createdBy ?? null, created_at: now, updated_at: now,
      };
      store.push(row);
      return row;
    },
    supabaseUpdateCharacterBoardTab: async (id: string, updates: Record<string, unknown>) => {
      const store = (localStore.__characterTabs as Record<string, unknown>[] | undefined) ?? [];
      const row = store.find((r) => r.id === id) ?? null;
      if (row) Object.assign(row, updates, { updated_at: new Date().toISOString() });
      return row;
    },
    supabaseDeleteCharacterBoardTab: async (id: string) => {
      const store = (localStore.__characterTabs as Record<string, unknown>[] | undefined) ?? [];
      const idx = store.findIndex((r) => r.id === id);
      if (idx >= 0) store.splice(idx, 1);
      return { ok: true };
    },
```

### 8-9. `src/stores/useCharacterBoardStore.ts` — 상태·로드·Realtime·액션

(a) import 확장: `@/services/supabaseService` import 목록에 `loadCharacterBoardTabs as svcLoadTabs, addCharacterBoardTab as svcAddTab, updateCharacterBoardTab as svcUpdateTab, deleteCharacterBoardTab as svcDeleteTab, rowToCharacterBoardTab` 추가(기존 별칭 규칙 `svcLoadCharacters` 스타일). `@/types` import 에 `CharacterBoardTab`, `CharacterBoardTabGroup`, `CharacterBoardTabRow` 추가.

(b) pending 버킷(83~86행) 아래에 추가:

```ts
const pendingTabFields = new Map<string, Map<string, PendingLocalField>>();
```

(c) 정렬 헬퍼 — pending 버킷 근처 모듈 레벨에 추가:

```ts
/** 탭 정렬: sortOrder → createdAt → id (reorderCharacters 계열과 동일한 tie-break 정신). */
function sortTabs(tabs: CharacterBoardTab[]): CharacterBoardTab[] {
  return tabs.slice().sort((a, b) =>
    a.sortOrder - b.sortOrder || a.createdAt.localeCompare(b.createdAt) || a.id.localeCompare(b.id));
}
```

(d) `CharacterBoardStore` 인터페이스: 상태 `episodeLinks: …`(107행) 아래에 `tabs: CharacterBoardTab[];` 추가. 액션 — `reorderCharacters` 선언(154행) 아래에:

```ts
  // ─── 사용자 정의 탭·그룹 (피드백 41) ───
  addTab: (name: string) => Promise<CharacterBoardTab | null>;
  renameTab: (id: string, name: string) => Promise<void>;
  deleteTab: (id: string) => Promise<void>;
  /** 탭의 그룹 배열 교체 — 그룹 CRUD·멤버 이동이 전부 이 액션으로 수렴(탭 단위 LWW). */
  updateTabGroups: (id: string, groups: CharacterBoardTabGroup[]) => Promise<void>;
```

그리고 `receiveRealtime` 의 `table` union(185행)에 `| 'character_board_tabs'` 추가.

(e) 초기 상태(195~204행)에 `tabs: [],` 추가.

(f) `load`(206~269행): `Promise.all` 을 5개로 확장하고 pending 병합·set 에 탭 반영 — before 의 210~215행:

```ts
      const [characters, costumes, mappings, costumeImages] = await Promise.all([
        svcLoadCharacters(),
        svcLoadCostumes(),
        svcLoadMap(),
        svcLoadCostumeImages(),
      ]);
```

after:

```ts
      const [characters, costumes, mappings, costumeImages, tabs] = await Promise.all([
        svcLoadCharacters(),
        svcLoadCostumes(),
        svcLoadMap(),
        svcLoadCostumeImages(),
        svcLoadTabs(),
      ]);
```

`const prevCostumeImageById = …`(228행) 아래에:

```ts
      const prevTabById = new Map(prev.tabs.map((t) => [t.id, t]));
```

`const freshLinks = buildEpisodeLinks(mappings);`(242행) 위에:

```ts
      const sortedTabs = sortTabs(tabs.map((t) => mergeIncomingWithPending(pendingTabFields, prevTabById.get(t.id), t)));
```

그리고 `set({ … })`(253행)에 `tabs: sortedTabs,` 추가(`episodeLinks: freshLinks,` 다음 줄).

(g) 낙관 갱신 헬퍼 — **모듈 레벨 함수**로 추가한다. 기존 `applyCharacterUpdate`(1021행 부근)는 create 콜백 안이 아니라 **모듈 레벨에서 `set`/`get` 을 파라미터로 받는 함수**이고(호출부가 `applyCharacterUpdate(set, get, id, …)` 형태), create 콜백은 객체 리터럴 직접 반환이라 내부에 `const` 를 둘 수 없다 — 반드시 같은 방식(파라미터 주입 + **비함수형 set**)을 따른다. 롤백 규칙은 기존과 동일("이 업데이트가 바꾼 필드만, 아직 낙관값 그대로일 때만"). `applyCharacterUpdate` 정의 근처에 추가:

```ts
/** 탭 낙관 갱신 공통 — 즉시 반영 + pending 추적 + 실패 시 조건부 롤백 (applyCharacterUpdate 미러, 피드백 41). */
async function applyTabUpdate(
  set: (partial: Partial<CharacterBoardStore>) => void,
  get: () => CharacterBoardStore,
  id: string,
  updates: Partial<Pick<CharacterBoardTab, 'name' | 'sortOrder' | 'groups'>>,
  failMsg: string,
): Promise<void> {
  const prevTab = get().tabs.find((t) => t.id === id) ?? null;
  trackPendingFields(pendingTabFields, id, updates as Record<string, unknown>);
  set({ tabs: sortTabs(get().tabs.map((t) => (t.id === id ? { ...t, ...updates } : t))) });
  try {
    await svcUpdateTab(id, updates);
  } catch (err) {
    console.error('[character-board] 탭 저장 실패:', err);
    if (prevTab) {
      const updRec = updates as unknown as Record<string, unknown>;
      const prevRec = prevTab as unknown as Record<string, unknown>;
      const reverted = get().tabs.map((t) => {
        if (t.id !== id) return t;
        const curRec = t as unknown as Record<string, unknown>;
        const restored: Record<string, unknown> = { ...curRec };
        for (const k of Object.keys(updates)) {
          if (curRec[k] === updRec[k]) restored[k] = prevRec[k];
        }
        return restored as unknown as CharacterBoardTab;
      });
      set({ tabs: sortTabs(reverted) });
    }
    toast.error(failMsg);
  }
}
```

(파라미터 `set` 의 정확한 타입 표기는 기존 `applyCharacterUpdate` 의 시그니처를 그대로 미러할 것.)

(h) 액션 구현 — `reorderCharacters` 구현부 뒤에 추가:

```ts
  // ─── 사용자 정의 탭·그룹 (피드백 41) ───

  addTab: async (name) => {
    const trimmed = name.trim();
    if (!trimmed) return null;
    const createdBy = useAuthStore.getState().currentUser?.id ?? null;
    const sortOrder = get().tabs.reduce((m, t) => Math.max(m, t.sortOrder + 1), 0);
    try {
      const created = await svcAddTab({ name: trimmed, sortOrder, createdBy });
      // 서버 결과로 머지 — realtime 도착 전 즉시 반영(중복 방지 위해 같은 id 제거 후 삽입).
      set((s) => ({ tabs: sortTabs([...s.tabs.filter((t) => t.id !== created.id), created]) }));
      return created;
    } catch (err) {
      console.error('[character-board] 탭 추가 실패:', err);
      toast.error('탭을 추가하지 못했어요');
      return null;
    }
  },

  renameTab: async (id, name) => {
    const trimmed = name.trim();
    if (!trimmed) return;
    await applyTabUpdate(set, get, id, { name: trimmed }, '탭 이름을 저장하지 못했어요');
  },

  deleteTab: async (id) => {
    const removed = get().tabs.find((t) => t.id === id) ?? null;
    set((s) => ({ tabs: s.tabs.filter((t) => t.id !== id) }));
    try {
      await svcDeleteTab(id);
    } catch (err) {
      console.error('[character-board] 탭 삭제 실패:', err);
      if (removed) set((s) => ({ tabs: sortTabs([...s.tabs, removed]) }));
      toast.error('탭을 삭제하지 못했어요');
    }
  },

  updateTabGroups: async (id, groups) => {
    await applyTabUpdate(set, get, id, { groups }, '그룹 변경을 저장하지 못했어요');
  },
```

(i) `receiveRealtime`(815행 이하): 기존 테이블 분기(예: `characters` 처리)와 **동형**으로 `character_board_tabs` 분기를 추가한다. 로직:

```ts
      // character_board_tabs (피드백 41)
      if (table === 'character_board_tabs') {
        if (eventType === 'DELETE') {
          const id = (old?.id ?? row?.id) as string | undefined;
          if (id) set((s) => ({ tabs: s.tabs.filter((t) => t.id !== id) }));
          return;
        }
        if (!row) return;
        const incoming = rowToCharacterBoardTab(row as unknown as CharacterBoardTabRow);
        set((s) => {
          const prevTab = s.tabs.find((t) => t.id === incoming.id);
          const merged = mergeIncomingWithPending(pendingTabFields, prevTab, incoming);
          return { tabs: sortTabs([...s.tabs.filter((t) => t.id !== incoming.id), merged]) };
        });
        return;
      }
```

(기존 분기가 switch 문이면 case 로, if 체인이면 if 로 — **기존 방식을 그대로 따른다.** `return`/`break` 도 주변과 통일.)

### 8-10. 테스트 — 신규 `tests/characterBoardTabs.test.ts` + 등록

package.json `test:character` 끝에 ` ./tests/characterBoardTabs.test.ts` 추가(작업 3 에서 추가한 characterFeedback4 뒤). 파일 전체:

```ts
/**
 * 피드백 41: 캐릭터 현황판 탭·그룹 — 순수 헬퍼 유닛 테스트 + 배선 앵커.
 */
import { readFileSync } from 'node:fs';
import test from 'node:test';
import assert from 'node:assert/strict';
import {
  sanitizeTabGroups, addGroup, renameGroup, removeGroup,
  moveCharacterToGroup, reorderWithinGroup, groupedCharacterIdSet,
} from '../src/utils/characterTabGroups.ts';

test('sanitizeTabGroups: JSONB 형식 방어', () => {
  assert.deepEqual(sanitizeTabGroups(null), []);
  assert.deepEqual(sanitizeTabGroups('문자열'), []);
  assert.deepEqual(
    sanitizeTabGroups([
      { id: 'g1', name: '주역', characterIds: ['a', 'a', 1, 'b'] },
      { id: 7, name: '형식 불량' },
      null,
    ] as unknown),
    [{ id: 'g1', name: '주역', characterIds: ['a', 'b'] }],
  );
});

test('그룹 CRUD + 멤버 이동 (탭 단위 불변 갱신)', () => {
  let groups = addGroup([], '주역');
  assert.equal(groups.length, 1);
  const gid = groups[0].id;
  groups = renameGroup(groups, gid, '주요 캐릭터');
  assert.equal(groups[0].name, '주요 캐릭터');
  groups = moveCharacterToGroup(groups, 'c1', gid);
  groups = moveCharacterToGroup(groups, 'c2', gid);
  assert.deepEqual(groups[0].characterIds, ['c1', 'c2']);
  // 다른 그룹으로 이동하면 원래 그룹에서 빠진다.
  groups = addGroup(groups, '조연');
  const gid2 = groups[1].id;
  groups = moveCharacterToGroup(groups, 'c1', gid2);
  assert.deepEqual(groups[0].characterIds, ['c2']);
  assert.deepEqual(groups[1].characterIds, ['c1']);
  // 미분류(null) 이동 = 모든 그룹에서 제거.
  groups = moveCharacterToGroup(groups, 'c1', null);
  assert.deepEqual(groups[1].characterIds, []);
  assert.deepEqual([...groupedCharacterIdSet(groups)], ['c2']);
  // 그룹 삭제 — 멤버십만 사라진다.
  groups = removeGroup(groups, gid);
  assert.equal(groups.length, 1);
  assert.equal(groups[0].id, gid2);
});

test('reorderWithinGroup: 대상 앞 삽입 + 방어', () => {
  const base = [{ id: 'g', name: 'G', characterIds: ['a', 'b', 'c'] }];
  assert.deepEqual(reorderWithinGroup(base, 'g', 'c', 'a')[0].characterIds, ['c', 'a', 'b']);
  assert.deepEqual(reorderWithinGroup(base, 'g', 'a', 'a')[0].characterIds, ['a', 'b', 'c']);
  assert.deepEqual(reorderWithinGroup(base, 'g', 'x', 'a')[0].characterIds, ['a', 'b', 'c']);
  assert.deepEqual(reorderWithinGroup(base, '없는그룹', 'a', 'b')[0].characterIds, ['a', 'b', 'c']);
});

test('배선 앵커: 마이그레이션·Realtime·IPC·store', () => {
  const mig = readFileSync('DEVLOG/migrations/2026-07-26-character-board-tabs.sql', 'utf8');
  assert.match(mig, /CREATE TABLE IF NOT EXISTS character_board_tabs/);
  assert.match(mig, /groups\s+JSONB NOT NULL DEFAULT '\[\]'/);
  assert.match(mig, /ALTER PUBLICATION supabase_realtime ADD TABLE character_board_tabs/);
  const sb = readFileSync('electron/supabase.ts', 'utf8');
  assert.match(sb, /'episode_character_mapping', 'character_board_tabs'\] as const/);
  const main = readFileSync('electron/main.ts', 'utf8');
  assert.match(main, /ipcMain\.handle\('supabase:load-character-board-tabs'/);
  assert.match(main, /ipcMain\.handle\('supabase:update-character-board-tab'/);
  const preload = readFileSync('electron/preload.ts', 'utf8');
  assert.match(preload, /supabase:add-character-board-tab/);
  assert.match(preload, /supabase:delete-character-board-tab/);
  const store = readFileSync('src/stores/useCharacterBoardStore.ts', 'utf8');
  assert.match(store, /pendingTabFields/);
  assert.match(store, /updateTabGroups/);
  assert.match(store, /character_board_tabs/);
  const mock = readFileSync('src/mocks/devElectronAPI.ts', 'utf8');
  assert.match(mock, /supabaseLoadCharacterBoardTabs/);
});
```

## 엣지 케이스

- **탭 0개(기본 상태):** load 는 빈 배열 — UI(작업 9)는 '전체' 탭만 보인다. 팀원 대부분에게 무변화.
- **동시 편집:** 두 사용자가 같은 탭의 그룹을 동시에 바꾸면 탭 row LWW — 설계 결정에서 수용. Realtime(~100ms)이 충돌 창을 좁힌다.
- **PostgREST 1000행 제한:** 탭은 소수(수십 개 이하) — `.range()` 페이지네이션 불필요(메모리 기록된 1000행 함정은 씬처럼 대량인 테이블 한정).
- **groups JSONB 손상:** `sanitizeTabGroups` 가 로드·Realtime 양쪽 경로(둘 다 `rowToCharacterBoardTab` 경유)에서 방어.
- **로드 실패:** 탭 로드가 `Promise.all` 에 묶여 실패 시 보드 전체가 기존 loadError 경로(에러+재시도 UI)로 빠진다 — **그래서 마이그레이션 선적용이 배포 게이트다**(부록 B-2).

## 검증 방법 / 완료 기준

- [ ] `npm run typecheck` 통과 / `npm run test:character` 125 pass (블록 4개 추가), fail 0 / `npm run build:vite` 통과
- [ ] 프리뷰(mock): 탭 추가·이름 변경·삭제·그룹 편집이 mock 저장소에 반영(새로고침 전까지 유지)되는지 — UI 는 작업 9 이후 확인
- [ ] 커밋 예: `[v1.93.0] 캐릭터 탭·그룹 데이터 계층 (피드백 41)`

---

# 작업 9 — 항목 41 (2/2): 탭·그룹 UI + 검색 색인

## UI 설계 (확정)

- sticky 헤더 최상단에 **탭 스트립**: `[전체] [사용자 탭…] [+ 탭]`. '전체' = 기존 화면 그대로(그리드/보기 방식/드래그 재배치 전부 기존 동작).
- 사용자 탭 선택 시: **그룹 섹션들 + 미분류 섹션**. 카드를 끌어 그룹 섹션에 놓으면 배치, 같은 그룹 안 카드 위에 놓으면 순서 조정, 미분류에 놓으면 그룹 해제. 그룹 추가/이름 변경(더블클릭·연필)/삭제(2단계 확인 — 브라우저 confirm 금지).
- 탭 이름 변경 = 칩 더블클릭, 탭 삭제 = 활성 탭의 X → 인라인 "삭제/취소" 확인. 탭 삭제 시 그룹 배치도 함께 사라진다(row 삭제) — 캐릭터 자체는 무관.
- **검색 색인(41-4):** 검색어가 탭/그룹 이름과 일치하면 그 탭/그룹에 배치된 캐릭터가 이름이 안 맞아도 검색에 걸린다('전체' 탭 포함 전역 색인).
- **모드 상호배타:** 키 비교 보기·보관 목록은 '전체' 탭 전용 — 켜면 탭이 '전체'로 돌아가고, 사용자 탭을 고르면 둘이 꺼진다.
- 보기 방식(작업 5)은 그룹 섹션 안에도 동일 적용(카드/컴팩트/리스트). 리스트 모드에서는 드래그 배치 비활성(카드/컴팩트에서만).

## 수정 내용

### 9-1. 신규 `src/components/characters/CharacterTabGroupsView.tsx` (전체)

```tsx
import { useRef, useState, type MouseEvent as ReactMouseEvent } from 'react';
import { Check, Pencil, Plus, X } from 'lucide-react';
import type { Character, CharacterBoardTab, CharacterBoardTabGroup, CharacterCostume } from '@/types';
import { CharacterCard, EMPTY_COSTUMES } from '@/components/characters/CharacterCard';
import { CharacterListRow } from '@/components/characters/CharacterListRow';
import {
  addGroup, groupedCharacterIdSet, moveCharacterToGroup, removeGroup, renameGroup, reorderWithinGroup,
} from '@/utils/characterTabGroups';
import type { CharacterBoardViewMode } from '@/utils/characterViewPersist';
import { cn } from '@/utils/cn';

/**
 * 커스텀 탭 본문 (피드백 41) — 그룹 섹션 + 미분류 + 카드 드래그 배치.
 * 그룹 편집은 전부 onUpdateGroups(다음 groups 배열) 한 콜백으로 수렴 — store 의 탭 단위 LWW 낙관 갱신과 맞물린다.
 */
export function CharacterTabGroupsView({
  tab,
  characters,
  byCharacter,
  viewMode,
  searching,
  onOpen,
  onContextMenu,
  onUpdateGroups,
}: {
  tab: CharacterBoardTab;
  /** 검색 필터가 이미 적용된 표시 대상(활성 캐릭터) 목록 — 스토어 정렬 순서 유지. */
  characters: Character[];
  byCharacter: Map<string, CharacterCostume[]>;
  viewMode: CharacterBoardViewMode;
  /** 검색어·태그 필터가 걸린 상태 — 빈 섹션 문구를 "필터 때문"으로 구분해 보여준다. */
  searching: boolean;
  onOpen: (characterId: string, costumeId?: string) => void;
  onContextMenu: (characterId: string, event: ReactMouseEvent<HTMLButtonElement>, costumeId?: string) => void;
  onUpdateGroups: (groups: CharacterBoardTabGroup[]) => void;
}) {
  // 드롭이 카드 → 섹션으로 버블되며 두 번 처리되지 않도록 ref 로 동기 소거 (F29 의 draggingCardIdRef 와 같은 이유).
  const draggingIdRef = useRef<string | null>(null);
  const [draggingId, setDraggingIdState] = useState<string | null>(null);
  const setDragging = (id: string | null) => {
    draggingIdRef.current = id;
    setDraggingIdState(id);
  };

  const [addingGroup, setAddingGroup] = useState(false);
  const [groupDraft, setGroupDraft] = useState('');
  const [editingGroupId, setEditingGroupId] = useState<string | null>(null);
  const [editDraft, setEditDraft] = useState('');
  const [confirmDeleteId, setConfirmDeleteId] = useState<string | null>(null);

  const byId = new Map(characters.map((c) => [c.id, c]));
  const grouped = groupedCharacterIdSet(tab.groups);
  // 미분류 = 표시 대상 중 어느 그룹에도 없는 캐릭터. (그룹에 남은 삭제된 캐릭터 id 는 byId 조회에서 자연 제외.)
  const ungrouped = characters.filter((c) => !grouped.has(c.id));

  const dragEnabled = viewMode !== 'list';

  const dropOnGroup = (groupId: string | null) => {
    const dragId = draggingIdRef.current;
    if (!dragId) return;
    setDragging(null);
    onUpdateGroups(moveCharacterToGroup(tab.groups, dragId, groupId));
  };
  const dropOnCard = (groupId: string | null, targetId: string) => {
    const dragId = draggingIdRef.current;
    if (!dragId) return;
    setDragging(null);
    if (dragId === targetId) return;
    if (groupId === null) {
      onUpdateGroups(moveCharacterToGroup(tab.groups, dragId, null));
      return;
    }
    let next = moveCharacterToGroup(tab.groups, dragId, groupId);
    next = reorderWithinGroup(next, groupId, dragId, targetId);
    onUpdateGroups(next);
  };

  const commitAddGroup = () => {
    const name = groupDraft.trim();
    if (name) onUpdateGroups(addGroup(tab.groups, name));
    setGroupDraft('');
    setAddingGroup(false);
  };
  const commitRename = () => {
    const name = editDraft.trim();
    if (editingGroupId && name) onUpdateGroups(renameGroup(tab.groups, editingGroupId, name));
    setEditingGroupId(null);
    setEditDraft('');
  };

  const gridClass = viewMode === 'list'
    ? 'flex flex-col gap-1.5'
    : viewMode === 'compact'
      ? 'grid grid-cols-[repeat(auto-fill,minmax(150px,1fr))] gap-3'
      : 'grid grid-cols-[repeat(auto-fill,minmax(180px,1fr))] gap-4';

  const renderEntry = (groupId: string | null) => (c: Character) =>
    viewMode === 'list' ? (
      <CharacterListRow
        key={c.id}
        character={c}
        costumes={byCharacter.get(c.id) ?? EMPTY_COSTUMES}
        onOpen={onOpen}
        onContextMenu={onContextMenu}
      />
    ) : (
      <CharacterCard
        key={c.id}
        character={c}
        costumes={byCharacter.get(c.id) ?? EMPTY_COSTUMES}
        onOpen={onOpen}
        onContextMenu={onContextMenu}
        compact={viewMode === 'compact'}
        onDragStartCard={dragEnabled ? setDragging : undefined}
        onDropCard={dragEnabled ? (targetId) => dropOnCard(groupId, targetId) : undefined}
        onDragEndCard={dragEnabled ? () => setDragging(null) : undefined}
        dragging={draggingId === c.id}
      />
    );

  const section = (group: CharacterBoardTabGroup | null) => {
    const members = group
      ? group.characterIds.map((id) => byId.get(id)).filter((c): c is Character => Boolean(c))
      : ungrouped;
    return (
      <section
        key={group?.id ?? '__ungrouped__'}
        onDragOver={dragEnabled ? (e) => { e.preventDefault(); e.dataTransfer.dropEffect = 'move'; } : undefined}
        onDrop={dragEnabled ? (e) => { e.preventDefault(); dropOnGroup(group?.id ?? null); } : undefined}
        className={cn('rounded-xl border border-bg-border/60 p-3 flex flex-col gap-3', draggingId && 'border-accent/40')}
      >
        <header className="flex items-center gap-2">
          {group && editingGroupId === group.id ? (
            <input
              autoFocus
              value={editDraft}
              onChange={(e) => setEditDraft(e.target.value)}
              onKeyDown={(e) => {
                if (e.key === 'Enter') commitRename();
                if (e.key === 'Escape') { setEditingGroupId(null); setEditDraft(''); }
              }}
              onBlur={commitRename}
              className="bg-transparent border border-bg-border rounded-md px-2 py-1 text-sm text-text-primary outline-none focus:border-accent/50"
            />
          ) : (
            <span className="text-sm font-semibold text-text-primary">{group ? group.name : '미분류'}</span>
          )}
          <span className="text-xs text-text-secondary">{members.length}</span>
          {group && editingGroupId !== group.id && (
            <>
              <button
                type="button"
                aria-label={`${group.name} 이름 바꾸기`}
                onClick={() => { setEditingGroupId(group.id); setEditDraft(group.name); }}
                className="rounded p-1 text-text-secondary hover:text-text-primary hover:bg-bg-border/30 cursor-pointer"
              >
                <Pencil size={12} />
              </button>
              {confirmDeleteId === group.id ? (
                <span className="flex items-center gap-1 text-xs">
                  <span className="text-text-secondary">그룹을 삭제할까요? (캐릭터는 미분류로)</span>
                  <button
                    type="button"
                    onClick={() => { onUpdateGroups(removeGroup(tab.groups, group.id)); setConfirmDeleteId(null); }}
                    className="rounded px-1.5 py-0.5 hover:bg-bg-border/30 cursor-pointer"
                    style={{ color: 'var(--status-error)' }}
                  >
                    삭제
                  </button>
                  <button type="button" onClick={() => setConfirmDeleteId(null)} className="rounded px-1.5 py-0.5 text-text-secondary hover:bg-bg-border/30 cursor-pointer">
                    취소
                  </button>
                </span>
              ) : (
                <button
                  type="button"
                  aria-label={`${group.name} 삭제`}
                  onClick={() => setConfirmDeleteId(group.id)}
                  className="rounded p-1 text-text-secondary hover:text-text-primary hover:bg-bg-border/30 cursor-pointer"
                >
                  <X size={12} />
                </button>
              )}
            </>
          )}
        </header>
        {members.length === 0 ? (
          <div className="rounded-lg border border-dashed border-bg-border/70 px-3 py-6 text-center text-xs text-text-secondary">
            {searching ? '검색·필터 조건에 맞는 캐릭터가 여기 없어요' : group ? '카드를 끌어다 놓아 배치해요' : '모든 캐릭터가 그룹에 배치됐어요'}
          </div>
        ) : (
          <div className={gridClass}>{members.map(renderEntry(group?.id ?? null))}</div>
        )}
      </section>
    );
  };

  return (
    <div className="flex flex-col gap-4">
      {tab.groups.map((g) => section(g))}
      {section(null)}
      {addingGroup ? (
        <div className="flex items-center gap-2">
          <input
            autoFocus
            value={groupDraft}
            onChange={(e) => setGroupDraft(e.target.value)}
            onKeyDown={(e) => {
              if (e.key === 'Enter') commitAddGroup();
              if (e.key === 'Escape') { setAddingGroup(false); setGroupDraft(''); }
            }}
            placeholder="그룹 이름"
            className="bg-transparent border border-bg-border rounded-md px-3 py-2 text-sm text-text-primary outline-none focus:border-accent/50"
          />
          <button type="button" aria-label="그룹 추가 확정" onClick={commitAddGroup} className="rounded-lg bg-accent px-3 py-2 text-sm text-white hover:opacity-90 cursor-pointer">
            <Check size={14} />
          </button>
        </div>
      ) : (
        <button
          type="button"
          onClick={() => setAddingGroup(true)}
          className="self-start flex items-center gap-1.5 rounded-lg border border-dashed border-bg-border px-3 py-2 text-sm text-text-secondary hover:border-accent/50 hover:text-text-primary cursor-pointer"
        >
          <Plus size={14} /> 그룹 추가
        </button>
      )}
    </div>
  );
}
```

### 9-2. `src/views/CharacterBoardView.tsx` — 탭 스트립 + 색인 + 분기 (작업 5 완료 상태 기준)

(a) import: lucide 줄에 `X` 추가. 아래 추가:

```tsx
import { CharacterTabGroupsView } from '@/components/characters/CharacterTabGroupsView';
import type { CharacterBoardTab } from '@/types';
```

(b) `TabButton` 컴포넌트(28~38행) 아래에 `BoardTabStrip` 컴포넌트 추가:

```tsx
/** 사용자 정의 탭 스트립 (피드백 41) — '전체' + 사용자 탭 + 추가. 이름 변경=더블클릭, 삭제=활성 탭 X → 2단계 확인. */
function BoardTabStrip({ tabs, activeTabId, onSelect, onAdd, onRename, onDelete }: {
  tabs: CharacterBoardTab[];
  activeTabId: string | null;
  onSelect: (id: string | null) => void;
  onAdd: (name: string) => void;
  onRename: (id: string, name: string) => void;
  onDelete: (id: string) => void;
}) {
  const [adding, setAdding] = useState(false);
  const [draft, setDraft] = useState('');
  const [editingId, setEditingId] = useState<string | null>(null);
  const [editDraft, setEditDraft] = useState('');
  const [confirmDeleteId, setConfirmDeleteId] = useState<string | null>(null);

  const commitAdd = () => {
    const name = draft.trim();
    if (name) onAdd(name);
    setDraft('');
    setAdding(false);
  };
  const commitRename = () => {
    const name = editDraft.trim();
    if (editingId && name) onRename(editingId, name);
    setEditingId(null);
    setEditDraft('');
  };

  return (
    <div className="flex flex-wrap items-center gap-1.5">
      <button
        type="button"
        onClick={() => onSelect(null)}
        className={cn('px-2.5 py-1 rounded-full text-xs border transition-colors cursor-pointer',
          activeTabId === null ? 'border-accent bg-accent/15 text-accent' : 'border-bg-border text-text-secondary hover:text-text-primary')}
      >
        전체
      </button>
      {tabs.map((t) => (
        editingId === t.id ? (
          <input
            key={t.id}
            autoFocus
            value={editDraft}
            onChange={(e) => setEditDraft(e.target.value)}
            onKeyDown={(e) => {
              if (e.key === 'Enter') commitRename();
              if (e.key === 'Escape') { setEditingId(null); setEditDraft(''); }
            }}
            onBlur={commitRename}
            className="w-28 bg-transparent border border-accent/50 rounded-full px-2.5 py-1 text-xs text-text-primary outline-none"
          />
        ) : (
          <span key={t.id} className="inline-flex items-center">
            <button
              type="button"
              onClick={() => onSelect(t.id)}
              onDoubleClick={() => { setEditingId(t.id); setEditDraft(t.name); }}
              title="더블클릭하면 이름을 바꿔요"
              className={cn('px-2.5 py-1 rounded-full text-xs border transition-colors cursor-pointer',
                activeTabId === t.id ? 'border-accent bg-accent/15 text-accent' : 'border-bg-border text-text-secondary hover:text-text-primary')}
            >
              {t.name}
            </button>
            {activeTabId === t.id && (
              confirmDeleteId === t.id ? (
                <span className="ml-1 flex items-center gap-1 text-xs">
                  <button
                    type="button"
                    onClick={() => { onDelete(t.id); setConfirmDeleteId(null); }}
                    className="rounded px-1.5 py-0.5 hover:bg-bg-border/30 cursor-pointer"
                    style={{ color: 'var(--status-error)' }}
                  >
                    삭제
                  </button>
                  <button type="button" onClick={() => setConfirmDeleteId(null)} className="rounded px-1.5 py-0.5 text-text-secondary hover:bg-bg-border/30 cursor-pointer">
                    취소
                  </button>
                </span>
              ) : (
                <button
                  type="button"
                  aria-label={`${t.name} 탭 삭제`}
                  onClick={() => setConfirmDeleteId(t.id)}
                  className="ml-0.5 rounded p-1 text-text-secondary hover:text-text-primary hover:bg-bg-border/30 cursor-pointer"
                >
                  <X size={11} />
                </button>
              )
            )}
          </span>
        )
      ))}
      {adding ? (
        <input
          autoFocus
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          onKeyDown={(e) => {
            if (e.key === 'Enter') commitAdd();
            if (e.key === 'Escape') { setAdding(false); setDraft(''); }
          }}
          onBlur={commitAdd}
          placeholder="탭 이름"
          className="w-28 bg-transparent border border-bg-border rounded-full px-2.5 py-1 text-xs text-text-primary outline-none focus:border-accent/50"
        />
      ) : (
        <button
          type="button"
          onClick={() => setAdding(true)}
          className="flex items-center gap-1 rounded-full border border-dashed border-bg-border px-2.5 py-1 text-xs text-text-secondary hover:border-accent/50 hover:text-text-primary cursor-pointer"
        >
          <Plus size={11} /> 탭
        </button>
      )}
    </div>
  );
}
```

(c) `CharacterGrid` — store 구독(41~45행) 아래에 추가:

```tsx
  const tabs = useCharacterBoardStore((s) => s.tabs);
  const addTab = useCharacterBoardStore((s) => s.addTab);
  const renameTab = useCharacterBoardStore((s) => s.renameTab);
  const deleteTab = useCharacterBoardStore((s) => s.deleteTab);
  const updateTabGroups = useCharacterBoardStore((s) => s.updateTabGroups);
```

상태(52행 `heightCompareMode` 부근) 아래에:

```tsx
  // 사용자 정의 탭 (피드백 41). null = '전체'. 다른 사용자가 탭을 지우면 자동으로 '전체'로 복귀.
  const [activeTabId, setActiveTabId] = useState<string | null>(null);
  const activeTab = activeTabId ? tabs.find((t) => t.id === activeTabId) ?? null : null;
  useEffect(() => {
    if (activeTabId && !activeTab) setActiveTabId(null);
  }, [activeTabId, activeTab]);
```

(d) 검색 색인 — `filtered` useMemo(83~91행) **위**에 추가:

```tsx
  // 피드백 41-4: 탭·그룹 이름을 검색 색인으로 — 이름이 일치하는 탭/그룹에 배치된 캐릭터도 검색에 걸린다.
  const indexedIds = useMemo(() => {
    const q = query.trim().toLowerCase();
    if (!q) return null;
    const ids = new Set<string>();
    for (const t of tabs) {
      const tabHit = t.name.toLowerCase().includes(q);
      for (const g of t.groups) {
        if (tabHit || g.name.toLowerCase().includes(q)) for (const id of g.characterIds) ids.add(id);
      }
    }
    return ids;
  }, [tabs, query]);
```

그리고 `filtered` 의 이름 매칭 줄 교체 — before: `if (q && !c.name.toLowerCase().includes(q)) return false;` → after:

```tsx
      if (q && !c.name.toLowerCase().includes(q) && !indexedIds?.has(c.id)) return false;
```

(useMemo deps 배열에 `indexedIds` 추가.)

(e) 모드 상호배타 — "키 비교 보기" 버튼 onClick(212행) 교체: `onClick={() => setHeightCompareMode((v) => !v)}` → `onClick={() => { setHeightCompareMode((v) => !v); setActiveTabId(null); }}`. "보관 N" 버튼 onClick(226행) 교체: `onClick={() => setShowArchived((value) => !value)}` → `onClick={() => { setShowArchived((value) => !value); setActiveTabId(null); }}`.

(f) sticky 헤더 첫 행에 탭 스트립 삽입 — sticky 래퍼(작업 4 의 4-2) 여는 태그 바로 다음:

```tsx
        {/* 사용자 정의 탭 (피드백 41) */}
        <BoardTabStrip
          tabs={tabs}
          activeTabId={activeTabId}
          onSelect={(id) => { setActiveTabId(id); if (id !== null) { setHeightCompareMode(false); setShowArchived(false); } }}
          onAdd={(name) => void addTab(name).then((t) => { if (t) setActiveTabId(t.id); })}
          onRename={(id, name) => void renameTab(id, name)}
          onDelete={(id) => void deleteTab(id)}
        />
```

(g) 본문 분기 — 기존 `{visibleCharacters.length === 0 ? … : filtered.length === 0 ? … : ( <div className={heightCompareMode …` 삼항 앞에 커스텀 탭 분기 추가:

```tsx
      {activeTab && !heightCompareMode && !showArchived ? (
        <CharacterTabGroupsView
          tab={activeTab}
          characters={filtered}
          byCharacter={byCharacter}
          viewMode={viewMode}
          searching={Boolean(query.trim()) || activeTags.length > 0}
          onOpen={openCharacterDetail}
          onContextMenu={openCardContextMenu}
          onUpdateGroups={(groups) => void updateTabGroups(activeTab.id, groups)}
        />
      ) : visibleCharacters.length === 0 ? (
        …기존 그대로…
```

(기존 삼항의 나머지 분기·그리드 블록은 무수정. `activeTab` 분기는 (e)·(f) 의 상호배타 규칙 때문에 실제로는 `!heightCompareMode && !showArchived` 가 항상 참이지만, race 방어용 이중 가드로 남긴다.)

### 9-3. 테스트 — `tests/characterBoardTabs.test.ts` 에 UI 앵커 블록 추가

```ts
test('UI 앵커: 탭 스트립·그룹 뷰·검색 색인', () => {
  const boardView = readFileSync('src/views/CharacterBoardView.tsx', 'utf8');
  assert.match(boardView, /function BoardTabStrip/);
  assert.match(boardView, /const indexedIds = useMemo/);
  assert.match(boardView, /!indexedIds\?\.has\(c\.id\)/);
  assert.match(boardView, /<CharacterTabGroupsView/);
  const groupsView = readFileSync('src/components/characters/CharacterTabGroupsView.tsx', 'utf8');
  assert.match(groupsView, /미분류/);
  assert.match(groupsView, /moveCharacterToGroup/);
  assert.match(groupsView, /reorderWithinGroup/);
  assert.match(groupsView, /draggingIdRef/);
});
```

## 엣지 케이스

- **드롭 이중 발화:** 카드 위 드롭은 카드 onDrop → 섹션 onDrop 순으로 버블된다. `draggingIdRef` 를 카드 처리에서 동기적으로 비워 섹션 처리가 조기 return — 이걸 state 만으로 하면 같은 이벤트 루프에서 stale 값을 읽어 **그룹 이동이 두 번 적용**된다. ref 필수.
- **검색 중 드래그:** `filtered` 가 부분 목록이어도 그룹 이동은 id 기반이라 안전. 그룹 내 순서 조정은 배열 원본 기준(`reorderWithinGroup`)이라 필터와 무관하게 일관.
- **다른 창(팝업)과 동시 편집:** 탭 row LWW + Realtime 수렴(작업 8 설계 결정). pending 보호(15초 TTL)로 자기 편집이 되돌아오는 문제 방지.
- **삭제된 캐릭터 id 가 그룹에 남는 경우:** `byId.get()` 실패로 렌더에서 제외 — 화면상 무해. JSONB 정리는 후속(부록 D).
- **탭만 있고 그룹이 없는 경우:** 미분류 섹션 하나 + '그룹 추가' 버튼.
- **미분류 섹션 내 순서:** 스토어 전역 정렬(sort_order) 순서 그대로 — 미분류 내 커스텀 순서는 지원하지 않는다(전체 탭에서 재배치).
- **에피소드 에셋 탭:** 무관 — `CharacterGrid` 안의 변경이라 영향 없음.

## 검증 방법 / 완료 기준

- [ ] `npm run typecheck` / `npm run test:character` 126 pass / `npm run build:vite` 통과
- [ ] 프리뷰(mock): 탭 추가→자동 선택→그룹 추가→카드 드래그로 배치→그룹 간 이동→같은 그룹 내 순서 조정→미분류로 드롭→그룹 이름 변경·삭제→탭 이름 변경·삭제(2단계 확인) 전부 동작 / '주요'라고 검색하면 '주요 캐릭터' 그룹 멤버가 전체 탭 검색에 걸림 / 키 비교·보관 토글 시 '전체'로 복귀
- [ ] 실기: 두 창(메인+팝업)에서 그룹 배치가 Realtime 으로 동기화
- [ ] 커밋 예: `[v1.93.0] 캐릭터 탭·그룹 UI + 검색 색인 (피드백 41)`

---

# 작업 10 — PR B 버전·업데이트 노트 + 마이그레이션 게이트

## 수정 내용

1. `package.json` `"version"` → `1.93.0` (착수 시점 재확인 정책).
2. `DEVLOG/update-notes.json` 맨 앞에 추가:

```json
{
  "version": "1.93.0",
  "title": "캐릭터를 나만의 탭과 그룹으로 정리할 수 있어요",
  "items": [
    {
      "category": "feature",
      "summary": "캐릭터 현황판에 나만의 정리 탭을 만들 수 있어요",
      "description": "'전체' 옆에 탭을 원하는 만큼 만들고(주요 캐릭터, 나이대별, 남녀 구분처럼 자유롭게), 탭 안에 그룹을 만들어 캐릭터 카드를 끌어다 배치할 수 있어요. 정리해 둔 배치는 팀원 모두에게 똑같이 보여요."
    },
    {
      "category": "feature",
      "summary": "탭·그룹 이름이 검색에도 걸려요",
      "description": "'주요 캐릭터'라는 그룹을 만들어 두면, 검색창에 '주요'라고만 쳐도 그 그룹에 넣어둔 캐릭터들이 같이 검색돼요. 직접 정리한 이름이 그대로 찾기 색인이 돼요."
    }
  ]
}
```

3. PR B 생성: 제목 `[v1.93.0] 캐릭터 탭·그룹 정리 + 검색 색인 (피드백 41)`. **PR 본문에 "머지 전 라이브 DB 마이그레이션 적용 필수 (부록 B-2)" 를 명시.**

## 완료 기준

- [ ] `npm run build:vite` 전체 통과
- [ ] **부록 B-2 마이그레이션 게이트 통과 전에는 머지·배포 금지** (게이트는 한솔 또는 한솔 지시를 받은 에이전트가 수행)

---

# 부록

## A. 수동 검증 시나리오

**프리뷰 공통 진입:** `npm run dev:renderer` → `http://localhost:5190/?preview=1` → mock 로그인 `배한솔`/`1234` → 사이드바 "캐릭터".

| # | 시나리오 | 기대 결과 | 환경 |
|---|---|---|---|
| A-1 (35) | 사이드바 펼침 → 캐릭터 항목 hover | `+` 버튼 없음. 메뉴 클릭 시 현황판 정상 이동 | 프리뷰 |
| A-2 (34) | 캐릭터 상세 → 복장 → "리깅 완성 공지" | 제목 기본값 `리깅 완료 공지`(머리말 없음) + 템플릿 칩 3종 + 제목 아래 머리말 안내 캡션 | 프리뷰 |
| A-3 (34) | 공지 전송 (실 슬랙) | 슬랙 메시지 제목 머리말 1회, 내용 줄 `"캐릭터" - "복장" 리깅완료` | 실기 |
| A-4 (39) | 복장 → "디자인 담당자" 클릭 | 팀원 목록(색 점) 표시, 입력어 필터, 클릭 추가+모달 유지, ↑↓+Enter, 자유 입력 유지 | 프리뷰 |
| A-5 (38) | 캐릭터 다수 상태로 아래로 스크롤 | 제목·탭 고정 + 검색·버튼·칩 sticky, 카드 비침 없음(블러), 라이트 모드에서도 확인 | 프리뷰 |
| A-6 (40) | 보기 방식 토글 3종 전환 → 새로고침 | 카드/컴팩트/리스트 렌더 + 마지막 선택 유지. 키 비교 중 토글 비활성 | 프리뷰 |
| A-7 (40) | 리스트 행 클릭/우클릭 | 카드와 동일하게 상세 열림/컨텍스트 메뉴 | 프리뷰 |
| A-8 (36) | 현황판 "새 창으로" 클릭 | 1160×780 팝업(AOT 꺼짐), 팝업 안 현황판 완전 동작, 팝업 안에는 "새 창으로" 버튼 없음 | 실기 |
| A-9 (36) | 메인 창에서 단계 토글 → 팝업 확인 | 팝업에 실시간 반영(역방향도) | 실기 |
| A-10 (36) | 앱 종료 → 재실행 | 열어뒀던 현황판 팝업 자동 복원 | 실기 |
| A-11 (41) | 탭 추가→그룹 추가→드래그 배치→이동→순서 조정→미분류 드롭 | 전부 즉시 반영, 새로고침 후 유지(실기), 2단계 삭제 확인 | 프리뷰+실기 |
| A-12 (41) | 그룹 이름으로 검색 ('전체' 탭에서) | 그룹 멤버 캐릭터가 이름 불일치여도 검색에 걸림 | 프리뷰 |
| A-13 (41) | 두 창 동시 편집 | 그룹 배치 Realtime 동기화, 마지막 편집 승리 | 실기 |

## B. 전체 검증 커맨드 / 배포 게이트

### B-1. 코드 검증 (각 PR 완료 시)

```bash
npm run typecheck
npm run test:character
npm run test:ui
npm run build:vite
```

`build:vite` 는 전 테스트 스위트(playground/auto-update/entity/notifications/presence/character/ui)를 포함한다. 기준선(헤더 참조) 대비 **fail 0** 이어야 하며, pass 증가분은 신규 테스트 수와 일치해야 한다.

### B-2. PR B 마이그레이션 게이트 (머지·배포 전 필수, 순서 고정)

1. 라이브 DB(프로젝트 `mpqifkpxalwxgcrddchv`)에 `DEVLOG/migrations/2026-07-26-character-board-tabs.sql` 전체를 적용한다 (Supabase SQL Editor 또는 MCP `apply_migration`).
2. 확인 쿼리: `select count(*) from character_board_tabs;` 가 오류 없이 0 을 반환하고, `select tablename from pg_publication_tables where pubname='supabase_realtime' and tablename='character_board_tabs';` 가 1행을 반환.
3. 구버전(1.92.x) 앱이 정상 동작하는지 확인(테이블 추가는 additive — 영향 없음이 정상).
4. 그 다음에 PR B 머지 → 빌드·배포는 `bflow-release-deploy` 스킬 절차(한솔 지시 시).

### B-3. 배포 (한솔 지시 시에만 — 자동 배포 금지)

빌드까지만 자동. G드라이브 동기화·manifest 갱신은 한솔이 명시할 때 `bflow-release-deploy` 스킬로 진행 (manifest.json 은 항상 마지막).

## C. 슬랙 워크플로 확인 체크리스트 (한솔 몫 — 앱 코드 아님)

작업 2 배포 후 실 공지 1회를 보내 확인:

1. **제목:** 워크플로 메시지 서식의 제목 줄이 `[모호 리깅 현황] - {title 변수}` 형태인지. 앱은 이제 머리말 없이 보내므로, 워크플로가 머리말을 안 붙이고 있(었)다면 반대로 머리말이 사라진다 → 그 경우 워크플로 서식에 머리말을 추가하면 된다(앱 재수정 불필요). **참고:** v1.91.1(PR #233) 본문에 전달된 템플릿 갱신 가이드를 적용할 때도 같은 원칙 — 머리말은 워크플로 서식 쪽에 둔다(이 계획서 작업 2 의 확정 결정).
2. **[내용] 줄:** `CH_name` 변수에 바인딩되어 있는지. 피드백 34 스크린샷의 "빈 불릿"은 앱이 보낸 `CH_name`(항상 비어 있지 않음)이 표시되지 않은 것일 수 있다 — 빈 줄이 재현되면 워크플로 변수 바인딩을 확인할 것.
3. **비고·이미지 관련 서식은 이번에 건드리지 않는다** (37번 제외 지시 — 빈 비고 불릿 정리는 나중 회차).

## D. 범위 외 후속 권장 (이번에 하지 말 것)

- **37번** 공지 비고 생략 시 빈 항목 제거 + 알리미 아이콘 교체 — **한솔 지시로 명시 제외.**
- 리스트 보기에서의 드래그 재배치 / 미분류 섹션 내 커스텀 순서.
- 탭 순서 드래그 재정렬(현재 생성순 sort_order 고정), 탭 선택 영속화, 그룹 색상·접기.
- 그룹 `characterIds` 의 죽은 id DB 정리(캐릭터 삭제 cascade) — 현재는 렌더 필터로 무해화.
- 그룹 뷰(`CharacterTabGroupsView`)의 per-card 인라인 드래그 콜백을 안정 참조로 바꾸는 memo 최적화(CQ-6 패턴) — v1 은 캐릭터 수십 명 규모라 수용, 수백 명 규모가 되면 착수.
- 제목에 머리말을 직접 타이핑했을 때 자동 strip — 캡션 안내로 갈음.
- 42~47번 자리표시자 항목 — 문서 작성 완료 후 다음 회차.

---

*작성: 2026-07-26, Claude (기준 스냅샷 `0978dcf`) — 검증 이력은 문서 헤더 참조.*





