# B flow 피드백 30~33 수정 계획서 (에이전트 실행용)

> **이 문서는 자기완결적이다.** 원본 피드백 문서(구글 독스)·이전 대화 없이 이 문서만으로 구현을 완료할 수 있도록 작성되었다.
> **작성 기준 스냅샷:** 2026-07-15, `main` HEAD `b04170349a5b531ac481dfcbb7fe5d2b72e41835` ([v1.90.0] 배플레이그라운드 2048 게임 추가), 작업 트리 미커밋 변경 없음. 본 문서의 모든 라인 번호·코드 인용은 이 스냅샷 기준이며, 인용 코드는 전부 실제 파일과 대조 검증되었다.
> **테스트 기준선 (이 스냅샷, 수정 전):** `npm run typecheck` 통과 / `test:playground` 461 pass · `test:character` 104 pass · `test:ui` 5 pass · `test:notifications` 142 pass · `test:entity` 239 pass · `test:presence` 33 pass · `test:auto-update` 29 pass — 전부 fail 0. 작업 중 실패가 나면 그 작업의 수정이 원인이다.
> **리뷰 이력:** 본 문서는 독립 리뷰 3패스를 거쳤다 — ① 사실 검증(78항목: before 블록·앵커·전제 실파일 대조, git HEAD·기준선 실측 일치), ② 구현 시뮬레이션(48항목: 타입·런타임·작업 간 충돌·z-order 전제), ③ 테스트 프로젝션(after 코드를 실제 파일로 조립해 테스트 38건 실행). 발견된 문제 — 작업 4의 characterTier2 앵커 갱신 누락(반영 완료, 반영 후 프로젝션 38/38 통과 확인), main.ts 핸들러 무수정 의도 명시, 프리뷰 mock 순서 조정, 미등록 테스트 파일 주의 — 는 전부 본문에 반영 완료된 상태다. 신규·갱신 테스트 정규식은 전부 after 코드 시뮬레이션으로 통과 확인된 것만 실었다.
> **라인 번호는 ±수십 줄 어긋날 수 있다.** 모든 위치는 라인 번호와 함께 **함수명/고유 코드 조각(앵커)** 을 병기했으므로, 라인이 어긋나면 반드시 코드 조각 검색으로 위치를 재확정할 것.
> **절대 규칙:** 이 문서에 명시되지 않은 리팩터링·포맷 변경·기능 추가를 하지 말 것. 수정 범위는 명시된 파일·블록으로 한정한다.
> **선행 문서와의 관계:** 같은 범위의 미실행 계획서 없음. 이 계획서 자체는 문서만 추가하는 커밋으로 저장소에 들어가며 src/electron 코드는 스냅샷과 동일하다.

---

# 공통 섹션

## 0. 한눈에 보는 상태 요약 (30~33)

피드백 문서는 "비플로우 관련 요청사항"(구글 독스, 작성자: 리깅 담당 팀원 — 캐릭터 현황판의 주 사용자). 문서는 `업데이트 완료 내역`(누적 1~29번)과 `업데이트 요청 사항`(누적 30번~) 두 섹션으로 나뉘며, 이번 범위는 **요청 사항 섹션의 30·31·33번**이다. **32번(마감일)은 한솔 지시로 이번 범위에서 제외**하고, 34~36번은 자리표시자("ㅇㅁㄴㅇ" 등)라 범위 외다.

| # | 피드백 요약 | 근본 원인 (코드로 확정) | 이번 작업 |
|---|---|---|---|
| **30** | 컴포지팅 탭에서 타임라인 씬 미리보기(캐러셀)가 알림창을 덮는다 | 캐러셀은 body 포털 `zIndex: 90`, 알림창은 헤더(`relative z-30`) stacking context 안이라 루트 기준 실효 z=30 → 90 > 30 | ★ **작업 1** |
| **31** | 리깅 완성 공지 개선 5건 — (a) 공지 문구 템플릿, (b) 이미지 Ctrl+V, (c) 경로 자동 링크, (d) 비고 없이 전송, (e) 이미지 링크 비노출 | (a) 템플릿에 대시 없음·제목 빈값 시작·CH_name 포맷 상이 (b) 모달에 paste 처리 없음 (c) Path 변수에 원본 경로 그대로 전송 (d) `hasNote` 게이트가 전송 차단 (e) 슬랙 워크플로 설정 사안(코드 무관) | ★ **작업 2**(c) · **작업 3**(a,b,d,e) |
| **32** | 마감일 수정 불가 문의 | — | **범위 외** (한솔 지시로 제외) |
| **33** | 기준 키 자동 설정 + 머리/바닥 기준선 드래그 조정 UI (향후 키 정렬 기반) | 기준 키(`characters.reference_height_px`)는 수동 입력만 존재. 업로드 이미지는 800px로 축소 저장되어 원본 세로 px를 보존하는 코드가 없음 | ★ **작업 4**(원본 크기 보존) · **작업 5**(자동 설정) · **작업 6**(드래그 에디터) |
| 공통 | 버전·업데이트 노트 | — | ★ **작업 7** |

## 1. 피드백 원문 요약 (첨부 이미지 내용 + 한솔 보충 포함)

실행 에이전트는 원본 문서의 이미지를 볼 수 없으므로, 이미지·채팅 보충이 담은 정보를 전부 글로 남긴다.

### 항목 30 — "컴포지팅 탭에서, 타임라인 미리보기가 알림창과 겹치는 문제"

> "알림창을 열면 그 뒤에 마우스를 올릴 때 오버레이로 보이는 씬 미리보기 탭들이 알림창보다 위에 뜨게되는 문제가 있군요"

첨부 이미지: 우상단 알림창(벨 아이콘 드롭다운)이 열린 상태에서, 그 아래 컴포지팅 타임라인 위에 마우스가 있을 때 씬 미리보기 카드(캐러셀)가 알림창을 **가리고** 있는 스크린샷.

### 항목 31 — "리깅 알리미 공지사항 업데이트 관련"

> 공지 문구 기본 템플릿 제안:
> **[제목]** `[모호 리깅 현황] - 드라마 톤 특수리깅 완료 공지`
> **[내용]** `"캐릭터 이름" - "캐릭터 복장 이름"`
> **[비고]** 추가할 설명 내용들 (생략 가능)
> **[링크]** "캐릭터 작업 폴더 링크"

> "공지할 수 있는 이미지를 Ctrl V 를 통해 넣을 수 있으면 좋겠습니다~! 보통은 모호 화면을 캡쳐해서 첨부하는데 (…) 쉽게 캡쳐 후 Ctrl V 로 붙여넣기 해서 이미지를 넣어버리면 참 좋을것같심다"

> "공지될 때 링크가, Ctrl Shift V 로 할 때 처럼 자동 링크 거는것도 가능한가요?? 첨부된 이미지 링크는 없는게 더 깔끔하지 않을까 싶습니다"

> "또한 아래 비고란 없이도 슬랙으로 보내기가 가능했으면 좋겠습니다잇~!!"

**한솔 보충 (채팅):** "Ctrl Shift V 자동 링크"는 스튜디오 오토핫키가 G드라이브 경로를 `jbbj://open/` 커스텀 프로토콜(파일탐색기 열기) 하이퍼링크로 걸어주는 기능을 말한다. 실제 예시 링크:
`jbbj://open/G:/%EA%B3%B5%EC%9C%A0%20%EB%93%9C%EB%9D%BC%EC%9D%B4%EB%B8%8C/%EC%82%AC%EC%9A%B0%EC%8A%A4%20%EC%BD%94%EB%A6%AC%EC%95%88%20%ED%8C%8C%ED%81%AC/[]%EC%82%AC%EC%BD%94%ED%8C%8D%20%EC%BA%90%EB%A6%AD%ED%84%B0%20%EC%84%B8%ED%8C%85/%ED%9C%A0%EC%B2%B4%EC%96%B4%20%ED%95%A0%EB%A8%B8%EB%8B%88`
(= `G:\공유 드라이브\사우스 코리안 파크\[]사코팍 캐릭터 세팅\휠체어 할머니` — 백슬래시→`/`, 폴더명 percent 인코딩, **드라이브 문자 `G:` 와 대괄호 `[]` 는 원문 유지**)
"애초에 웹훅에 하이퍼링크 서식이 있네, 경로에 Path 변수를 넣었으니까, 앞에 jbbj://open 이라는 프로토콜을 예시에 맞게 잘 설정해주면 될것같아." — 즉 슬랙 워크플로 메시지에는 이미 Path 변수를 URL로 쓰는 하이퍼링크 서식이 있고, 앱은 Path 변수에 원본 경로 대신 jbbj 링크를 넣어주면 된다.

### 항목 33 — "기준키 자동 구현 관련"

> "기준키는 지금 제가 첨부하는 이미지가 딱 캐릭터 크기라서, 현재 첨부되는 이미지의 세로 길이를 기준으로 자동 설정하면 딱 좋을것같습니다! (모든 캐릭터 파일은 1280 720 프로젝트 기준으로 제작되고 있습니다, 썸네일 출력은 딱 캐릭터 이미지를 잘라서 출력하여 첨부하고 있습니닷)"

> "일단은 자동으로 키를 설정 한 다음, 유저가 직접 키를 조정할 수 있도록 하는데 (…) 예를들어 모자를 쓴 캐릭터가 있으면 이 캐릭터 크기를 저렇게 머리 부분으로 드래그 해서 조정해서, px 단위를 맞춰주는 그런 기능 어떨까요~~ 이건 바닥 기준도 맞춰서 드래그 할 수 있으면 좋겠네요!! 혹시나 뭐 발이 너무 대각선으로 그려진 캐릭터라 까치발 든것처럼 표시될수도 있으니 말이죠!!! 이러면 이거 키 정렬 쌉가능~?"

첨부 이미지: 캐릭터 이미지 위에 수평 기준선을 머리 위치로 드래그해 내리는 개념 스케치.

**한솔 보충 (채팅):** "기준 키 구현은 나중에 이 기능을 활용해서 캐릭터를 키 별로 정렬하는 것도 생각하고 있어서, 그걸 위한 작업이야." — 즉 기준 키의 의미가 기존 "임의 단위"에서 **실제 프로젝트(1280×720) 픽셀 기준**으로 바뀐다.

## 2. 아키텍처 전제 (전 작업 공통 배경 — 전부 코드로 확인됨)

- **프로젝트:** B flow — Electron + React 18 + TypeScript + Tailwind + Zustand + Supabase(PostgreSQL/Realtime/Storage). 저장소 `C:\Bflow-BGonly` (레포 이름은 역사적 이유로 Bflow-BGonly, 정식 명칭 B flow).
- **데이터 흐름 (절대 규칙):** 렌더러는 Supabase를 직접 호출하지 않는다. `renderer store → src/services/supabaseService.ts(window.electronAPI.* 호출) → electron/preload.ts(ipcRenderer.invoke) → electron/main.ts(ipcMain.handle + wrapIpc) → electron/supabase.ts(supabase-js)` 단방향.
- **낙관적 업데이트:** 데이터 변경은 로컬 즉시 반영 → 서버 저장 → 실패 시 "이 업데이트가 바꾼 필드만, 아직 낙관값 그대로일 때만" 롤백 + `toast.error`. 캐릭터 보드의 표준 헬퍼는 `useCharacterBoardStore`의 `applyCharacterUpdate`/`applyCostumeUpdate`.
- **테스트:** vitest 아님 — Node 22 내장 `node --test`. 테스트 파일은 레포 루트 `tests/` 폴더의 camelCase `*.test.ts`, import 는 **상대경로 + 명시적 `.ts` 확장자**(`@/` alias 를 런타임 import 하면 `ERR_MODULE_NOT_FOUND`). **새 테스트 파일은 package.json 의 해당 `test:*` 스크립트에 명시적으로 나열해야만 실행된다** — 나열하지 않으면 build 가 통과해도 한 번도 실행되지 않는다.
- **검증 커맨드:** 코드 변경 후 `npm run typecheck` + 관련 `npm run test:*` + `npm run build:vite` 통과 필수 (CLAUDE.md 규칙).
- **디자인 토큰:** 배경 `#0F1117` / 카드 `#1A1D27` / 보더 `#2D3041` / 텍스트 `#E8E8EE` / 텍스트 약 `#8B8DA3` / 액센트 `#6C5CE7`. Tailwind 클래스는 `bg-bg-card`, `border-bg-border`, `text-text-primary`, `text-text-secondary`, `text-accent` 등 토큰 별칭 사용.
- **캐릭터 현황판 레이어:** `src/constants/characterLayers.ts` 의 `CHARACTER_LAYER_CLASS = { modal: 'z-50', lightbox: 'z-[70]', menu: 'z-[80]', popover: 'z-[85]', editor: 'z-[90]' }`.
- **미리보기(수동 검증) 실행:** `npm run dev:renderer` → 브라우저에서 `http://localhost:5190/?preview=1` → 스플래시 후 로그인 화면에서 mock 사용자 `배한솔` / 비밀번호 `1234`. Electron API 는 `src/mocks/devElectronAPI.ts` 가 대체한다.
- **버전/문서 규칙:** 기능 추가 = 마이너 버전. `DEVLOG/update-notes.json` 최상단에 항목 추가 — **비개발자 톤 필수**(기술 용어·파일명·컴포넌트명·IPC 채널명 금지, "상황+영향+결과" 시나리오 문장). 커밋 메시지는 한글.
- **슬랙 리깅 공지 구조:** 앱은 메시지를 조립하지 않는다. `RiggingAnnounceModal → sendRiggingAnnounce → IPC 'slack:send-rigging-webhook' → Slack Workflow 트리거 URL` 로 평면 변수 `{title, CH_name, Path, bigo, image}` 만 JSON POST 하고, 메시지 레이아웃(하이퍼링크 서식 포함)은 Slack Workflow Builder 쪽 설정이다. 따라서 (e) "이미지 링크 비노출"은 앱 코드 변경이 아니라 슬랙 워크플로 설정 사안이다.

## 3. 코드베이스 지도 (이번 작업 관련만)

| 파일 | 역할 | 이번 작업 |
|---|---|---|
| `src/views/compositing-dashboard/timeline/TimelinePanel.tsx` | 컴포지팅 타임라인 + hover 씬 미리보기 캐러셀(body 포털) | 작업 1 수정 |
| `src/services/slackWebhookService.ts` | 슬랙 워크플로 변수 조립(리깅 공지 `sendRiggingAnnounce`) | 작업 2 수정 |
| `src/components/characters/RiggingAnnounceModal.tsx` | 리깅 완성 공지 모달 (253줄, 전체 교체) | 작업 3 수정 |
| `src/components/characters/FeaturedImageSlot.tsx` | 캐릭터 이미지 업로드 단일 수렴점(`handleUpload`) + 전역 paste 리스너 | 작업 3·5·6 수정 |
| `tests/riggingAnnounce.test.ts` | B11 공지 소스 앵커 테스트 — 모달/서비스 수정 시 반드시 동반 갱신 | 작업 2·3 수정 |
| `DEVLOG/migrations/2026-07-15-costume-image-natural-size.sql` | 신규 마이그레이션(원본 크기 컬럼) | 작업 4 생성 |
| `src/types/index.ts` | 도메인 타입 + ElectronAPI 시그니처 집중 | 작업 4·6 수정 |
| `electron/supabase.ts` | 메인 프로세스 DB 함수(`addCostumeImage` 4312행, `CharacterCostumeImageRow` 4032행) | 작업 4 수정 |
| `electron/preload.ts` | IPC 브리지(`supabaseAddCostumeImage` 734행) | 작업 4 수정 |
| `src/services/supabaseService.ts` | 렌더러 서비스(`rowToCostumeImage` 991행, `addCharacterCostumeImage` 1013행) | 작업 4 수정 |
| `src/stores/useCharacterBoardStore.ts` | 캐릭터 보드 store(`addCostumeImage` 575행, `setCharacterReferenceHeight` 기존) | 작업 4 수정 |
| `src/mocks/devElectronAPI.ts` | 프리뷰 mock(`supabaseAddCostumeImage` 1585행, `storageUploadCharacterImage` 1579행) | 작업 4 수정 |
| `src/utils/imageUtils.ts` | `resizeBlob`/`resizeDataUrl`(800px 축소) — 원본 크기 측정 유틸 추가 | 작업 5 수정 |
| `src/utils/characterHeightGuides.ts` | 신규 — 기준선 계산 순수 유틸(alias-free) | 작업 6 생성 |
| `src/components/characters/CharacterHeightEditor.tsx` | 신규 — 기준선 드래그 에디터 | 작업 6 생성 |
| `src/components/characters/CostumeDetail.tsx` | 기준 키 입력 UI(221~238행) + 공지 모달 진입점 | 작업 6 수정 |
| `tests/characterTier2.test.ts` | T2-3 기준 키(43~68행)·store 액션(125~131행) 소스 앵커 테스트 | 작업 4에서 앵커 1건 갱신 + 작업 6에서 라벨 유지 확인 |
| `tests/compositingCarouselLayer.test.ts` / `tests/characterHeightKey.test.ts` | 신규 테스트 | 작업 1·6 생성 |
| `package.json` | 테스트 스크립트 등록 + 버전 | 작업 1·6·7 수정 |
| `DEVLOG/update-notes.json` | 업데이트 노트(배열 최신이 첫 요소) | 작업 7 수정 |

**참조 전용(수정 금지):** `src/components/NotificationPanel.tsx`(알림창 — z 구조 이해용), `src/components/layout/Header.tsx`(`relative z-30` — **절대 올리지 말 것**, 5절 참고), `src/components/characters/CharacterImageFitEditor.tsx`(에디터 패턴 원본), `electron/main.ts`·`electron/clipboardFiles.ts`(클립보드 IPC — 무변경 재사용).

## 4. 공통 규칙

- **커밋:** 작업당 1커밋, 메시지는 한글로 변경 내용을 명확히. 예시는 각 작업 끝에 있다. 테스트 갱신·추가는 해당 코드 수정과 같은 커밋에 포함.
- **작업 브랜치:** `main` 최신에서 분기, 브랜치명 `fix/feedback-30-33` 권장.
- **금지:** 이 문서에 없는 리팩터링·의존성 추가·파일 이동. `electron/main.ts` 는 이번 범위에서 수정하지 않는다(기존 IPC 재사용). `Header.tsx` 의 z-index 변경 금지.
- **버전 표기 정책:** 이 문서는 다음 버전을 `1.91.0` 으로 가정한다(스냅샷 main = 1.90.0). **구현 착수 시점에 origin/main 의 `package.json` 버전을 다시 확인**하고, 1.90.0 보다 올라가 있으면 그 기준 마이너 +1 로 조정한다(update-notes·커밋 메시지·PR 제목 동일 적용).

## 5. 작업 순서

**1 → 2 → 3 → 4 → 5 → 6 → 7.** 근거:
- 작업 1은 다른 작업과 완전 독립(컴포지팅 뷰만) — 가장 저위험이라 먼저 끝내 리듬을 잡는다.
- 작업 3(모달 전체 교체)은 작업 2(서비스의 `buildJbbjOpenLink`)와 같은 테스트 파일을 갱신하므로 2 다음에 한다. 모달 교체본은 서비스 변경과 무관하게 컴파일된다(호출 시그니처 불변).
- 작업 5(자동 설정)와 작업 6(에디터)은 작업 4(원본 크기 배선)의 타입·store 시그니처에 의존한다.
- 작업 6은 작업 3이 만든 `FeaturedImageSlot` paste 가드 셀렉터 문자열을 다시 수정한다(순서 의존 — before 블록이 작업 3 완료 상태 기준).

**작업 간 의존성 요약:** 3→(2), 5→(4), 6→(3, 4). 나머지 독립.

## 6. 전역 리스크 & 주의점

1. **소스 앵커 테스트 동반 갱신:** `tests/riggingAnnounce.test.ts` 와 `tests/characterTier2.test.ts` 는 소스 문자열을 정규식으로 고정한다. 이 문서의 각 작업이 지정한 테스트 갱신을 빠뜨리면 `npm run test:character` 가 반드시 깨진다. 지정된 갱신 외에 임의로 앵커를 지우지 말 것.
2. **새 테스트 파일 등록:** `tests/compositingCarouselLayer.test.ts` → `test:ui`, `tests/characterHeightKey.test.ts` → `test:character` 에 **명시 등록**해야 실행된다(작업 1·6에 정확한 diff 있음).
3. **마이그레이션 적용 전 배포 금지:** 작업 4 이후의 앱은 이미지 추가 시 `natural_width/natural_height` 컬럼에 값을 쓴다(값이 있을 때만 conditional insert 라 마이그레이션 전에도 대부분 동작하지만, 원본 측정이 성공한 업로드는 실패한다). **부록 B-2 의 라이브 DB 적용 게이트를 배포 전에 반드시 통과**할 것. 컬럼은 추가 전용이라 구버전 앱과 호환된다.
4. **슬랙 워크플로(한솔 몫)와의 조율:** 앱은 Path 변수 내용만 바꾼다(경로 텍스트 → jbbj URL). 슬랙 메시지에서 실제 클릭 가능한 링크가 되려면 Slack Workflow Builder 의 메시지 서식이 Path 변수를 하이퍼링크 URL 로 쓰고 있어야 한다(한솔이 이미 서식 있음 확인). (d) 빈 비고의 표시, (e) 이미지 URL 텍스트 비노출도 워크플로 설정 사안 — 부록 B-3 체크리스트로 한솔에게 전달.
5. **`@/` alias 금지(테스트 대상 유틸):** `src/utils/characterHeightGuides.ts` 는 node --test 가 직접 import 하므로 어떤 `@/` import 도 넣지 말 것(간접 import 포함).
6. **기존 라이브 데이터:** 기준 키의 의미가 "임의 단위 → 실제 px"로 바뀌지만, 기존 입력값(예: mock 시드 500/600/650)은 그대로 둔다 — 마이그레이션·백필 없음(부록 C 참고). 자동 설정은 **값이 비어 있을 때만** 동작하므로 기존 값을 덮지 않는다.
7. **대형 파일 주의:** `electron/supabase.ts`(4,593줄)·`src/services/supabaseService.ts`·`src/types/index.ts` 는 크다. 반드시 앵커 검색으로 위치를 잡을 것.

---

# 작업 1 — 항목 30: 캐러셀이 알림창을 덮는 z-order 수정 (+ 가림 검사)

## 문제/요청 요약

알림창을 열어둔 채 컴포지팅 타임라인 위로 마우스가 지나가면 씬 미리보기 캐러셀이 알림창 **위에** 그려진다. 캐러셀을 알림창 아래로 내리고, 알림창·모달이 좌표를 덮고 있을 때는 hover 미리보기가 아예 발화하지 않게 한다.

## 근거 (파일: `src/views/compositing-dashboard/timeline/TimelinePanel.tsx`, `src/components/NotificationPanel.tsx`, `src/components/layout/Header.tsx`)

캐러셀은 body 포털에 `zIndex: 90` (TimelinePanel.tsx:629-632, 앵커 `createPortal(`):

```tsx
      {carousel && typeof document !== 'undefined' && createPortal(
        <div
          className="fixed left-0 pointer-events-none"
          style={{ top: carousel.top, width: '100vw', height: 150, zIndex: 90 }}
```

알림창은 포털이 아니라 헤더 안 인라인 렌더(NotificationPanel.tsx:334 `className="absolute right-0 top-full mt-2 z-[9999] rounded-xl"`)이고, 부모 헤더가 `<header className="relative z-30 ...">` (Header.tsx:31)로 stacking context 를 만들므로 알림창의 실효 z 는 루트 기준 **30**이다. 90 > 30 → 캐러셀이 항상 위.

또한 hover 는 요소 이벤트 외에 window `pointermove` + rect 히트테스트(TimelinePanel.tsx:411-464 `flushDocumentHoverPointerMove`)로도 발화하며 **가림을 검사하지 않는다** — 알림창이 진행률 바를 덮어도 좌표가 바 rect 안이면 캐러셀이 뜬다(씬 상세 모달 `z-[80]` 열림 중에도 동일한 잠재 버그).

## 근본 원인

루트 stacking context 에서 캐러셀(90)이 헤더 서브트리(실효 30)보다 높고, window 레벨 히트테스트가 최상단 요소 확인 없이 발화한다.

## 수정 내용

### (a) `TimelinePanel.tsx` — 캐러셀 레이어 상수 추가 (26~28행, 앵커: `const CAROUSEL_SLOT_GAP = 78;`)

**before (원문):**
```ts
const CAROUSEL_SLOT_GAP = 78;
```
**after:**
```ts
const CAROUSEL_SLOT_GAP = 78;
/** 캐러셀 포털 레이어 z — 핀 카드(20)보다 위, 헤더(z-30)·사이드바(z-40)·모달(z-50+)보다 아래 (피드백 30). */
const CAROUSEL_LAYER_Z = 25;
```

25 인 근거(루트 기준 전수 비교): 위여야 하는 것 — 핀 SceneCard `zIndex: 20`(`src/views/compositing-dashboard/cards/SceneCard.tsx:145`), 타임라인 리사이즈 핸들 `zIndex: 5`(TimelinePanel.tsx:902). 아래여야 하는 것 — 헤더 `z-30`(알림창), 사이드바 `z-40`, BulkActionBar `z-40`, 드래그 선택 박스 `z-50`, `CompositingSceneModal z-[80]`, 그 외 모든 모달·토스트. **헤더의 z-30 을 올리는 대안은 금지** — main 영역 안에서 인라인 렌더되는 전 앱 모달(z-50~200)이 헤더 뒤로 깔리는 광범위 회귀를 만든다.

### (b) `TimelinePanel.tsx` — zIndex 적용 (632행, 앵커: `height: 150, zIndex: 90`)

**before (원문):**
```tsx
          style={{ top: carousel.top, width: '100vw', height: 150, zIndex: 90 }}
```
**after:**
```tsx
          style={{ top: carousel.top, width: '100vw', height: 150, zIndex: CAROUSEL_LAYER_Z }}
```

### (c) `TimelinePanel.tsx` — 가림(occlusion) 검사 (413~416행, 앵커: `const { clientX, clientY } = pointer;`)

`flushDocumentHoverPointerMove` 안, 포인터 좌표를 꺼낸 직후에 삽입한다.

**before (원문):**
```ts
    const pointer = pendingHoverPointerRef.current;
    if (!pointer) return;

    const { clientX, clientY } = pointer;
    const cards = document.querySelectorAll<HTMLElement>('[data-compositing-carousel-card="true"]');
```
**after:**
```ts
    const pointer = pendingHoverPointerRef.current;
    if (!pointer) return;

    const { clientX, clientY } = pointer;
    // 피드백 30: 가림(occlusion) 검사 — 알림창·모달 등 다른 UI 가 이 좌표를 덮고 있으면 hover 미리보기를
    //   발화시키지 않는다. 최상단 요소가 캐러셀 카드(pointerEvents:auto)거나 타임라인 트랙 내부일 때만 통과.
    //   rAF 당 1회 실행이라 elementFromPoint 비용은 무시 가능.
    const topElement = document.elementFromPoint(clientX, clientY);
    const overTimeline = !!topElement && (
      topElement.closest('[data-compositing-carousel-card="true"]') !== null
      || (trackContainerRef.current?.contains(topElement) ?? false)
    );
    if (!overTimeline) {
      lastHoverKeyRef.current = null;
      return;
    }
    const cards = document.querySelectorAll<HTMLElement>('[data-compositing-carousel-card="true"]');
```

주의: `trackContainerRef` 는 이미 함수 스코프에 있다(438행에서 사용, ref 라 useCallback deps 추가 불필요). 이 early-return 은 기존 "아무것도 안 맞음" 경로(464행 `lastHoverKeyRef.current = null;`)와 동일한 의미라 캐러셀 닫힘 동작(요소 pointerleave → `scheduleProbeClear`)을 바꾸지 않는다. 캐러셀 카드 버튼은 `pointerEvents: 'auto'`(681행)라 카드 위에서는 `closest` 분기가 통과한다.

### (d) 신규 테스트 — `tests/compositingCarouselLayer.test.ts` (새 파일, 전체 내용)

```ts
/**
 * 피드백 30 — 컴포지팅 캐러셀 레이어/가림 검사 배선 고정.
 */
import { readFileSync } from 'node:fs';
import test from 'node:test';
import assert from 'node:assert/strict';

const panel = readFileSync('src/views/compositing-dashboard/timeline/TimelinePanel.tsx', 'utf8');

test('캐러셀 z 는 헤더(z-30) 아래 상수로 고정 — 알림창을 덮지 않는다', () => {
  assert.match(panel, /const CAROUSEL_LAYER_Z = 25;/);
  assert.match(panel, /zIndex: CAROUSEL_LAYER_Z/);
  assert.doesNotMatch(panel, /height: 150, zIndex: 90/);
});

test('hover 히트테스트는 최상단 요소 가림 검사를 통과해야 발화', () => {
  assert.match(panel, /document\.elementFromPoint\(clientX, clientY\)/);
  assert.match(panel, /trackContainerRef\.current\?\.contains\(topElement\)/);
});
```

### (e) `package.json` — 테스트 등록 (앵커: `"test:ui":`)

**before (원문):**
```json
    "test:ui": "node --test ./tests/sidebarNavVisibility.test.ts",
```
**after:**
```json
    "test:ui": "node --test ./tests/sidebarNavVisibility.test.ts ./tests/compositingCarouselLayer.test.ts",
```

## 엣지 케이스 & 주의사항

- 캐러셀 z 를 25로 내리면 좌측 첫 파트 hover 시 가장자리 저투명 카드가 사이드바(`fixed z-40`) **뒤로** 들어간다(현재는 위로 떴음). 시각적으로 더 정돈되는 방향이라 수용 — 별도 대응 금지.
- BulkActionBar(`fixed z-40`, 하단)와 겹치면 캐러셀이 아래로 간다 — hover 미리보기가 능동 UI 에 양보하는 의도된 결과.
- `elementFromPoint` 가 null(포인터가 뷰포트 밖)이면 발화하지 않는다 — 올바른 동작.
- `tests/compositingTimelineCarousel.test.ts` 와 `tests/compositingTimelineHoverSurface.test.ts` 는 저장소에 존재하지만 **어느 test:* 스크립트에도 등록되지 않은(실행 안 되는) 파일**이다. 이번 변경으로 두 파일의 앵커는 깨지지 않음이 확인됐다 — 등록도 수정도 하지 말 것.

## 검증 방법

1. `npm run typecheck` && `npm run test:ui` → 신규 테스트 2건 포함 통과.
2. 수동(부록 A-1): 알림창을 연 채 그 뒤 타임라인 좌표에 마우스 → **캐러셀이 뜨지 않고** 알림창이 항상 위. 알림창 없이는 캐러셀 hover·클릭 핀·더블클릭 상세가 기존대로 동작.

## 완료 기준

- [ ] `zIndex: 90` 리터럴이 캐러셀 포털에서 제거되고 상수(25) 참조로 대체
- [ ] 가림 검사로 알림창/모달 위 좌표에서 hover 미발화
- [ ] `tests/compositingCarouselLayer.test.ts` 등록·통과, 기존 test:ui 5건 유지
- [ ] 커밋 1개 — 예: `컴포지팅 캐러셀이 알림창을 덮던 z-order 수정 + hover 가림 검사 (피드백 30)`

---

# 작업 2 — 항목 31(c): 공지 경로를 jbbj://open/ 링크로 전송

## 문제/요청 요약

리깅 공지의 Path 변수에 Windows 원본 경로(`G:\공유 드라이브\...`)가 그대로 나가 슬랙에서 클릭할 수 없다. 스튜디오 공용 `jbbj://open/` 프로토콜 링크로 변환해 보낸다(슬랙 워크플로의 하이퍼링크 서식이 이 변수를 URL 로 사용).

## 근거 (파일: `src/services/slackWebhookService.ts`)

47~56행 (앵커 `export async function sendRiggingAnnounce`):

```ts
/** 리깅 완성 작업공지 전송. 워크플로 변수 title/CH_name/Path/bigo/image 를 채운다. 실패 시 throw. */
export async function sendRiggingAnnounce(params: RiggingAnnounceParams): Promise<{ ok: boolean }> {
  const payload: Record<string, string> = {
    title: params.title,
    CH_name: params.characterName,
    Path: params.folderPath ?? '',
    bigo: buildRiggingBigo(params.notes),
    image: params.imageUrl ?? '',
  };
  return window.electronAPI.sendRiggingWebhook(payload);
}
```

`jbbj://` 프로토콜은 앱 어디에도 등록·처리되지 않는다(앱이 등록하는 커스텀 스킴은 `bflow://` 뿐 — electron/main.ts:4486 `const PROTOCOL = 'bflow';`). OS(스튜디오 오토핫키 환경)에 등록된 외부 프로토콜이므로 **앱 쪽 프로토콜 등록은 하지 않는다.**

## 근본 원인

Path 변수 조립 시 경로를 URL 로 변환하는 코드가 없다.

## 수정 내용

### (a) `slackWebhookService.ts` — `buildJbbjOpenLink` 추가 + Path 매핑 변경 (41~56행, 앵커: `export function buildRiggingBigo`)

`buildRiggingBigo` 함수 **앞**에 새 함수를 추가하고, `sendRiggingAnnounce` 의 Path 줄을 교체한다.

**before (원문):**
```ts
/** 비고 배열 → 슬랙 bigo 문자열(빈 줄 제거 + 줄바꿈 결합). */
export function buildRiggingBigo(notes: string[]): string {
```
**after:**
```ts
/**
 * 캐릭터 작업 폴더 경로 → 스튜디오 공용 jbbj://open/ 링크 (피드백 31c).
 * 오토핫키 Ctrl+Shift+V 가 만드는 링크와 같은 포맷 — 백슬래시는 /, 각 폴더명은 percent 인코딩,
 * 드라이브 문자(G:)와 대괄호([])는 원문 유지(스튜디오 예시 링크와 바이트 단위 일치 확인됨).
 * 예) G:\공유 드라이브\사우스 코리안 파크 → jbbj://open/G:/%EA%B3%B5%EC%9C%A0%20%EB%93%9C%EB%9D%BC%EC%9D%B4%EB%B8%8C/...
 */
export function buildJbbjOpenLink(path: string): string {
  const encoded = path
    .replace(/\\/g, '/')
    .split('/')
    .map((seg) => (/^[A-Za-z]:$/.test(seg)
      ? seg
      : encodeURIComponent(seg).replace(/%5B/g, '[').replace(/%5D/g, ']')))
    .join('/');
  return `jbbj://open/${encoded}`;
}

/** 비고 배열 → 슬랙 bigo 문자열(빈 줄 제거 + 줄바꿈 결합). */
export function buildRiggingBigo(notes: string[]): string {
```

**before (원문, `sendRiggingAnnounce` 내부):**
```ts
    Path: params.folderPath ?? '',
```
**after:**
```ts
    // 피드백 31(c): Path 는 원본 경로 대신 클릭하면 탐색기가 열리는 jbbj://open/ 링크로 보낸다
    //   (슬랙 워크플로 메시지의 하이퍼링크 서식이 이 변수를 URL 로 쓴다 — 한솔 확인).
    Path: params.folderPath ? buildJbbjOpenLink(params.folderPath) : '',
```

`RiggingAnnounceParams` 인터페이스와 그 주석(33행 `folderPath: string | null`)은 그대로 둔다 — 모달은 계속 원본 경로를 넘기고, 변환은 서비스가 담당한다.

### (b) 테스트 갱신 — `tests/riggingAnnounce.test.ts` 36행 (앵커: `Path: params\.folderPath`)

**before (원문):**
```ts
  assert.match(svc, /Path: params\.folderPath \?\? ''/);
```
**after:**
```ts
  assert.match(svc, /Path: params\.folderPath \? buildJbbjOpenLink\(params\.folderPath\) : ''/);
```

### (c) 단위 테스트 추가 — `tests/riggingAnnounce.test.ts` 파일 끝에 append

import 줄(7행)도 함께 갱신한다.

**before (원문, 7행):**
```ts
import { buildRiggingBigo } from '../src/services/slackWebhookService.ts';
```
**after:**
```ts
import { buildRiggingBigo, buildJbbjOpenLink } from '../src/services/slackWebhookService.ts';
```

파일 끝에 append (아래 기대값은 전부 **실행 시뮬레이션으로 통과 확인된 값** — 첫 케이스는 스튜디오 오토핫키 실제 예시 링크와 바이트 단위 일치):

```ts
test('buildJbbjOpenLink: 스튜디오 오토핫키 링크 포맷과 일치 (피드백 31c)', () => {
  // 실제 스튜디오 예시 링크와 바이트 단위 일치 — 드라이브 문자·대괄호는 원문, 한글·공백은 percent 인코딩.
  assert.equal(
    buildJbbjOpenLink('G:\\공유 드라이브\\사우스 코리안 파크\\[]사코팍 캐릭터 세팅\\휠체어 할머니'),
    'jbbj://open/G:/%EA%B3%B5%EC%9C%A0%20%EB%93%9C%EB%9D%BC%EC%9D%B4%EB%B8%8C/%EC%82%AC%EC%9A%B0%EC%8A%A4%20%EC%BD%94%EB%A6%AC%EC%95%88%20%ED%8C%8C%ED%81%AC/[]%EC%82%AC%EC%BD%94%ED%8C%8D%20%EC%BA%90%EB%A6%AD%ED%84%B0%20%EC%84%B8%ED%8C%85/%ED%9C%A0%EC%B2%B4%EC%96%B4%20%ED%95%A0%EB%A8%B8%EB%8B%88',
  );
  // URL 특수문자(#, %)는 안전하게 인코딩된다.
  assert.equal(buildJbbjOpenLink('G:\\공유 드라이브\\A#B 50%'), 'jbbj://open/G:/%EA%B3%B5%EC%9C%A0%20%EB%93%9C%EB%9D%BC%EC%9D%B4%EB%B8%8C/A%23B%2050%25');
  // 이미 슬래시인 경로도 동일 처리.
  assert.equal(buildJbbjOpenLink('G:/이미 슬래시/경로'), 'jbbj://open/G:/%EC%9D%B4%EB%AF%B8%20%EC%8A%AC%EB%9E%98%EC%8B%9C/%EA%B2%BD%EB%A1%9C');
});
```

## 엣지 케이스 & 주의사항

- 빈 경로/미연결(`folderPath === null || ''`)이면 기존처럼 Path='' — `buildJbbjOpenLink` 를 타지 않는다.
- 저장된 경로는 항상 비인코딩 Windows 원본 경로라는 전제(경로는 폴더 선택 다이얼로그로 저장됨 — `characters.work_folder_path`). 이중 인코딩 우려 없음.
- UNC 경로(`\\server\...`)는 스튜디오에서 쓰지 않는다 — 별도 처리 금지(YAGNI).
- 대괄호를 원문 유지하는 이유: 오토핫키가 만든 기존 링크가 그렇게 동작 중이므로, jbbj 핸들러의 디코딩 방식과 무관하게 검증된 포맷을 그대로 따른다.

## 검증 방법

1. `npm run typecheck` && `npm run test:character` → riggingAnnounce.test.ts 갱신분 포함 통과.
2. 수동(부록 A-2): 프리뷰에서 공지 전송 시 브라우저 콘솔의 `[DEV 리깅 공지 웹훅] 페이로드:` 로그에서 Path 가 `jbbj://open/G:/...` 형태인지 확인.

## 완료 기준

- [ ] `buildJbbjOpenLink` export + Path 매핑 교체
- [ ] 위 테스트 3케이스 + 갱신 앵커 통과, 기존 buildRiggingBigo 테스트 유지
- [ ] 커밋 1개 — 예: `리깅 공지 경로를 jbbj://open 탐색기 링크로 전송 (피드백 31c)`

---

# 작업 3 — 항목 31(a)(b)(d)(e): 리깅 공지 모달 개편 (템플릿·Ctrl+V·비고 선택화)

## 문제/요청 요약

(a) 제목 템플릿을 `[모호 리깅 현황] - …` 포맷으로 바꾸고 기본 제목을 미리 채우며, [내용](CH_name)을 `"캐릭터" - "복장"` 포맷으로. (b) 캡처 이미지를 Ctrl+V 로 공지에 첨부. (d) 비고 없이도 전송 가능. (e) 이미지 링크 비노출은 슬랙 워크플로 설정 사안 — 코드 무변경, 한솔 안내만(부록 B-3).

## 근거 (파일: `src/components/characters/RiggingAnnounceModal.tsx` 253줄 전체 — 스냅샷 원문 확인됨)

핵심 현재 동작:
- 템플릿(16~20행): `'[모호 리깅 현황] 리깅 완료 공지'` 등 **대시 없음**, 제목은 `useState('')` 빈값 시작(40행).
- CH_name(83행): `` characterName: `${character.name} · ${costume.name}` ``.
- 비고 필수(67행 `const hasNote = buildRiggingBigo(notes).length > 0;`, 78행 `if (!hasNote) { toast.error('비고를 한 줄 이상 적어주세요'); return; }`, 243행 `disabled={sending || !hasNote}`).
- 이미지는 복장에 등록된 이미지 중 선택만 가능 — paste 처리 없음.
- 첫 비고 input 이 `autoFocus`(208행) — 전역 paste 가드가 "input 안이면 무시" 방식이면 Ctrl+V 가 영원히 안 먹는 함정.

재사용할 기존 인프라(무변경):
- `window.electronAPI.clipboardReadImageFile()` — 탐색기 파일 복사 폴백(FileNameW, electron/main.ts:4023, 프리뷰 mock 은 null 반환).
- `uploadCharacterImage(characterId, costumeId, base64)` (src/services/supabaseService.ts:1080) → Supabase Storage `scene-images` 버킷 `characters/{id}/{costumeId}/` 공개 URL.
- `resizeBlob`(src/utils/imageUtils.ts) / `dataUrlToFile`(src/utils/dataUrlToFile.ts) / `deleteImage`(src/services/storageService.ts:16).
- `FeaturedImageSlot.tsx` 432~460행의 paste 패턴(원본 — 단 그쪽 가드는 그대로 복사하면 안 됨, 위 함정 참고).

## 근본 원인

모달이 B11 초기 스펙(비고 필수·등록 이미지 선택만) 그대로다.

## 수정 내용

### (a) `RiggingAnnounceModal.tsx` — 파일 전체 교체

작업 2·3의 모달 측 변경이 파일 전반에 걸치므로 전체 교체가 가장 안전하다. **아래 내용으로 파일 전체를 교체한다** (이 코드는 계획 단계에서 정규식 시뮬레이션 26건을 통과한 검증본):

```tsx
import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { Loader2, Plus, X } from 'lucide-react';
import { toast } from 'sonner';
import { useCharacterBoardStore } from '@/stores/useCharacterBoardStore';
import { useModalFocus } from '@/hooks/useModalFocus';
import { CHARACTER_LAYER_CLASS } from '@/constants/characterLayers';
import { sendRiggingAnnounce } from '@/services/slackWebhookService';
import { uploadCharacterImage } from '@/services/supabaseService';
import { deleteImage } from '@/services/storageService';
import { resizeBlob } from '@/utils/imageUtils';
import { dataUrlToFile } from '@/utils/dataUrlToFile';
import type { Character, CharacterCostume } from '@/types';

interface NoteRow {
  id: number;
  text: string;
}

/** 공지 전용으로 붙여넣은 이미지 — 복장 이미지 목록(character_costume_images)에는 등록하지 않는 one-off 업로드. */
interface PastedImage {
  id: string;
  url: string;
}

/** 제목 템플릿 — 버튼을 누르면 제목(title)만 채운다(비고는 직접 작성). '[모호 리깅 현황] - …' 포맷 (피드백 31a). */
const TITLE_TEMPLATES: { key: string; label: string; title: string }[] = [
  { key: 'general', label: '일반 리깅 완료', title: '[모호 리깅 현황] - 리깅 완료 공지' },
  { key: 'drama', label: '드라마 톤 특수리깅 완료', title: '[모호 리깅 현황] - 드라마 톤 특수리깅 완료 공지' },
  { key: 'fix', label: '수정·보완 완료', title: '[모호 리깅 현황] - 리깅 수정·보완 완료 공지' },
];

/**
 * 리깅 완성 슬랙 공지 모달 (B11 + 피드백 31).
 * - 제목은 '일반 리깅 완료' 템플릿이 미리 채워져 있고, 다른 템플릿 버튼이나 직접 입력으로 바꾼다.
 * - 캐릭터 작업 폴더 경로가 자동으로 뜨고(연결돼 있으면), 슬랙에는 jbbj://open/ 링크로 변환돼 나간다.
 * - 이미지는 그 복장의 이미지 중 고르거나(기본 대표), 화면 캡처를 Ctrl+V 로 바로 붙여넣는다(공지 전용 업로드).
 * - 비고는 선택 사항 — 없어도 보낼 수 있다(피드백 31d). 여러 줄은 슬랙에서 줄바꿈으로 이어진다.
 * - 워크플로 변수 CH_name="캐릭터" - "복장" / Path=폴더 jbbj 링크 / bigo=비고(줄바꿈) / image=고른 이미지 공개 URL.
 */
export function RiggingAnnounceModal({
  character,
  costume,
  onClose,
}: {
  character: Character;
  costume: CharacterCostume;
  onClose: () => void;
}) {
  const imagesByCostume = useCharacterBoardStore((s) => s.imagesByCostume);
  const images = useMemo(() => imagesByCostume.get(costume.id) ?? [], [imagesByCostume, costume.id]);

  // 제목은 기본 템플릿(일반 리깅 완료)이 미리 채워진 상태로 시작 — 비고 없이도 바로 보낼 수 있게 (피드백 31a·31d).
  const [title, setTitle] = useState(TITLE_TEMPLATES[0].title);
  const [activeTpl, setActiveTpl] = useState<string | null>(TITLE_TEMPLATES[0].key);
  const nextRowId = useRef(1);
  const [rows, setRows] = useState<NoteRow[]>([{ id: 0, text: '' }]);
  // 기본 선택 이미지 = 대표(primary), 없으면 첫 이미지, 그것도 없으면 이미지 없이.
  const [selectedImageId, setSelectedImageId] = useState<string | null>(
    () => (images.find((i) => i.isPrimary) ?? images[0])?.id ?? null,
  );
  const [pasted, setPasted] = useState<PastedImage[]>([]);
  const [pasting, setPasting] = useState(false);
  const [sending, setSending] = useState(false);

  const dialogRef = useRef<HTMLDivElement>(null);
  const modalFocus = useModalFocus(dialogRef, { autoFocus: false });

  // 정리(cleanup)용 최신값 참조 — Escape 리스너는 deps 없이 한 번만 걸리므로 ref 로 최신 상태를 본다.
  const pastedRef = useRef<PastedImage[]>([]);
  useEffect(() => { pastedRef.current = pasted; }, [pasted]);

  /** 공지 전용 업로드 정리 — keepUrl(전송에 쓴 이미지)만 남기고 스토리지에서 삭제(고아 방지, fire-and-forget). */
  const cleanupPastedUploads = (keepUrl: string | null) => {
    for (const img of pastedRef.current) {
      if (keepUrl && img.url === keepUrl) continue;
      deleteImage(img.url).catch((e) => console.warn('[rigging-announce] 붙여넣기 이미지 정리 실패:', e));
    }
  };

  /** 취소·백드롭·Escape 공통 닫기 — 보내지 않은 붙여넣기 이미지를 정리하고 닫는다. */
  const handleClose = () => {
    cleanupPastedUploads(null);
    onClose();
  };
  const handleCloseRef = useRef(handleClose);
  useEffect(() => { handleCloseRef.current = handleClose; });

  useEffect(() => {
    // 최상단 모달 패턴(capture + stopImmediatePropagation) — 부모 CharacterDetailModal 의 Escape 리스너까지
    //   함께 발동해 상세 뷰가 닫히며 작성 내용이 날아가는 것을 막는다(코덱스 P2).
    const onKey = (event: KeyboardEvent) => {
      if (event.key !== 'Escape') return;
      event.preventDefault();
      event.stopImmediatePropagation();
      handleCloseRef.current();
    };
    window.addEventListener('keydown', onKey, { capture: true });
    return () => window.removeEventListener('keydown', onKey, { capture: true });
  }, []);

  /** 피드백 31(b): 캡처 이미지 업로드 — 복장 이미지에 등록하지 않는 공지 전용(one-off) 업로드. */
  const pasteLockRef = useRef(false);
  const uploadPastedFile = useCallback(async (file: File) => {
    if (pasteLockRef.current) return;
    if (!file.type.startsWith('image/')) {
      toast.info('이미지 파일만 붙여넣을 수 있어요');
      return;
    }
    pasteLockRef.current = true;
    setPasting(true);
    try {
      const isPng = file.type === 'image/png' || file.name.toLowerCase().endsWith('.png');
      const base64 = await resizeBlob(file, 800, isPng ? 0.92 : 0.8, isPng ? 'image/png' : 'image/jpeg');
      const res = await uploadCharacterImage(character.id, costume.id, base64);
      if (!res.ok || !res.url) throw new Error(res.error ?? '업로드 실패');
      const entry: PastedImage = {
        id: `pasted-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
        url: res.url,
      };
      setPasted((prev) => [...prev, entry]);
      setSelectedImageId(entry.id);
    } catch (err) {
      console.error('[rigging-announce] 붙여넣기 이미지 업로드 실패:', err);
      toast.error('이미지 붙여넣기에 실패했어요');
    } finally {
      pasteLockRef.current = false;
      setPasting(false);
    }
  }, [character.id, costume.id]);

  // 피드백 31(b): 공지 이미지 Ctrl+V.
  //   비고 입력칸에 포커스가 있어도(모달 열림 직후 autoFocus) 클립보드에 '이미지'가 있으면 첨부로 처리한다 —
  //   텍스트 붙여넣기는 이미지 item 이 없으므로 그대로 입력칸에 들어간다.
  useEffect(() => {
    const onPaste = (event: ClipboardEvent) => {
      const item = Array.from(event.clipboardData?.items ?? []).find((entry) => entry.type.startsWith('image/'));
      const file = item?.getAsFile();
      if (file) {
        event.preventDefault();
        void uploadPastedFile(file);
        return;
      }
      // 탐색기 '파일 복사'는 clipboardData 에 안 실리는 환경이 있어 메인 프로세스에서 파일 경로('FileNameW')를 직접 읽는다.
      //   텍스트 붙여넣기면 아래가 null 을 반환해 아무 일도 일어나지 않는다.
      void (async () => {
        const pastedFile = await window.electronAPI.clipboardReadImageFile();
        if (!pastedFile) return;
        const f = dataUrlToFile(pastedFile.dataUrl, pastedFile.fileName);
        if (f) void uploadPastedFile(f);
      })();
    };
    window.addEventListener('paste', onPaste);
    return () => window.removeEventListener('paste', onPaste);
  }, [uploadPastedFile]);

  const notes = rows.map((r) => r.text);

  // 고를 수 있는 이미지 = 복장에 등록된 이미지 + 이번 공지에서 붙여넣은 이미지.
  const selectable = useMemo(
    () => [
      ...images.map((img) => ({ id: img.id, url: img.url, isPrimary: img.isPrimary, isPasted: false })),
      ...pasted.map((img) => ({ id: img.id, url: img.url, isPrimary: false, isPasted: true })),
    ],
    [images, pasted],
  );
  const selectedImage = selectedImageId ? selectable.find((i) => i.id === selectedImageId) ?? null : null;

  const addRow = () => setRows((prev) => [...prev, { id: nextRowId.current++, text: '' }]);
  const removeRow = (id: number) =>
    setRows((prev) => (prev.length <= 1 ? prev : prev.filter((r) => r.id !== id)));
  const updateRow = (id: number, text: string) =>
    setRows((prev) => prev.map((r) => (r.id === id ? { ...r, text } : r)));

  const onSend = async () => {
    if (sending || pasting) return;
    setSending(true);
    try {
      await sendRiggingAnnounce({
        // 피드백 31(a): [내용] 줄 포맷 — '"캐릭터 이름" - "복장 이름"'. 복장이 여럿이어도 어떤 복장인지 구분된다.
        characterName: `"${character.name}" - "${costume.name}"`,
        title: title.trim(),
        folderPath: character.workFolderPath,
        notes,
        imageUrl: selectedImage?.url ?? null,
      });
      toast.success('슬랙에 리깅 완성 공지를 보냈어요');
      // 전송에 쓴 이미지는 남기고, 붙여넣기만 하고 안 쓴 이미지는 정리.
      cleanupPastedUploads(selectedImage?.url ?? null);
      onClose();
    } catch (err) {
      console.error('[rigging-announce] 전송 실패:', err);
      toast.error('공지 전송에 실패했어요');
    } finally {
      setSending(false);
    }
  };

  return (
    <div
      data-rigging-announce="true"
      className={`fixed inset-0 ${CHARACTER_LAYER_CLASS.modal} flex items-center justify-center bg-overlay/60 backdrop-blur-sm p-6`}
      onMouseDown={(event) => { if (event.target === event.currentTarget) handleClose(); }}
    >
      <div
        ref={dialogRef}
        role="dialog"
        aria-modal="true"
        aria-label="리깅 완성 공지"
        tabIndex={-1}
        onKeyDown={modalFocus.onKeyDown}
        className="bg-bg-card border border-bg-border rounded-2xl w-full max-w-lg p-5 flex flex-col gap-4 outline-none max-h-[85vh] overflow-y-auto"
      >
        <div className="flex flex-col gap-0.5">
          <h2 className="text-lg font-semibold text-text-primary">리깅 완성 공지</h2>
          <span className="text-xs text-text-secondary">{character.name} · {costume.name} 리깅 완성을 슬랙에 알려요</span>
        </div>

        {/* 템플릿 — 누르면 제목만 채운다 */}
        <div className="flex flex-col gap-1.5">
          <span className="text-xs text-text-secondary">템플릿 — 누르면 제목이 채워져요</span>
          <div className="flex flex-wrap gap-1.5">
            {TITLE_TEMPLATES.map((t) => {
              const on = activeTpl === t.key;
              return (
                <button
                  key={t.key}
                  type="button"
                  onClick={() => { setTitle(t.title); setActiveTpl(t.key); }}
                  className={`rounded-full px-2.5 py-1 text-[11px] border transition-colors ${
                    on ? 'border-accent bg-accent/15 text-accent' : 'border-bg-border text-text-secondary hover:border-bg-border/60'
                  }`}
                >
                  {t.label}
                </button>
              );
            })}
          </div>
        </div>

        {/* 제목 */}
        <div className="flex flex-col gap-1.5">
          <span className="text-xs text-text-secondary">제목</span>
          <input
            value={title}
            onChange={(e) => { setTitle(e.target.value); setActiveTpl(null); }}
            placeholder="예) 드라마 톤 특수리깅 완료 공지"
            className="bg-transparent border border-bg-border rounded-md px-3 py-2 text-sm text-text-primary outline-none focus:border-accent/50"
          />
        </div>

        {/* 경로 (자동) — 슬랙에는 jbbj://open/ 링크로 변환돼 나간다 (피드백 31c) */}
        <div className="flex flex-col gap-1.5">
          <span className="text-xs text-text-secondary">캐릭터 경로 — 슬랙에서 클릭하면 폴더가 열려요</span>
          {character.workFolderPath ? (
            <div
              className="text-sm text-text-primary bg-bg-border/10 border border-bg-border/70 rounded-md px-3 py-2 break-all"
              title={character.workFolderPath}
            >
              {character.workFolderPath}
            </div>
          ) : (
            <div className="text-sm text-text-secondary bg-bg-border/10 border border-dashed border-bg-border/70 rounded-md px-3 py-2">
              연결된 경로가 없어요 — 캐릭터 상세에서 작업 폴더를 연결하면 여기 자동으로 떠요.
            </div>
          )}
        </div>

        {/* 이미지 고르기 + Ctrl+V 붙여넣기 */}
        <div className="flex flex-col gap-1.5">
          <span className="text-xs text-text-secondary">공지에 붙일 이미지</span>
          {selectable.length === 0 ? (
            <div className="text-xs text-text-secondary">이 복장에 등록된 이미지가 없어요 — 캡처를 Ctrl+V 로 붙여넣거나, 이미지 없이 보냅니다.</div>
          ) : (
            <div className="flex flex-wrap gap-2">
              {selectable.map((img) => {
                const on = img.id === selectedImageId;
                return (
                  <button
                    key={img.id}
                    type="button"
                    onClick={() => setSelectedImageId(on ? null : img.id)}
                    title={on ? '선택 해제' : '이 이미지로 공지'}
                    className={`relative h-16 w-16 rounded-lg overflow-hidden border-2 transition-colors ${
                      on ? 'border-accent' : 'border-bg-border/70 hover:border-bg-border'
                    }`}
                  >
                    <img src={img.url} alt="" className="h-full w-full object-cover" />
                    {img.isPrimary && (
                      <span className="absolute top-0.5 left-0.5 text-[9px] leading-none px-1 py-0.5 rounded bg-black/60 text-white">대표</span>
                    )}
                    {img.isPasted && (
                      <span className="absolute top-0.5 left-0.5 text-[9px] leading-none px-1 py-0.5 rounded bg-accent/80 text-white">붙여넣기</span>
                    )}
                  </button>
                );
              })}
            </div>
          )}
          <span className="text-[11px] text-text-secondary">
            {pasting
              ? '이미지 올리는 중…'
              : selectedImage
                ? '슬랙에서 이 이미지 미리보기가 함께 표시돼요. 캡처는 Ctrl+V 로 추가할 수 있어요.'
                : '이미지 없이 텍스트만 보냅니다. 캡처는 Ctrl+V 로 추가할 수 있어요.'}
          </span>
        </div>

        {/* 비고 (선택, 여러 줄) */}
        <div className="flex flex-col gap-1.5">
          <span className="text-xs text-text-secondary">비고 (선택 — 여러 줄 가능, 슬랙에서 줄바꿈으로 표시)</span>
          <div className="flex flex-col gap-1.5">
            {rows.map((row, idx) => (
              <div key={row.id} className="flex items-center gap-1.5">
                <input
                  autoFocus={idx === 0}
                  value={row.text}
                  onChange={(e) => updateRow(row.id, e.target.value)}
                  onKeyDown={(e) => {
                    if (e.key === 'Enter') { e.preventDefault(); addRow(); }
                  }}
                  placeholder={idx === 0 ? '예) 서준 잠옷버전에 퀭한 이미지 추가! (생략 가능)' : '비고 추가'}
                  className="flex-1 bg-transparent border border-bg-border rounded-md px-3 py-2 text-sm text-text-primary outline-none focus:border-accent/50"
                />
                <button
                  type="button"
                  onClick={() => removeRow(row.id)}
                  disabled={rows.length <= 1}
                  title="이 줄 삭제"
                  className="shrink-0 p-1.5 rounded-md text-text-secondary hover:bg-bg-border/40 disabled:opacity-30 disabled:hover:bg-transparent"
                >
                  <X size={14} />
                </button>
              </div>
            ))}
          </div>
          <button
            type="button"
            onClick={addRow}
            className="self-start inline-flex items-center gap-1 text-xs text-accent hover:bg-accent/10 rounded-md px-2 py-1"
          >
            <Plus size={12} /> 비고 추가
          </button>
        </div>

        <div className="flex justify-end gap-2 pt-1">
          <button type="button" onClick={handleClose} className="px-3 py-1.5 rounded-lg text-sm text-text-secondary hover:bg-bg-border/40 cursor-pointer">취소</button>
          <button
            type="button"
            onClick={onSend}
            disabled={sending || pasting}
            className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-sm bg-accent text-white disabled:opacity-50 cursor-pointer"
          >
            {sending && <Loader2 size={13} className="animate-spin" />}
            슬랙으로 보내기
          </button>
        </div>
      </div>
    </div>
  );
}
```

교체 후 원본 대비 의미 변화 체크리스트(자가 점검용): ① 기본 제목/템플릿 대시 ② CH_name 따옴표 포맷 ③ `hasNote`·`buildRiggingBigo` import 제거, 전송 게이트 제거 ④ paste 리스너+one-off 업로드+정리 ⑤ 모든 닫기 경로가 `handleClose` 경유 ⑥ 루트에 `data-rigging-announce` ⑦ 그 외 구조·클래스·문구는 원본 유지.

### (b) `FeaturedImageSlot.tsx` — 배경 paste 이중 발화 차단 (435~438행, 앵커: `data-character-context-menu`)

공지 모달은 캐릭터 상세(FeaturedImageSlot 이 마운트된 화면) 위에 뜨므로, 가드에 모달 마커를 추가하지 않으면 Ctrl+V 한 번에 **복장 이미지 등록과 공지 첨부가 동시에** 일어난다.

**before (원문):**
```ts
      if (
        uploading || imageMenu || addMenu || fitEditorOpen
        || document.querySelector('[data-character-lightbox], [data-character-fit-editor], [data-character-context-menu]')
      ) return;
```
**after:**
```ts
      if (
        uploading || imageMenu || addMenu || fitEditorOpen
        || document.querySelector('[data-character-lightbox], [data-character-fit-editor], [data-character-context-menu], [data-rigging-announce]')
      ) return;
```

### (c) 테스트 갱신 — `tests/riggingAnnounce.test.ts`

이 파일의 아래 앵커들을 갱신·추가한다 (모두 after 코드에 대한 시뮬레이션 통과 확인됨). **그 외 기존 앵커는 전부 그대로 통과하므로 건드리지 말 것** (`const TITLE_TEMPLATES`, 템플릿 onClick/onChange, primary 기본 선택, workFolderPath, addRow, stopImmediatePropagation, keydown capture 등).

60행 (앵커: `characterName:`):

**before (원문):**
```ts
  assert.match(modal, /characterName: `\$\{character\.name\} · \$\{costume\.name\}`/);
```
**after:**
```ts
  // 피드백 31(a): [내용] 줄은 '"캐릭터" - "복장"' 포맷.
  assert.match(modal, /characterName: `"\$\{character\.name\}" - "\$\{costume\.name\}"`/);
```

파일 끝에 append:

```ts
test('피드백 31(a·d): 기본 제목 미리 채움 + 비고 없이 전송 가능', () => {
  const modal = readFileSync('src/components/characters/RiggingAnnounceModal.tsx', 'utf8');
  assert.match(modal, /useState\(TITLE_TEMPLATES\[0\]\.title\)/);
  assert.match(modal, /\[모호 리깅 현황\] - 리깅 완료 공지/);
  // 비고 필수 게이트 제거 — 전송 버튼은 전송/업로드 중에만 잠긴다.
  assert.doesNotMatch(modal, /비고를 한 줄 이상/);
  assert.doesNotMatch(modal, /hasNote/);
  assert.match(modal, /disabled=\{sending \|\| pasting\}/);
});

test('피드백 31(b): 공지 이미지 Ctrl+V — 공지 전용 업로드 + 미전송분 정리', () => {
  const modal = readFileSync('src/components/characters/RiggingAnnounceModal.tsx', 'utf8');
  assert.match(modal, /data-rigging-announce/);
  assert.match(modal, /clipboardReadImageFile/);
  // 복장 이미지 목록(addCostumeImage)에 등록하지 않는 one-off 업로드.
  assert.match(modal, /uploadCharacterImage\(character\.id, costume\.id, base64\)/);
  assert.doesNotMatch(modal, /addCostumeImage/);
  // 취소 시 전부, 전송 시 쓴 이미지만 남기고 정리.
  assert.match(modal, /cleanupPastedUploads\(null\)/);
  assert.match(modal, /cleanupPastedUploads\(selectedImage\?\.url \?\? null\)/);
  // 배경 화면(FeaturedImageSlot)의 paste 가드가 모달 열림을 감지해 이중 업로드를 막는다.
  const slot = readFileSync('src/components/characters/FeaturedImageSlot.tsx', 'utf8');
  assert.match(slot, /\[data-rigging-announce\]/);
});
```

### (d) `src/mocks/devElectronAPI.ts` — 프리뷰 mock 업로드 URL 교체 (1579행, 앵커: `storageUploadCharacterImage:`)

프리뷰에서 붙여넣은 이미지 썸네일이 실제로 렌더되도록(부록 A-2 4단계 검증 성립) mock URL 을 로드 가능한 경로로 바꾼다.

**before (원문):**
```ts
    storageUploadCharacterImage: async () => ({ ok: true, url: 'mock://character-image' }),
```
**after:**
```ts
    // 프리뷰에서 업로드/붙여넣기 이미지가 실제로 렌더되도록 로드 가능한 경로를 돌려준다 (피드백 31b·33 검증용).
    storageUploadCharacterImage: async () => ({ ok: true, url: MOCK_CHARACTER_IMAGE_URL }),
```
(`MOCK_CHARACTER_IMAGE_URL` 은 같은 파일 433행에 이미 정의 — `'/splash/opening_image_cropped.png'`. 이 문자열을 앵커로 삼는 테스트는 없음이 확인됐다.)

## 엣지 케이스 & 주의사항

- **비고 input 포커스 중 Ctrl+V:** 이미지 item 이 있으면 `preventDefault` 후 첨부(요청 의도), 텍스트면 이미지 item 이 없어 그대로 input 에 붙는다. FeaturedImageSlot 의 "input 안이면 무시" 가드를 **복사하지 말 것** — 이 모달은 항상 input 에 포커스가 있어 Ctrl+V 가 영원히 안 먹게 된다.
- **탐색기 파일 복사 붙여넣기(FileNameW 폴백):** 텍스트 붙여넣기 시에도 폴백이 실행되지만 클립보드에 파일이 없으면 null 이라 무해하다. 프리뷰 mock 은 항상 null — 실기(Electron)에서만 동작(부록 A-2).
- **연속 붙여넣기:** `pasteLockRef` 로 업로드 중 재진입을 막는다(이전 업로드 완료 후 다시 Ctrl+V 하면 추가됨 — 여러 장 첨부 가능, 선택은 1장).
- **고아 파일:** 전송에 쓴 붙여넣기 이미지는 슬랙 메시지가 참조하므로 **지우지 않는다**(영구 보존 허용 — 800px 축소본이라 용량 무시 가능). 안 쓴 것만 정리.
- **(e) 이미지 링크 비노출:** 앱은 이미 image 변수를 별도 전송한다. 슬랙 메시지 본문에 image URL 텍스트가 보인다면 그것은 Slack Workflow Builder 메시지 서식 문제 — 코드 수정 금지, 부록 B-3 으로 한솔에게 전달.

## 검증 방법

1. `npm run typecheck` && `npm run test:character` → riggingAnnounce.test.ts 전체(기존+신규) 통과.
2. 수동(부록 A-2): 기본 제목 채워짐 / 비고 없이 전송 버튼 활성 / 콘솔 페이로드의 CH_name 이 `"캐릭터" - "복장"` / (프리뷰) 스크린샷 Ctrl+V 시 '붙여넣기' 배지 썸네일 추가·자동 선택.

## 완료 기준

- [ ] 모달 전체 교체본 적용(자가 점검 체크리스트 ①~⑦ 확인)
- [ ] FeaturedImageSlot 가드에 `[data-rigging-announce]` 추가 + 프리뷰 mock 업로드 URL 교체((d))
- [ ] 테스트 갱신·추가 통과 (기존 앵커 무손상)
- [ ] 커밋 1개 — 예: `리깅 공지 개편 — 기본 템플릿·Ctrl+V 이미지·비고 선택화 (피드백 31)`

---

# 작업 4 — 항목 33 기반: 이미지 원본 크기(natural size) 보존 체인

## 문제/요청 요약

기준 키 자동 설정·드래그 조정의 환산 기준은 **업로드 원본의 세로 px**인데, 저장 이미지는 800px 로 축소된다(원본 크기가 소실됨). 업로드 시 원본 크기를 측정해 `character_costume_images.natural_width/natural_height` 에 보존한다.

## 근거

- 업로드 파이프라인: `FeaturedImageSlot.handleUpload`(381~418행)가 유일한 수렴점 — `resizeBlob(file, 800, …)` 로 **축소 후** 업로드하므로 원본 크기는 resize 전 `file` 에서만 얻을 수 있다. 메인 프로세스도 안전망 재리사이즈(electron/storage.ts `MAX_PX = 800`)를 하므로 저장본에서는 복원 불가.
- `character_costume_images` 테이블에 원본 크기 컬럼 없음 (src/types/index.ts:355 `CharacterCostumeImageRow`, electron/supabase.ts:4032 동일 인터페이스).
- 기준 키 컬럼은 이미 존재: `characters.reference_height_px INTEGER`, CHECK `chk_characters_reference_height (… > 0 AND … < 5000)` (DEVLOG/migrations/2026-07-08-character-board-duedate-height.sql — 라이브 적용됨). 저장 경로 `setCharacterReferenceHeight`(store) → `supabase:update-character` IPC 도 기존 그대로 재사용한다.

## 근본 원인

원본 픽셀 크기를 측정·보존하는 코드가 어디에도 없다.

## 수정 내용

### (a) 신규 마이그레이션 — `DEVLOG/migrations/2026-07-15-costume-image-natural-size.sql` (새 파일, 전체 내용)

```sql
-- ============================================================
-- 복장 이미지 원본 크기(natural_width/height) + 기준 키 의미 갱신 (피드백 33)
-- date: 2026-07-15
--
-- 원칙:
-- - 추가 전용(additive) — 기존 행은 NULL 유지(과거 업로드는 원본 크기 미기록, UI 가 폴백 처리).
-- - 구버전 앱과 호환: 구버전은 새 컬럼을 모르지만 SELECT * 로 받아 무시한다.
-- ============================================================

BEGIN;

-- 1) 업로드 원본 픽셀 크기 (리사이즈 전 — 저장본은 최대 800px 축소본이라 원본 크기가 따로 필요)
ALTER TABLE character_costume_images
  ADD COLUMN IF NOT EXISTS natural_width INTEGER,
  ADD COLUMN IF NOT EXISTS natural_height INTEGER;

DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM pg_constraint WHERE conname = 'chk_costume_images_natural_size'
  ) THEN
    ALTER TABLE character_costume_images ADD CONSTRAINT chk_costume_images_natural_size
      CHECK (
        (natural_width IS NULL OR natural_width > 0)
        AND (natural_height IS NULL OR natural_height > 0)
      );
  END IF;
END $$;

COMMENT ON COLUMN character_costume_images.natural_width IS
  '업로드 원본 이미지 가로 px(리사이즈 전). 과거 업로드/측정 실패는 NULL.';
COMMENT ON COLUMN character_costume_images.natural_height IS
  '업로드 원본 이미지 세로 px(리사이즈 전). 기준 키 자동 설정·드래그 조정의 환산 기준. 과거 업로드/측정 실패는 NULL.';

-- 2) 기준 키 의미 갱신 (피드백 33): 임의 단위 → 실제 프로젝트(1280x720) 픽셀 기준
COMMENT ON COLUMN characters.reference_height_px IS
  '캐릭터 키(px, 1280x720 프로젝트 기준). 이미지 업로드 시 원본 세로 px 로 자동 설정되고 기준선 드래그로 조정. NULL이면 미설정(키 비교에서 균일 표시).';

COMMIT;

-- PostgREST 스키마 캐시 갱신 — 컬럼 추가 직후 API 가 새 컬럼을 모르는 상태 방지.
NOTIFY pgrst, 'reload schema';
```

**라이브 DB 적용은 별도 게이트(부록 B-2)** — 파일 커밋과 적용 시점을 분리해서 관리한다.

### (b) `src/types/index.ts` — 도메인/Row/ElectronAPI 타입 (3곳)

267행 `interface CharacterCostumeImage` (앵커: `isPrimary: boolean;` 바로 위 아님 — `imageFit` 아래):

**before (원문):**
```ts
  /** 썸네일/대표 이미지 표시 변환값. 원본 이미지는 수정하지 않는다. */
  imageFit: CharacterImageFit;
  /** 이 복장의 대표 이미지 여부. costume 당 최대 1개(부분 유니크). */
  isPrimary: boolean;
```
**after:**
```ts
  /** 썸네일/대표 이미지 표시 변환값. 원본 이미지는 수정하지 않는다. */
  imageFit: CharacterImageFit;
  /** 업로드 원본 크기(px, 리사이즈 전). 과거 업로드/측정 실패는 null — 기준 키 환산 폴백 처리. (피드백 33) */
  naturalWidth: number | null;
  naturalHeight: number | null;
  /** 이 복장의 대표 이미지 여부. costume 당 최대 1개(부분 유니크). */
  isPrimary: boolean;
```

355행 `interface CharacterCostumeImageRow` (앵커: `is_primary: boolean | null;`):

**before (원문):**
```ts
  is_primary: boolean | null;
  sort_order: number | null;
```
**after:**
```ts
  natural_width?: number | null;
  natural_height?: number | null;
  is_primary: boolean | null;
  sort_order: number | null;
```

1556행 ElectronAPI `supabaseAddCostumeImage` input (앵커: `supabaseAddCostumeImage: (input: {`) — input 객체 타입에 두 필드 추가 (`isPrimary?: boolean;` 줄 위에):
```ts
    naturalWidth?: number | null;
    naturalHeight?: number | null;
```

### (c) `electron/supabase.ts` — Row 타입 + insert (2곳)

4032행 `interface CharacterCostumeImageRow` — (b)의 Row 와 동일하게 `natural_width?: number | null; natural_height?: number | null;` 두 줄을 `is_primary` 줄 위에 추가.

4312행 `export async function addCostumeImage(input: {` — input 타입에 `naturalWidth?: number | null; naturalHeight?: number | null;` 추가(`isPrimary?: boolean;` 위), insert 객체 조립부(앵커: `if (input.imageBackground !== undefined) insert.image_background = input.imageBackground;`) 뒤에:

**before (원문):**
```ts
  if (input.imageBackground !== undefined) insert.image_background = input.imageBackground;
  const { data, error } = await supabase
```
**after:**
```ts
  if (input.imageBackground !== undefined) insert.image_background = input.imageBackground;
  // 피드백 33: 원본 크기는 값이 있을 때만 컬럼에 쓴다 — 마이그레이션 전 DB 에서도 무측정 업로드는 계속 동작.
  if (input.naturalWidth != null) insert.natural_width = input.naturalWidth;
  if (input.naturalHeight != null) insert.natural_height = input.naturalHeight;
  const { data, error } = await supabase
```

primary 경합 재시도(4351~4358행)는 `{ ...insert, is_primary: false }` 로 같은 insert 객체를 재사용하므로 추가 배선 불필요.

### (d) `electron/preload.ts` — 734행 `supabaseAddCostumeImage` input 타입에 동일 두 필드 추가 (`isPrimary?: boolean;` 위):
```ts
    naturalWidth?: number | null;
    naturalHeight?: number | null;
```

### (e) `src/services/supabaseService.ts` — 매퍼 + 서비스 입력 (2곳)

991행 `rowToCostumeImage` (앵커: `isPrimary: row.is_primary === true,`):

**before (원문):**
```ts
    isPrimary: row.is_primary === true,
    sortOrder: row.sort_order ?? 0,
```
**after:**
```ts
    naturalWidth: row.natural_width ?? null,
    naturalHeight: row.natural_height ?? null,
    isPrimary: row.is_primary === true,
    sortOrder: row.sort_order ?? 0,
```

1013행 `addCharacterCostumeImage` input 타입에 `naturalWidth?: number | null; naturalHeight?: number | null;` 추가(`isPrimary?: boolean;` 위) — 함수 본문은 input 을 IPC 로 그대로 넘기므로 무변경.

### (f) `src/stores/useCharacterBoardStore.ts` — `addCostumeImage` 시그니처 확장 (2곳)

158행 인터페이스 (앵커: `addCostumeImage: (costumeId: string, url: string, role?: CostumeImageRole)`):

**before (원문):**
```ts
  addCostumeImage: (costumeId: string, url: string, role?: CostumeImageRole) => Promise<CharacterCostumeImage | null>;
```
**after:**
```ts
  addCostumeImage: (
    costumeId: string,
    url: string,
    role?: CostumeImageRole,
    /** 업로드 원본 크기(px, 리사이즈 전 측정값) — 기준 키 자동 설정·드래그 조정용 (피드백 33). */
    naturalSize?: { width: number; height: number },
  ) => Promise<CharacterCostumeImage | null>;
```

575행 구현 (앵커: `addCostumeImage: async (costumeId, url, role = 'design') => {`):

**before (원문):**
```ts
  addCostumeImage: async (costumeId, url, role = 'design') => {
    const createdBy = useAuthStore.getState().currentUser?.id ?? null;
    // 복장의 첫 이미지면 대표(primary)로 지정 — 기존 단일 이미지 소비처가 이 값을 계속 읽는다.
    const isPrimary = (get().imagesByCostume.get(costumeId)?.length ?? 0) === 0;
    try {
      const created = await svcAddCostumeImage({ costumeId, url, role, isPrimary, createdBy });
```
**after:**
```ts
  addCostumeImage: async (costumeId, url, role = 'design', naturalSize) => {
    const createdBy = useAuthStore.getState().currentUser?.id ?? null;
    // 복장의 첫 이미지면 대표(primary)로 지정 — 기존 단일 이미지 소비처가 이 값을 계속 읽는다.
    const isPrimary = (get().imagesByCostume.get(costumeId)?.length ?? 0) === 0;
    try {
      const created = await svcAddCostumeImage({
        costumeId, url, role, isPrimary, createdBy,
        naturalWidth: naturalSize?.width ?? null,
        naturalHeight: naturalSize?.height ?? null,
      });
```

### (g) `src/mocks/devElectronAPI.ts` — mock row 에 원본 크기 필드 추가

1585행 `supabaseAddCostumeImage` 의 row 조립 (앵커: `is_primary: input.isPrimary ?? false,`):

**before (원문):**
```ts
        is_primary: input.isPrimary ?? false,
        sort_order: input.sortOrder ?? siblings.length,
```
**after:**
```ts
        natural_width: input.naturalWidth ?? null,
        natural_height: input.naturalHeight ?? null,
        is_primary: input.isPrimary ?? false,
        sort_order: input.sortOrder ?? siblings.length,
```
(mock 업로드 URL 교체는 작업 3 (d)에서 이미 완료된 상태다.)

### (h) 테스트 갱신 — `tests/characterTier2.test.ts` 128행 (앵커: `addCostumeImage: async \(costumeId`)

(f)의 store 시그니처 확장으로 이 기존 앵커가 깨진다 — **같은 커밋에서 반드시 함께 갱신**한다. (이 갱신을 반영한 프로젝션에서 관련 테스트 38/38 통과가 확인됐다.)

**before (원문):**
```ts
  assert.match(store, /addCostumeImage: async \(costumeId, url, role = 'design'\)/);
```
**after:**
```ts
  assert.match(store, /addCostumeImage: async \(costumeId, url, role = 'design', naturalSize\)/);
```

## 엣지 케이스 & 주의사항

- **마이그레이션 전 라이브 DB:** conditional insert 라 측정 실패(null) 업로드는 계속 동작하지만, 측정 성공 업로드는 unknown column 으로 실패한다 → **배포 전 적용 게이트(부록 B-2) 필수.**
- 과거 행의 `natural_*` 는 전부 NULL — 에디터(작업 6)가 폴백 처리한다.
- `rowToCostumeImage` 는 realtime payload 의 row 에도 쓰인다 — snake_case 필드 추가만으로 자동 호환(옵셔널).
- **`electron/main.ts` 는 수정하지 않는다(의도된 무수정):** `'supabase:add-costume-image'` 핸들러(3517행)의 인라인 input 타입 주석에는 naturalWidth/naturalHeight 가 없지만, IPC 는 객체 전체를 그대로 직렬화해 전달하고 `sbAddCostumeImage` 의 새 필드가 옵셔널이라 typecheck·런타임 모두 무해하다. 주석을 갱신하려고 main.ts 를 건드리지 말 것.

## 검증 방법

`npm run typecheck` && `npm run test:character` — (h)의 characterTier2 앵커 갱신이 같은 커밋에 포함된 상태에서 전부 통과해야 한다((h) 누락 시 이 작업 시점부터 test:character 가 실패한다). SQL 은 아직 적용하지 않는다(부록 B-2).

## 완료 기준

- [ ] 마이그레이션 파일 생성(적용은 게이트로 분리)
- [ ] 타입 3곳·electron 2곳·preload·svc 2곳·store 2곳·mock 1곳 배선 완료, typecheck 통과
- [ ] characterTier2.test.ts 128행 앵커 갱신 포함, `npm run test:character` 통과
- [ ] 커밋 1개 — 예: `복장 이미지 원본 크기 보존 체인 — natural_width/height (피드백 33 기반)`

---

# 작업 5 — 항목 33(a): 업로드 시 기준 키 자동 설정

## 문제/요청 요약

캐릭터 이미지는 딱 캐릭터 크기로 잘려 출력되므로(1280×720 프로젝트), 업로드 이미지의 **원본 세로 px** 를 기준 키로 자동 설정한다. 이미 값이 있으면 덮지 않는다.

## 근거 (파일: `src/utils/imageUtils.ts`, `src/components/characters/FeaturedImageSlot.tsx`)

`handleUpload`(FeaturedImageSlot.tsx:381~418) 원문 — 파일선택/드롭/Ctrl+V/클립보드 파일이 전부 이 함수로 수렴하며, `resizeBlob(file, 800, …)` **직전의 `file`** 이 원본 크기를 잴 수 있는 유일한 지점:

```ts
    try {
      const isPng = file.type === 'image/png' || file.name.toLowerCase().endsWith('.png');
      const base64 = await resizeBlob(file, 800, isPng ? 0.92 : 0.8, isPng ? 'image/png' : 'image/jpeg');
      const res = await uploadCharacterImage(character.id, targetCostume.id, base64);
      if (!res.ok || !res.url) throw new Error(res.error ?? '업로드 실패');
      // 대표 이미지 자동 지정·featured_* 동기화는 store(addCostumeImage)+DB 트리거가 담당 —
      //   앱이 featured 를 직접 쓰지 않으므로 예전 이미지 파일을 지우지 않는다(P1 해소).
      const created = await addCostumeImage(targetCostume.id, res.url, opts?.role ?? 'design');
```

기존 stale-closure 방지 선례(같은 함수 405~410행): 업로드 완료 후 `useCharacterBoardStore.getState().characters.find(...)` 로 **최신값 재조회** 후 조건부 갱신 — 자동 설정도 동일 패턴을 쓴다.

## 근본 원인

원본 크기 측정 유틸이 없고, 업로드 완료 후 기준 키를 채우는 로직이 없다.

## 수정 내용

### (a) `src/utils/imageUtils.ts` — `measureImageSize` 추가 (파일 끝에 append)

```ts
/**
 * 이미지 파일의 원본 픽셀 크기 측정 (리사이즈 전 — 피드백 33).
 * 실패 시 null — 호출측은 측정 실패로 업로드를 막지 말 것.
 * createImageBitmap 이 가장 싸고, 미지원/디코딩 실패 시 Image 폴백.
 */
export async function measureImageSize(file: File | Blob): Promise<{ width: number; height: number } | null> {
  try {
    if (typeof createImageBitmap === 'function') {
      const bitmap = await createImageBitmap(file);
      const size = { width: bitmap.width, height: bitmap.height };
      bitmap.close();
      if (size.width > 0 && size.height > 0) return size;
    }
  } catch { /* Image 폴백으로 계속 */ }
  try {
    const dataUrl = await new Promise<string>((resolve, reject) => {
      const reader = new FileReader();
      reader.onload = () => resolve(reader.result as string);
      reader.onerror = reject;
      reader.readAsDataURL(file);
    });
    return await new Promise((resolve) => {
      const img = new Image();
      img.onload = () => resolve(img.width > 0 && img.height > 0 ? { width: img.width, height: img.height } : null);
      img.onerror = () => resolve(null);
      img.src = dataUrl;
    });
  } catch {
    return null;
  }
}
```

### (b) `src/utils/characterHeightGuides.ts` — 신규 파일 (작업 6과 공유, **여기서 먼저 생성**, 전체 내용은 작업 6 (a) 참고 — `autoHeightFromNatural` 을 이 작업에서 사용한다)

### (c) `FeaturedImageSlot.tsx` — 측정 + 자동 설정 배선 (3곳)

import (25행 부근, 앵커: `import { resizeBlob } from '@/utils/imageUtils';`):

**before (원문):**
```ts
import { resizeBlob } from '@/utils/imageUtils';
```
**after:**
```ts
import { measureImageSize, resizeBlob } from '@/utils/imageUtils';
import { autoHeightFromNatural } from '@/utils/characterHeightGuides';
```

store 구독 (356행 부근, 앵커: `const addCostumeImage = useCharacterBoardStore((s) => s.addCostumeImage);`) — 바로 아래에 추가:
```ts
  const setCharacterReferenceHeight = useCharacterBoardStore((s) => s.setCharacterReferenceHeight);
```

`handleUpload` 본문 (앵커: 위 근거 인용 블록):

**before (원문):**
```ts
      const isPng = file.type === 'image/png' || file.name.toLowerCase().endsWith('.png');
      const base64 = await resizeBlob(file, 800, isPng ? 0.92 : 0.8, isPng ? 'image/png' : 'image/jpeg');
      const res = await uploadCharacterImage(character.id, targetCostume.id, base64);
      if (!res.ok || !res.url) throw new Error(res.error ?? '업로드 실패');
      // 대표 이미지 자동 지정·featured_* 동기화는 store(addCostumeImage)+DB 트리거가 담당 —
      //   앱이 featured 를 직접 쓰지 않으므로 예전 이미지 파일을 지우지 않는다(P1 해소).
      const created = await addCostumeImage(targetCostume.id, res.url, opts?.role ?? 'design');
```
**after:**
```ts
      // 피드백 33: 리사이즈(800px) 전에 원본 크기를 측정 — 저장본은 축소되므로 여기가 유일한 측정 지점.
      //   측정 실패(null)해도 업로드는 계속한다.
      const naturalSize = await measureImageSize(file);
      const isPng = file.type === 'image/png' || file.name.toLowerCase().endsWith('.png');
      const base64 = await resizeBlob(file, 800, isPng ? 0.92 : 0.8, isPng ? 'image/png' : 'image/jpeg');
      const res = await uploadCharacterImage(character.id, targetCostume.id, base64);
      if (!res.ok || !res.url) throw new Error(res.error ?? '업로드 실패');
      // 대표 이미지 자동 지정·featured_* 동기화는 store(addCostumeImage)+DB 트리거가 담당 —
      //   앱이 featured 를 직접 쓰지 않으므로 예전 이미지 파일을 지우지 않는다(P1 해소).
      const created = await addCostumeImage(targetCostume.id, res.url, opts?.role ?? 'design', naturalSize ?? undefined);
```

같은 함수, `setSelectedImageId(created.id);` 줄(401행) **바로 아래**에 삽입:
```ts
      // 피드백 33(a): 기준 키 자동 설정 — 아직 값이 없을 때만(이미 입력된 값은 덮지 않음), 원본 세로 px 로.
      //   최신값을 store 에서 재조회 — 업로드 대기 중 다른 사용자가 값을 넣었을 수 있다(stale-closure 방지).
      if (naturalSize) {
        const latest = useCharacterBoardStore.getState().characters.find((c) => c.id === character.id);
        const auto = autoHeightFromNatural(naturalSize.height);
        if (latest && latest.referenceHeightPx == null && auto != null) {
          const saved = await setCharacterReferenceHeight(character.id, auto);
          if (saved) toast.success(`기준 키를 ${auto}px로 자동 설정했어요 (이미지 세로 크기)`);
        }
      }
```

useCallback deps (418행, 앵커: `}, [character.id, costume, onEnsureCostume, addCostumeImage, renameCharacter]);`):

**before (원문):**
```ts
  }, [character.id, costume, onEnsureCostume, addCostumeImage, renameCharacter]);
```
**after:**
```ts
  }, [character.id, costume, onEnsureCostume, addCostumeImage, renameCharacter, setCharacterReferenceHeight]);
```

## 엣지 케이스 & 주의사항

- **CHECK 제약(0 < v < 5000):** `autoHeightFromNatural` 이 1~4999 로 클램프 — 5000px 이상 이미지도 DB 에러 없이 4999 로 저장(롤백 토스트 방지).
- **이미 값이 있으면 절대 덮지 않는다** — 재업로드·추가 복장 업로드는 자동 설정을 건너뛴다. 값을 다시 잡고 싶으면 기준 키 input 을 비워(=null) 다음 업로드에서 자동 설정되거나, 에디터(작업 6)로 조정.
- 측정 실패는 조용히 스킵(업로드 정상 진행, 토스트 없음).
- svg 등 비트맵이 아닌 이미지는 createImageBitmap 이 실패할 수 있으나 Image 폴백이 처리(그래도 실패 시 null).

## 검증 방법

1. `npm run typecheck` && `npm run test:character`.
2. 수동(부록 A-3): 기준 키가 빈 캐릭터에 이미지 업로드 → 토스트 + 기준 키 input 에 원본 세로 px 자동 반영(다른 사용자에게도 realtime 전파). 값이 있는 캐릭터엔 변화 없음.

## 완료 기준

- [ ] 원본 측정 → addCostumeImage 전달 → null 일 때만 자동 설정 + 토스트
- [ ] typecheck·test:character 통과
- [ ] 커밋 1개 — 예: `이미지 업로드 시 기준 키 자동 설정 (피드백 33a)`

---

# 작업 6 — 항목 33(b): 머리/바닥 기준선 드래그 조정 에디터

## 문제/요청 요약

모자를 쓴 캐릭터·까치발 캐릭터처럼 이미지 끝과 실제 키가 다른 경우, 이미지 위에 수평 기준선 2개(머리/바닥)를 드래그해 기준 키(px)를 조정한다. 환산 기준은 이미지 **원본** 세로 px(작업 4의 `natural_height`).

## 근거

- 미러링할 에디터 선례: `src/components/characters/CharacterImageFitEditor.tsx` — `fixed inset-0` + `CHARACTER_LAYER_CLASS.editor`(z-[90]) 오버레이, DOM 마커(`data-character-fit-editor`), `onLoad` 에서 `naturalWidth/Height` 측정, `setPointerCapture` 드래그, ESC capture 닫기, `loadFailed` 상태. 단 FitEditor 는 이미지별 `imageFit` 커밋이라 **겸용하지 않고 별도 컴포넌트**를 만든다(기준 키는 캐릭터 단위 값 — 커밋 대상·수명이 다름).
- 저장 경로: 기존 `setCharacterReferenceHeight(id, px)`(useCharacterBoardStore — `applyCharacterUpdate` 낙관+롤백) 재사용. 신규 IPC/스키마 불필요.
- 진입 위치: `CostumeDetail.tsx` 기준 키 input(221~238행) 옆. `CostumeDetail` 은 이미 152행에서 `setCharacterReferenceHeight` 를 구독하고, 로컬 상태 모달 선례(`announceOpen` + `RiggingAnnounceModal`)가 있다.
- 배경 paste 가드: `FeaturedImageSlot.tsx` 의 셀렉터(작업 3 완료 상태)에 에디터 마커 추가 필요 — 에디터 열림 중 Ctrl+V 가 배경 업로드로 새지 않게.

## 근본 원인

신규 기능 — 해당 UI 없음.

## 수정 내용

### (a) 신규 파일 — `src/utils/characterHeightGuides.ts` (전체 내용, **alias-free — `@/` import 절대 금지**, 작업 5에서 먼저 생성해도 됨)

```ts
/**
 * 캐릭터 기준 키 드래그 조정(피드백 33) — 머리/바닥 기준선 계산 순수 유틸.
 * node --test 에서 직접 import 하므로 '@/' alias 를 쓰지 않는다 (alias-free).
 */

/** 기준선 위치 — 이미지 세로(0=맨 위, 1=맨 아래)에 대한 비율. top < bottom 을 유지한다. */
export interface HeightGuides {
  topRatio: number;
  bottomRatio: number;
}

/** 편집 시작 기본값 — 이미지 전체 높이(위 0 ~ 아래 1). */
export const DEFAULT_HEIGHT_GUIDES: HeightGuides = { topRatio: 0, bottomRatio: 1 };

/** 기준선 사이 최소 간격(이미지 높이 대비 비율) — 두 선이 겹치거나 뒤집히지 않게 한다. */
export const MIN_GUIDE_GAP_RATIO = 0.02;

/** DB CHECK(chk_characters_reference_height: 0 < v < 5000)와 같은 클램프 범위. */
export const MIN_HEIGHT_PX = 1;
export const MAX_HEIGHT_PX = 4999;

function clamp(value: number, min: number, max: number): number {
  return Math.min(max, Math.max(min, value));
}

/**
 * 드래그 결과를 유효한 기준선으로 정규화.
 * - 각 선을 0~1 로 클램프.
 * - which 로 지정한 '방금 움직인 선'이 반대 선을 밀지 않고 최소 간격 앞에서 멈춘다.
 */
export function clampGuides(next: HeightGuides, which: 'top' | 'bottom'): HeightGuides {
  const top = clamp(next.topRatio, 0, 1);
  const bottom = clamp(next.bottomRatio, 0, 1);
  if (bottom - top >= MIN_GUIDE_GAP_RATIO) return { topRatio: top, bottomRatio: bottom };
  if (which === 'top') {
    return { topRatio: clamp(bottom - MIN_GUIDE_GAP_RATIO, 0, 1 - MIN_GUIDE_GAP_RATIO), bottomRatio: bottom };
  }
  return { topRatio: top, bottomRatio: clamp(top + MIN_GUIDE_GAP_RATIO, MIN_GUIDE_GAP_RATIO, 1) };
}

/**
 * 기준선 → 키(px) 환산. baseHeightPx = 이 이미지 '원본' 세로 픽셀(natural_height).
 * 결과는 DB CHECK 범위(1~4999)로 클램프한 정수.
 */
export function guidesToHeightPx(guides: HeightGuides, baseHeightPx: number): number {
  const span = Math.max(0, guides.bottomRatio - guides.topRatio);
  return clamp(Math.round(span * baseHeightPx), MIN_HEIGHT_PX, MAX_HEIGHT_PX);
}

/**
 * 업로드 시 자동 설정값(피드백 33a) — 원본 세로 px 를 DB CHECK 범위로 클램프한 정수.
 * 측정 실패(null·0 이하)면 null 을 돌려 자동 설정을 건너뛴다.
 */
export function autoHeightFromNatural(naturalHeight: number | null | undefined): number | null {
  if (naturalHeight == null || !Number.isFinite(naturalHeight) || naturalHeight <= 0) return null;
  return clamp(Math.round(naturalHeight), MIN_HEIGHT_PX, MAX_HEIGHT_PX);
}
```

### (b) 신규 파일 — `src/components/characters/CharacterHeightEditor.tsx` (전체 내용)

```tsx
import { useEffect, useMemo, useRef, useState, type PointerEvent as ReactPointerEvent } from 'react';
import { Loader2, X } from 'lucide-react';
import { toast } from 'sonner';
import { useCharacterBoardStore } from '@/stores/useCharacterBoardStore';
import { CHARACTER_LAYER_CLASS } from '@/constants/characterLayers';
import type { Character, CharacterCostume } from '@/types';
import {
  DEFAULT_HEIGHT_GUIDES,
  type HeightGuides,
  clampGuides,
  guidesToHeightPx,
} from '@/utils/characterHeightGuides';

/**
 * 기준 키 드래그 조정 에디터 (피드백 33b).
 * - 복장 대표 이미지 위에 머리/바닥 기준선 2개를 드래그해 캐릭터 키(px)를 맞춘다
 *   (모자를 쓴 캐릭터는 머리선을 내리고, 까치발처럼 그려진 캐릭터는 바닥선을 올린다).
 * - 환산 기준은 이미지의 '원본' 세로 px(natural_height). 원본 크기 미기록(과거 업로드)이면
 *   저장본(최대 800px) 세로로 계산하고 경고를 띄운다 — 새로 업로드하면 정확해진다.
 * - 저장은 기존 setCharacterReferenceHeight(캐릭터 단위, 낙관+롤백) 재사용 — 신규 IPC/스키마 없음.
 * - CharacterImageFitEditor 와 같은 최상단 에디터 레이어/ESC 패턴. fit(썸네일 변환)은 적용하지 않고
 *   원본 비율 그대로 표시한다 — 기준선↔px 환산이 단순해지도록.
 */
export function CharacterHeightEditor({
  character,
  costume,
  onClose,
}: {
  character: Character;
  costume: CharacterCostume;
  onClose: () => void;
}) {
  const imagesByCostume = useCharacterBoardStore((s) => s.imagesByCostume);
  const setCharacterReferenceHeight = useCharacterBoardStore((s) => s.setCharacterReferenceHeight);

  // 편집 대상 = 이 복장의 대표 이미지(없으면 첫 이미지). 진입 버튼이 이미지 존재를 보장하지만 방어적으로 처리.
  const image = useMemo(() => {
    const list = imagesByCostume.get(costume.id) ?? [];
    return list.find((i) => i.isPrimary) ?? list[0] ?? null;
  }, [imagesByCostume, costume.id]);

  const [guides, setGuides] = useState<HeightGuides>(DEFAULT_HEIGHT_GUIDES);
  const [loadedHeight, setLoadedHeight] = useState<number | null>(null); // 저장본 naturalHeight — 원본 미기록 시 폴백
  const [loadFailed, setLoadFailed] = useState(false);
  const [saving, setSaving] = useState(false);
  const imgRef = useRef<HTMLImageElement>(null);
  const dragRef = useRef<{ which: 'top' | 'bottom'; pointerId: number } | null>(null);

  // 환산 기준: 원본 세로 px(natural_height) 우선, 없으면 로드된 저장본(≤800px) 세로 — 경고 표시.
  const baseHeightPx = image?.naturalHeight ?? loadedHeight;
  const isFallbackBase = image != null && image.naturalHeight == null;
  const previewPx = baseHeightPx != null && baseHeightPx > 0 ? guidesToHeightPx(guides, baseHeightPx) : null;

  useEffect(() => {
    // 최상단 모달 패턴 — 부모 CharacterDetailModal 의 Escape 리스너까지 닫히는 것을 막는다(RiggingAnnounceModal 선례).
    const onKey = (event: KeyboardEvent) => {
      if (event.key !== 'Escape') return;
      event.preventDefault();
      event.stopImmediatePropagation();
      onClose();
    };
    window.addEventListener('keydown', onKey, { capture: true });
    return () => window.removeEventListener('keydown', onKey, { capture: true });
  }, [onClose]);

  const beginDrag = (which: 'top' | 'bottom') => (event: ReactPointerEvent<HTMLDivElement>) => {
    event.preventDefault();
    event.currentTarget.setPointerCapture(event.pointerId);
    dragRef.current = { which, pointerId: event.pointerId };
  };

  const moveDrag = (event: ReactPointerEvent<HTMLDivElement>) => {
    const drag = dragRef.current;
    if (!drag || drag.pointerId !== event.pointerId) return;
    const rect = imgRef.current?.getBoundingClientRect();
    if (!rect || rect.height <= 0) return;
    const ratio = (event.clientY - rect.top) / rect.height;
    setGuides((prev) => clampGuides(
      drag.which === 'top' ? { ...prev, topRatio: ratio } : { ...prev, bottomRatio: ratio },
      drag.which,
    ));
  };

  const endDrag = (event: ReactPointerEvent<HTMLDivElement>) => {
    if (dragRef.current?.pointerId !== event.pointerId) return;
    dragRef.current = null;
  };

  const onSave = async () => {
    if (previewPx == null || saving) return;
    setSaving(true);
    const ok = await setCharacterReferenceHeight(character.id, previewPx);
    setSaving(false);
    if (ok) {
      toast.success(`기준 키를 ${previewPx}px로 저장했어요`);
      onClose();
    }
    // 실패 시 토스트('키 저장에 실패했어요')와 롤백은 store 가 담당 — 에디터는 열린 채 재시도 가능.
  };

  /** 기준선 1개 — 라벨 + 드래그 손잡이(터치 영역은 선보다 두껍게). */
  const guideLine = (which: 'top' | 'bottom', ratio: number, label: string) => (
    <div
      role="slider"
      aria-label={`${label} 기준선`}
      aria-valuemin={0}
      aria-valuemax={100}
      aria-valuenow={Math.round(ratio * 100)}
      className="absolute left-0 right-0 h-5 -mt-2.5 cursor-ns-resize touch-none group"
      style={{ top: `${ratio * 100}%` }}
      onPointerDown={beginDrag(which)}
      onPointerMove={moveDrag}
      onPointerUp={endDrag}
      onPointerCancel={endDrag}
    >
      <div className="absolute left-0 right-0 top-1/2 -mt-px h-0.5 bg-accent shadow-[0_0_6px_rgba(108,92,231,0.8)]" />
      <span className="absolute left-1.5 top-1/2 -translate-y-[130%] rounded bg-accent px-1.5 py-0.5 text-[10px] font-semibold text-white select-none">
        {label}
      </span>
    </div>
  );

  return (
    <div
      data-character-height-editor
      className={`fixed inset-0 ${CHARACTER_LAYER_CLASS.editor} flex items-center justify-center bg-black/70 backdrop-blur-sm p-6`}
      onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}
    >
      <div className="bg-bg-card border border-bg-border rounded-2xl p-5 flex flex-col gap-4 max-w-[min(88vw,720px)] max-h-[90vh]">
        <div className="flex items-start justify-between gap-4">
          <div className="flex flex-col gap-0.5">
            <h2 className="text-lg font-semibold text-text-primary">기준 키 조정 — {character.name}</h2>
            <span className="text-xs text-text-secondary">
              머리·바닥 기준선을 드래그해 실제 키 범위를 맞춰요 (모자·까치발 보정).
            </span>
          </div>
          <button
            type="button"
            onClick={onClose}
            title="닫기 (Esc)"
            className="shrink-0 p-1.5 rounded-md text-text-secondary hover:bg-bg-border/40"
          >
            <X size={16} />
          </button>
        </div>

        {!image ? (
          <div className="text-sm text-text-secondary py-10 text-center">이 복장에 이미지가 없어요 — 이미지를 먼저 추가해주세요.</div>
        ) : loadFailed ? (
          <div className="text-sm text-text-secondary py-10 text-center">이미지를 불러오지 못했어요 — 잠시 후 다시 열어주세요.</div>
        ) : (
          <div className="relative self-center overflow-hidden rounded-lg border border-bg-border bg-[conic-gradient(#26293a_0_25%,#1d2030_0_50%,#26293a_0_75%,#1d2030_0)] bg-[length:16px_16px]">
            <img
              ref={imgRef}
              src={image.url}
              alt={`${character.name} 기준 키 조정`}
              draggable={false}
              onLoad={(event) => {
                const el = event.currentTarget;
                setLoadFailed(false);
                setLoadedHeight(Math.max(1, el.naturalHeight));
              }}
              onError={() => setLoadFailed(true)}
              className="block max-h-[52vh] max-w-[80vw] select-none"
            />
            {guideLine('top', guides.topRatio, '머리')}
            {guideLine('bottom', guides.bottomRatio, '바닥')}
          </div>
        )}

        <div className="flex items-center justify-between gap-3">
          <div className="flex flex-col gap-0.5 min-w-0">
            <span className="text-sm text-text-primary font-semibold">
              {previewPx != null ? `키 ${previewPx}px` : '계산 중…'}
              {character.referenceHeightPx != null && (
                <span className="ml-2 text-xs font-normal text-text-secondary">(현재 저장값 {character.referenceHeightPx}px)</span>
              )}
            </span>
            {isFallbackBase && (
              <span className="text-[11px] text-yellow-400/90">
                이 이미지는 원본 크기 정보가 없는 예전 업로드라 축소본 기준으로 계산돼요 — 이미지를 새로 올리면 정확해져요.
              </span>
            )}
          </div>
          <div className="flex items-center gap-2 shrink-0">
            <button type="button" onClick={onClose} className="px-3 py-1.5 rounded-lg text-sm text-text-secondary hover:bg-bg-border/40">취소</button>
            <button
              type="button"
              onClick={onSave}
              disabled={previewPx == null || saving}
              className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-sm bg-accent text-white disabled:opacity-50"
            >
              {saving && <Loader2 size={13} className="animate-spin" />}
              이 키로 저장
            </button>
          </div>
        </div>
      </div>
    </div>
  );
}
```

### (c) `CostumeDetail.tsx` — 진입 버튼 + 에디터 렌더 (4곳)

import (14행 부근, 앵커: `import { RiggingAnnounceModal }`):

**before (원문):**
```ts
import { RiggingAnnounceModal } from '@/components/characters/RiggingAnnounceModal';
```
**after:**
```ts
import { RiggingAnnounceModal } from '@/components/characters/RiggingAnnounceModal';
import { CharacterHeightEditor } from '@/components/characters/CharacterHeightEditor';
```

store 구독 (152행, 앵커: `const setCharacterReferenceHeight = useCharacterBoardStore((s) => s.setCharacterReferenceHeight);`) — 바로 아래에 추가:
```ts
  const imagesByCostume = useCharacterBoardStore((s) => s.imagesByCostume);
```

로컬 상태 (앵커: `const [announceOpen, setAnnounceOpen] = useState(false);` — 180행 부근) 바로 아래:
```ts
  const [heightEditorOpen, setHeightEditorOpen] = useState(false);
```

기준 키 input 블록 교체 (221~238행, 앵커: `기준 키(px)`) — **주의: 라벨 문자열 `기준 키(px)` 와 input 의 기존 속성·핸들러는 그대로 유지**(tests/characterTier2.test.ts 43~60행 T2-3 앵커가 `기준 키\(px\)` 와 `setCharacterReferenceHeight\(character\.id` 를 고정):

**before (원문):**
```tsx
        {/* 기준 키 (캐릭터 단위, T2-3) — 나열 시 상대 크기 비교용 */}
        <div className="flex flex-col gap-1.5">
          <div className="text-xs text-text-secondary" title="캐릭터 나열(키 비교 보기)에서 상대 크기 기준으로 쓰는 값이에요. 실제 이미지 픽셀과 무관.">기준 키(px)</div>
          <input
            type="number"
            min={1}
            max={4999}
            inputMode="numeric"
            value={heightDraft}
            onChange={(e) => { setHeightDraft(e.target.value); }}
            onFocus={() => { heightFocused.current = true; }}
            onBlur={commitHeight}
            onKeyDown={(e) => { if (e.key === 'Enter') (e.target as HTMLInputElement).blur(); }}
            placeholder="예: 600"
            aria-label="캐릭터 기준 키(px)"
            className="h-8 w-24 rounded-md border border-bg-border bg-transparent px-2 text-sm text-text-primary outline-none focus:border-accent/50"
          />
        </div>
```
**after:**
```tsx
        {/* 기준 키 (캐릭터 단위, T2-3 + 피드백 33) — 이미지 원본 세로 px 기준, 업로드 시 자동 설정 + 드래그 조정 */}
        <div className="flex flex-col gap-1.5">
          <div className="text-xs text-text-secondary" title="캐릭터 키(px, 1280x720 프로젝트 기준). 이미지를 올리면 원본 세로 크기로 자동 설정되고, '이미지로 조정'으로 머리·바닥 기준선을 맞출 수 있어요.">기준 키(px)</div>
          <div className="flex items-center gap-1.5">
            <input
              type="number"
              min={1}
              max={4999}
              inputMode="numeric"
              value={heightDraft}
              onChange={(e) => { setHeightDraft(e.target.value); }}
              onFocus={() => { heightFocused.current = true; }}
              onBlur={commitHeight}
              onKeyDown={(e) => { if (e.key === 'Enter') (e.target as HTMLInputElement).blur(); }}
              placeholder="예: 600"
              aria-label="캐릭터 기준 키(px)"
              className="h-8 w-24 rounded-md border border-bg-border bg-transparent px-2 text-sm text-text-primary outline-none focus:border-accent/50"
            />
            <button
              type="button"
              onClick={() => setHeightEditorOpen(true)}
              disabled={(imagesByCostume.get(costume.id)?.length ?? 0) === 0}
              title={(imagesByCostume.get(costume.id)?.length ?? 0) > 0
                ? '이미지 위에서 머리·바닥 기준선을 드래그해 키를 맞춰요'
                : '이 복장에 이미지가 있어야 조정할 수 있어요'}
              className="h-8 shrink-0 rounded-md border border-bg-border px-2 text-xs text-text-secondary hover:border-accent/50 hover:text-accent disabled:opacity-40 disabled:hover:border-bg-border disabled:hover:text-text-secondary"
            >
              이미지로 조정
            </button>
          </div>
        </div>
```

에디터 렌더 (앵커: `{announceOpen && (` 블록 — 356행 부근, `<RiggingAnnounceModal ... />` 닫힌 뒤) 바로 아래에 추가:
```tsx
      {heightEditorOpen && (
        <CharacterHeightEditor character={character} costume={costume} onClose={() => setHeightEditorOpen(false)} />
      )}
```

### (d) `src/types/index.ts` — 기준 키 의미 주석 갱신 (189행, 앵커: `referenceHeightPx: number | null;` 바로 위 주석)

**before (원문):**
```ts
  /** 캐릭터 나열 시 상대 크기 비교 기준값(스튜디오 임의 단위, 실제 업로드 픽셀과 무관). null=균일 표시. (T2-3) */
  referenceHeightPx: number | null;
```
**after:**
```ts
  /** 캐릭터 키(px, 1280x720 프로젝트 기준) — 이미지 업로드 시 원본 세로 px 자동 설정 + 기준선 드래그 조정(피드백 33). null=미설정. */
  referenceHeightPx: number | null;
```
(T2-3 테스트 앵커는 `referenceHeightPx: number \| null;` 줄 자체만 고정 — 주석 변경 안전.)

### (e) `FeaturedImageSlot.tsx` — paste 가드에 에디터 마커 추가 (앵커: 작업 3 (b) 완료 상태의 셀렉터 문자열)

**before (작업 3 완료 상태):**
```ts
        || document.querySelector('[data-character-lightbox], [data-character-fit-editor], [data-character-context-menu], [data-rigging-announce]')
```
**after:**
```ts
        || document.querySelector('[data-character-lightbox], [data-character-fit-editor], [data-character-context-menu], [data-rigging-announce], [data-character-height-editor]')
```

### (f) 신규 테스트 — `tests/characterHeightKey.test.ts` (새 파일, 전체 내용)

유닛 파트(위 6개 test)는 계획 단계에서 **실행으로 6/6 통과 확인**된 코드다.

```ts
/**
 * 피드백 33 — 기준 키 자동 설정 + 드래그 조정: 순수 계산 실행 검증 + 배선 고정.
 */
import { readFileSync } from 'node:fs';
import test from 'node:test';
import assert from 'node:assert/strict';
import {
  DEFAULT_HEIGHT_GUIDES,
  MIN_GUIDE_GAP_RATIO,
  clampGuides,
  guidesToHeightPx,
  autoHeightFromNatural,
} from '../src/utils/characterHeightGuides.ts';

test('기본 기준선은 이미지 전체(0~1) — 전체 높이 = 원본 px 그대로', () => {
  assert.equal(guidesToHeightPx(DEFAULT_HEIGHT_GUIDES, 500), 500);
  assert.equal(guidesToHeightPx(DEFAULT_HEIGHT_GUIDES, 650), 650);
});

test('기준선을 안쪽으로 좁히면 비율만큼 px 가 줄어든다 (모자 쓴 캐릭터 시나리오)', () => {
  // 모자가 이미지 위 10% 를 차지 → 머리선을 0.1 로 내리면 500px 의 90% = 450px.
  assert.equal(guidesToHeightPx({ topRatio: 0.1, bottomRatio: 1 }, 500), 450);
  // 바닥선도 5% 올리면 85% = 425px.
  assert.equal(guidesToHeightPx({ topRatio: 0.1, bottomRatio: 0.95 }, 500), 425);
});

test('환산 결과는 DB CHECK 범위(1~4999)로 클램프', () => {
  assert.equal(guidesToHeightPx({ topRatio: 0, bottomRatio: 1 }, 12000), 4999);
  assert.equal(guidesToHeightPx({ topRatio: 0.499, bottomRatio: 0.5 }, 100), 1); // 0.1px → 최소 1
});

test('clampGuides: 움직인 선이 반대 선을 넘지 못하고 최소 간격 앞에서 멈춘다', () => {
  const t = clampGuides({ topRatio: 0.99, bottomRatio: 0.5 }, 'top');
  assert.ok(t.bottomRatio - t.topRatio >= MIN_GUIDE_GAP_RATIO - 1e-9);
  assert.equal(t.bottomRatio, 0.5);
  const b = clampGuides({ topRatio: 0.5, bottomRatio: 0.1 }, 'bottom');
  assert.ok(b.bottomRatio - b.topRatio >= MIN_GUIDE_GAP_RATIO - 1e-9);
  assert.equal(b.topRatio, 0.5);
});

test('clampGuides: 화면 밖 드래그는 0~1 로 클램프', () => {
  assert.deepEqual(clampGuides({ topRatio: -0.3, bottomRatio: 1.4 }, 'top'), { topRatio: 0, bottomRatio: 1 });
});

test('autoHeightFromNatural: 원본 세로 px 반올림 + 클램프, 실패값은 null', () => {
  assert.equal(autoHeightFromNatural(650), 650);
  assert.equal(autoHeightFromNatural(719.6), 720);
  assert.equal(autoHeightFromNatural(12000), 4999);
  assert.equal(autoHeightFromNatural(0), null);
  assert.equal(autoHeightFromNatural(null), null);
  assert.equal(autoHeightFromNatural(Number.NaN), null);
});

// ─── 배선 고정 (소스 앵커) ───

test('33-a 배선: 업로드 원본 측정 + 기준 키 자동 설정', () => {
  const slot = readFileSync('src/components/characters/FeaturedImageSlot.tsx', 'utf8');
  assert.match(slot, /measureImageSize\(file\)/);
  assert.match(slot, /autoHeightFromNatural\(naturalSize\.height\)/);
  assert.match(slot, /referenceHeightPx == null/);
  assert.match(slot, /setCharacterReferenceHeight\(character\.id, auto\)/);
  const utils = readFileSync('src/utils/imageUtils.ts', 'utf8');
  assert.match(utils, /export async function measureImageSize/);
});

test('33 배선: natural_width/height 저장 체인 (타입→매퍼→insert→마이그레이션)', () => {
  const types = readFileSync('src/types/index.ts', 'utf8');
  assert.match(types, /naturalWidth: number \| null;/);
  assert.match(types, /natural_height\?: number \| null;/);
  const svc = readFileSync('src/services/supabaseService.ts', 'utf8');
  assert.match(svc, /naturalWidth: row\.natural_width \?\? null/);
  const esb = readFileSync('electron/supabase.ts', 'utf8');
  assert.match(esb, /insert\.natural_width = input\.naturalWidth/);
  assert.match(esb, /insert\.natural_height = input\.naturalHeight/);
  const mig = readFileSync('DEVLOG/migrations/2026-07-15-costume-image-natural-size.sql', 'utf8');
  assert.match(mig, /ADD COLUMN IF NOT EXISTS natural_height INTEGER/);
  assert.doesNotMatch(mig, /DROP\s+(TABLE|COLUMN)/i);
});

test('33-b 배선: 기준 키 드래그 에디터', () => {
  const editor = readFileSync('src/components/characters/CharacterHeightEditor.tsx', 'utf8');
  assert.match(editor, /data-character-height-editor/);
  assert.match(editor, /guidesToHeightPx/);
  assert.match(editor, /setCharacterReferenceHeight\(character\.id, previewPx\)/);
  const detail = readFileSync('src/components/characters/CostumeDetail.tsx', 'utf8');
  assert.match(detail, /이미지로 조정/);
  assert.match(detail, /CharacterHeightEditor/);
  const slot = readFileSync('src/components/characters/FeaturedImageSlot.tsx', 'utf8');
  assert.match(slot, /\[data-character-height-editor\]/);
});
```

### (g) `package.json` — 테스트 등록 (앵커: `"test:character":`)

`test:character` 스크립트 값 끝에 ` ./tests/characterHeightKey.test.ts` 를 추가한다 (기존 나열 파일 12개 유지, 뒤에 1개 추가).

**before (원문, 줄 끝부분):**
```
… ./tests/characterFeedback3.test.ts ./tests/clipboardFilePaste.test.ts ./tests/dropEdgeFor.test.ts",
```
**after:**
```
… ./tests/characterFeedback3.test.ts ./tests/clipboardFilePaste.test.ts ./tests/dropEdgeFor.test.ts ./tests/characterHeightKey.test.ts",
```

## 엣지 케이스 & 주의사항

- **원본 크기 미기록(과거 업로드) 폴백:** 저장본(≤800px) 세로로 계산 + 경고 문구 표시. 임의로 다른 추정을 하지 말 것 — 새로 업로드하면 정확해진다는 안내가 전부다.
- **기준선 위치는 저장하지 않는다** — 재편집 시 항상 전체 범위(0~1)에서 시작한다(위치 복원용 컬럼 추가는 범위 외, 부록 C).
- **fit(썸네일 변환) 미적용 표시** — scale/translate 역산이 필요 없도록 원본 contain 표시로 한정한다.
- 저장 실패 시 store 가 롤백+토스트 처리 — 에디터는 닫지 않는다(재시도 가능).
- 키 비교 보기(`CharacterBoardView` 의 `heightCompareMode`)는 기존 그대로 — 값의 의미가 바뀌어도 상대 비교 로직은 무변경. **일반 그리드에 '키 정렬' 옵션을 추가하지 말 것**(향후 과제, 부록 C).

## 검증 방법

1. `npm run typecheck` && `npm run test:character` → 신규 파일 9 test 포함 통과.
2. 수동(부록 A-3): 에디터 열기 → 기준선 드래그 → px 실시간 갱신 → 저장 → input 값 갱신 + 키 비교 보기에서 카드 높이 반영.

## 완료 기준

- [ ] 유틸+에디터 신규 파일, CostumeDetail 진입, 가드 마커, 주석/툴팁 갱신
- [ ] `tests/characterHeightKey.test.ts` 등록·통과 + 기존 characterTier2 T2-3 앵커 무손상
- [ ] 커밋 1개 — 예: `기준 키 머리·바닥 기준선 드래그 조정 에디터 (피드백 33b)`

---

# 작업 7 — 마무리: 버전 + 업데이트 노트

## 수정 내용

### (a) `package.json` — `"version": "1.90.0"` → `"version": "1.91.0"` (4절 버전 표기 정책 참고 — 착수 시점 origin/main 기준 재확인)

### (b) `DEVLOG/update-notes.json` — 배열 **맨 앞**에 아래 항목 추가 (비개발자 톤 검수 완료본 — 문구를 임의 수정하지 말 것):

```json
{
  "version": "1.91.0",
  "title": "리깅 공지가 더 편해지고, 캐릭터 기준 키가 자동으로 잡혀요",
  "items": [
    {
      "category": "bugfix",
      "summary": "컴포지팅 화면에서 씬 미리보기가 알림창을 가리지 않아요",
      "description": "알림창을 열어둔 채 아래 타임라인에 마우스가 지나가면 씬 미리보기 카드가 알림창 위로 튀어나오던 문제를 고쳤어요. 이제 알림창이 항상 위에 보이고, 알림창이 덮고 있는 곳에서는 미리보기가 아예 뜨지 않아요."
    },
    {
      "category": "feature",
      "summary": "리깅 완성 공지에 캡처 이미지를 바로 붙여넣을 수 있어요",
      "description": "모호 화면을 캡처한 뒤 공지 창에서 Ctrl+V만 누르면 이미지가 바로 첨부돼요. 파일로 저장했다가 불러올 필요가 없어요."
    },
    {
      "category": "feature",
      "summary": "리깅 공지의 폴더 경로가 클릭하면 바로 열리는 링크로 나가요",
      "description": "공지에 글자로만 적히던 캐릭터 작업 폴더 경로가, 이제 슬랙에서 누르면 탐색기가 바로 열리는 링크로 나가요. 경로를 복사해서 따라 들어갈 필요가 없어요."
    },
    {
      "category": "ux",
      "summary": "리깅 공지를 비고 없이도 보낼 수 있고, 제목이 미리 채워져요",
      "description": "예전에는 비고를 꼭 적어야 공지를 보낼 수 있었는데 이제 생략해도 돼요. 제목도 기본 문구가 미리 들어가 있어서 바로 보내기 좋아요."
    },
    {
      "category": "feature",
      "summary": "캐릭터 기준 키가 이미지를 넣는 순간 자동으로 설정돼요",
      "description": "캐릭터 이미지를 올리면 이미지의 세로 크기를 읽어서 기준 키가 자동으로 채워져요. 모자나 삐죽 나온 부분 때문에 값이 어긋나면 '이미지로 조정'을 눌러 머리와 바닥 기준선을 직접 끌어서 맞출 수 있어요."
    }
  ]
}
```

## 완료 기준

- [ ] 버전·노트 갱신, `npm run build:vite` 전체 통과(부록 B-1)
- [ ] 커밋 1개 — 예: `[v1.91.0] 피드백 30·31·33 — 공지 개편·기준 키 자동화 릴리스 노트`

---

# 부록 A — 수동 검증 시나리오 (프리뷰 모드)

실행: `npm run dev:renderer` → 브라우저 `http://localhost:5190/?preview=1` → 로그인 `배한솔` / `1234`.

### A-1 (작업 1)
1. 컴포지팅 탭 → 에피소드 선택 → 우상단 벨로 알림창 열기.
2. 알림창이 덮고 있는 영역 뒤의 타임라인 진행률 바 좌표에 마우스 → **캐러셀 미발화**, 알림창 최상위 유지.
3. 알림창 닫고 같은 좌표 hover → 캐러셀 정상 표시, 카드 클릭 핀·더블클릭 상세 정상.
4. 씬 상세 모달 열림 중 모달 뒤 타임라인 좌표 → 캐러셀이 모달 위로 뜨지 않음.

### A-2 (작업 2·3)
1. 캐릭터 현황판 → 캐릭터 카드 → 복장 상세 → `리깅 완성 공지`.
2. 제목이 `[모호 리깅 현황] - 리깅 완료 공지` 로 미리 채워져 있고 '일반 리깅 완료' 칩 활성.
3. 비고를 **비운 채** 전송 버튼 활성 확인 → 전송 → 브라우저 콘솔 `[DEV 리깅 공지 웹훅] 페이로드:` 에서 `CH_name` 이 `"캐릭터" - "복장"`, `Path` 가 `jbbj://open/G:/...`(mock 시드 경로 기준), `bigo` 가 `""` 인지 확인.
4. 스크린샷을 클립보드에 복사(Win+Shift+S) → 모달에서 Ctrl+V → '붙여넣기' 배지 썸네일 추가 + 자동 선택. 비고 input 에 텍스트 붙여넣기는 평소처럼 동작.
5. 취소로 닫기 → 콘솔에 오류 없음(정리 fire-and-forget). ※ 탐색기 '파일 복사' 붙여넣기(FileNameW)는 mock 이 null 이라 프리뷰에서 검증 불가 — 실기 배포 후 확인 항목.

### A-3 (작업 5·6)
1. 캐릭터 상세 → 기준 키 input 을 비워 저장(=null) → 이미지 추가(파일 선택) → `기준 키를 NNNpx로 자동 설정했어요` 토스트 + input 반영 (NNN = 올린 파일의 실제 세로 px).
2. 기준 키에 값이 있는 캐릭터에 이미지 추가 → 자동 설정 안 됨(값 유지).
3. `이미지로 조정` 버튼(이미지 없는 복장에선 비활성) → 에디터: 머리선을 아래로 드래그 → px 라벨 실시간 감소 → `이 키로 저장` → input·토스트 반영.
4. 캐릭터 보드 `키 비교 보기` → 저장된 키 순 정렬·카드 높이 비례 표시 유지.

# 부록 B — 전체 검증 & 게이트

### B-1 전체 커맨드 (모든 작업 완료 후)

```bash
npm run typecheck
npm run test:character   # 기준선 104 + 신규(작업 2·3 추가분, characterHeightKey 9) 전부 통과
npm run test:ui          # 기준선 5 + compositingCarouselLayer 2
npm run build:vite       # 전체 테스트 체인 + vite build
```

- [ ] 전부 통과 (다른 test:* 스크립트는 build:vite 체인이 함께 실행)
- [ ] 커밋 7개(작업별) — src 외 파일 오염 없음(`git status` 확인)

### B-2 라이브 DB 마이그레이션 게이트 (배포 전 필수)

`DEVLOG/migrations/2026-07-15-costume-image-natural-size.sql` 을 라이브 Supabase(프로젝트 mpqifkpxalwxgcrddchv)에 적용한다. **적용 주체는 한솔 승인 하에**: Supabase MCP `apply_migration` 가능한 세션(Claude) 또는 Supabase SQL Editor 수동 실행. 적용 확인 쿼리:
```sql
SELECT column_name FROM information_schema.columns
WHERE table_name = 'character_costume_images' AND column_name LIKE 'natural_%';
```
2행(natural_width, natural_height)이 나와야 한다. **적용 전에는 새 빌드를 배포하지 말 것**(전역 리스크 3).

### B-3 슬랙 워크플로 확인 체크리스트 (한솔 몫 — PR 본문에 포함해 전달)

앱 배포와 별개로 Slack Workflow Builder(리깅 공지 트리거)에서:
- [ ] 메시지의 링크 서식이 **Path 변수를 URL** 로 사용하는지 (링크 텍스트는 자유 — 예: "캐릭터 작업 폴더")
- [ ] 비고(bigo)가 빈 문자열일 때 메시지가 어색하지 않은지 (필요 시 워크플로에서 비고 줄 구성 조정)
- [ ] 이미지(image) 변수 URL 이 본문에 **글자로 노출되지 않는지** (미리보기 언펄만 — 항목 31e)

# 부록 C — 범위 외 후속 권장 (이번에 구현하지 말 것 — 기록만)

1. **항목 32 마감일:** 한솔 지시로 제외 — 별도 라운드에서 다룬다.
2. **일반 그리드 '키 정렬' 옵션:** 기준 키 데이터가 쌓인 뒤 `CharacterBoardView` 의 `displayCharacters` useMemo(키 비교 모드 정렬이 이미 있는 곳)에 정렬 모드 분기로 확장. F29 수동 순서(sortOrder)와의 UX 충돌은 heightCompareMode 처럼 드래그 비활성으로 풀 것.
3. **기준선 위치(비율) 저장:** 재편집 시 이전 머리/바닥 위치 복원 — characters 테이블 컬럼 2개 추가 필요. 수요 확인 후.
4. **과거 이미지 natural size 백필:** 원본 파일이 로컬에만 있어 서버측 백필 불가 — 재업로드가 유일한 경로라는 점을 팀에 안내.
5. **인앱 공지사항 위젯:** 리깅 완료를 앱 안 공지로도 쌓는 기능(electron/main.ts:1896 TODO 주석) — 슬랙 공지와 별개 기획 필요.
