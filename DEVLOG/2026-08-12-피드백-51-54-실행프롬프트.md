# 피드백 51~54 실행 프롬프트 (구현 에이전트 핸드오프용)

아래 프롬프트를 구현 에이전트(Codex·Opus·다른 Claude 세션)에게 그대로 전달한다. 이 프롬프트는 **구현 → 코덱스 리뷰 루프 → 머지 → 배포**까지의 전체 체인을 위임한다 — 한솔이 이 프롬프트를 전달하는 행위 자체가 머지·배포 승인이다. PR 단위로 나눠 전달해도 된다(프롬프트의 "이번 회차" 지정만 바꿀 것).

````text
C:\Bflow-BGonly 저장소에서 피드백 수정 작업을 수행해줘.

**유일한 사양 문서: `C:\Bflow-BGonly\DEVLOG\2026-08-12-피드백-51-54-수정계획.md`**

이 문서는 자기완결적이며 독립 리뷰 2패스를 거쳤다. 문서를 처음부터 끝까지 읽고, 명시된 작업 순서대로 구현할 것.
이번 회차: **PR 1 (작업 1~2, v1.100.0)** 부터 시작한다. PR 2 (작업 3~4, v1.101.0) 는 PR 1 이 main 에 머지된 뒤 main 에서 새로 분기해 진행한다. 항목 51 은 코드 작업이 없다(계획서 부록 A-1) — 구현하지 말고 구현 기록에만 남길 것.

규칙:
1. 계획서의 **절대 규칙**을 따른다 — 문서에 명시되지 않은 리팩터링·포맷 변경·기능 추가 금지. 수정 범위는 명시된 파일·블록으로 한정.
2. 라인 번호가 어긋나면 병기된 **앵커(고유 코드 조각)** 검색으로 위치를 재확정한다. 앵커도 못 찾으면 임의 진행하지 말고 해당 작업을 건너뛰고 마지막에 보고한다.
3. 각 작업의 **before 블록과 실제 코드가 다르면** 그 작업을 중단하고 차이를 보고한다 (스냅샷 이후 코드가 변경된 신호다).
4. 작업당 커밋 1개 + PR 당 버전 커밋 1개. 커밋 메시지는 한글로 변경 내용을 명확히. 계획서가 지정한 테스트 갱신·추가(신규 테스트 파일의 package.json 등록 포함)를 코드 수정과 같은 커밋에 포함한다.
5. 각 작업 완료 시 해당 작업의 '검증 방법'을, PR 완료 시 부록 B 전체 검증을 실행하고 결과 수치를 기록한다. 버전은 착수 시점 origin/main 기준으로 재확인한다(계획서 §0 — 한솔 확정: v1.100.0 계열 유지, v2.0.0 점프 아님). `package.json` + `package-lock.json`(2곳) + `DEVLOG/update-notes.json[0]` 3자 일치.
6. `DEVLOG/update-notes.json` 최신 항목을 계획서의 비개발자 톤 규칙대로 작성한다 (기술 용어·식별자·파일 경로 금지).
7. 작업 브랜치: PR 1 은 `fix/feedback-52-53`, PR 2 는 `feat/feedback-54-costume-presence`. PR 본문은 저장소 PR 관례(업데이트 요약[비개발자 톤] + 상세 기술 설명 + 테스트 가이드)를 따른다.
8. PR 생성 후 **codex-review-loop 로 리뷰 루프를 돈다**: PR 에 `@codex review` 코멘트로 트리거 → P1/P2/P3 지적을 수정·푸시 → 재트리거 — "Didn't find any major issues" 또는 APPROVED 같은 명시 완료 신호까지 반복. 지적이 계획서의 명시 범위를 벗어난 기능 추가를 요구하면 코드를 바꾸지 말고 근거를 코멘트로 남긴다.
9. 리뷰 완료 후 **머지한다** (squash 아님 — 저장소 관례대로 merge commit, 기존 PR 머지 방식 확인 후 동일하게). PR 1 머지 후 PR 2 회차 진행.
10. PR 2 까지 머지되면 **배포한다**: `npm run build` (정식 빌드 — 전체 테스트 + electron-builder + manifest 생성) → G드라이브 `G:\공유 드라이브\JBBJ 자료실\한솔이의 두근두근 실험실\Bflow-BGonly\` 에 dist 동기화. **빌드 파일을 전부 먼저 올리고 `manifest.json` 을 마지막에 갱신**한다(반쯤 올라간 빌드가 최신으로 보이면 안 됨). 배포 후 `BFLOW-Setup.exe`/`latest.yml`/`manifest.json` 의 SHA-256 이 로컬과 일치하는지 검증한다. (`bflow-release-deploy` 스킬이 있으면 그것을 사용)
11. 완료 후 `DEVLOG/2026-08-12-피드백-51-54-구현.md` 를 작성한다(42~50 구현 기록 `DEVLOG/2026-08-06-피드백-42-50-구현.md` 형식 준수). 계획서와 실제 코드가 달랐던 지점이 있으면 반드시 기록.

완료 보고에는 PR·머지 커밋 해시, 테스트 결과 수치(기준선 대비 증감), 배포 SHA 검증 결과, 건너뛰거나 계획과 달라진 부분을 포함할 것.
````

## 구현 후 남는 일 (사용자 안내용)

1. **한솔 몫 — 항목 51 완결** (앱 밖, 계획서 부록 A-1): 슬랙 워크플로(트리거 `11544189535185`) 템플릿에서 빈 불릿을 만드는 `• {bigo}` 줄 삭제(+빈 값 불릿 줄 추가 확인) + 아이콘을 `DEVLOG/assets/2026-08-06-리깅알리미-아이콘-moho.png` 로 교체 + 테스트 채널 실기 3종. **v1.100.0 배포 직후에 하는 것이 창이 가장 짧다.**
2. **실기 확인** (계획서 부록 A-2): 실제 Moho 로 복장 작업 파일을 열어 캐릭터 카드/리스트/모달 표시 + 씬 프레즌스 회귀 1회.
3. 배포 타임스탬프를 메모리에 기록 — 다음 피드백 라운드의 상태 대조 기준.
