# 피드백 34~41 (37 제외) 실행 프롬프트 (구현 에이전트용)

> 아래 코드 블록을 구현 에이전트(코덱스·오푸스·다른 Claude 세션)에 그대로 붙여넣는다.
> 계획서가 자기완결적이므로 프롬프트는 문서를 가리키고 규율을 재강조하는 역할만 한다 — 계획 내용을 여기 중복 요약하지 않는다(요약본과 원문이 어긋나면 두 개의 진실이 생긴다).
> PR 이 2개(A: 작업 1~7 / B: 작업 8~10)이므로, 아래 프롬프트는 **PR A 부터** 시킨다. PR B 는 PR A 머지 후 두 번째 블록을 쓴다.

## PR A 프롬프트 (작업 1~7 — 항목 34·35·36·38·39·40)

```text
C:\Bflow-BGonly 저장소에서 피드백 수정 작업을 수행해줘.

**유일한 사양 문서: `C:\Bflow-BGonly\DEVLOG\2026-07-26-피드백-34-41-수정계획.md`**
(이 파일이 없으면 계획서 PR이 아직 머지되지 않은 것이다 — 진행하지 말고 보고할 것.)

이 문서는 자기완결적이며 독립 리뷰 패스를 거쳤다(헤더의 리뷰 이력 참조). 문서를 처음부터 끝까지 읽고, 이번 라운드는 **PR A 범위(작업 1→2→3→4→5→6→7)만** 순서대로 구현할 것. 작업 8~10(PR B)은 이번에 하지 않는다.

규칙:
1. 계획서의 **절대 규칙**을 따른다 — 문서에 명시되지 않은 리팩터링·포맷 변경·기능 추가 금지. **37번(공지 비고·알리미 아이콘) 관련 수정 절대 금지.** 수정 범위는 명시된 파일·블록으로 한정.
2. 라인 번호가 어긋나면 병기된 **앵커(고유 코드 조각)** 검색으로 위치를 재확정한다. 앵커도 못 찾으면 임의 진행하지 말고 해당 작업을 건너뛰고 마지막에 보고한다.
3. 각 작업의 **before 블록과 실제 코드가 다르면** 그 작업을 중단하고 차이를 보고한다 (스냅샷 `0978dcf` 이후 코드가 변경된 신호다). 작업 4→5→6 은 연쇄 수정이라, 5·6 의 before 는 직전 작업의 after 상태 기준이다.
4. 작업당 커밋 1개, 커밋 메시지는 한글(각 작업의 완료 기준에 예시가 있다). 계획서가 지정한 테스트 갱신·추가를 코드 수정과 **같은 커밋**에 포함한다. 6절 리스크 3의 "살아남아야 하는 앵커" 줄들은 글자 하나 바꾸지 않는다.
5. 각 작업 완료 시 그 작업의 '검증 방법'을, 전체 완료 시 부록 B-1 전체 검증(`npm run typecheck` → `test:character`/`test:ui` → `build:vite`)을 실행하고 결과 수치를 기록한다.
6. 버전 번호는 계획서 4절 정책대로 **구현 착수 시점의 origin/main 버전 기준 마이너 +1** 로 확정한다 (계획서 가정은 1.92.0).
7. 완료 후 `DEVLOG/2026-07-26-피드백-34-40-구현.md` 를 간단히 작성한다 — 계획서와 실제 코드가 달랐던 지점이 있으면 반드시 기록.
8. 작업 브랜치: main 최신에서 `fix/feedback-34-40` 분기. 완료 후 PR 을 생성하되 **머지는 하지 말 것**(한솔 승인 대기). PR 본문:
   - '업데이트 요약' 섹션은 비개발자 톤 — 계획서 작업 7의 update-notes 문구를 그대로 재사용해도 된다. 저장소의 pr-creator 스킬 구조를 따르면 더 좋다.
   - 계획서 부록 C(슬랙 워크플로 확인 체크리스트)를 PR 본문에 포함해 한솔이 배포 후 확인할 수 있게 한다.
   - 팝업 창(작업 6)은 브라우저 프리뷰로 검증 불가 — PR 본문에 "실기 확인 필요" 항목(부록 A 의 A-8~A-10)을 명시한다.
9. 계획서 문서 자체(`DEVLOG/2026-07-26-피드백-34-41-*.md`)는 수정하지 않는다.

완료 보고에는 작업별 커밋 해시, 테스트 결과 수치(계획서 헤더의 기준선 대비), 건너뛰거나 계획과 달라진 부분을 포함할 것.
```

## PR B 프롬프트 (작업 8~10 — 항목 41, PR A 머지 후에만)

```text
C:\Bflow-BGonly 저장소에서 피드백 41(캐릭터 탭·그룹) 구현을 수행해줘.

**유일한 사양 문서: `C:\Bflow-BGonly\DEVLOG\2026-07-26-피드백-34-41-수정계획.md`** — 이번 라운드는 **PR B 범위(작업 8→9→10)만** 구현한다.

선행 조건 확인(둘 중 하나라도 아니면 진행하지 말고 보고):
- PR A(작업 1~7)가 main 에 머지되어 있을 것 — `src/utils/characterViewPersist.ts` 와 `tests/characterFeedback4.test.ts` 가 main 에 존재하는지로 확인.
- 작업 브랜치는 그 main 최신에서 `feat/feedback-41-tabs` 로 분기.

규칙은 PR A 프롬프트의 1~9와 동일하며, 추가로:
- 버전 가정은 1.93.0 (착수 시점 origin/main 기준 마이너 +1 재확인).
- 구현 기록은 `DEVLOG/2026-07-26-피드백-41-구현.md`.
- **PR 본문 최상단에 "머지 전 라이브 DB 마이그레이션 적용 필수 — 계획서 부록 B-2"를 명시**한다. 마이그레이션 적용 자체는 구현 에이전트의 일이 아니다(한솔 또는 지시받은 세션이 수행).
- 신규 유틸 `src/utils/characterTabGroups.ts` 는 node --test 가 직접 import 한다 — '@/' alias import 금지(계획서 6절 리스크 5).

완료 보고에는 작업별 커밋 해시, 테스트 결과 수치, 계획과 달라진 부분을 포함할 것.
```

## 구현 이후 체인 (한솔용 안내)

1. 구현 완료·PR A 생성 확인
2. `codex-review-loop` 스킬로 PR A 리뷰 루프 (명시적 완료 신호까지 재트리거)
3. 한솔 승인 후 PR A 머지 → (배포는 선택 — PR B 까지 묶어도 됨)
4. PR B 프롬프트로 구현 → `codex-review-loop`
5. **PR B 머지 전**: 계획서 부록 B-2 — 마이그레이션 `DEVLOG/migrations/2026-07-26-character-board-tabs.sql` 라이브 적용 + 확인 쿼리 (Supabase MCP 있는 Claude 세션 또는 SQL Editor)
6. 한솔 승인 후 PR B 머지
7. `bflow-release-deploy` 스킬로 릴리스 노트·빌드·배포 (manifest.json 마지막 갱신 원칙)
8. 부록 C — 슬랙 워크플로(리깅 공지 트리거) 서식 확인: 제목 머리말 1회 / [내용] 줄 CH_name 바인딩
9. 부록 A 의 실기 확인 항목(A-3, A-8~A-10, A-13) 점검
10. 배포 타임스탬프를 메모리에 기록 — 다음 피드백 라운드의 상태 대조 기준
