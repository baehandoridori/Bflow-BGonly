# 정식 공개 · 그룹 레일 · 태그 접기 실행 프롬프트 (구현 에이전트용)

> 아래 코드 블록을 구현 에이전트(코덱스·오푸스·다른 Claude 세션)에 그대로 붙여넣는다.
> 계획서가 자기완결적이므로 프롬프트는 문서를 가리키고 규율을 재강조하는 역할만 한다 — 계획 내용을 여기 중복 요약하지 않는다.
> 전 라운드(피드백 34~41)는 v1.93.0 으로 구현·배포 완료 — 이 라운드는 그 위의 **단일 PR** 이다.

```text
C:\Bflow-BGonly 저장소에서 캐릭터 현황판 개편 작업을 수행해줘.

**유일한 사양 문서: `C:\Bflow-BGonly\DEVLOG\2026-07-26-정식공개-그룹레일-태그접기-수정계획.md`**
(이 파일이 없으면 계획서 PR이 아직 머지되지 않은 것이다 — 진행하지 말고 보고할 것.)

이 문서는 자기완결적이며 독립 리뷰 패스를 거쳤다(헤더의 리뷰 이력 참조). 문서를 처음부터 끝까지 읽고, **작업 1→2→3→4 순서**로 구현할 것. 작업 3 착수 전에 `DEVLOG/mockups/2026-07-26-탭그룹-시안.html` 을 브라우저로 열어 시안 A 인터랙션(레일 드롭 배치·« 접기·⠿ 순서 변경·자동 스크롤·태그 접기)을 직접 확인한다.

규칙:
1. 계획서의 **절대 규칙**을 따른다 — 명시되지 않은 리팩터링·기능 추가 금지. **37번(공지 비고·알리미 아이콘) 관련 수정 금지.** metadata IPC·`resolveAllowedView`·완료 기록물 문서(`DEVLOG/2026-07-26-피드백-34-41-*.md`) 수정 금지.
2. 라인 번호가 어긋나면 병기된 **앵커** 검색으로 재확정. 앵커도 못 찾으면 임의 진행하지 말고 해당 작업을 건너뛰고 마지막에 보고.
3. 각 작업의 **before 블록과 실제 코드가 다르면** 중단하고 차이를 보고 (스냅샷 `ca3888d`/v1.93.0 이후 변경 신호).
4. 작업당 커밋 1개(한글 메시지, 예시는 각 작업 완료 기준에), 지정된 테스트 갱신을 같은 커밋에 포함. 4절 리스크 2 의 "살아남아야 하는 앵커"는 글자 하나 바꾸지 않는다. **작업 1 의 테스트 수술을 훅 파일 삭제와 같은 커밋에서 하지 않으면 test:character 가 로드 단계에서 크래시한다(리스크 1).**
5. 각 작업 완료 시 그 작업의 '검증 방법'을, 전체 완료 시 부록 B 를 실행하고 수치 기록. 기준선: character 126 → 최종 129 pass.
6. 버전은 **착수 시점 origin/main 기준 마이너 +1** (가정 1.94.0). **`package.json` + `package-lock.json` 2곳** — lock 을 빠뜨리면 test:playground 1 fail (계획서 1절 함정 1).
7. node --test 가 직접 import 하는 유틸의 상대 import 는 **`.ts` 확장자 명시** (1절 함정 2).
8. 완료 후 `DEVLOG/2026-07-26-정식공개-그룹레일-구현.md` 작성 — 계획서와 실코드가 달랐던 지점 필수 기록.
9. 작업 브랜치: main 최신에서 `feat/board-public-rail` 분기. 완료 후 PR 생성하되 **머지는 하지 말 것**(한솔 승인 대기). PR 본문:
   - '업데이트 요약'은 비개발자 톤(계획서 작업 4 문구 재사용 가능), pr-creator 스킬 구조 권장.
   - **전면 공개 명시**: 배포되면 모든 팀원에게 캐릭터 메뉴가 새로 나타난다(공지 여부는 한솔 판단).
   - 실기 확인 항목(부록 A 의 P-2·P-10 + 이월 항목) 명시.

완료 보고에는 작업별 커밋 해시, 테스트 수치(기준선 대비), 계획과 달라진 부분을 포함할 것.
```

## 구현 이후 체인 (한솔용 안내)

1. 구현 완료·PR 생성 확인
2. `codex-review-loop` 스킬로 리뷰 루프 (명시적 완료 신호까지 재트리거)
3. 한솔 승인 후 머지 — **DB 마이그레이션 없음** (이번 라운드는 코드만)
4. `bflow-release-deploy` 스킬로 릴리스 노트·빌드·배포 (manifest.json 마지막 갱신 원칙)
5. 부록 A 실기 항목 점검: P-2(비공개였던 계정으로 공개 확인)·P-10(두 창 동기화) + 전 라운드 이월분(A-3 슬랙 공지 포맷·A-8~A-10 팝업)
6. 전면 공개는 팀 전원에게 메뉴가 새로 보이는 변화 — 슬랙 공지 여부 판단(공지는 한솔 지시 시에만)
7. 배포 타임스탬프를 메모리에 기록
