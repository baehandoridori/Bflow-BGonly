# 피드백 55·57 구현 기록

> **계획서**: `DEVLOG/2026-09-05-피드백-55-57-수정계획.md` (스냅샷 `a1d9e5e2`, v1.110.1)
> **실행**: 2026-09-05, Claude Opus 5. 착수 시점 `origin/main` = `8bcfc648` (계획서·실행프롬프트 PR #262 머지 상태, `package.json` 1.110.1). 여기서 `fix/피드백-55-57` 분기.
> **테스트 기준선(착수 시점, 실측)**: typecheck 통과 · character 148 · ui 17 · entity 249 · presence 38 · playground 461 · auto-update 29 · notifications 144, 전부 fail 0. calendar 는 아래 §4-2 참조.
> **결과**: 계획서의 작업 1 → 2 → 3 → 부록 A 순서를 그대로 따랐고, **before 블록이 실파일과 다른 곳은 한 곳도 없었다**. 건너뛴 작업 없음. 구현 뒤 로컬 코드리뷰에서 나온 결함 4건을 사용자 지시("문제 해결 후 머지")에 따라 6번째 커밋으로 수정했다 — §5-3.

---

## 1. PR·커밋

| PR | 버전 | 작업 | 상태 |
|---|---|---|---|
| [#263](https://github.com/baehandoridori/Bflow-BGonly/pull/263) | v1.111.0 | 작업 1~3 (55·57-1·57-2·57-3) | 생성 완료, **머지 대기** (한솔이 코덱스 리뷰 루프 후 결정) |

### 커밋 (6개, 브랜치 `fix/피드백-55-57`)

- `1a0e664fe6c325d3b582c17a133662ad807e648d` 피드백 55: 같은 이름 캐릭터 추가 차단 + 기존 카드 열기·비슷한 이름 제안
- `8f9629b42f7d0eae35044d7f06050ae0e4d2074b` 피드백 57-1: 작업 폴더 만들기 전 확인 창 + 새 창 확인 창 호스트
- `d850986a097b0246cb82f0c8629d0e2e91da6a39` 피드백 57-2·3: 작업 폴더·파일 경로 지우기와 버튼 이름 정리
- `a9b07494dd239b7d0b8592645112eb53ab7195b9` v1.111.0 버전 반영과 업데이트 내역 추가
- `7c7bfd1a…` 피드백 55·57 구현 기록 추가
- `0e1d556f…` 로컬 코드리뷰 반영 — 확인 창 겹침·폴더 되살림·경로 행 가독성 수정 (이 문서 갱신 포함)

모든 커밋 경계에서 `npm run typecheck` 통과 + `test:character` 155 → 157 → 159 → 161, fail 0. 계획서 §4-2가 요구한 대로 신설 테스트 파일 `tests/characterFeedback5.test.ts` 를 작업 1에서 생성하고 작업 2·3에서 append 했으므로 빨간 커밋이 없다.

---

## 2. 작업별 구현 결과

### 작업 1 — 항목 55: 같은 이름 캐릭터 추가 차단 + 기존 카드 열기 + 비슷한 이름 제안

- `src/utils/characterName.ts` 에 순수 유틸 4개 추가 — `characterNameKey`(NFC + 전체 공백 제거 + 소문자), `findDuplicateCharacters`(활성 우선), `suggestSimilarCharacters`(활성만·정확 일치 제외·양방향 포함·최대 3), `buildCharacterFolderConfirmMessage`(작업 2에서 사용하지만 같은 파일이라 이 커밋에 포함).
- `AddCharacterModal.tsx` 전체 교체. 활성 중복이면 `submit()` 조기 반환 + '추가' 버튼 비활성 이중 차단, `기존 카드 열기` 제공. 보관본만 겹치면 차단하지 않고 `복원해서 열기` — `restoreCharacter` 가 `Promise<void>` 라 store 를 재조회해 여전히 `archived` 면 창을 닫지 않는다.
- 새 prop `onOpenExisting` 을 두 진입점(현황판 헤더·상세 모달 좌측 목록)에 배선.
- **(c-2)** `CharacterGrid` 의 `pendingOpenId` 소비 효과를 양방향으로 고쳤다: `if (pendingCharacter) setShowArchived(pendingCharacter.status === 'archived')`. 보관 목록을 보는 중 활성 카드로 이동해도 목록 모드가 함께 돌아온다.
- 옛 문구 "같은 이름의 캐릭터가 이미 있어요 — 그래도 추가할 수 있어요."(GAP-D)는 사라졌다.

### 작업 2 — 항목 57-1: 작업 폴더 '만들기' 확인 창

- `characterFolderService.createAndLinkCharacterFolder` 의 **환경 확인 뒤 · 실제 `pathCreateFolder` 호출 전** 에 `ConfirmDialog.show({ confirmLabel: '폴더 만들기' })` 삽입. 취소하면 토스트 없이 조용히 `false`.
- 문구는 기준 경로 원문 + "이미 같은 이름의 폴더가 있으면 …" + 임시 이름일 때만 경고 줄 1개.
- `WidgetPopup.tsx` 팝업 루트에 `<ConfirmDialogHost />` 1개 추가. 없으면 새 창의 보관·삭제·폴더 만들기·경로 지우기가 전부 브라우저 기본 `window.confirm` 으로 떨어졌다. `<Toaster>` 블록은 건드리지 않았다.

### 작업 3 — 항목 57-2·57-3: 경로 지우기 + 버튼 이름

- `PathActionRow` 에 옵셔널 `onClear` 추가 → 경로가 있을 때만 [지우기] 렌더. '선택' 고정 문구를 `{path ? '경로 수정' : '경로 지정'}` 로, `title` 도 상태별로.
- `CharacterDetailModal` 에 `handleClearFolder` / `handleClearFile(targetCostume)` 추가. 둘 다 `confirmLabel: '연결 지우기'` 확인 창을 거친 뒤 `updateCharacterFolder(id, null)` / `updateCostumeField(id, { workFilePath: null })`, 성공(`true`)일 때만 토스트.
- 확인 창 tone 은 기본(보라) — 실제 폴더·파일을 지우는 게 아니라 '보관'의 danger(빨강)와 구분한다.
- 기존 앵커(`onClick={onOpen}` 다음 줄 `title={path}`, `!path && onCreate`, 만들기 버튼 title, F26 표시 이름)는 전부 보존했다.

### 부록 A-1 — 버전

착수 시점 `origin/main` 의 `package.json` 을 재확인해 1.110.1 → **1.111.0**(마이너 +1). `package.json` 1곳 + `package-lock.json` 2곳(최상위 version, packages 루트 version) + `DEVLOG/update-notes.json[0].version` 3자 일치. update-notes 는 계획서 부록 A-1 완성본을 그대로 넣었다(비개발자 톤, 항목 3개).

### 항목 56 — 코드 작업 없음

계획서대로 이번 범위에서 제외했다(한솔 지시). 앱 코드는 v1.96.0에서 이미 수정됐고 잔여 증상은 슬랙 워크플로 템플릿 사안이다.

---

## 3. 테스트 수치

| 스위트 | 기준선 | 작업 1 | 작업 2 | 작업 3·최종 | 기준선 대비 |
|---|---:|---:|---:|---:|---:|
| typecheck | 통과 | 통과 | 통과 | **통과** | — |
| character | 148 | 155 | 157 | **161** | +13 |
| ui | 17 | — | — | **17** | 0 |
| entity | 249 | — | — | **249** | 0 |
| presence | 38 | — | — | **38** | 0 |
| playground | 461 | — | — | **461** | 0 |
| auto-update | 29 | — | — | **29** | 0 |
| notifications | 144 | — | — | **144** | 0 |

전 스위트 fail 0. 증가분 내역:

- character +4 — `tests/characterName.test.ts` 에 순수 유틸 테스트 4블록 append (중복 키 / 같은 이름 찾기 / 비슷한 이름 제안 / 폴더 확인 문구)
- character +7 — `tests/characterFeedback5.test.ts` 신설 (작업 1에서 3개, 작업 2에서 2개, 작업 3에서 2개 append)
- character +2 — 로컬 코드리뷰 반영 커밋에서 확인 창 호스트 가드·폴더 되살림 방지 테스트 추가 (159 → **161**)

신규 테스트 파일은 계획대로 작업 1 커밋에서 `package.json` 의 `test:character` 에 등록했다. `tests/characterFeedbackBatch.test.ts:27` 의 B4 앵커는 계획서 (e)대로 `disabled={saving || !!blockingDuplicate}` 로 정밀화했다(정밀화 전에도 우연히 통과하던 상태였다).

**빌드**: `npx vite build` 통과, `node scripts/generate-manifest.js --allow-missing-installer` 가 v1.111.0 manifest 생성. `npm run build:vite` 는 게이트 중 `test:calendar` 에서 멈춘다 — §4-2 참조.

### 수동 미리보기 확인 (`?preview=1`, 배한솔/1234)

계획서가 지정한 시나리오를 전부 관찰했다.

- **작업 1** — '한솔' → 차단 문구 + [기존 카드 열기] + '추가' 비활성(`disabled` 실측) / ' 한 솔 ' → 동일 차단(공백 무시) / '삐' → "비슷한 이름: [삐쭈]" · '추가' 활성 / '삐쭈님' → 동일(양방향) / '도윤' → 안내 문구만 / [삐쭈] 클릭 → 추가 창 닫히고 삐쭈 상세 / 상세 모달 좌측 추가 → '혜원' → [기존 카드 열기] → 우측이 혜원으로 전환 / 혜원 보관 후 '혜원' → "보관된 캐릭터 중에 …" + [복원해서 열기] → 토스트 '캐릭터를 복원했어요' + 혜원 선택 / **(c-2)** 보관 목록(혜원 1장)에서 헤더 '캐릭터 추가' → '한솔' → [기존 카드 열기] → 한솔 상세가 열리고 뒤 목록이 활성(한솔·삐쭈)으로 복귀.
- **작업 2** — 이름 없이 [추가] → '새 캐릭터' 상세, 작업 폴더 행 `미등록 · [만들기][선택]` → [만들기] → 확인 창 4줄(경로 + 임시 이름 경고) → [취소] 시 무변화·토스트 없음. 확인 창이 떠 있는 동안 행의 [만들기]·[선택]이 `disabled` 인 것도 실측(의도된 동작). 이름을 '도윤'으로 바꾼 뒤 [만들기] → 임시 이름 줄이 사라짐 → [폴더 만들기] → 토스트 '폴더를 만들고 작업 폴더로 연결했어요' + 행이 `도윤 · [선택][열기]`.
- **작업 3** — 한솔 상세에서 두 행 모두 `[경로 수정][열기][지우기]`. 작업 폴더 [지우기] → 확인 창 문구 일치 → [취소] 무변화 → [연결 지우기] → 토스트 '작업 폴더 연결을 지웠어요' + `미등록 · [만들기][경로 지정]` → [만들기] → 확인 → `한솔` 복구 → [지우기] → [경로 지정] → `A_014`. 작업 파일 [지우기] → "'교복' 복장의 작업 파일 연결을 지울까요? …" → 토스트 '작업 파일 연결을 지웠어요' + `미등록 · [경로 지정]` → [경로 지정] → `main.psd`.
- **부록 A-3 통합** — 추가 창 [기존 카드 열기] → 상세 [지우기] → [만들기] 확인 → [폴더 만들기] → 행 갱신까지 한 흐름으로 동작(창끼리 포커스 충돌 없음). 확인 창에서 Escape → 확인 창만 닫히고 상세 모달은 남고 경로도 그대로. 라이트 모드에서도 [경로 지정]/[지우기] 문구와 확인 창이 읽힌다.

---

## 4. 계획서와 달라진 지점

### 4-1. before 블록·라인 번호 — 불일치 0

계획서의 모든 before 블록이 실파일과 **정확히 1회씩** 일치했고, 병기된 라인 번호도 어긋나지 않았다(`AddCharacterModal.tsx` 79행, `CharacterBoardView.tsx` 230·779행, `CharacterDetailModal.tsx` 326·361·626·931행, `CostumeDetail.tsx` 28·134·297행, `characterFolderService.ts` 49행, `WidgetPopup.tsx` 48·1126행, `characterFeedbackBatch.test.ts` 27행). 앵커 검색으로 위치를 재확정해야 했던 경우도 없었다. 중단·건너뛴 작업 없음.

작업 트리가 CRLF 이므로 스크립트 치환 시 `\r\n` 을 LF 로 정규화해 매칭하고 다시 CRLF 로 되돌리는 방식을 썼으며, 매 치환마다 "before 가 정확히 1회 매치"를 강제했다.

### 4-2. `test:calendar` 가 이 워크트리에서 2건 실패 (계획서 기준선은 1건)

계획서는 기존 실패 1건(`ScheduleView counts every event overlapping the displayed month`)을 예고했다. 실제로는 2건이 실패한다.

1. `ScheduleView counts every event overlapping the displayed month` — 계획서가 예고한 **기존 실패**. 범위 밖이라 건드리지 않았다.
2. `ElectronAPI calendar event write inputs expose only fields accepted by IPC` — **환경 아티팩트**. `tests/calendarIpcValidation.test.ts:1487` 이 `join(process.cwd(), 'node_modules', 'typescript', 'bin', 'tsc')` 를 하드코딩하는데, 이번 작업은 git worktree(`.claude/worktrees/…`)에서 진행했고 워크트리에는 자체 `node_modules` 가 없다(전부 부모 저장소 `C:\Bflow-BGonly\node_modules` 에서 해석된다). 실패 원인이 `MODULE_NOT_FOUND: …\node_modules\typescript\bin\tsc` 인 것을 확인했다. 이번 diff 는 캘린더·`src/types`·tsconfig 파일을 하나도 건드리지 않았다(`git diff --name-only origin/main` 로 확인). 본 저장소 정상 체크아웃에서는 재현되지 않을 것으로 본다.

계획서 부록 B의 지시대로 `npx vite build && node scripts/generate-manifest.js --allow-missing-installer` 를 직접 실행해 빌드 통과를 증명했다.

### 4-3. 독립 검증에서 잡아 되돌린 불일치 1건 (테스트 픽스처 백슬래시)

구현 후 계획서 대조 검증(6개 차원 병렬 + 적대적 재검증)을 돌렸고, 5개 차원은 지적 없음, 1건이 확정됐다.

`tests/characterName.test.ts:93` 의 `const root` 가 계획서 (f-2) after 코드의 `'G:\\공유 드라이브\\...'`(이스케이프된 백슬래시)가 아니라 `'G:\공유 드라이브\...'`(단일 백슬래시)로 들어갔다. JS 문자열에서 `\공`·`\사`·`\[` 는 인식되지 않는 이스케이프라 백슬래시가 소거돼, 실제 런타임 값이 구분자 없는 `G:공유 드라이브사우스 코리안 파크[]사코팍 캐릭터 세팅` 이 된다. `assert.ok(msg.includes(root))` 가 같은 변수를 재사용하는 왕복 단언이라 테스트는 그대로 통과했고(159/0) 어떤 게이트에도 걸리지 않았지만, "윈도우 경로 원문이 확인 문구에 그대로 실린다"는 이 테스트의 의도가 무력화된다.

원인은 셸 heredoc 이 `\\` 를 `\` 로 접은 것이다(다른 추가 라인의 정규식 이스케이프는 원래 단일 백슬래시라 영향이 없었다). 계획서 표기와 바이트 단위로 같아지도록 되돌렸고, 이 줄은 성격상 작업 1의 것이므로 새 커밋을 만들지 않고 **작업 1 커밋을 amend 한 뒤 나머지 3개를 cherry-pick 해 이력을 다시 썼다**(계획서가 지정한 5커밋 구성 유지, `--force-with-lease` 로 push). 재작성 전후 트리 차이는 이 한 줄뿐이며(`git diff` 로 확인), 네 커밋 경계에서 `test:character` 155 → 157 → 159 → 159 · fail 0 과 `typecheck` 통과를 다시 실측했다.

### 4-4. 그 밖에 계획서와 달라진 코드·문구 — 없음

위 1건을 제외하면 after 코드 블록은 전부 계획서 표기 그대로 적용했다. 계획서에 없는 리팩터·포맷 변경·기능 추가는 하지 않았다.

---

## 5. 남은 일

### 5-1. 머지 전

- 코덱스 리뷰 루프(`@codex review`) 후 한솔이 머지 여부 결정. 계획서 지시대로 이 세션에서는 머지하지 않았다.

### 5-2. 실기(Electron) 확인 — 미리보기로 검증 불가 (부록 A-4)

1. 실제 탐색기 대화상자로 [경로 지정]/[경로 수정] → 경로 저장.
2. [만들기] 확인 → 실제 G드라이브 기준 경로에 폴더 생성(이미 있으면 '이미 있던 폴더를 작업 폴더로 연결했어요').
3. '새 창으로' 팝업에서 확인 창이 브라우저 기본 confirm 이 아니라 앱 스타일(어두운 카드 + 보라 확인 버튼)로 뜨는지.

미리보기 mock 에는 `widgetOpenPopup` 이 없어 '새 창으로' 버튼 자체가 나오지 않으므로 3번은 실기 전용이다.

### 5-3. 로컬 코드리뷰 라운드 (커밋 `0e1d556f` 에서 전부 반영)

코덱스가 사용량 한도로 못 돌아 로컬 리뷰를 5개 축으로 병렬 진행했다(경로 null 소비자 / 중복 차단 회귀 / 비동기·확인 창 경합 / 테스트 실효성 뮤테이션 / UI·접근성). 확정된 결함은 아래 4건이고 전부 고쳤다. 계획서 절대 규칙을 넘는 변경이지만, 사용자가 "문제 해결 후 머지"로 명시 지시했다.

**① 확인 창이 겹치면 앞 요청이 영영 응답을 못 받고, 문구가 바뀐 채 확인을 누르게 된다 (medium, 이번 PR 이 새로 연 경로)**

`ConfirmDialogHost` 는 모듈 전역 단일 슬롯이라 두 번째 `show()` 가 첫 번째 promise 의 `resolve` 를 그냥 버렸다. 기존에는 모든 확인 창이 클릭 핸들러에서 **즉시** 떠 오버레이가 다음 클릭을 막았기 때문에 겹칠 수 없었다. 그런데 이번에 추가한 폴더 만들기 확인은 기준 경로 조회(네트워크)가 **끝난 뒤**에 뜬다 — 그 사이 화면이 안 잠겨 사용자가 보관·삭제 확인 창을 먼저 열 수 있고, 뒤늦게 뜬 만들기 확인이 그걸 덮어쓴다. 결과: 사용자가 요청한 보관·삭제가 조용히 사라지고, 읽던 문구가 손 밑에서 `[폴더 만들기]` 로 바뀌어 **공용 드라이브에 의도치 않은 폴더가 생길 수 있었다**(57-1 이 막으려던 사고 그 자체).

→ 이미 확인 창이 떠 있으면 덮어쓰지 않고 새 요청을 취소로 닫도록 했다(fail-closed). 호스트가 사라질 때도 대기 중인 요청을 취소로 닫는다. 씬 작업 링크 쪽에 이미 있던 같은 모양의 지연 확인(`sceneWorkLinkActions.ts`)도 함께 보호된다.

**② 작업 폴더를 지운 직후 작업 파일 경로를 지정하면 폴더 연결이 조용히 되살아난다 (low, 이번 PR 이 새로 연 경로)**

`handlePickFile` 은 파일 저장 후 폴더가 비어 있으면 파일의 상위 폴더를 유추해 연결한다. [지우기]가 생기기 전에는 "한 번도 지정 안 한 캐릭터"에만 걸리던 동작인데, 이제 사용자가 일부러 끊은 연결을 확인 창도 토스트도 없이 되살린다. 리깅 완성 공지의 경로 링크가 가리키는 곳까지 같이 바뀐다.

→ "어느 캐릭터에서 지웠는지"를 기억해 그 캐릭터에서만 자동 유추를 건너뛴다. 폴더 이력이 없는 캐릭터의 자동 연결은 그대로 살아 있다(미리보기 실측: 새 카드는 파일 지정 시 `A_014` 자동 연결, 지운 카드는 `미등록` 유지).

**③ 경로 행 가독성 회귀 (medium-high)**

버튼이 `[선택]` → `[경로 수정][열기][지우기]` 로 늘면서 버튼군이 88.7 → 176.2px(+99%)가 됐는데, 왼쪽이 `min-w-0` 라 늘어난 폭을 경로 이름이 전부 흡수했다. 실측: 1400px(메인 창 기본)에서 이름 영역 127 → **39.4px**(`홍…` 만 보임), 라벨은 2줄, 1024px 에서는 라벨 4줄 + 행 높이 2배.

→ 한 줄에 안 들어가면 버튼 줄을 아래로 내리고 라벨에 `whitespace-nowrap` 을 줬다. 실측(수정 후): 1400px 이름 영역 **222px**·라벨 1줄, 1024px **206px**·라벨 1줄. 행 높이는 91px 로 늘었지만 무엇이 연결됐는지 항상 읽힌다.

**④ 중복 차단 문구가 스크린리더에 안 읽힘 (low)**

문구의 성격이 "경고"에서 "추가 버튼을 잠그는 검증 오류"로 바뀌었는데 평범한 `div` 였다. 저장소의 차단성 검증 관례(`role="alert"`)를 따랐다.

**⑤ 테스트가 회귀를 못 잡던 구멍 (뮤테이션 테스트로 확인)**

의도적 결함 36개를 넣어보니 **12개가 초록불로 통과**했다. 그중 이번 기능과 직결된 것들을 메웠다 — 특히:
- **게이트 자기참조**: 등록 확인이 그 파일 안에 있어서, `package.json` 에서 파일을 빼면 7개가 조용히 사라진 채 통과했다(152/0 초록불). 이미 등록돼 있는 `characterName.test.ts` 로 옮겼다.
- **두 경로 행의 지우기 교차 배선**: `onClear` 를 서로 맞바꿔 **작업 폴더 [지우기]가 작업 파일 연결을 지우게** 만들어도 통과했다. 행 경계를 넘지 않는 앵커로 고정했다.
- 차단 대상 뒤바뀜(활성↔보관), 제안 칩 무동작, `openExisting` 이 창을 안 닫음, 보관본 제외 필터·한국어 정렬·빈 이름 가드, 확인 문구 줄 구조.

수정 후 같은 뮤테이션 11종을 다시 넣어 **전부 실패(정상 감지)** 를 확인했다. `test:character` 159 → **161**.

### 5-4. 리뷰에서 나왔지만 고치지 않은 것

- **이름 자동 지정·인라인 rename 은 중복 검사를 우회한다 (medium)**: 이름을 비우고 추가한 뒤 대표 이미지를 넣으면 파일명으로 자동 rename 되는데(`FeaturedImageSlot`), 이 경로에는 중복 검사가 없어 동명 활성 카드 2장이 만들어진다. 즉 55 의 차단은 추가 모달에 한정된다. **계획서 30행에서 한솔이 "이름 변경·파일명 자동 지정 경로는 손대지 않는다"로 명시 제외한 범위**라 건드리지 않았다. 서버 측 유일 제약(§5-5-1)과 함께 다뤄야 할 사안.
- **realtime 재구독 시 복장 파일 캐시를 따라잡지 않음 (low~medium, 기존 문제)**: 채널이 끊겼다 재연결되는 동안의 경로 변경이 유실돼, 파일이 없는 복장에 "편집 중" 배지가 남을 수 있다. 이 PR 이전에도 경로 재지정에서 같은 일이 났고(이번엔 "등록된 파일이 없는데 편집 중"이라는 더 이상한 화면이 될 뿐), 프레즌스 기반 코드라 별건으로 다룬다.
- **확인 창 대기 중 대상이 삭제되면 일반 실패 토스트 (medium, 구조적)**: 확인을 기다리는 사이 다른 사용자가 그 캐릭터·복장을 지우면 `.single()` 이 던져 "저장에 실패했어요"만 뜬다. 만들기의 경우 실제 폴더는 이미 만들어진 뒤라 고아 폴더가 남는다. 오류 코드별 안내가 필요해 별건.
- **라이트 모드 red-400 대비 2.7:1 (기존 26곳 공통)**: 이 PR 회귀가 아니고 danger 색 토큰화가 선행이라 별건.

### 5-5. 코덱스 리뷰 — 사용량 한도로 미실행

`@codex review` 트리거(`5552232653`, 2026-09-05T13:46Z, head `7684c58f`)에 대해 봇이 9초 만에 "You have reached your Codex usage limits for code reviews" 만 남겼다. line comments 0 · reviews 0 · trigger reaction 없음 — 리뷰가 시작되지 않았다. 한도가 풀린 뒤 재트리거가 필요하다. 그 사이 위 5-3 은 로컬 코드리뷰로 찾은 것이다.

### 5-6. 범위 외 후속 (계획서 부록 C — 이번에 손대지 않음)

1. `characters.name` 서버 측 유일 제약 — 두 사용자가 동시에 같은 이름을 만들면 여전히 카드가 두 장 생긴다. 라이브 DB 기존 중복 조사가 선행이라 별도 마이그레이션 라운드로.
2. 이름 변경(상세 모달 인라인 rename)·이미지 파일명 자동 지정의 중복 검사 — 한솔 결정으로 제외.
3. 카드 우클릭 메뉴(`CharacterImageContextMenu`)의 경로 지우기.
4. `restoreCharacter` 가 boolean 을 돌려주도록 시그니처 변경.
5. 확인 창 대기 중 스피너 — 확인 창을 `handleCreateFolder` 앞단으로 옮기면 기준 경로를 두 번 조회하거나 서비스 시그니처를 바꿔야 해 유지.
6. 기존 `onCreated` 경로의 같은 결함은 (c-2)가 효과 자체를 고쳐 함께 정상화됐다 — 별도 작업 불필요.

---

*작성: Claude Opus 5 — 2026-09-05*
