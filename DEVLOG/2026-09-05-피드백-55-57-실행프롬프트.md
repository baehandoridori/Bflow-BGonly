# 피드백 55·57 실행 프롬프트 (구현 에이전트용)

> 계획서 `DEVLOG/2026-09-05-피드백-55-57-수정계획.md`와 짝. **이 계획서 PR이 main에 머지된 뒤** 아래 코드 블록을 그대로 복사해 구현 에이전트(Opus 등)에게 전달한다. 56번은 사용자 지시로 제외.

````text
C:\Bflow-BGonly 저장소에서 피드백 수정 작업을 수행해줘.

**유일한 사양 문서: `C:\Bflow-BGonly\DEVLOG\2026-09-05-피드백-55-57-수정계획.md`**

이 문서는 자기완결적이며 독립 리뷰 3패스(사실 검증·구현 시뮬레이션·테스트 시뮬레이션)와 커밋 단위 실제 적용 시뮬레이션(typecheck·테스트·빌드·미리보기)을 거쳤다. 문서를 처음부터 끝까지 읽고, 명시된 작업 순서(작업 1 → 2 → 3 → 부록 A)대로 구현할 것.

규칙:
1. 계획서의 **절대 규칙**을 따른다 — 문서에 명시되지 않은 리팩터링·포맷 변경·기능 추가 금지. 수정 범위는 명시된 파일·블록으로 한정. 56번(완료 공지)과 캘린더 테스트 기존 실패 1건은 범위 밖이니 건드리지 않는다.
2. 착수 전 `git fetch origin` 후 `origin/main` 최신에서 `fix/피드백-55-57` 브랜치를 만든다. 계획서·실행프롬프트 파일은 이미 main에 있으니 작업 커밋에 섞지 않는다. 라인 번호가 어긋나면 병기된 **앵커(고유 코드 조각)** 검색으로 위치를 재확정한다. 앵커도 못 찾으면 임의 진행하지 말고 해당 작업을 건너뛰고 마지막에 보고한다.
3. 각 작업의 **before 블록과 실제 코드가 다르면** 그 작업을 중단하고 차이를 보고한다(스냅샷 `a1d9e5e2` 이후 코드가 변경된 신호다). 작업 트리 파일은 CRLF다 — 편집 도구를 쓰면 되고, 스크립트로 치환할 땐 `\r\n`을 정규화해 매칭한다.
4. 커밋은 작업당 1개(작업 1·2·3) + 버전 커밋(부록 A-1) + 구현 기록 커밋(부록 A-2, PR 생성 후) = 5개. 커밋 메시지는 계획서의 각 작업 '완료 기준'·부록 A-1·A-2에 적힌 한글 문장 그대로. 계획서가 지정한 테스트 갱신·추가를 코드 수정과 같은 커밋에 포함한다. **모든 커밋 경계에서 `npm run typecheck`와 `npm run test:character`가 통과**해야 한다(작업 1: 155/0, 작업 2: 157/0, 작업 3: 159/0). 작업 1은 건너뛰기 대상이 아니다(작업 2·3의 테스트 append가 의존) — 작업 1의 before 블록이 다르면 전체를 중단하고 보고한다.
5. 버전은 착수 시점 `origin/main`의 `package.json`을 재확인해 마이너 +1(예상 1.111.0). `package.json` + `package-lock.json`(2곳) + `DEVLOG/update-notes.json[0]` 3자 일치. update-notes 문구는 부록 A-1의 완성본을 그대로 쓴다(비개발자 톤).
6. 각 작업 완료 시 그 작업의 '검증 방법'(커맨드 + 미리보기 시나리오)을, 전체 완료 시 부록 B의 전체 검증을 실행하고 결과 수치를 기록한다. 미리보기 실행법은 계획서 §4-7(`$env:PORT='5190'; npm run dev:renderer` → `http://localhost:5190/?preview=1` → 스플래시 클릭 → 로그인 `배한솔`/`1234`).
7. PR을 1개 생성한다(제목 예: `[v1.111.0] 캐릭터 중복 추가 방지와 작업 경로 정리`). 본문 `📋 업데이트 요약`은 update-notes 문구(비개발자 톤), `🔧 상세 기술 설명`은 개발자 톤, 테스트 가이드에 부록 A-3·A-4 포함. **머지는 하지 않는다.**
8. PR 생성 후 `DEVLOG/<실행일>-피드백-55-57-구현.md`를 `DEVLOG/2026-08-12-피드백-51-54-구현.md` 구조대로 작성해 5번째 커밋으로 push 한다. 계획서와 실제 코드가 달랐던 지점이 있으면 반드시 기록.

완료 보고에는 작업별 커밋 해시, 테스트 결과 수치(character/ui/entity/presence/playground + build:vite 또는 vite build 결과), 미리보기에서 관찰한 시나리오, 건너뛰거나 계획과 달라진 부분을 포함할 것.
````

## 전달 후 체인

1. 구현 완료·PR 생성 확인
2. `codex-review-loop` 스킬로 PR 리뷰 루프(명시적 완료 신호까지 재트리거)
3. 한솔 승인 후 머지 → `bflow-release-deploy` 스킬로 릴리스 노트·빌드·G드라이브 배포(manifest 마지막 갱신)
4. 배포 타임스탬프를 메모리에 기록 — 다음 피드백 라운드의 상태 대조 기준
