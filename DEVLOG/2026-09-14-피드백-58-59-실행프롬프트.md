# 피드백 58·59 실행 프롬프트 (구현 에이전트용)

> 계획서 `DEVLOG/2026-09-14-피드백-58-59-수정계획.md`와 짝. **이 계획서 PR이 main에 머지된 뒤** 아래 코드 블록을 그대로 복사해 구현 에이전트(Opus·Codex 등)에게 전달한다.

````text
C:\Bflow-BGonly 저장소에서 피드백 58·59 구현 작업을 수행해줘.

**유일한 사양 문서: `C:\Bflow-BGonly\DEVLOG\2026-09-14-피드백-58-59-수정계획.md`**

이 문서는 자기완결적이며 독립 리뷰(사실 검증·별도 워크트리 실제 구현 시뮬레이션·적대적 코드 리뷰·실행 시점 모호성 검토)와 커밋 단위 실제 적용 시뮬레이션(typecheck·전 스위트·vite build·미리보기·라이브 DB 예행연습)을 거쳤다. 문서를 처음부터 끝까지 읽고, 명시된 작업 순서(작업 1 → 2 → 3 → 4 → 부록 A)대로 구현할 것.

규칙:
1. 계획서의 **절대 규칙**을 따른다 — 문서에 명시되지 않은 리팩터링·포맷 변경·기능 추가 금지. 수정 범위는 명시된 파일·블록으로 한정. 라이브 DB 에 마이그레이션을 적용하지 않는다(`apply_migration`·SQL Editor 실행 모두 금지 — 한솔 게이트, 부록 A-2).
2. 착수 전 `git fetch origin` 후 `origin/main` 최신에서 `fix/피드백-58-59` 브랜치를 만든다(다른 세션과 `dist`·작업 트리를 공유하지 않도록 git worktree 권장 — `node_modules` 준비 방법은 계획서 헤더 "실행 위치" 참조). 계획서·실행프롬프트 파일은 이미 main에 있으니 작업 커밋에 섞지 않는다. 라인 번호가 어긋나면 병기된 **앵커(고유 코드 조각)** 검색으로 위치를 재확정한다.
3. **before 블록이 실파일과 다르거나 앵커를 못 찾으면**(스냅샷 `0eb57259` 이후 코드가 변경된 신호): 작업 1·2·3 중 하나라도 해당되면 그 시점에서 **전체 작업을 중단**하고 차이를 보고한다. 작업 4에서만 해당되면 **작업 4를 건너뛰고** 작업 1~3 + 부록 A를 마친 뒤 보고한다(이때 `test:ui` 기대치 17, `test:entity` 274). 어떤 경우에도 임의로 앵커를 추정해 진행하지 않는다. 작업 트리 파일은 CRLF다 — 편집 도구를 쓰면 되고, 스크립트로 치환할 땐 `\r\n`을 정규화해 매칭한다. `git add` 때 "LF will be replaced by CRLF" 경고는 정상이다.
4. **새 파일(SQL 2·TS 3·TSX 1·테스트 5)은 계획서 코드 블록을 그대로 쓴다.** 셸 heredoc으로 쓰지 말 것(`\\`가 접혀 테스트 정규식이 조용히 바뀐다) — 편집 도구(Write)로 쓰고, 계획서 §4-7의 추출·대조 명령으로 문서 블록과 일치(줄 끝 제외)를 확인한다. 작업 4 (e)는 기존 새 파일 끝에 블록을 append 한다.
5. 커밋은 작업당 1개(작업 1·2·3·4) + 버전 커밋(부록 A-1) + 구현 기록 커밋(부록 A-3, PR 생성 후) = 6개. 커밋 **제목 줄**은 계획서의 각 작업 '완료 기준'·부록 A-1·A-3에 적힌 한글 문장 그대로(본문·`Co-Authored-By` 트레일러는 실행 환경 규칙대로 붙여도 된다). 테스트 신설·`package.json` 등록은 코드 수정과 같은 커밋에 포함한다. **모든 커밋 경계에서 `npm run typecheck`와 그 작업 '검증 방법'의 스위트가 통과**해야 한다(작업 1: entity 255/0, 작업 2: entity 263/0 + 전 스위트, 작업 3: entity 274/0, 작업 4: entity 275/0 + ui 23/0).
6. 버전은 착수 시점 `origin/main`의 `package.json`을 재확인해 마이너 +1(예상 1.118.0, `npm version minor --no-git-tag-version`). `package.json` + `package-lock.json`(2곳) + `DEVLOG/update-notes.json[0]` 3자 일치. update-notes 문구는 부록 A-1의 완성본을 그대로 쓴다(비개발자 톤). `update-notes.json` 은 추가 줄만 생겨야 한다(기존 항목 재포맷 금지). 버전 커밋 뒤에도 `npm run typecheck` 통과를 확인한다.
7. 각 작업 완료 시 그 작업의 '검증 방법'(커맨드 + 미리보기 시나리오)을, 전체 완료 시 부록 B의 전체 검증(`npm run build:vite` 포함)을 실행하고 결과 수치를 기록한다. 미리보기 실행법은 계획서 §2 마지막 항목(`$env:PORT='5190'; npm run dev:renderer` → `http://localhost:5190/?preview=1` → 스플래시 클릭 → 로그인 `배한솔`/`1234`, 씬 카드는 더블클릭).
8. PR을 1개 생성한다(제목 예: `[v1.118.0] 팀 할 일 목록과 캐릭터 현황판 바로 열기`). 본문 `📋 업데이트 요약`은 update-notes 문구(비개발자 톤), `🔧 상세 기술 설명`은 개발자 톤(세션 토큰 경계·내용 없는 실시간 신호·커밋 구성), 테스트 가이드에 부록 A-4·A-5 포함. **머지는 하지 않는다.** PR 본문에 "라이브 DB 마이그레이션 게이트(부록 A-2)를 머지 전에 통과해야 한다"를 명시한다.
9. PR 생성 후 `DEVLOG/<실행일>-피드백-58-59-구현.md`를 `DEVLOG/2026-09-05-피드백-55-57-구현.md` 구조대로 작성해 6번째 커밋으로 push 한다. 계획서와 실제 코드가 달랐던 지점, **라이브 적용 대기 중인 SQL 경로**(`DEVLOG/migrations/2026-09-14-comment-thread-todos.sql`, `DEVLOG/verification/2026-09-14-comment-thread-todos-smoke.sql`)를 반드시 기록.

완료 보고에는 작업별 커밋 해시, 테스트 결과 수치(entity/ui/character/notifications/gantt/calendar/presence/playground/auto-update + build:vite 결과), 미리보기에서 관찰한 시나리오, 건너뛰거나 계획과 달라진 부분, 라이브 적용 대기 SQL 경로를 포함할 것.
````

## 전달 후 체인

1. 구현 완료·PR 생성 확인
2. `codex-review-loop` 스킬로 PR 리뷰 루프(명시적 완료 신호까지 재트리거)
3. **부록 A-2 마이그레이션 게이트**(한솔 또는 한솔 지시를 받은 에이전트): 라이브 DB 에 마이그레이션 적용 → 스모크 `passed: true` → 확인 쿼리 5개 → 적용 기록 문서
4. 한솔 승인 후 머지 → `bflow-release-deploy` 스킬로 릴리스 노트·빌드·G드라이브 배포(manifest 마지막 갱신)
5. 배포 타임스탬프를 메모리에 기록 — 다음 피드백 라운드의 상태 대조 기준
