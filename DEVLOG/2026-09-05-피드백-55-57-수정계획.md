# B flow 피드백 55·57 수정 계획서 (에이전트 실행용)

> **이 문서는 자기완결적이다.** 원본 피드백 문서(구글 독스)·이전 대화 없이 이 문서만으로 구현을 완료할 수 있도록 작성되었다.
> **작성 기준 스냅샷:** 2026-09-05, `main` HEAD `a1d9e5e2` (v1.110.1, 작업 트리 클린 — 미커밋 변경 없음). 본 문서의 모든 라인 번호·코드 인용은 이 스냅샷 기준이며, 인용 코드는 전부 실제 파일과 대조 검증되었다 — 아래 "시뮬레이션 실측" 참조.
> **테스트 기준선 (이 스냅샷, 수정 전, 2026-09-05 실측):** `npm run typecheck` 통과 · `test:character` 148 · `test:ui` 17 · `test:presence` 38 · `test:entity` 249 · `test:playground` 461 · `test:auto-update` 29 · `test:notifications` 144 — 전부 fail 0. `test:calendar` 936 중 **935 통과 / 1 실패(`ScheduleView counts every event overlapping the displayed month`)** — 이 1건은 이번 범위와 무관한 **기존 실패**이며 이번 작업이 캘린더 파일을 건드리지 않으므로 그대로 둔다(고치지 말 것). 그 외 스위트에서 실패가 나면 그 작업의 수정이 원인이다.
> **시뮬레이션 실측:** 본 문서의 after 코드 전부를 위 스냅샷에 **커밋 단위로 순서대로** 실제 적용해 각 경계에서 `typecheck` 통과 · `test:character` 155/0(작업 1) → 157/0(작업 2) → 159/0(작업 3) · `test:ui` 17/0 · `vite build` 통과 · 브라우저 미리보기(`?preview=1`)에서 각 작업의 시나리오 동작 확인을 마친 뒤 되돌렸다. 즉 이 문서의 before 블록은 실파일과 정확히 1회 일치하고, after 코드는 그대로 컴파일·테스트·동작한다.
> **리뷰 이력:** 본 문서는 독립 리뷰 3패스를 거쳤다 — ① 사실 검증(모든 before 블록·라인·앵커·기술 전제를 실파일과 대조: 블로커 0, 사소 4) ② 구현 시뮬레이션(실행 에이전트 입장에서 처음부터 따라가며 점검: 중요 1 — 보관 목록을 보는 중 '기존 카드 열기'로 활성 카드를 열면 목록 모드가 안 돌아오는 결함 → 작업 1 (c-2) 신설; 사소 7) ③ 테스트 시뮬레이션(신규·갱신 단언 전부를 after 코드에 실행: 실패 0, 사소 2). 발견 사항은 전부 본문에 반영했고, 반영으로 바뀐 부분(제안 양방향·(c-2)·테스트 파일 3분할·CRLF 안전 정규식)은 커밋 단위 재시뮬레이션과 **독립 재검증 2패스**(반영분 사실 검증: 불일치 0 / 반영분 구현 시뮬레이션: 문서 표현·순서 사소 5 → 반영)로 확인했다. 상세는 문서 말미 "리뷰 반영 기록".
> **라인 번호는 ±수십 줄 어긋날 수 있다.** 모든 위치는 라인 번호와 함께 **함수명/고유 코드 조각(앵커)** 을 병기했으므로, 라인이 어긋나면 반드시 코드 조각 검색으로 위치를 재확정할 것.
> **절대 규칙:** 이 문서에 명시되지 않은 리팩터링·포맷 변경·기능 추가를 하지 말 것. 수정 범위는 명시된 파일·블록으로 한정한다.
> **선행 문서와의 관계:** `DEVLOG/2026-08-12-피드백-51-54-수정계획.md`는 전부 실행 완료(v1.100.0~v1.102.0 배포됨, `DEVLOG/2026-08-12-피드백-51-54-구현.md`) — 겹침 없음. 이 문서가 유일한 미실행 계획서다.

---

# 공통 섹션

## 0. 한눈에 보는 상태 요약 (55·57)

피드백 작성자는 리깅 담당 팀원(캐릭터 현황판·리깅 워크플로 중심 사용). 구글 독스 "비플로우 관련 요청사항" 문서의 **"업데이트 요청 사항" 섹션** 1·2·3번 항목 = 문서 전체 누적 번호 **55·56·57**. **56번은 사용자(한솔) 지시로 이번 범위에서 제외**한다(앱 코드는 v1.96.0에서 이미 수정됐고 잔여 증상은 슬랙 워크플로 템플릿 쪽 — `DEVLOG/2026-08-06-피드백-42-50-구현.md` §5-1 참조. 건드리지 말 것). 누적 58~62번 자리는 자리표시자("ㅇㅁㄴ" 등)라 범위 외.

| # | 피드백 요약 | 근본 원인 (코드로 확정) | 이번 작업 |
|---|---|---|---|
| **55** | 캐릭터 생성 시 같은 이름의 카드가 있으면 제안하거나 생성을 막기(중복 생성 방지) | `AddCharacterModal.tsx`가 같은 이름을 **경고만 하고 추가를 허용**한다(주석 "GAP-D — 경고만 하고 추가는 막지 않는다"). 판정도 앞뒤 공백·대소문자만 무시해 '찜질방 사장'/'찜질방사장'은 다른 이름으로 본다. 기존 카드로 가는 동선도 없다 | ★ **작업 1** |
| 56 | [3트] 완료 공지 빈 불릿·아이콘 | (범위 외 — 사용자 제외) | **하지 않음** |
| **57-1** | 폴더 '만들기'를 누르면 바로 생성됨 — 확인 창이 필요 | `characterFolderService.createAndLinkCharacterFolder`가 기준 경로 조회 직후 확인 없이 `pathCreateFolder`를 호출한다. 또한 **새 창(팝업) 캐릭터 현황판에는 확인 창 호스트가 없어** 앱 확인 창이 브라우저 기본 `window.confirm`으로 떨어진다(`WidgetPopup.tsx`에 `ConfirmDialogHost` 부재) | ★ **작업 2** |
| **57-2** | 한 번 지정한 경로를 '삭제'만 하는 기능 | `CostumeDetail.tsx`의 `PathActionRow`에 경로 해제 동선이 없다(선택/열기만). store는 `updateCharacterFolder(id, null)`·`updateCostumeField(id, { workFilePath: null })`로 이미 null 저장을 지원한다 | ★ **작업 3** |
| **57-3** | '선택' 텍스트를 '경로 지정'/'경로 수정'으로 | `PathActionRow`의 버튼 문구가 상태와 무관하게 '선택' 고정 | ★ **작업 3** |

**한솔 결정(2026-09-05, 질문 도구로 확정):**
- 55 정책: **활성 캐릭터와 같은 이름이면 '추가'를 막고 '기존 카드 열기' 버튼 제공.** 보관된 캐릭터와만 겹치면 막지 않고 경고 + '복원해서 열기' 제공.
- 55 판정 범위: **공백·대소문자 무시 매칭** + **입력 중 비슷한 이름 제안(부분 일치 최대 3개, 누르면 그 카드 열기)**. 이름 변경(상세 모달 인라인 rename)·파일명 자동 지정 경로는 **손대지 않는다**.
- 57-3: 상태별 **'경로 지정'(경로 없음) / '경로 수정'(경로 있음)**.
- 57-2: '지우기'는 **확인 창을 거친 뒤** 해제. 57-1 '만들기' 확인 창은 요청대로 무조건 넣는다.

## 1. 피드백 원문 요약 (첨부 이미지 내용 포함)

**55 — 원문 요지:** "캐릭터 생성시, 이미 만들어진 카드가 있으면 **제안하거나 생성 추가로 못하게 하기**?! 중복 생성을 방지하는 기능이 있으면 좋겠군요." 첨부 이미지 없음. 현재 앱은 같은 이름을 입력하면 이름 칸 아래에 노란 문구 "같은 이름의 캐릭터가 이미 있어요 — 그래도 추가할 수 있어요."가 뜨지만 '추가'가 그대로 눌린다.

**57 — 원문 요지 (3개 하위 항목):**
1. "폴더 '만들기' 할 때, 만들기를 누르면 **폴더가 자동으로 생성된다, 라는 확인 창**이 뜨도록 하기. 실수로 만들면 다시 들어가서 삭제해줘야 하는 불편함이 있네요."
2. "한번 지정하면 선택 / 열기 뿐인데, 경로를 그냥 **'삭제'만 하는 기능**이 있어도 좋을듯합니다."
3. "'선택' 텍스트보다 **'경로지정' 또는 '경로 수정'** 이 더 직관적이지 않을까 싶기는 합니다!"

**57 첨부 이미지(1장, 라이트 모드 스크린샷) 내용:** 캐릭터 상세 모달의 복장 상세 영역. 위쪽에 "기준 키(px) [예: 600] [이미지로 조정]"과 "이 복장 키(px) [대표 미설정]" 입력이 보이고, 그 아래 두 칸으로 나뉜 경로 행이 있다 — 왼쪽 칸 "작업 폴더 / **희선 캐릭터**" 오른쪽에 **[선택] [열기]** 버튼 2개, 오른쪽 칸 "작업 파일 / 미등록"(잘려 보임). 그 아래 "이 복장이 출연하는 에피소드 — 먼저 위 '출연 에피소드'에서 이 캐릭터를 에피소드에 연결하면…" 안내 문구. 즉 이미지가 가리키는 UI는 `CostumeDetail.tsx`의 `PathActionRow` 두 줄이다.

## 2. 아키텍처 전제 (이번 작업의 공통 배경)

- **B flow**: Electron + React 18 + TypeScript + Tailwind + Zustand 앱. 저장소 `C:\Bflow-BGonly`. 데이터는 Supabase(PostgreSQL). **렌더러는 Supabase를 직접 부르지 않는다** — `window.electronAPI.*` (preload IPC) → 메인 프로세스 → Supabase. 이번 작업은 **DB 스키마·IPC 채널·메인 프로세스 변경이 없다** (모두 렌더러 + 순수 유틸).
- **캐릭터 현황판 데이터 흐름**: `src/stores/useCharacterBoardStore.ts`(zustand)가 `characters`(활성+보관 전부, `status: 'active' | 'archived'`)·`costumes`·`byCharacter`(Map)를 들고 있다. 변경은 **낙관적 업데이트** — `applyCharacterUpdate`/`applyCostumeUpdate`가 즉시 반영 → 서비스 호출 → 실패 시 필드 단위 롤백 + 토스트. `updateCharacterFolder(id, workFolderPath: string | null)`와 `updateCostumeField(id, { workFilePath: string | null })`은 **이미 null을 받는다**(타입·서비스·메인 핸들러 모두 `string | null`). `restoreCharacter(id)`는 `Promise<void>`이며 성공 시 토스트 '캐릭터를 복원했어요', 실패 시 토스트만 띄우고 예외를 삼킨다.
- **확인 창**: `src/components/common/ConfirmDialog.tsx` — `ConfirmDialog.show({ message, confirmLabel?, cancelLabel?, tone? }): Promise<boolean>`. 실제 렌더는 `<ConfirmDialogHost />`가 마운트된 곳에서만 되고(현재 `App.tsx`에만), 호스트가 없으면 `window.confirm`으로 폴백한다. 창은 `z-[9999]`라 캐릭터 상세 모달(`z-50`)·추가 모달 위에 뜬다. Escape는 capture 단계에서 `stopImmediatePropagation`으로 잡아 뒤의 모달을 같이 닫지 않는다. **캐릭터 현황판은 '새 창으로' 버튼으로 별도 팝업 창(`src/views/WidgetPopup.tsx`, 별도 렌더러 프로세스)에서도 열린다** — 팝업에는 `Toaster`만 있고 `ConfirmDialogHost`가 없다(작업 2에서 추가).
- **캐릭터 추가 진입점 2곳**: ① 현황판 헤더 '캐릭터 추가'(`CharacterBoardView.tsx`, 생성 후 `setPendingOpenId`로 상세 자동 오픈. **이 버튼은 '보관 N' 목록을 보는 중에도 눌린다**) ② 상세 모달 좌측 목록 하단 '캐릭터 추가'(`CharacterDetailModal.tsx`, 생성 후 `setSelectedId`로 선택 전환. 보관 모드에서는 숨김). 둘 다 `AddCharacterModal`을 렌더한다. 상세 모달은 선택 id가 현재 목록 필터 밖이면 자동으로 '전체 보기'로 전환해 유지한다(딥링크와 같은 경로) — 기존 카드로 이동할 때 그대로 재사용한다. 그리드 쪽 `pendingOpenId` 소비 효과(`CharacterGrid`, 228~246행)는 대상이 보관본이면 `setShowArchived(true)`만 하고 **활성이면 `false`로 되돌리지 않는다** — 작업 1 (c-2)에서 고친다.
- **임시 이름**: 이름 없이 추가하면 store가 `nextTempCharacterName`으로 '새 캐릭터', '새 캐릭터 2'…를 부여한다(`src/utils/characterName.ts`, import 없는 node-safe 순수 유틸). 이번 55 헬퍼도 이 파일에 둔다.
- **폴더 만들기 흐름**: `CharacterDetailModal.handleCreateFolder`(creatingFolder=true) → `characterFolderService.createAndLinkCharacterFolder(character, updateCharacterFolder)` → 기준 경로(`readCharacterWorkFolderRoot`, 설정 → 관리자 탭에서 지정한 팀 공용 경로) 조회 → `window.electronAPI.pathCreateFolder(root, character.name)`(메인이 Windows 금지문자 정제·생성, 이미 있으면 `existed: true`) → `updateCharacterFolder(id, path)` → 토스트. dev 미리보기 mock은 기준 경로 `G:\공유 드라이브\사우스 코리안 파크\[]사코팍 캐릭터 세팅`과 `pathCreateFolder` 성공 응답을 흉내 낸다.
- **테스트**: Node 22 내장 `node --test` (vitest 아님). `tests/*.test.ts`는 `package.json`의 `test:*` 스크립트에 **명시 나열**해야 실행된다. 대부분 소스 문자열(정규식) 앵커 테스트 — 이번 수정으로 갱신·추가하는 앵커는 아래 각 작업에서 지정한 대로 **같은 커밋에서** 반영한다. 순수 유틸 테스트는 `../src/utils/xxx.ts` 상대경로 import(확장자 포함), `@/` alias 금지. `typecheck`는 `tests/playground*.test.ts`만 포함하므로 캐릭터 테스트 파일은 typecheck 대상이 아니다(미사용 const 가 있어도 무방).
- **줄바꿈**: 저장소는 `core.autocrlf=true` + `* text=auto`. 작업 트리 파일은 **CRLF**다. 편집 도구(Edit)는 자동 처리하지만, 스크립트로 문자열 치환을 하면 `\r\n`을 LF로 정규화해 매칭해야 한다. **소스 앵커 정규식에서 줄 끝을 잡을 땐 `\n`만 쓰지 말고 `\s*`(또는 `\r?\n`)를 쓴다** — `\n`만 쓰면 CRLF 체크아웃에서 절대 매치되지 않는 죽은 가드가 된다.

## 3. 코드베이스 지도 (이번 작업 관련만)

| 파일 | 역할 | 작업 |
|---|---|---|
| `src/utils/characterName.ts` | 임시 이름·파일명 자동 지정 순수 유틸 (import 없음, node-safe) — **55 헬퍼 3개 + 57-1 확인 문구 빌더 추가** | 1 (2에서 사용) |
| `src/components/characters/AddCharacterModal.tsx` | 캐릭터 추가 모달 — 중복 차단·기존 카드 열기·복원·비슷한 이름 제안 | 1 |
| `src/views/CharacterBoardView.tsx` | 현황판 뷰(786줄) — `AddCharacterModal` 배선(776~781행) + `CharacterGrid`의 `pendingOpenId` 소비 효과(230행) | 1 |
| `src/components/characters/CharacterDetailModal.tsx` | 상세 모달(좌 목록 + 우 상세, 939줄). `AddCharacterModal` 배선(931~936행), 경로 핸들러(326~361행), `CostumeDetail` 배선(626~633행) | 1, 3 |
| `src/components/characters/CostumeDetail.tsx` | 복장 상세 — `PathActionRow`(28~83행)와 두 경로 행(297~310행) | 3 |
| `src/services/characterFolderService.ts` | 폴더 만들기 서비스 — 확인 창 삽입 | 2 |
| `src/views/WidgetPopup.tsx` | 위젯 팝업 창 루트 — `ConfirmDialogHost` 추가(1140줄 파일, 48행·1126행) | 2 |
| `src/components/common/ConfirmDialog.tsx` | 확인 창(변경 없음, 참조만) | 2, 3 |
| `tests/characterName.test.ts` | 순수 유틸 테스트 — 신규 블록 4개 append | 1 |
| `tests/characterFeedbackBatch.test.ts` | B4 앵커 `disabled={saving}`를 '추가' 버튼 조건으로 정밀화(27행) | 1 |
| `tests/characterFeedback5.test.ts` | **신설** — 55·57 배선 앵커. 작업 1에서 파일 생성(55 테스트), 작업 2·3에서 각자 테스트 블록 append | 1, 2, 3 |
| `package.json` | `test:character`에 신규 테스트 등록 + 버전 | 1, 부록 A |
| `package-lock.json` | 버전 2곳 | 부록 A |
| `DEVLOG/update-notes.json` | 업데이트 내역 [0] 추가 | 부록 A |

## 4. 공통 규칙

1. **착수 전제·브랜치**: 이 계획서와 실행프롬프트는 별도 PR로 `main`에 먼저 들어간다(한솔이 프롬프트를 전달하는 시점 = 그 PR 머지 후). 착수 시 `git fetch origin` 후 **`origin/main` 최신에서** `fix/피드백-55-57` 분기. 스냅샷(`a1d9e5e2`)보다 main이 앞서 있으면 before 블록 대조로 충돌을 확인한다. 계획서·실행프롬프트 파일은 작업 커밋에 섞지 않는다(이미 main에 있음).
2. **커밋**: 작업당 1커밋(작업 1·2·3) + 버전 커밋(4) + 구현 기록 커밋(5, PR 생성 후 PR 번호를 넣어 같은 브랜치에 추가) = **5커밋, PR 1개**. 메시지는 한글, 기존 스타일(`피드백 53: 카드 복장 넘김을 휠에서 좌우 버튼으로 교체` 식). 각 작업의 테스트 갱신·추가는 **코드 수정과 같은 커밋**에 포함한다. **모든 커밋 경계에서 `npm run typecheck`와 `npm run test:character`가 통과해야 한다**(작업 1: 155/0, 작업 2: 157/0, 작업 3: 159/0 — 신설 테스트 파일을 작업별로 나눠 append 하므로 빨간 커밋이 생기지 않는다). **작업 1은 건너뛰기 대상이 아니다** — 작업 2·3의 테스트 append가 작업 1이 만든 파일·상수·게이트 등록에 의존한다. 작업 1의 before 블록이 실파일과 다르면 전체를 중단하고 보고한다.
3. **버전 정책**: 계획서에 버전을 고정하지 않는다 — 구현 착수 시점에 `origin/main`의 `package.json` 버전을 재확인해 **마이너 +1**(기능 추가). 2026-09-05 기준 1.110.1 → 예상 **1.111.0**. `package.json` + `package-lock.json`(2곳: 3행 최상위 `version`, 9행 `packages[""].version`) + `DEVLOG/update-notes.json[0].version` **3자 일치** 필수(`tests/playgroundMarketUiWiring.test.ts`가 강제).
4. **update-notes.json**: 배열 맨 앞에 `{ version, title, items: [{ category, summary, description }] }` 1개를 추가한다(기존 [0] 구조 그대로). `category`는 `feature | ux | bugfix | stability | change | docs` 중에서만. **비개발자 톤 필수** — 기술 용어·파일명·컴포넌트명·경로 금지, "상황 → 영향 → 결과" 1~3문장. 부록 A에 완성본이 있다.
5. 코드 스타일: 기존 파일 컨벤션(2스페이스, 싱글쿼트, 세미콜론, 주석 한글, "왜"를 남기는 톤). `npm run lint`는 게이트가 아니다 — `typecheck` + 관련 `test:*` + (마지막에) `build:vite`가 게이트.
6. 문구 톤: 사용자에게 보이는 문구(버튼·확인 창·토스트)는 기존 앱처럼 "~해요/~할까요?" 존댓말, 기술 용어 없이.
7. **미리보기 실행법**(각 작업의 검증에서 공통): `scripts/dev-renderer.mjs`는 `PORT` 환경변수가 없으면 5173에 뜬다. PowerShell: `$env:PORT='5190'; npm run dev:renderer` / bash: `PORT=5190 npm run dev:renderer` (또는 `.claude/launch.json`의 `vite-preview` 구성). 실제 포트는 터미널 출력으로 확인 → `http://localhost:<포트>/?preview=1` → 스플래시 아무 곳 클릭 → 로그인 `배한솔`/`1234` → 좌측 '캐릭터'.

## 5. 작업 순서

**작업 1 → 작업 2 → 작업 3 → 부록 A(버전 커밋 → 구현 기록 커밋).** 세 작업은 파일이 일부 겹치지만(`CharacterDetailModal.tsx`는 1·3, `characterName.ts`는 1에서 추가·2에서 사용, `tests/characterFeedback5.test.ts`는 1에서 생성·2·3에서 append) 서로 의존하지 않는다. 순서를 바꾸면 각 작업의 before 블록이 실파일과 달라질 수 있으니 바꾸지 말 것.

## 6. 전역 리스크 & 주의점

1. **소스 앵커 테스트 보존**: 아래 앵커들은 다른 테스트가 고정하고 있다 — after 코드가 그대로 유지하도록 설계돼 있으니 **임의로 문구·순서를 바꾸지 말 것**.
   - `CostumeDetail.tsx`: `onClick={onOpen}` 바로 다음 줄이 `title={path}` (B9), `title={path ? displayCharacterPathName(path) : undefined}>{displayCharacterPathName(path)}` (F26), `!path && onCreate`, `title="기준 경로에 캐릭터 이름으로 폴더를 만들어 연결"`.
   - `AddCharacterModal.tsx`: `onCreated?: (character: Character) => void`, `onCreated?.(created); onClose();`, `disabled={!name.trim() || saving}`는 **없어야** 함.
   - `CharacterBoardView.tsx`: `onCreated={(c) => { setTab('board'); setPendingOpenId(c.id); }}`, `if (showArchived && archivedCharacters.length === 0) setShowArchived(false);`(254행, 건드리지 않음).
   - `CharacterDetailModal.tsx`: `onCreated={(c) => setSelectedId(c.id)}`, `if (addOpen) return;`, `createAndLinkCharacterFolder`, `const [creatingFolder, setCreatingFolder]`, `onCreateFolder={handleCreateFolder}`.
   - `characterFolderService.ts`: `window.electronAPI.pathCreateFolder(root, character.name)`, `updateCharacterFolder(character.id, result.path)`.
   - `WidgetPopup.tsx`: `<Toaster` 블록은 그대로(다른 테스트가 `App.tsx`의 Toaster 개수를 세지만 팝업은 세지 않는다 — 그래도 건드리지 않는다).
2. **`useMemo`/`useCallback` deps**: 이 저장소는 `noUnusedLocals`를 켜지 않아 typecheck가 잔재·누락 deps를 잡지 않는다. after 코드의 deps 배열을 그대로 쓸 것.
3. **확인 창 대기 중 스피너**: 작업 2의 확인 창은 `handleCreateFolder`가 `creatingFolder=true`로 만든 뒤 서비스 안에서 뜬다. 확인 창이 떠 있는 동안 '만들기' 버튼에 스피너가 돌고 '경로 지정'이 비활성인 것은 **의도된 동작**(확인 창이 모달이라 어차피 누를 수 없음). 이를 고치려고 핸들러 구조를 바꾸지 말 것.
4. **프레즌스(피드백 54)와 경로 null**: 복장 `workFilePath`를 null로 저장하면 메인의 복장 파일 캐시가 realtime으로 갱신된다 — `costumeFileCacheAffected`가 `work_file_path` null도 실변경으로 처리함을 확인했다(`electron/main.ts` `const nextPath = (row.work_file_path as string | null) ?? null;`). 메인 코드 변경 불필요.
5. **미리보기 한계**: `?preview=1` mock에서 폴더 만들기·경로 선택은 고정 응답이다(폴더 선택 → `G:\공유 드라이브\JBBJ\A_014`, 파일 선택 → `...\A_014\main.psd`, 폴더 만들기 → 항상 성공). 실제 탐색기 대화상자·실제 폴더 생성은 Electron 실기 확인 항목. **mock 캐릭터 3명('한솔'·'삐쭈'·'혜원') 모두 작업 폴더 경로가, 모든 복장에 작업 파일 경로가 처음부터 등록돼 있다** — 경로가 없는 카드가 필요하면 '캐릭터 추가'(이름 비움 → '새 캐릭터')로 새로 만든다.

---

# 작업 1 — 항목 55: 같은 이름 캐릭터 추가 차단 + 기존 카드 열기 + 비슷한 이름 제안

## 문제/요청 요약

같은 이름을 입력해도 경고만 뜨고 추가되어 카드가 두 장 생긴다. 활성 캐릭터와 이름이 같으면(공백·대소문자 무시) 추가를 막고 그 카드를 바로 열 수 있게 하고, 보관된 캐릭터와 겹치면 복원해서 열 수 있게 하며, 입력 중 비슷한 이름의 카드를 제안한다. 기존 카드로 이동할 때 보관 목록을 보는 중이었다면 목록 모드를 되돌린다.

## 근거 (파일: `src/components/characters/AddCharacterModal.tsx`, 전체 79행)

15~20행 (앵커 `// 동시/중복 생성으로 같은 이름의 카드가 2장 생기는 사고 예방`):
```tsx
  // 동시/중복 생성으로 같은 이름의 카드가 2장 생기는 사고 예방 — 경고만 하고 추가는 막지 않는다 (GAP-D).
  const duplicateName = useMemo(() => {
    const normalized = name.trim().toLowerCase();
    if (!normalized) return null;
    return characters.find((c) => c.name.trim().toLowerCase() === normalized) ?? null;
  }, [characters, name]);
```
56~61행: 중복이면 `'같은 이름의 캐릭터가 이미 있어요 — 그래도 추가할 수 있어요.'`(보관본이면 `'보관된 캐릭터 중에 같은 이름이 있어요 — 복원해서 쓸 수도 있어요.'`) 문구만 표시. 74행 `<button type="button" onClick={submit} disabled={saving} ...>추가</button>` — 중복이어도 활성.

`characters`는 store의 전체 목록(활성+보관)이라 `find`가 보관본을 먼저 잡으면 활성 중복이 있어도 보관 문구가 나올 수 있다(정렬 순서 의존).

`src/views/CharacterBoardView.tsx` 228~246행(`CharacterGrid`의 `pendingOpenId` 소비 효과, 앵커 `if (pendingCharacter?.status === 'archived') setShowArchived(true);`): 대상이 보관본일 때만 `showArchived`를 켜고 활성일 때 끄지 않는다. 헤더 '캐릭터 추가'(512행)는 `showArchived`와 무관하게 보이므로, 보관 목록을 보는 중 활성 카드로 이동하면 상세 모달이 `archivedMode=true`로 열려 엉뚱한 보관 카드가 선택되거나 곧바로 닫힌다.

## 근본 원인

의도된 설계(GAP-D: 경고만)가 요청과 다르다. 판정 키가 앞뒤 공백·대소문자만 정규화해 띄어쓰기 차이를 놓치고, 기존 카드로 가는 동선이 없다. 그리드의 상세 자동 오픈 효과는 보관→활성 방향 전환을 고려하지 않았다.

## 수정 내용

### (a) `src/utils/characterName.ts` — 파일 끝(48행 `return withoutExt;` `}` 뒤)에 append (앵커: 파일 마지막 함수 `deriveCharacterNameFromFileName`)

**before (원문, 36~48행 — 이 뒤에 추가한다):**
```ts
export function deriveCharacterNameFromFileName(fileName: string | null | undefined): string | null {
  const raw = (fileName ?? '').trim();
  if (!raw) return null;
  // 마지막 확장자만 제거(예: '한솔 SWver12.png' → '한솔 SWver12', 'a.b.png' → 'a.b').
  const withoutExt = raw.replace(/\.[^.\s]+$/, '').trim();
  if (!withoutExt) return null;
  const lower = withoutExt.toLowerCase();
  if (GENERIC_BASENAMES.has(lower)) return null;
  if (GENERIC_PREFIXES.some((p) => lower.startsWith(p))) return null;
  // 순수 숫자 / 날짜·타임스탬프 형태(숫자·구분자만)는 이름으로 부적절.
  if (/^[\d\s._:\-]+$/.test(withoutExt)) return null;
  return withoutExt;
}
```
**after (위 블록은 그대로 두고, 그 아래에 다음을 추가):**
```ts

/**
 * 중복 판정용 이름 키(피드백 55) — 유니코드 정규화(NFC) + 모든 공백 제거 + 소문자.
 * '찜질방 사장'/'찜질방사장', 'Kim'/'kim' 을 같은 이름으로 본다.
 */
export function characterNameKey(name: string | null | undefined): string {
  return (name ?? '').normalize('NFC').replace(/\s+/g, '').toLowerCase();
}

type NamedCharacterLike = { name: string; status: 'active' | 'archived' };

/**
 * 입력 이름과 겹치는 기존 캐릭터를 찾는다(피드백 55).
 * 활성 캐릭터가 우선 — 활성·보관이 동시에 겹치면 활성만 돌려준다(보관은 활성이 없을 때만).
 * 빈 이름(공백만)은 임시 이름이 부여되므로 겹침 없음.
 */
export function findDuplicateCharacters<T extends NamedCharacterLike>(
  characters: ReadonlyArray<T>,
  name: string,
): { active: T | null; archived: T | null } {
  const key = characterNameKey(name);
  if (!key) return { active: null, archived: null };
  const matches = characters.filter((c) => characterNameKey(c.name) === key);
  const active = matches.find((c) => c.status !== 'archived') ?? null;
  const archived = active ? null : matches.find((c) => c.status === 'archived') ?? null;
  return { active, archived };
}

/**
 * 입력 중 '비슷한 이름' 제안(피드백 55) — 활성 캐릭터 중 이름 키와 입력 키가 한쪽이 다른 쪽을 포함하는 것
 * (정확히 같은 이름은 제외 — 그건 중복 차단이 맡는다). 이름 순 정렬 후 최대 limit 개.
 * 양방향인 이유: '한솔' 이 있는데 '한솔이' 를 치는(접미사·오타) 경우도 잡아야 중복 카드를 막는다.
 */
export function suggestSimilarCharacters<T extends NamedCharacterLike>(
  characters: ReadonlyArray<T>,
  name: string,
  limit = 3,
): T[] {
  const key = characterNameKey(name);
  if (!key) return [];
  return characters
    .filter((c) => c.status !== 'archived')
    .filter((c) => {
      const candidate = characterNameKey(c.name);
      return candidate !== key && (candidate.includes(key) || key.includes(candidate));
    })
    .sort((a, b) => a.name.localeCompare(b.name, 'ko'))
    .slice(0, limit);
}

/** 작업 폴더 '만들기' 확인 창 문구(피드백 57-1) — 어디에 어떤 이름으로 만들지 + 임시 이름 경고. */
export function buildCharacterFolderConfirmMessage(root: string, characterName: string): string {
  const lines = [
    `아래 위치에 '${characterName}' 폴더를 만들고 작업 폴더로 연결할까요?`,
    root,
    '이미 같은 이름의 폴더가 있으면 새로 만들지 않고 그 폴더를 연결만 해요.',
  ];
  if (isTempCharacterName(characterName)) {
    lines.push('지금 이름은 임시 이름이에요 — 폴더 이름으로 쓰기 전에 캐릭터 이름부터 정하는 게 좋아요.');
  }
  return lines.join('\n');
}
```
(`buildCharacterFolderConfirmMessage`는 작업 2에서 쓰지만, 같은 파일이므로 **작업 1 커밋에서 함께 추가**한다 — 작업 2 커밋이 이 파일을 다시 건드리지 않게. `isTempCharacterName`은 같은 파일 15행에 이미 있다.)

### (b) `src/components/characters/AddCharacterModal.tsx` — 파일 전체 교체 (1~79행)

**before:** 파일 전체(현재 79행짜리 원문 — 근거 절의 인용이 그 일부다. 교체 전 파일 첫 줄이 `import { useEffect, useMemo, useRef, useState } from 'react';`, 마지막 줄이 `}`인지 확인).

**after (파일 전체):**
```tsx
import { useEffect, useMemo, useRef, useState } from 'react';
import { useCharacterBoardStore } from '@/stores/useCharacterBoardStore';
import { useModalFocus } from '@/hooks/useModalFocus';
import { CHARACTER_LAYER_CLASS } from '@/constants/characterLayers';
import { findDuplicateCharacters, suggestSimilarCharacters } from '@/utils/characterName';
import type { Character } from '@/types';

export function AddCharacterModal({
  onClose,
  onCreated,
  onOpenExisting,
}: {
  onClose: () => void;
  onCreated?: (character: Character) => void;
  /** 피드백 55: 같은/비슷한 이름의 기존 카드로 이동 — 그리드는 상세 열기, 상세 모달은 선택 전환으로 처리한다. */
  onOpenExisting?: (character: Character) => void;
}) {
  const addCharacter = useCharacterBoardStore((s) => s.addCharacter);
  const restoreCharacter = useCharacterBoardStore((s) => s.restoreCharacter);
  const characters = useCharacterBoardStore((s) => s.characters);
  const [name, setName] = useState('');
  const [memo, setMemo] = useState('');
  const [saving, setSaving] = useState(false);
  const dialogRef = useRef<HTMLDivElement>(null);
  const modalFocus = useModalFocus(dialogRef, { autoFocus: false });
  // 피드백 55: 같은 이름(공백·대소문자 무시)의 활성 카드가 있으면 추가를 막고 그 카드로 보낸다.
  //   보관된 카드와만 겹치면 막지 않고 '복원해서 열기'를 함께 제안한다. (예전 GAP-D 는 경고만 하고 추가를 허용했다.)
  const duplicate = useMemo(() => findDuplicateCharacters(characters, name), [characters, name]);
  const blockingDuplicate = duplicate.active;
  const archivedDuplicate = duplicate.archived;
  // 피드백 55('제안'): 입력 중 이름을 포함하는 활성 카드를 최대 3개 보여줘 바로 열 수 있게 한다.
  const similar = useMemo(() => suggestSimilarCharacters(characters, name), [characters, name]);

  const submit = async () => {
    if (saving || blockingDuplicate) return;
    setSaving(true);
    // 이름을 비워도 추가 가능 — store 가 임시 이름을 부여하고, 이미지 추가 시 파일 이름으로 자동 채운다 (B4).
    const created = await addCharacter(name, memo.trim() || undefined);
    setSaving(false);
    if (created) { onCreated?.(created); onClose(); }
  };

  const openExisting = (character: Character) => {
    onOpenExisting?.(character);
    onClose();
  };

  // 보관된 같은 이름 카드를 되살려 그 카드로 이동. 복원 실패(토스트는 store 가 띄움)면 창을 닫지 않는다.
  const restoreAndOpen = async (character: Character) => {
    if (saving) return;
    setSaving(true);
    await restoreCharacter(character.id);
    setSaving(false);
    const latest = useCharacterBoardStore.getState().characters.find((c) => c.id === character.id);
    if (!latest || latest.status === 'archived') return;
    openExisting(latest);
  };

  useEffect(() => {
    const onKey = (event: KeyboardEvent) => {
      if (event.key !== 'Escape') return;
      event.preventDefault();
      onClose();
    };
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [onClose]);

  return (
    <div className={`fixed inset-0 ${CHARACTER_LAYER_CLASS.modal} flex items-center justify-center bg-overlay/50 p-6`} onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}>
      <div
        ref={dialogRef}
        role="dialog"
        aria-modal="true"
        aria-label="캐릭터 추가"
        tabIndex={-1}
        onKeyDown={modalFocus.onKeyDown}
        className="bg-bg-card border border-bg-border rounded-2xl w-full max-w-md p-5 flex flex-col gap-4 outline-none"
      >
        <h2 className="text-lg font-semibold text-text-primary">캐릭터 추가</h2>
        <div className="flex flex-col gap-1.5">
          <span className="text-xs text-text-secondary">이름</span>
          <input autoFocus value={name} onChange={(e) => setName(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter') submit(); }} placeholder="캐릭터 이름 (비워도 돼요)" className="bg-transparent border border-bg-border rounded-md px-3 py-2 text-text-primary outline-none focus:border-accent/50" />
          {blockingDuplicate ? (
            <div className="flex flex-wrap items-center gap-2 text-[11px]" style={{ color: 'rgb(var(--char-stage-feedback))' }}>
              <span>{`'${blockingDuplicate.name}' 캐릭터가 이미 있어요 — 같은 이름으로는 추가할 수 없어요.`}</span>
              {onOpenExisting && (
                <button
                  type="button"
                  onClick={() => openExisting(blockingDuplicate)}
                  className="rounded-md border border-accent/40 px-2 py-0.5 text-[11px] text-accent hover:bg-accent/10 cursor-pointer"
                >
                  기존 카드 열기
                </button>
              )}
            </div>
          ) : archivedDuplicate ? (
            <div className="flex flex-wrap items-center gap-2 text-[11px]" style={{ color: 'rgb(var(--char-stage-feedback))' }}>
              <span>보관된 캐릭터 중에 같은 이름이 있어요 — 새로 만들지 말고 복원해서 쓸 수도 있어요.</span>
              {onOpenExisting && (
                <button
                  type="button"
                  onClick={() => { void restoreAndOpen(archivedDuplicate); }}
                  disabled={saving}
                  className="rounded-md border border-accent/40 px-2 py-0.5 text-[11px] text-accent hover:bg-accent/10 disabled:opacity-50 cursor-pointer"
                >
                  복원해서 열기
                </button>
              )}
            </div>
          ) : (
            <span className="text-[11px] text-text-secondary">
              이름은 나중에 지어도 돼요. 대표 이미지를 넣으면 파일 이름으로 자동으로 채워져요.
            </span>
          )}
          {onOpenExisting && similar.length > 0 && (
            <div className="flex flex-wrap items-center gap-1.5 text-[11px] text-text-secondary">
              <span>비슷한 이름:</span>
              {similar.map((c) => (
                <button
                  key={c.id}
                  type="button"
                  onClick={() => openExisting(c)}
                  title="이 카드를 열어요"
                  className="rounded-md border border-bg-border px-2 py-0.5 text-[11px] text-text-primary hover:border-accent/50 hover:text-accent cursor-pointer"
                >
                  {c.name}
                </button>
              ))}
            </div>
          )}
        </div>
        <div className="flex flex-col gap-1.5">
          <span className="text-xs text-text-secondary">메모 (선택)</span>
          <textarea value={memo} onChange={(e) => setMemo(e.target.value)} rows={2} placeholder="메모" className="bg-transparent border border-bg-border rounded-md px-3 py-2 text-text-primary outline-none focus:border-accent/50 resize-none" />
        </div>
        <div className="flex justify-end gap-2">
          <button type="button" onClick={onClose} className="px-3 py-1.5 rounded-lg text-sm text-text-secondary hover:bg-bg-border/40 cursor-pointer">취소</button>
          <button
            type="button"
            onClick={submit}
            disabled={saving || !!blockingDuplicate}
            title={blockingDuplicate ? '같은 이름의 캐릭터가 이미 있어요' : undefined}
            className="px-3 py-1.5 rounded-lg text-sm bg-accent text-white disabled:opacity-50 cursor-pointer"
          >
            추가
          </button>
        </div>
      </div>
    </div>
  );
}
```

### (c) `src/views/CharacterBoardView.tsx` — `AddCharacterModal` 배선 (776~781행, 앵커: `onCreated={(c) => { setTab('board'); setPendingOpenId(c.id); }}`)

**before (원문):**
```tsx
      {addOpen && (
        <AddCharacterModal
          onClose={() => setAddOpen(false)}
          onCreated={(c) => { setTab('board'); setPendingOpenId(c.id); }}
        />
      )}
```
**after:**
```tsx
      {addOpen && (
        <AddCharacterModal
          onClose={() => setAddOpen(false)}
          onCreated={(c) => { setTab('board'); setPendingOpenId(c.id); }}
          onOpenExisting={(c) => { setTab('board'); setPendingOpenId(c.id); }}
        />
      )}
```
(`setPendingOpenId`는 `CharacterGrid`의 기존 효과가 소비해 상세를 연다. 보관본→활성 양방향 모드 전환은 아래 (c-2)가 담당.)

### (c-2) `src/views/CharacterBoardView.tsx` — 상세 자동 오픈 효과의 목록 모드 전환 (230행, 앵커: `if (pendingCharacter?.status === 'archived') setShowArchived(true);` — 파일 내 유일)

**before (원문, 228~230행):**
```tsx
    if (pendingOpenId) {
      const pendingCharacter = characters.find((c) => c.id === pendingOpenId);
      if (pendingCharacter?.status === 'archived') setShowArchived(true);
```
**after:**
```tsx
    if (pendingOpenId) {
      const pendingCharacter = characters.find((c) => c.id === pendingOpenId);
      // 보관 목록을 보는 중 활성 카드로 이동(피드백 55 '기존 카드 열기')하면 목록 모드도 되돌린다 — 안 그러면 보관 모드 상세가 엉뚱한 카드를 연다.
      if (pendingCharacter) setShowArchived(pendingCharacter.status === 'archived');
```
(230행이 주석 1줄 + 코드 1줄로 바뀌고 앞뒤 줄은 그대로. `setShowArchived`는 같은 값이면 리렌더를 유발하지 않는다. 같은 배치에서 `setDetailRequest`가 호출되므로 상세 모달이 올바른 `archivedMode`로 마운트된다. 254행의 `if (showArchived && archivedCharacters.length === 0) setShowArchived(false);`는 건드리지 않는다.)

### (d) `src/components/characters/CharacterDetailModal.tsx` — `AddCharacterModal` 배선 (931~936행, 앵커: `onCreated={(c) => setSelectedId(c.id)}`)

**before (원문):**
```tsx
      {addOpen && (
        <AddCharacterModal
          onClose={() => setAddOpen(false)}
          onCreated={(c) => setSelectedId(c.id)}
        />
      )}
```
**after:**
```tsx
      {addOpen && (
        <AddCharacterModal
          onClose={() => setAddOpen(false)}
          onCreated={(c) => setSelectedId(c.id)}
          onOpenExisting={(c) => setSelectedId(c.id)}
        />
      )}
```

### (e) 테스트 앵커 정밀화 — `tests/characterFeedbackBatch.test.ts:27` (앵커: `assert.match(addModal, /disabled=\{saving\}/);`)

옛 정규식은 after 코드에서도 '복원해서 열기' 버튼의 `disabled={saving}`에 우연히 매치돼 **깨지지는 않는다**. 하지만 B4의 의도("추가 버튼은 이름이 비어도 눌린다")를 '추가' 버튼의 실제 조건으로 고정하도록 정밀화한다 — 갱신 전에도 통과하는 것을 보고 혼란하지 말 것.

**before:**
```ts
  assert.match(addModal, /disabled=\{saving\}/);
```
**after:**
```ts
  assert.match(addModal, /disabled=\{saving \|\| !!blockingDuplicate\}/); // 피드백 55: 같은 이름이면 추가 차단
```

### (f) 순수 유틸 테스트 추가 — `tests/characterName.test.ts`

**(f-1) import 블록(3~8행) before:**
```ts
import {
  TEMP_CHARACTER_NAME_BASE,
  deriveCharacterNameFromFileName,
  isTempCharacterName,
  nextTempCharacterName,
} from '../src/utils/characterName.ts';
```
**after:**
```ts
import {
  TEMP_CHARACTER_NAME_BASE,
  buildCharacterFolderConfirmMessage,
  characterNameKey,
  deriveCharacterNameFromFileName,
  findDuplicateCharacters,
  isTempCharacterName,
  nextTempCharacterName,
  suggestSimilarCharacters,
} from '../src/utils/characterName.ts';
```
**(f-2) 파일 끝(44행 `});` 뒤)에 append:**
```ts

test('피드백 55: 중복 판정 키 — 공백·대소문자·유니코드 정규화 무시', () => {
  assert.equal(characterNameKey('찜질방 사장'), '찜질방사장');
  assert.equal(characterNameKey('  찜질방  사장 '), '찜질방사장');
  assert.equal(characterNameKey('Kim'), 'kim');
  assert.equal(characterNameKey('한솔'.normalize('NFD')), '한솔');
  assert.equal(characterNameKey(''), '');
  assert.equal(characterNameKey('   '), '');
  assert.equal(characterNameKey(null), '');
});

test('피드백 55: 같은 이름 찾기 — 활성 우선, 보관은 활성이 없을 때만', () => {
  const list = [
    { id: 'a', name: '찜질방 사장', status: 'active' as const },
    { id: 'b', name: '찜질방사장', status: 'archived' as const },
    { id: 'c', name: '미영', status: 'archived' as const },
  ];
  assert.deepEqual(findDuplicateCharacters(list, '찜질방사장'), { active: list[0], archived: null });
  assert.deepEqual(findDuplicateCharacters(list, ' 미영 '), { active: null, archived: list[2] });
  assert.deepEqual(findDuplicateCharacters(list, '도윤'), { active: null, archived: null });
  assert.deepEqual(findDuplicateCharacters(list, '   '), { active: null, archived: null });
});

test('피드백 55: 비슷한 이름 제안 — 활성만, 정확히 같은 이름 제외, 양방향 포함, 최대 3개', () => {
  const list = [
    { id: 'a', name: '찜질방 사장', status: 'active' as const },
    { id: 'b', name: '찜질방 알바', status: 'active' as const },
    { id: 'c', name: '찜질방 손님 1', status: 'active' as const },
    { id: 'd', name: '찜질방 손님 2', status: 'active' as const },
    { id: 'e', name: '찜질방 유령', status: 'archived' as const },
    { id: 'f', name: '미영', status: 'active' as const },
  ];
  const got = suggestSimilarCharacters(list, '찜질방');
  assert.equal(got.length, 3);
  assert.ok(got.every((c) => c.status === 'active' && c.name.includes('찜질방')));
  // 입력이 기존 이름보다 길어도(접미사·오타) 제안한다.
  assert.deepEqual(suggestSimilarCharacters(list, '찜질방 사장님').map((c) => c.id), ['a']);
  // 정확히 같은 이름은 제외 — 그건 중복 차단 쪽에서 처리한다.
  assert.deepEqual(suggestSimilarCharacters(list, '미영'), []);
  assert.deepEqual(suggestSimilarCharacters(list, ''), []);
  assert.equal(suggestSimilarCharacters(list, '찜질방', 1).length, 1);
});

test('피드백 57-1: 폴더 만들기 확인 문구 — 경로·이름 + 임시 이름 경고', () => {
  const root = 'G:\\공유 드라이브\\사우스 코리안 파크\\[]사코팍 캐릭터 세팅';
  const msg = buildCharacterFolderConfirmMessage(root, '희선');
  assert.ok(msg.includes("'희선' 폴더를 만들고 작업 폴더로 연결할까요?"));
  assert.ok(msg.includes(root));
  assert.ok(msg.includes('이미 같은 이름의 폴더가 있으면'));
  assert.ok(!msg.includes('임시 이름'));
  const temp = buildCharacterFolderConfirmMessage(root, '새 캐릭터 2');
  assert.ok(temp.includes('임시 이름'));
});
```

### (g) 배선 앵커 테스트 신설 — `tests/characterFeedback5.test.ts` (새 파일, **작업 1 분량 전체**. 57 테스트는 작업 2·3에서 append)

```ts
/**
 * 2026-09-05 캐릭터 현황판 피드백 55·57 배선 고정 테스트.
 * 소스-문자열 검사 — 리팩터 시 앵커가 깨지면 함께 갱신할 것.
 *   55: 같은 이름 캐릭터 추가 차단 + 기존 카드 열기 + 비슷한 이름 제안 (작업 1)
 *   57-1: 작업 폴더 '만들기' 확인 창 + 새 창 팝업 확인 창 호스트 (작업 2에서 append)
 *   57-2·57-3: 경로 '지우기' + '경로 지정'/'경로 수정' (작업 3에서 append)
 */
import { readFileSync } from 'node:fs';
import test from 'node:test';
import assert from 'node:assert/strict';

const addModal = readFileSync('src/components/characters/AddCharacterModal.tsx', 'utf8');
const boardView = readFileSync('src/views/CharacterBoardView.tsx', 'utf8');
const detailModal = readFileSync('src/components/characters/CharacterDetailModal.tsx', 'utf8');
const costumeDetail = readFileSync('src/components/characters/CostumeDetail.tsx', 'utf8');
const folderService = readFileSync('src/services/characterFolderService.ts', 'utf8');
const widgetPopup = readFileSync('src/views/WidgetPopup.tsx', 'utf8');
const packageJson = JSON.parse(readFileSync('package.json', 'utf8')) as { scripts: Record<string, string> };

test('피드백 55: 추가 모달 — 같은 이름이면 추가 차단 + 기존 카드 열기 + 보관본 복원 + 비슷한 이름 제안', () => {
  assert.match(addModal, /import \{ findDuplicateCharacters, suggestSimilarCharacters \} from '@\/utils\/characterName'/);
  assert.match(addModal, /onOpenExisting\?: \(character: Character\) => void/);
  assert.match(addModal, /const duplicate = useMemo\(\(\) => findDuplicateCharacters\(characters, name\), \[characters, name\]\)/);
  assert.match(addModal, /const similar = useMemo\(\(\) => suggestSimilarCharacters\(characters, name\), \[characters, name\]\)/);
  // 차단: submit 조기 반환 + 추가 버튼 비활성.
  assert.match(addModal, /if \(saving \|\| blockingDuplicate\) return;/);
  assert.match(addModal, /disabled=\{saving \|\| !!blockingDuplicate\}/);
  // 기존 카드 열기 / 복원해서 열기 / 비슷한 이름 버튼.
  assert.match(addModal, /onClick=\{\(\) => openExisting\(blockingDuplicate\)\}/);
  assert.match(addModal, /기존 카드 열기/);
  assert.match(addModal, /void restoreAndOpen\(archivedDuplicate\);/);
  assert.match(addModal, /복원해서 열기/);
  assert.match(addModal, /비슷한 이름:/);
  assert.match(addModal, /\{similar\.map\(\(c\) => \(/);
  // 복원 실패(여전히 archived)면 창을 닫지 않는다.
  assert.match(addModal, /if \(!latest \|\| latest\.status === 'archived'\) return;/);
  // 옛 경고-후-허용 문구는 사라져야 한다.
  assert.doesNotMatch(addModal, /그래도 추가할 수 있어요/);
  // 기존 흐름 보존: 빈 이름 허용 + 생성 후 onCreated.
  assert.match(addModal, /onCreated\?\.\(created\); onClose\(\);/);
  assert.doesNotMatch(addModal, /disabled=\{!name\.trim\(\) \|\| saving\}/);
});

test('피드백 55: 두 진입점(그리드·상세 모달)이 onOpenExisting 을 배선하고, 보관 목록 중 활성 카드로 이동하면 목록 모드가 돌아온다', () => {
  assert.match(boardView, /onOpenExisting=\{\(c\) => \{ setTab\('board'\); setPendingOpenId\(c\.id\); \}\}/);
  assert.match(detailModal, /onOpenExisting=\{\(c\) => setSelectedId\(c\.id\)\}/);
  // (c-2) 대상이 활성이면 showArchived 를 false 로 되돌려 보관 모드 상세가 엉뚱한 카드를 열지 않게 한다.
  assert.match(boardView, /if \(pendingCharacter\) setShowArchived\(pendingCharacter\.status === 'archived'\);/);
  assert.doesNotMatch(boardView, /if \(pendingCharacter\?\.status === 'archived'\) setShowArchived\(true\);/);
});

test('이 테스트 파일은 test:character 게이트에 등록돼 있다', () => {
  assert.match(packageJson.scripts['test:character'], /\.\/tests\/characterFeedback5\.test\.ts/);
});
```
(`costumeDetail`·`folderService`·`widgetPopup` 상수는 이 커밋에서는 쓰이지 않지만 작업 2·3의 append 블록이 쓴다 — 미사용이어도 테스트 실행에 문제없고 typecheck 대상도 아니다.)

### (h) `package.json` — `test:character`에 등록 (15행, 앵커: `./tests/costumePresenceWiring.test.ts",`)

**before (행 끝부분):**
```
./tests/characterStatusFilter.test.ts ./tests/costumePresenceWiring.test.ts",
```
**after:**
```
./tests/characterStatusFilter.test.ts ./tests/costumePresenceWiring.test.ts ./tests/characterFeedback5.test.ts",
```

## 엣지 케이스 & 주의사항

- **빈 이름**: `characterNameKey('')`가 빈 문자열 → 중복·제안 모두 없음 → 기존처럼 임시 이름으로 추가된다.
- **활성·보관 동시 겹침**: 활성이 우선 → 차단 문구. 보관본만 겹침 → 차단하지 않고 '복원해서 열기'(누르면 `restoreCharacter` → 복원 성공 시 카드로 이동, 실패 시 store 토스트만 뜨고 창은 남는다).
- **Enter 키**: 차단 상태에서 Enter는 `submit()` 조기 반환으로 아무 일도 하지 않는다(의도).
- **임시 이름 입력**: '새 캐릭터'를 직접 치면 이미 있는 '새 캐릭터'와 겹쳐 차단된다 — 정상.
- **비슷한 이름 제안**은 `onOpenExisting`이 있을 때만 렌더한다(두 진입점 모두 전달하므로 항상 보임). 포함 관계는 양방향('한솔' 있을 때 '한솔이' 입력도 제안). 정확히 같은 이름은 제안에서 제외(차단 문구가 담당). 한 글자 이름의 캐릭터가 있으면 그 글자가 든 입력마다 제안되지만 최대 3개라 허용.
- **보관 목록 중 이동**: 헤더 '캐릭터 추가'는 보관 목록을 보는 중에도 눌린다. 그 상태에서 활성 카드로 이동하면 (c-2)가 목록을 활성으로 되돌리고 상세를 연다. 보관본으로 이동(복원 실패로 여전히 보관본인 경우는 창이 닫히지 않으므로 발생하지 않음)하면 보관 목록으로 전환.
- **실시간**: 다른 사용자가 같은 이름을 방금 만들면 store `characters`가 갱신되며 `useMemo`가 재계산돼 즉시 차단으로 바뀐다. 서버 측 유일 제약은 없다(부록 C).
- **DB·IPC 변경 없음.**

## 검증 방법

```bash
npm run typecheck
npm run test:character
```
- 이 커밋에서 `test:character` = **155 통과 / fail 0**(기존 148 + characterName 4 + characterFeedback5 3). `typecheck` 통과.
- 미리보기(§4-7): '캐릭터 추가'.
  - '한솔' 입력 → 노란 문구 "'한솔' 캐릭터가 이미 있어요 — 같은 이름으로는 추가할 수 없어요." + [기존 카드 열기], '추가' 비활성.
  - ' 한 솔 ' 입력 → 동일하게 차단(공백 무시).
  - '삐' 입력 → "비슷한 이름: [삐쭈]" 표시, '추가' 활성. [삐쭈] 클릭 → 추가 창 닫히고 삐쭈 상세 열림.
  - '삐쭈님' 입력 → 마찬가지로 "비슷한 이름: [삐쭈]"(양방향).
  - '도윤' 입력 → 안내 문구만, '추가' 활성.
  - [기존 카드 열기] 클릭 → 추가 창 닫히고 '한솔' 상세 모달이 열린다.
  - 상세 모달 좌측 '캐릭터 추가' → '혜원' → [기존 카드 열기] → 우측 상세가 혜원으로 바뀐다.
  - '혜원' 상세에서 '보관' → 확인 → 좌측 '캐릭터 추가' → '혜원' → "보관된 캐릭터 중에 같은 이름이 있어요…" + [복원해서 열기], '추가' 활성. [복원해서 열기] → 토스트 '캐릭터를 복원했어요' + 혜원 상세 선택.
  - **(c-2)**: '혜원'을 다시 보관 → 상세 닫기 → 헤더 '보관 1' 버튼으로 보관 목록 → 헤더 '캐릭터 추가' → '한솔' → [기존 카드 열기] → **한솔 상세가 열리고, 뒤의 목록이 활성 목록으로 돌아와 있다**(보관 카드가 열리거나 아무것도 안 열리면 실패).

## 완료 기준
- [ ] (a)~(h) 반영, `typecheck` 통과, `test:character` 155/0
- [ ] 미리보기 시나리오 9개 전부 관찰됨
- [ ] 커밋 1개: `피드백 55: 같은 이름 캐릭터 추가 차단 + 기존 카드 열기·비슷한 이름 제안`

---

# 작업 2 — 항목 57-1: 작업 폴더 '만들기' 전 확인 창 (+ 새 창 팝업 확인 창 호스트)

## 문제/요청 요약

'만들기'를 누르는 즉시 공유 드라이브에 폴더가 생긴다. 만들기 전에 "어디에 어떤 이름으로 만드는지"를 보여주는 확인 창을 띄운다. 캐릭터 현황판이 새 창(팝업)에서도 쓰이므로 팝업에도 확인 창 호스트를 둔다.

## 근거

`src/services/characterFolderService.ts` 49~54행 (앵커 `if (!window.electronAPI?.pathCreateFolder) {`):
```ts
  if (!window.electronAPI?.pathCreateFolder) {
    toast.error('현재 환경에서는 폴더를 만들 수 없어요');
    return false;
  }

  const result: PathCreateFolderResult = await window.electronAPI.pathCreateFolder(root, character.name);
```
환경 확인 직후 바로 생성 호출 — 확인 단계가 없다.

`src/views/WidgetPopup.tsx` — `grep ConfirmDialogHost src/views/WidgetPopup.tsx` 결과 없음. 1126~1137행에 `<Toaster … />`만 있다. `ConfirmDialog.show`는 호스트가 없으면 `window.confirm` 폴백(`src/components/common/ConfirmDialog.tsx` 86~93행) — 팝업에서 캐릭터 보관·삭제·(이번) 폴더 만들기·경로 지우기가 전부 브라우저 기본 창으로 뜬다.

## 근본 원인

확인 단계가 설계에 없었고, 팝업 렌더러는 `App.tsx`와 별개 트리라 앱의 확인 창 호스트가 적용되지 않는다(캐릭터 현황판 표면을 늘릴 때 팝업 경로를 함께 점검하는 기존 교훈과 같은 부류).

## 수정 내용

### (a) `src/services/characterFolderService.ts` — import 추가 (3행, 앵커: `import { readMetadataFromSupabase, writeMetadataToSupabase } from '@/services/supabaseService';`)

**before (원문, 1~3행):**
```ts
import { toast } from 'sonner';
import type { Character } from '@/types';
import { readMetadataFromSupabase, writeMetadataToSupabase } from '@/services/supabaseService';
```
**after:**
```ts
import { toast } from 'sonner';
import type { Character } from '@/types';
import { readMetadataFromSupabase, writeMetadataToSupabase } from '@/services/supabaseService';
import { ConfirmDialog } from '@/components/common/ConfirmDialog';
import { buildCharacterFolderConfirmMessage } from '@/utils/characterName';
```

### (b) `src/services/characterFolderService.ts` — 확인 창 삽입 (49~54행, 앵커: 위 근거 블록)

**before (원문):**
```ts
  if (!window.electronAPI?.pathCreateFolder) {
    toast.error('현재 환경에서는 폴더를 만들 수 없어요');
    return false;
  }

  const result: PathCreateFolderResult = await window.electronAPI.pathCreateFolder(root, character.name);
```
**after:**
```ts
  if (!window.electronAPI?.pathCreateFolder) {
    toast.error('현재 환경에서는 폴더를 만들 수 없어요');
    return false;
  }

  // 피드백 57-1: 실수로 눌러 공용 드라이브에 폴더가 생기지 않게 — 어디에 어떤 이름으로 만드는지 보여주고 확인받는다.
  const confirmed = await ConfirmDialog.show({
    message: buildCharacterFolderConfirmMessage(root, character.name),
    confirmLabel: '폴더 만들기',
  });
  if (!confirmed) return false;

  const result: PathCreateFolderResult = await window.electronAPI.pathCreateFolder(root, character.name);
```
(`buildCharacterFolderConfirmMessage`는 작업 1 (a)에서 이미 추가돼 있다. 기준 경로 미설정·환경 불가 토스트는 확인 창 **앞**에서 그대로 처리되고, 취소하면 아무 토스트 없이 조용히 끝난다 — 의도.)

### (c) `src/views/WidgetPopup.tsx` — import (48행, 앵커: `import { Toaster, toast as sonnerToast } from 'sonner';`)

**before:**
```tsx
import { Toaster, toast as sonnerToast } from 'sonner';
```
**after:**
```tsx
import { Toaster, toast as sonnerToast } from 'sonner';
import { ConfirmDialogHost } from '@/components/common/ConfirmDialog';
```

### (d) `src/views/WidgetPopup.tsx` — 호스트 마운트 (1126~1127행, 앵커: `{/* Sonner 토스트 (휴가 등록 완료/실패 브로드캐스트 표시용) */}`)

**before:**
```tsx
      {/* Sonner 토스트 (휴가 등록 완료/실패 브로드캐스트 표시용) */}
      <Toaster
```
**after:**
```tsx
      {/* 확인 창 호스트 — 새 창의 캐릭터 현황판(보관·삭제·폴더 만들기·경로 지우기)도 앱과 같은 확인 창을 쓴다.
          없으면 브라우저 기본 confirm 으로 떨어진다(피드백 57-1). */}
      <ConfirmDialogHost />

      {/* Sonner 토스트 (휴가 등록 완료/실패 브로드캐스트 표시용) */}
      <Toaster
```
(`<Toaster …/>` 블록 자체는 변경하지 않는다.)

### (e) 테스트 append — `tests/characterFeedback5.test.ts` 파일 끝에 추가 (작업 1 (g)가 만든 파일. 마지막 `});` 뒤)

```ts

test('피드백 57-1: 폴더 만들기 전 확인 창 — 환경 확인 뒤·실제 생성 전', () => {
  assert.match(folderService, /import \{ ConfirmDialog \} from '@\/components\/common\/ConfirmDialog'/);
  assert.match(folderService, /import \{ buildCharacterFolderConfirmMessage \} from '@\/utils\/characterName'/);
  assert.match(folderService, /const confirmed = await ConfirmDialog\.show\(\{\s*message: buildCharacterFolderConfirmMessage\(root, character\.name\),\s*confirmLabel: '폴더 만들기',\s*\}\);/);
  assert.match(folderService, /if \(!confirmed\) return false;/);
  const envCheck = folderService.indexOf("if (!window.electronAPI?.pathCreateFolder) {");
  const confirmAt = folderService.indexOf('const confirmed = await ConfirmDialog.show(');
  const createAt = folderService.indexOf('window.electronAPI.pathCreateFolder(root, character.name)');
  assert.ok(envCheck > -1 && confirmAt > envCheck && createAt > confirmAt, '확인 창은 환경 확인 뒤, 실제 폴더 생성 전에 떠야 한다');
});

test('피드백 57-1: 새 창 팝업에도 확인 창 호스트 — 브라우저 기본 confirm 폴백 방지', () => {
  assert.match(widgetPopup, /import \{ ConfirmDialogHost \} from '@\/components\/common\/ConfirmDialog'/);
  assert.match(widgetPopup, /<ConfirmDialogHost \/>/);
  // 호스트는 팝업 루트에 1개만.
  assert.equal((widgetPopup.match(/<ConfirmDialogHost \/>/g) ?? []).length, 1);
});
```
- 이 작업으로 깨지는 기존 앵커 없음(`characterBoardAssetWorkflow.test.ts`의 `pathCreateFolder(root, character.name)`·`updateCharacterFolder(character.id, result.path)` 앵커는 after에도 그대로 있다).

## 엣지 케이스 & 주의사항

- 확인 창 문구는 기준 경로 원문(`G:\…`)을 그대로 보여준다(`whitespace-pre-line`이라 줄바꿈 유지). 실제 폴더 이름은 메인이 금지문자를 정제하므로 특수문자가 있는 이름은 문구와 미세하게 다를 수 있다 — 허용.
- 임시 이름('새 캐릭터 N')이면 문구에 경고 줄이 한 줄 더 붙는다. 만들기를 막지는 않는다.
- 확인 창 동안 `creatingFolder`가 true라 '만들기'에 스피너·'경로 지정' 비활성 — 의도(§6-3).
- Escape로 확인 창을 닫으면 `false` → 조용히 종료, 상세 모달은 닫히지 않는다(확인 창의 capture 리스너가 이벤트를 삼킨다).
- `ConfirmDialogHost`는 팝업 루트에 **1개만**. `WidgetPopup.tsx`에 `IsPopupContext.Provider`가 두 군데(938행·1113행) 있는데, 이번 삽입 위치는 1126행 Toaster 앞 한 곳뿐이다.

## 검증 방법

```bash
npm run typecheck
npm run test:character
```
- 이 커밋에서 `test:character` = **157 통과 / fail 0**.
- 미리보기(§4-7): mock 캐릭터 3명은 모두 작업 폴더 경로가 있어 [만들기]가 보이지 않는다(작업 3 전에는 [지우기]도 없음). **'캐릭터 추가'에서 이름을 비우고 [추가] → '새 캐릭터' 카드가 만들어지며 상세가 열린다**(같은 세션에서 반복하거나 이름 없는 카드가 이미 있으면 '새 캐릭터 2' 등 — 아래 문구의 번호는 무시) → 작업 폴더 행이 `미등록 · [만들기][선택]`(이 시점엔 아직 '선택') → [만들기] 클릭 → 확인 창:
  > 아래 위치에 '새 캐릭터' 폴더를 만들고 작업 폴더로 연결할까요?
  > G:\공유 드라이브\사우스 코리안 파크\[]사코팍 캐릭터 세팅
  > 이미 같은 이름의 폴더가 있으면 새로 만들지 않고 그 폴더를 연결만 해요.
  > 지금 이름은 임시 이름이에요 — 폴더 이름으로 쓰기 전에 캐릭터 이름부터 정하는 게 좋아요.
  > [취소] [폴더 만들기]
  - [취소] → 아무 변화 없음(경로 여전히 '미등록', 토스트 없음).
  - 헤더 연필로 이름을 '도윤'으로 바꾼 뒤 [만들기] → 문구가 "'도윤' 폴더를 …"이고 임시 이름 줄이 **없다** → [폴더 만들기] → 토스트 '폴더를 만들고 작업 폴더로 연결했어요', 행에 '도윤' + [선택][열기].
- 팝업 호스트: 미리보기에서는 팝업 창을 열 수 없다(mock에 `widgetOpenPopup` 없음 → '새 창으로' 버튼 미표시). **실기(Electron) 확인 항목**: 캐릭터 현황판 '새 창으로' → 팝업에서 캐릭터 '보관' 또는 [만들기] → 브라우저 기본 confirm이 아니라 앱 스타일 확인 창(어두운 카드, 보라 확인 버튼)이 뜬다.

## 완료 기준
- [ ] (a)~(e) 반영, `typecheck` 통과, `test:character` 157/0
- [ ] 미리보기에서 확인 창 문구(임시 이름 줄 유무 포함)·취소·확정 동작 관찰
- [ ] 커밋 1개: `피드백 57-1: 작업 폴더 만들기 전 확인 창 + 새 창 확인 창 호스트`

---

# 작업 3 — 항목 57-2·57-3: 작업 폴더·작업 파일 경로 '지우기' + 버튼 이름 '경로 지정'/'경로 수정'

## 문제/요청 요약

한 번 등록한 경로는 바꾸거나 열 수만 있다. 확인 창을 거쳐 경로 연결만 지우는 [지우기]를 두 행(작업 폴더·작업 파일)에 추가하고, '선택' 버튼을 경로 유무에 따라 '경로 지정'/'경로 수정'으로 바꾼다.

## 근거 (파일: `src/components/characters/CostumeDetail.tsx`)

28~83행 `function PathActionRow(` — props `label/path/onPick/onOpen/onCreate/creating`. 버튼: `!path && onCreate`일 때 '만들기', 항상 '선택'(62~69행), `path`일 때 '열기'(70~79행). 해제 동선 없음.

134~148행 `export function CostumeDetail({` — props에 `onPickFolder/onPickFile/onCreateFolder/creatingFolder`. 297~310행에서 두 `PathActionRow`를 렌더.

`src/components/characters/CharacterDetailModal.tsx` 326~361행: `handlePickFolder`·`handleCreateFolder`·`ensureCostume`·`handlePickFile`. 626~633행 `<CostumeDetail … />` 배선. `ConfirmDialog`(23행)·`toast`(2행)·`CharacterCostume` 타입(9행)은 이미 import돼 있다.

## 근본 원인

UI에 해제 동선이 없을 뿐, store(`updateCharacterFolder(id, null)`, `updateCostumeField(id, { workFilePath: null })`)·서비스·메인·프레즌스 캐시는 전부 null을 지원한다.

## 수정 내용

### (a) `CostumeDetail.tsx` — `PathActionRow` 전체 교체 (28~83행, 앵커: `function PathActionRow({`)

**before (원문):**
```tsx
function PathActionRow({
  label,
  path,
  onPick,
  onOpen,
  onCreate,
  creating = false,
}: {
  label: string;
  path: string | null;
  onPick: () => void;
  onOpen: () => void;
  onCreate?: () => void;
  creating?: boolean;
}) {
  return (
    <div className="flex items-center justify-between gap-3 rounded-lg border border-bg-border/70 bg-bg-border/10 px-3 py-2">
      <div className="min-w-0">
        <div className="text-xs text-text-secondary">{label}</div>
        <div className="text-sm text-text-primary truncate" title={path ? displayCharacterPathName(path) : undefined}>{displayCharacterPathName(path)}</div>
      </div>
      <div className="flex items-center gap-1.5 shrink-0">
        {!path && onCreate && (
          <button
            type="button"
            onClick={onCreate}
            disabled={creating}
            title="기준 경로에 캐릭터 이름으로 폴더를 만들어 연결"
            className="inline-flex items-center gap-1 rounded-md border border-accent/40 px-2 py-1.5 text-xs text-accent hover:bg-accent/10 disabled:opacity-50"
          >
            {creating && <Loader2 size={11} className="animate-spin" />}
            만들기
          </button>
        )}
        <button
          type="button"
          onClick={onPick}
          disabled={creating}
          className="rounded-md border border-bg-border px-2 py-1.5 text-xs text-text-secondary hover:text-text-primary hover:border-text-secondary/50 disabled:opacity-50"
        >
          선택
        </button>
        {path && (
          <button
            type="button"
            onClick={onOpen}
            title={path}
            className="rounded-md border border-bg-border px-2 py-1.5 text-xs text-text-secondary hover:text-text-primary hover:border-text-secondary/50"
          >
            열기
          </button>
        )}
      </div>
    </div>
  );
}
```
**after:**
```tsx
function PathActionRow({
  label,
  path,
  onPick,
  onOpen,
  onCreate,
  onClear,
  creating = false,
}: {
  label: string;
  path: string | null;
  onPick: () => void;
  onOpen: () => void;
  onCreate?: () => void;
  /** 피드백 57-2: 등록된 경로만 지운다(실제 폴더·파일은 그대로). 경로가 있을 때만 버튼 표시. */
  onClear?: () => void;
  creating?: boolean;
}) {
  return (
    <div className="flex items-center justify-between gap-3 rounded-lg border border-bg-border/70 bg-bg-border/10 px-3 py-2">
      <div className="min-w-0">
        <div className="text-xs text-text-secondary">{label}</div>
        <div className="text-sm text-text-primary truncate" title={path ? displayCharacterPathName(path) : undefined}>{displayCharacterPathName(path)}</div>
      </div>
      <div className="flex items-center gap-1.5 shrink-0">
        {!path && onCreate && (
          <button
            type="button"
            onClick={onCreate}
            disabled={creating}
            title="기준 경로에 캐릭터 이름으로 폴더를 만들어 연결"
            className="inline-flex items-center gap-1 rounded-md border border-accent/40 px-2 py-1.5 text-xs text-accent hover:bg-accent/10 disabled:opacity-50"
          >
            {creating && <Loader2 size={11} className="animate-spin" />}
            만들기
          </button>
        )}
        {/* 피드백 57-3: '선택' → 상태별 '경로 지정'/'경로 수정' — 이름만 보고 무엇을 하는 버튼인지 알 수 있게. */}
        <button
          type="button"
          onClick={onPick}
          disabled={creating}
          title={path ? `${label} 경로를 다른 곳으로 바꿔요` : `탐색기에서 골라 ${label} 경로를 등록해요`}
          className="rounded-md border border-bg-border px-2 py-1.5 text-xs text-text-secondary hover:text-text-primary hover:border-text-secondary/50 disabled:opacity-50"
        >
          {path ? '경로 수정' : '경로 지정'}
        </button>
        {path && (
          <button
            type="button"
            onClick={onOpen}
            title={path}
            className="rounded-md border border-bg-border px-2 py-1.5 text-xs text-text-secondary hover:text-text-primary hover:border-text-secondary/50"
          >
            열기
          </button>
        )}
        {path && onClear && (
          <button
            type="button"
            onClick={onClear}
            disabled={creating}
            title={`${label} 연결을 지워요 — 실제 폴더·파일은 그대로예요`}
            className="rounded-md border border-bg-border px-2 py-1.5 text-xs text-text-secondary hover:text-red-400 hover:border-red-400/50 disabled:opacity-50"
          >
            지우기
          </button>
        )}
      </div>
    </div>
  );
}
```

### (b) `CostumeDetail.tsx` — `CostumeDetail` props (134~148행, 앵커: `export function CostumeDetail({`)

**before (원문):**
```tsx
export function CostumeDetail({
  character,
  costume,
  onPickFolder,
  onPickFile,
  onCreateFolder,
  creatingFolder,
}: {
  character: Character;
  costume: CharacterCostume;
  onPickFolder: () => void;
  onPickFile: () => void;
  onCreateFolder: () => void;
  creatingFolder: boolean;
}) {
```
**after:**
```tsx
export function CostumeDetail({
  character,
  costume,
  onPickFolder,
  onPickFile,
  onCreateFolder,
  creatingFolder,
  onClearFolder,
  onClearFile,
}: {
  character: Character;
  costume: CharacterCostume;
  onPickFolder: () => void;
  onPickFile: () => void;
  onCreateFolder: () => void;
  creatingFolder: boolean;
  /** 피드백 57-2: 작업 폴더/작업 파일 경로 지우기 — 확인 창은 부모(상세 모달)가 띄운다. */
  onClearFolder: () => void;
  onClearFile: () => void;
}) {
```

### (c) `CostumeDetail.tsx` — 두 경로 행 배선 (297~310행, 앵커: `label="작업 폴더"`)

**before (원문):**
```tsx
        <PathActionRow
          label="작업 폴더"
          path={character.workFolderPath}
          onPick={onPickFolder}
          onOpen={() => openStoredCharacterPath(character.workFolderPath, '작업 폴더')}
          onCreate={onCreateFolder}
          creating={creatingFolder}
        />
        <PathActionRow
          label="작업 파일"
          path={costume.workFilePath}
          onPick={onPickFile}
          onOpen={() => openStoredCharacterPath(costume.workFilePath, '작업 파일')}
        />
```
**after:**
```tsx
        <PathActionRow
          label="작업 폴더"
          path={character.workFolderPath}
          onPick={onPickFolder}
          onOpen={() => openStoredCharacterPath(character.workFolderPath, '작업 폴더')}
          onCreate={onCreateFolder}
          onClear={onClearFolder}
          creating={creatingFolder}
        />
        <PathActionRow
          label="작업 파일"
          path={costume.workFilePath}
          onPick={onPickFile}
          onOpen={() => openStoredCharacterPath(costume.workFilePath, '작업 파일')}
          onClear={onClearFile}
        />
```

### (d) `CharacterDetailModal.tsx` — `handleClearFolder` 추가 (326~330행 `handlePickFolder` 블록 **바로 뒤**, 앵커: `const handlePickFolder = useCallback(async () => {`)

**before (원문, 326~331행):**
```tsx
  const handlePickFolder = useCallback(async () => {
    const folder = await chooseWorkFolder();
    if (!folder) return;
    await updateCharacterFolder(character.id, folder);
  }, [character.id, updateCharacterFolder]);

```
**after:**
```tsx
  const handlePickFolder = useCallback(async () => {
    const folder = await chooseWorkFolder();
    if (!folder) return;
    await updateCharacterFolder(character.id, folder);
  }, [character.id, updateCharacterFolder]);

  // 피드백 57-2: 등록된 경로만 지운다 — 실제 폴더는 건드리지 않는다. 실수 방지로 확인 창을 거친다(보관·삭제와 같은 패턴).
  const handleClearFolder = useCallback(async () => {
    const ok = await ConfirmDialog.show({
      message: `'${character.name}'의 작업 폴더 연결을 지울까요?\n실제 폴더는 그대로 두고 현황판에 등록된 경로만 지워요. 다시 '경로 지정'으로 연결할 수 있어요.`,
      confirmLabel: '연결 지우기',
    });
    if (!ok) return;
    const saved = await updateCharacterFolder(character.id, null);
    if (saved) toast.success('작업 폴더 연결을 지웠어요');
  }, [character.id, character.name, updateCharacterFolder]);

```

### (e) `CharacterDetailModal.tsx` — `handleClearFile` 추가 (349~361행 `handlePickFile` 블록 **바로 뒤**, 앵커: `}, [activeCostume, character.id, character.workFolderPath, ensureCostume, updateCharacterFolder, updateCostumeField]);` — 파일 내 유일)

**before (원문, 361~362행):**
```tsx
  }, [activeCostume, character.id, character.workFolderPath, ensureCostume, updateCharacterFolder, updateCostumeField]);

```
**after:**
```tsx
  }, [activeCostume, character.id, character.workFolderPath, ensureCostume, updateCharacterFolder, updateCostumeField]);

  // 피드백 57-2: 복장 작업 파일 연결 지우기 — 파일 자체는 그대로. 프레즌스(피드백 54) 캐시는 경로 null 도 실변경으로 처리한다.
  const handleClearFile = useCallback(async (targetCostume: CharacterCostume) => {
    const ok = await ConfirmDialog.show({
      message: `'${targetCostume.name}' 복장의 작업 파일 연결을 지울까요?\n실제 파일은 그대로 두고 현황판에 등록된 경로만 지워요.`,
      confirmLabel: '연결 지우기',
    });
    if (!ok) return;
    const saved = await updateCostumeField(targetCostume.id, { workFilePath: null });
    if (saved) toast.success('작업 파일 연결을 지웠어요');
  }, [updateCostumeField]);

```

### (f) `CharacterDetailModal.tsx` — `CostumeDetail` 배선 (626~633행, 앵커: `onCreateFolder={handleCreateFolder}`)

**before (원문):**
```tsx
              <CostumeDetail
                character={character}
                costume={activeCostume}
                onPickFolder={handlePickFolder}
                onPickFile={() => handlePickFile(activeCostume)}
                onCreateFolder={handleCreateFolder}
                creatingFolder={creatingFolder}
              />
```
**after:**
```tsx
              <CostumeDetail
                character={character}
                costume={activeCostume}
                onPickFolder={handlePickFolder}
                onPickFile={() => handlePickFile(activeCostume)}
                onCreateFolder={handleCreateFolder}
                creatingFolder={creatingFolder}
                onClearFolder={handleClearFolder}
                onClearFile={() => handleClearFile(activeCostume)}
              />
```

### (g) 테스트 append — `tests/characterFeedback5.test.ts` 파일 끝에 추가 (작업 2 (e) 블록 뒤)

```ts

test('피드백 57-2·57-3: 경로 행 — 지우기 버튼 + 상태별 버튼 이름', () => {
  // PathActionRow: onClear prop + 경로 있을 때만 지우기.
  assert.match(costumeDetail, /onClear\?: \(\) => void;/);
  assert.match(costumeDetail, /\{path && onClear && \(/);
  assert.match(costumeDetail, /onClick=\{onClear\}/);
  assert.match(costumeDetail, /지우기/);
  // 57-3: '선택' 고정 문구 제거 → 상태별. (\s 는 CRLF 도 먹는다 — 작업 트리가 CRLF 라 \n 만 쓰면 죽은 가드가 된다.)
  assert.match(costumeDetail, /\{path \? '경로 수정' : '경로 지정'\}/);
  assert.doesNotMatch(costumeDetail, />\s*선택\s*<\/button>/);
  // 기존 앵커 보존: 만들기 버튼 조건·title, 열기 버튼 title=경로.
  assert.match(costumeDetail, /!path && onCreate/);
  assert.match(costumeDetail, /title="기준 경로에 캐릭터 이름으로 폴더를 만들어 연결"/);
  assert.match(costumeDetail, /onClick=\{onOpen\}\s*\n\s*title=\{path\}/);
  // CostumeDetail props → 두 행에 배선.
  assert.match(costumeDetail, /onClearFolder: \(\) => void;/);
  assert.match(costumeDetail, /onClearFile: \(\) => void;/);
  assert.match(costumeDetail, /onClear=\{onClearFolder\}/);
  assert.match(costumeDetail, /onClear=\{onClearFile\}/);
});

test('피드백 57-2: 상세 모달 — 확인 창 뒤 경로만 null 로 저장', () => {
  assert.match(detailModal, /const handleClearFolder = useCallback\(async \(\) => \{/);
  assert.match(detailModal, /updateCharacterFolder\(character\.id, null\)/);
  assert.match(detailModal, /const handleClearFile = useCallback\(async \(targetCostume: CharacterCostume\) => \{/);
  assert.match(detailModal, /updateCostumeField\(targetCostume\.id, \{ workFilePath: null \}\)/);
  assert.equal((detailModal.match(/confirmLabel: '연결 지우기'/g) ?? []).length, 2);
  assert.match(detailModal, /onClearFolder=\{handleClearFolder\}/);
  assert.match(detailModal, /onClearFile=\{\(\) => handleClearFile\(activeCostume\)\}/);
});
```
- 깨지는 기존 앵커 없음 — `characterFeedbackBatch.test.ts` B9(`onClick={onOpen}` 다음 줄 `title={path}`)·F26 앵커, `characterBoardAssetWorkflow.test.ts`의 `!path && onCreate`·만들기 title 앵커는 after에 그대로 있다.

## 엣지 케이스 & 주의사항

- [지우기]는 `path && onClear`일 때만 보인다. 지운 뒤 작업 폴더 행에는 [만들기][경로 지정]이 다시 나타난다.
- 확인 창 tone은 기본(보라 확인 버튼) — 폴더·파일 자체를 지우는 게 아니므로 '보관'의 danger(빨강)와 구분한다. 문구가 "실제 폴더/파일은 그대로"를 명시한다.
- 저장 실패 시 `applyCharacterUpdate`/`applyCostumeUpdate`가 롤백 + 에러 토스트를 띄우고 `false`를 돌려주므로 성공 토스트는 뜨지 않는다.
- `handleClearFile(activeCostume)`는 `activeCostume`이 있을 때만 렌더되는 분기 안이라 null 걱정 없음(기존 `handlePickFile(activeCostume)`와 동일 패턴).
- 카드 우클릭 메뉴(`CharacterImageContextMenu`)에는 지우기를 **추가하지 않는다**(부록 C).

## 검증 방법

```bash
npm run typecheck
npm run test:character
npm run test:ui
```
- 이 커밋에서 `test:character` = **159 통과 / fail 0**, `test:ui` 17/0.
- 미리보기(§4-7): '한솔' 상세 → 작업 폴더 행이 `한솔 · [경로 수정][열기][지우기]`, 작업 파일 행이 `[드라마 퀄리티] 한솔 SWver12.moho · [경로 수정][열기][지우기]`로 보인다(문구 확인).
  - 작업 폴더 [지우기] → 확인 창 "'한솔'의 작업 폴더 연결을 지울까요? / 실제 폴더는 그대로 두고 현황판에 등록된 경로만 지워요. 다시 '경로 지정'으로 연결할 수 있어요." + [취소][연결 지우기]. [취소] → 변화 없음. 다시 [지우기] → [연결 지우기] → 토스트 '작업 폴더 연결을 지웠어요', 행이 `미등록 · [만들기][경로 지정]`.
  - [만들기] → 확인 창(작업 2) → 확인 창이 떠 있는 동안 행의 [만들기][경로 지정]이 비활성(스피너) → [폴더 만들기] → 행이 `한솔 · [경로 수정][열기][지우기]`.
  - [지우기] → [연결 지우기] → [경로 지정] → (mock) 행이 `A_014 · [경로 수정][열기][지우기]`.
  - 작업 파일 [지우기] → "'교복' 복장의 작업 파일 연결을 지울까요? / 실제 파일은 그대로 두고 현황판에 등록된 경로만 지워요." → [연결 지우기] → 토스트 '작업 파일 연결을 지웠어요', 행이 `미등록 · [경로 지정]`. [경로 지정] → `main.psd · [경로 수정][열기][지우기]`.

## 완료 기준
- [ ] (a)~(g) 반영, `typecheck` 통과
- [ ] `test:character` **159 통과 / fail 0**, `test:ui` 17 통과
- [ ] 미리보기 시나리오 관찰(문구 정확히 일치)
- [ ] 커밋 1개: `피드백 57-2·3: 작업 폴더·파일 경로 지우기와 버튼 이름 정리`

---

# 부록 A — 버전 커밋·구현 기록 커밋 + 통합 시나리오

## A-1. 버전·업데이트 내역 (커밋 4)

1. `origin/main`의 `package.json` 버전을 재확인해 **마이너 +1**(2026-09-05 기준 1.110.1 → `1.111.0`). `package.json`(1곳)·`package-lock.json`(2곳: 3행 최상위 `"version"`, 9행 `packages[""].version`) 갱신.
2. `DEVLOG/update-notes.json` 배열 **맨 앞**에 다음 객체를 추가(버전 문자열은 1에서 정한 값으로):
```json
  {
    "version": "1.111.0",
    "title": "캐릭터 중복 추가 방지와 작업 경로 정리",
    "items": [
      {
        "category": "feature",
        "summary": "이미 있는 이름으로는 캐릭터를 또 만들 수 없고, 대신 그 카드로 바로 갈 수 있어요",
        "description": "캐릭터 추가 창에 같은 이름을 넣으면 예전엔 경고만 뜨고 그대로 만들어져서 카드가 두 장 생기곤 했어요. 이제 같은 이름(띄어쓰기·대소문자 차이는 무시)이 있으면 추가 버튼이 잠기고 '기존 카드 열기'로 그 카드를 바로 열 수 있어요. 보관해 둔 캐릭터와 이름이 겹치면 '복원해서 열기'로 되살려 쓸 수 있고, 이름을 치는 동안 비슷한 이름의 카드도 최대 3개까지 보여줘요."
      },
      {
        "category": "ux",
        "summary": "작업 폴더 '만들기'를 누르면 어디에 어떤 이름으로 만들지 먼저 물어봐요",
        "description": "버튼을 잘못 눌러 공유 드라이브에 폴더가 바로 생기면 다시 들어가 지워야 했어요. 이제 만들기 전에 기준 경로와 폴더 이름을 보여주는 확인 창이 뜨고, 아직 임시 이름인 캐릭터면 이름부터 정하라고 알려줘요. 새 창으로 연 캐릭터 현황판에서도 같은 확인 창이 나와요."
      },
      {
        "category": "ux",
        "summary": "작업 폴더·작업 파일 경로를 지울 수 있고, 버튼 이름이 더 알기 쉬워졌어요",
        "description": "한 번 등록한 경로는 바꾸거나 열 수만 있었는데, 이제 '지우기'로 연결만 끊을 수 있어요(실제 폴더·파일은 그대로예요). '선택' 버튼은 경로가 없을 땐 '경로 지정', 있을 땐 '경로 수정'으로 바뀌어 무엇을 하는 버튼인지 바로 보여요."
      }
    ]
  },
```
3. 커밋 메시지: `v1.111.0 버전 반영과 업데이트 내역 추가` (버전은 실제 값). 이 커밋 후 `npm run test:playground`(버전 3자 일치 검사) 통과 확인.

## A-2. 구현 기록 (커밋 5 — PR 생성 후)

`DEVLOG/<실행일>-피드백-55-57-구현.md`를 `DEVLOG/2026-08-12-피드백-51-54-구현.md` 구조(계획서 경로·기준선·PR/커밋 표·작업별 결과·계획과 달랐던 점·검증 수치)로 작성해 같은 브랜치에 5번째 커밋으로 추가하고 push 한다(PR 번호를 표에 넣기 위해 PR 생성 뒤에 쓴다). 커밋 메시지: `피드백 55·57 구현 기록 추가`.

## A-3. 통합 시나리오 (작업 3 커밋 후, 미리보기)

- [ ] 추가 창에서 [기존 카드 열기]로 연 상세에서 바로 작업 폴더 [지우기] → [만들기] 확인 창 → [폴더 만들기] → 행 갱신까지 한 흐름으로 동작(창끼리 Escape·포커스 충돌 없음).
- [ ] 확인 창이 떠 있는 동안 Escape → 확인 창만 닫히고 상세 모달·추가 모달은 남는다.
- [ ] 라이트 모드('라이트 모드로 전환' 버튼)에서도 [경로 지정]/[지우기] 문구·확인 창이 읽힌다.

## A-4. 실기(Electron) 확인 항목 — 미리보기로 검증 불가, PR 본문 '테스트 가이드'에 적을 것

- 실제 탐색기 대화상자로 [경로 지정]/[경로 수정] → 경로 저장.
- [만들기] 확인 → 실제 G드라이브 기준 경로에 폴더 생성(이미 있으면 '이미 있던 폴더를 작업 폴더로 연결했어요').
- '새 창으로' 팝업에서 확인 창이 앱 스타일로 뜬다(브라우저 기본 confirm 아님).

# 부록 B — 전체 검증 (모든 작업 완료 후)

```bash
npm run typecheck
npm run test:character
npm run test:ui
npm run test:entity
npm run test:presence
npm run test:playground
npm run build:vite
```
- [ ] `typecheck` 통과 · `test:character` 159/0 · `test:ui` 17/0 · `test:entity` 249/0 · `test:presence` 38/0 · `test:playground` 461/0(버전 3자 일치 검사 포함)
- [ ] `build:vite` 통과 — 이 스크립트는 `test:calendar`도 돌리는데 **기존 실패 1건(`ScheduleView counts every event overlapping the displayed month`)** 때문에 중단될 수 있다. 그 경우 그 1건이 기준선과 동일한 기존 실패임을 확인하고, `npx vite build && node scripts/generate-manifest.js --allow-missing-installer`를 직접 실행해 빌드 통과를 증명한 뒤 완료 보고에 그 사실을 적는다. **캘린더 테스트를 고치지 말 것**(범위 밖).
- [ ] 커밋 4개(작업 1·2·3 + 버전), 브랜치 `fix/피드백-55-57`, 작업 트리에 `tests/.calendar-ipc-type-contract-*.ts` 같은 테스트 임시 파일이 남아 있으면 커밋에 포함하지 말 것.
- [ ] PR 1개 생성(제목 예: `[v1.111.0] 캐릭터 중복 추가 방지와 작업 경로 정리`). 본문의 `📋 업데이트 요약`은 A-1의 update-notes 문구를 그대로 쓰고(비개발자 톤), `🔧 상세 기술 설명`은 개발자 톤 가능. 테스트 가이드에 A-3·A-4 포함. **머지는 하지 않는다**(한솔이 코덱스 리뷰 루프 후 결정).
- [ ] **PR 생성 후** 구현 기록을 5번째 커밋으로 추가·push(부록 A-2, PR 번호 포함) — 총 커밋 5개.

# 부록 C — 범위 외 후속 권장 (이번에 구현하지 말 것 — 기록만)

1. **서버 측 유일 제약**: `characters.name`에 대소문자·공백 무시 유일 인덱스가 없어 두 사용자가 동시에 같은 이름을 만들면 여전히 두 장이 생길 수 있다. 라이브 DB에 기존 중복이 있는지 먼저 조사해야 하므로 별도 마이그레이션 라운드로.
2. **이름 변경·파일명 자동 지정의 중복 검사**: 한솔 결정으로 이번 범위에서 제외. 상세 모달 인라인 rename(`renameCharacter`)과 임시 이름 자동 지정(`FeaturedImageSlot`)은 여전히 중복 이름을 만들 수 있다.
3. **카드 우클릭 메뉴의 경로 지우기**: `CharacterImageContextMenu` card variant에는 열기만 있다 — 필요해지면 같은 `handleClear*` 패턴으로.
4. **`restoreCharacter`가 boolean을 돌려주도록**: 지금은 `void`라 추가 모달이 store 상태를 재조회해 성공 여부를 판단한다. store 시그니처를 바꾸면 다른 호출부 영향이 있어 이번엔 손대지 않았다.
5. **확인 창 대기 중 스피너**: §6-3. 확인 창을 `handleCreateFolder` 앞단으로 옮기면 기준 경로 조회를 두 번 하거나 서비스 시그니처를 바꿔야 해 이번엔 유지.
6. **기존 `onCreated` 경로의 같은 결함**: (c-2)가 `pendingOpenId` 소비 효과 자체를 고치므로 새 캐릭터 생성 후 자동 오픈(보관 목록을 보는 중 생성)도 함께 정상화된다 — 별도 작업 불필요, 기록만.

---

# 리뷰 반영 기록

독립 검증 3패스(2026-09-05, 계획서 경로 + 저장소 경로만 전달) 결과와 반영:

| # | 패스 | 심각도 | 발견 | 반영 |
|---|---|---|---|---|
| 1 | ②구현 시뮬레이션 | **중요** | 헤더 '캐릭터 추가'는 보관 목록을 보는 중에도 눌리는데, 그 상태에서 [기존 카드 열기]/[복원해서 열기]로 활성 카드를 열면 그리드 효과가 `showArchived`를 되돌리지 않아 보관 모드 상세가 엉뚱한 카드를 열거나 닫힌다 | 작업 1 **(c-2)** 신설(230행 → 주석+코드 2줄), 테스트 앵커 2줄, 검증 시나리오 추가, §2·부록 C-6 기록 |
| 2 | ② | 사소 | 신설 테스트 파일을 작업 1에서 통째로 만들면 작업 1·2 커밋이 빨간 상태로 main 이력에 남는다 | 테스트 파일을 작업별 3분할(작업 1 생성, 2·3 append) → 모든 커밋 경계 녹색(155→157→159). §4-2·각 작업 검증 절 갱신 |
| 3 | ② | 사소 | 비슷한 이름 제안이 한 방향 포함만 검사('한솔' 있을 때 '한솔이' 입력은 제안 없음) | `suggestSimilarCharacters` 양방향 포함으로 변경 + 테스트 단언 추가 |
| 4 | ② | 사소 | `npm run dev:renderer`는 PORT 미지정 시 5173에 뜬다(5190은 launch.json 전용) | §4-7 실행법 정정(`$env:PORT='5190'` / launch.json) |
| 5 | ② | 사소 | 작업 2 검증이 "경로 없는 캐릭터"를 전제하지만 mock 3명 모두 폴더 경로 보유 | 작업 2 검증을 "이름 비워 새 카드 생성 → 임시 이름 경고 줄 확인 → 이름 변경 후 재확인"으로 구체화, §6-5 mock 사실 정정(3명 모두 폴더, 모든 복장 파일 경로) |
| 6 | ② | 사소 | 구현 기록 문서·계획서의 커밋 위치 미지정 | §4-1(계획서는 선행 PR로 main), §4-2·부록 A-2(구현 기록 = 5번째 커밋), 부록 B "커밋 5개" |
| 7 | ①③② | 사소 | `doesNotMatch(/\n\s*선택\n\s*<\/button>/)`는 CRLF 작업 트리에서 수정 전 파일에도 매치되지 않는 죽은 가드 | `/>\s*선택\s*<\/button>/`로 교체(before-CRLF·before-LF 매치, after 불일치 실측), §2 줄바꿈 규칙에 원칙 추가 |
| 8 | ①③② | 사소 | (e) B4 앵커는 after에서도 '복원해서 열기' 버튼 때문에 우연히 통과 — "깨진다"는 서술 부정확 | (e)·§3 표를 "정밀화"로 서술 수정 |
| 9 | ① | 사소 | `CharacterBoardView.tsx` 행수 787→786, `<CostumeDetail/>` 배선 626~634→626~633 | 수치 정정(§3·작업 3 근거·(f)) |

반영 후 재검증: 변경분(양방향 제안·(c-2)·3분할 테스트·CRLF 정규식)을 포함한 after 코드를 커밋 단위로 다시 적용해 `typecheck`·`test:character` 155/157/159·`test:ui` 17 통과, (c-2) 시나리오를 미리보기에서 실측했다(헤더 "시뮬레이션 실측" 수치가 그 결과).

독립 재검증 2패스(반영분만, 계획서 경로 + 저장소 경로만 전달): ① 반영분 사실 검증 — (c-2) before 블록 1회 일치·앵커 유일·테스트 무간섭, 3분할 테스트를 이어 붙여 `node --check` 통과 및 커밋 경계별 녹색, CRLF 정규식 before-CRLF 매치/after 불일치 실측, 양방향 제안 단언 실행 일치, 수치·행수·포트·mock 서술 전부 소스와 일치 — **불일치 0**. ② 반영분 구현 시뮬레이션 — 블로커·중요 0, 사소 5(부록 B에서 구현 기록 커밋 순서가 PR 생성보다 앞에 읽힘 / A-2 커밋 메시지가 "예:" / 작업 1 건너뛰기 시 append 불가 미명시 / (c-2) "한 줄" 표현 / 작업 2 시나리오의 임시 이름 번호) → 전부 본문에 반영(부록 B 순서·A-2 확정·§4-2 작업 1 건너뛰기 불가·(c-2) 표현·작업 2 시나리오 병기).
