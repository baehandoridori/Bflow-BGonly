# B flow 피드백 30·31·33 구현 기록

> **계획서:** `DEVLOG/2026-07-15-피드백-30-33-수정계획.md` (독립 리뷰 3패스본)
> **브랜치:** `fix/feedback-30-33` (origin/main `1a99ba2` 에서 분기)
> **버전:** 1.91.0 (착수 시점 origin/main = 1.90.0 → 마이너 +1, 계획서 가정과 일치)
> **구현일:** 2026-07-15

## 작업별 커밋

| 작업 | 커밋 | 내용 |
|---|---|---|
| 1 | `294e85f` | 컴포지팅 캐러셀 z-order 수정(90→상수 25) + hover 가림 검사 (피드백 30) |
| 2 | `4bf37df` | 공지 경로 jbbj://open 링크 변환 — `buildJbbjOpenLink` (피드백 31c) |
| 3 | `bce29ec` | 리깅 공지 모달 개편 — 기본 템플릿·Ctrl+V 이미지·비고 선택화 (피드백 31 a·b·d) |
| 4 | `267378e` | 복장 이미지 원본 크기 보존 체인 — natural_width/height (피드백 33 기반) |
| 5 | `78a0cce` | 이미지 업로드 시 기준 키 자동 설정 (피드백 33a) |
| 6 | `481ceaa` | 기준 키 머리·바닥 기준선 드래그 조정 에디터 (피드백 33b) |
| 7 | `e6cdada` | [v1.91.0] 버전 + 업데이트 노트 |

## 검증 결과 (부록 B-1, 기준선 대비)

`npm run build:vite` 전체 체인 통과 (exit 0):

| 스위트 | 기준선 | 결과 |
|---|---|---|
| typecheck | 통과 | 통과 |
| test:playground | 461 pass | **461 pass / 0 fail** |
| test:auto-update | 29 pass | **29 pass / 0 fail** |
| test:entity | 239 pass | **239 pass / 0 fail** |
| test:notifications | 142 pass | **142 pass / 0 fail** |
| test:presence | 33 pass | **33 pass / 0 fail** |
| test:character | 104 pass | **116 pass / 0 fail** (신규 12: 작업 2 +1, 작업 3 +2, 작업 6 +9) |
| test:ui | 5 pass | **7 pass / 0 fail** (신규 2: 작업 1) |
| vite build + manifest | 통과 | 통과 (개발용 manifest v1.91.0) |

## 계획서와 실제가 달랐던 지점

1. **`package-lock.json` 버전 갱신 (계획서 누락, 작업 7 커밋에 포함).**
   계획서 작업 7은 `package.json` 버전만 명시했으나, `tests/playgroundMarketUiWiring.test.ts` 의
   "release metadata stays aligned" 테스트가 `package-lock.json` 의 `version`·`packages[''].version` 이
   `package.json` 과 일치할 것을 요구한다. 첫 전체 검증에서 이 테스트 1건이 실패해
   (`'1.90.0' !== '1.91.0'`), lock 파일의 버전 필드 2곳을 1.91.0 으로 갱신 후 재검증 → 전부 통과.
   버전 갱신의 필수 동반 수정으로 판단해 작업 7 커밋에 포함했다.

2. **그 외 이탈 없음.** 모든 before 블록·앵커가 계획서에 병기된 라인 번호 그대로 실제 코드와
   일치했고(스냅샷 `b041703` 이후 관련 파일 무변경), 계획서의 after 코드를 그대로 적용했다.
   건너뛴 작업 없음.

## 배포 전 남은 게이트 (계획서 부록 B-2·B-3 — PR 본문에도 포함)

- **B-2:** 라이브 Supabase 에 `DEVLOG/migrations/2026-07-15-costume-image-natural-size.sql` 적용
  (한솔 승인 하에). **적용 전에는 새 빌드를 배포하지 말 것.**
- **B-3:** Slack Workflow Builder 에서 Path 변수 하이퍼링크 서식 / 빈 비고 표시 / image URL
  텍스트 비노출 확인 (한솔 몫).
