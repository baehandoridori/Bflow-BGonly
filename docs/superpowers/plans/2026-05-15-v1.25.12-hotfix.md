# v1.25.12 Hotfix — 씬 추가 UUID race + 벌크 삭제 카운트 표기

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 씬 추가 시 이미지 첨부 race condition (UUID 미할당) 수정 + 통합 뷰 벌크 삭제 카운트를 사용자 보는 단위(머지드 카드)로 정정.

**Architecture:** 두 가지 독립적인 버그 fix.
1. **버그 1**: `waitForSceneUuid` 폴링 헬퍼 추가 + 이미지 업로드 IIFE가 UUID 기반 직접 IPC를 호출하도록 변경 (인덱스 기반 `resolveSceneUuid` 우회).
2. **버그 2**: `countSelectedScenes` 머지드 단위 dedup 카운트 헬퍼 추가 + ConfirmDialog 문구 갱신.

**Tech Stack:** TypeScript, React, Zustand, node:test (테스트는 순수 함수만 단위 테스트).

**Spec:** [docs/superpowers/specs/2026-05-15-v1.26-comments-images-drawing-design.md](../specs/2026-05-15-v1.26-comments-images-drawing-design.md) §3

---

## File Structure

| 파일 | 역할 |
|---|---|
| `src/services/supabaseService.ts` | `waitForSceneUuid()` 헬퍼 신규 + 기존 `resolveSceneUuidBySceneId` 활용 |
| `src/views/ScenesView.tsx` | `handleAddScene` 내 이미지 업로드 IIFE 수정 (UUID 폴링 + 직접 IPC) |
| `src/utils/bulkOperations.ts` | `countSelectedScenes()` 헬퍼 신규 |
| `src/views/ScenesView.tsx` | `handleBulkDelete` 등 confirm 메시지 갱신 |
| `tests/waitForSceneUuid.test.ts` | **신규** — UUID 폴링 동작 단위 테스트 |
| `tests/countSelectedScenes.test.ts` | **신규** — 머지드 단위 카운트 단위 테스트 |
| `DEVLOG/update-notes.json` | v1.25.12 항목 추가 |
| `package.json` | 버전 bump |

---

## Chunk 1: 버그 1 — 씬 추가 시 이미지 첨부 UUID race

### Task 1.1: `waitForSceneUuid` 단위 테스트

**Files:**
- Create: `tests/waitForSceneUuid.test.ts`

- [ ] **Step 1: 테스트 파일 작성 (실패하는 상태)**

```ts
import test from 'node:test';
import assert from 'node:assert/strict';

// Note: waitForSceneUuid는 useDataStore.getState() 를 폴링하므로
// 테스트에선 store mock을 통해 검증한다.
// useDataStore 자체가 zustand 라 SSR-safe 모듈이라 import 가능하지만,
// 여기선 폴링 로직만 분리해 검증할 수 있도록 inner helper 를 export 한다.

import { waitForSceneUuidWithStore } from '../src/services/supabaseService.ts';

type FakeScene = { sceneId: string; id?: string };
type FakePart = { sheetName: string; scenes: FakeScene[] };
type FakeStore = { episodes: { parts: FakePart[] }[] };

function makeStoreGetter(store: FakeStore): () => FakeStore {
  return () => store;
}

test('waitForSceneUuid: UUID가 이미 있으면 즉시 반환', async () => {
  const store: FakeStore = {
    episodes: [{ parts: [{ sheetName: 'EP01_A_BG', scenes: [{ sceneId: 'a001', id: 'uuid-1' }] }] }],
  };
  const uuid = await waitForSceneUuidWithStore(makeStoreGetter(store), 'EP01_A_BG', 'a001', 1000);
  assert.equal(uuid, 'uuid-1');
});

test('waitForSceneUuid: UUID가 늦게 들어오면 폴링 후 반환', async () => {
  const store: FakeStore = {
    episodes: [{ parts: [{ sheetName: 'EP01_A_BG', scenes: [{ sceneId: 'a001' }] }] }],
  };
  const getter = makeStoreGetter(store);
  // 200ms 후에 UUID 채움
  setTimeout(() => { store.episodes[0].parts[0].scenes[0].id = 'uuid-late'; }, 200);
  const start = Date.now();
  const uuid = await waitForSceneUuidWithStore(getter, 'EP01_A_BG', 'a001', 2000);
  const elapsed = Date.now() - start;
  assert.equal(uuid, 'uuid-late');
  assert.ok(elapsed >= 150 && elapsed < 600, `폴링 시간이 비정상: ${elapsed}ms`);
});

test('waitForSceneUuid: 타임아웃 시 null 반환', async () => {
  const store: FakeStore = {
    episodes: [{ parts: [{ sheetName: 'EP01_A_BG', scenes: [{ sceneId: 'a001' }] }] }],
  };
  const uuid = await waitForSceneUuidWithStore(makeStoreGetter(store), 'EP01_A_BG', 'a001', 300);
  assert.equal(uuid, null);
});

test('waitForSceneUuid: 씬이 아예 없으면 폴링하다 타임아웃', async () => {
  const store: FakeStore = {
    episodes: [{ parts: [{ sheetName: 'EP01_A_BG', scenes: [] }] }],
  };
  const uuid = await waitForSceneUuidWithStore(makeStoreGetter(store), 'EP01_A_BG', 'a001', 200);
  assert.equal(uuid, null);
});

test('waitForSceneUuid: sheetName 매치 실패 시 폴링하다 타임아웃', async () => {
  const store: FakeStore = {
    episodes: [{ parts: [{ sheetName: 'EP01_A_BG', scenes: [{ sceneId: 'a001', id: 'uuid-1' }] }] }],
  };
  const uuid = await waitForSceneUuidWithStore(makeStoreGetter(store), 'EP01_A_ACT', 'a001', 200);
  assert.equal(uuid, null);
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `node --test tests/waitForSceneUuid.test.ts`
Expected: FAIL — `waitForSceneUuidWithStore` export가 없음.

### Task 1.2: `waitForSceneUuid` 헬퍼 구현

**Files:**
- Modify: `src/services/supabaseService.ts` (resolveSceneUuid 근처)

- [ ] **Step 1: `waitForSceneUuidWithStore`와 `waitForSceneUuid` 추가**

`src/services/supabaseService.ts`에 추가 (기존 `resolveSceneUuidBySceneId` 바로 아래):

```ts
/**
 * 옵티미스틱 추가된 씬은 store에 sceneId만 있고 id(UUID)는 비어있을 수 있다.
 * addScene IPC 가 끝난 후 syncInBackground 로 UUID 가 채워지는데,
 * 그 사이에 이미지 업로드 같은 후속 작업이 sceneIndex 기반 UUID 해석을 하면
 * `resolveSceneUuid` 가 throw 한다 (v1.25.11 까지의 버그).
 *
 * 이 헬퍼는 store 를 100ms 간격으로 폴링해 UUID 가 채워질 때까지 기다린다.
 * 타임아웃이면 null 을 반환해 호출자가 사용자에게 명확한 토스트를 띄울 수 있게 한다.
 *
 * Inner form (테스트용) — store getter 를 주입받는다.
 */
export async function waitForSceneUuidWithStore(
  getStore: () => { episodes: { parts: { sheetName: string; scenes: { sceneId: string; id?: string }[] }[] }[] },
  sheetName: string,
  sceneId: string,
  timeoutMs: number = 2000,
): Promise<string | null> {
  const intervalMs = 100;
  const deadline = Date.now() + timeoutMs;
  while (Date.now() < deadline) {
    const store = getStore();
    const part = store.episodes.flatMap((ep) => ep.parts).find((p) => p.sheetName === sheetName);
    const scene = part?.scenes.find((s) => s.sceneId === sceneId);
    if (scene?.id) return scene.id;
    await new Promise((resolve) => setTimeout(resolve, intervalMs));
  }
  return null;
}

/** 실 사용 형태 — useDataStore를 자동으로 주입. */
export async function waitForSceneUuid(
  sheetName: string,
  sceneId: string,
  timeoutMs: number = 2000,
): Promise<string | null> {
  return waitForSceneUuidWithStore(() => useDataStore.getState(), sheetName, sceneId, timeoutMs);
}
```

- [ ] **Step 2: 테스트 실행 (통과 확인)**

Run: `node --test tests/waitForSceneUuid.test.ts`
Expected: PASS (5/5 테스트)

- [ ] **Step 3: 타입체크**

Run: `npm run typecheck`
Expected: 오류 없음.

- [ ] **Step 4: 커밋**

```bash
git add tests/waitForSceneUuid.test.ts src/services/supabaseService.ts
git commit -m "feat(supabaseService): waitForSceneUuid 폴링 헬퍼 추가 — UUID race 대비"
```

### Task 1.3: `handleAddScene` 이미지 업로드 IIFE 수정

**Files:**
- Modify: `src/views/ScenesView.tsx:3053-3076` (통합 모드), `src/views/ScenesView.tsx:3115-3137` (개별 모드)

- [ ] **Step 1: 통합 모드 IIFE 수정**

기존 ([src/views/ScenesView.tsx:3053-3076](../../../src/views/ScenesView.tsx:3053)):
```ts
if (images?.storyboard || images?.guide) {
  (async () => {
    try {
      const { saveImage } = await import('@/utils/imageUtils');
      for (const sheet of sheets) {
        const latestPart2 = useDataStore.getState().episodes
          .flatMap((ep) => ep.parts)
          .find((p) => p.sheetName === sheet);
        const latestIndex = latestPart2?.scenes.findIndex((s) => s.sceneId === sceneId) ?? -1;
        if (latestIndex < 0) continue;
        if (images.storyboard) {
          const url = await saveImage(images.storyboard, sheet, sceneId, 'storyboard');
          handleFieldUpdateForSheet(sheet, latestIndex, 'storyboardUrl', url);
        }
        if (images.guide) {
          const url = await saveImage(images.guide, sheet, sceneId, 'guide');
          handleFieldUpdateForSheet(sheet, latestIndex, 'guideUrl', url);
        }
      }
    } catch (err) {
      console.error('[씬 추가 이미지 업로드 실패]', err);
    }
  })();
}
```

로 다음으로 교체:
```ts
if (images?.storyboard || images?.guide) {
  (async () => {
    const { saveImage } = await import('@/utils/imageUtils');
    const { waitForSceneUuid } = await import('@/services/supabaseService');
    for (const sheet of sheets) {
      const uuid = await waitForSceneUuid(sheet, sceneId);
      if (!uuid) {
        sonnerToast.error(`이미지 업로드 동기화 실패: ${sceneId}. 새로고침 후 다시 시도해 주세요.`);
        continue;
      }
      try {
        if (images.storyboard) {
          const url = await saveImage(images.storyboard, sheet, sceneId, 'storyboard');
          await window.electronAPI?.supabaseUpdateSceneField?.(uuid, 'storyboardUrl', url);
          useDataStore.getState().updateSceneByUuid(uuid, { storyboardUrl: url });
        }
        if (images.guide) {
          const url = await saveImage(images.guide, sheet, sceneId, 'guide');
          await window.electronAPI?.supabaseUpdateSceneField?.(uuid, 'guideUrl', url);
          useDataStore.getState().updateSceneByUuid(uuid, { guideUrl: url });
        }
      } catch (err) {
        console.error('[씬 추가 이미지 업로드 실패]', err);
        sonnerToast.error(`이미지 업로드 실패: ${sceneId}`);
      }
    }
  })();
}
```

- [ ] **Step 2: 개별 모드 IIFE 동일 패턴으로 수정**

기존 [src/views/ScenesView.tsx:3115-3137](../../../src/views/ScenesView.tsx:3115)도 동일하게 교체. `targetSheet` 변수명만 바뀜:

```ts
if (images?.storyboard || images?.guide) {
  (async () => {
    const { saveImage } = await import('@/utils/imageUtils');
    const { waitForSceneUuid } = await import('@/services/supabaseService');
    const uuid = await waitForSceneUuid(targetSheet, sceneId);
    if (!uuid) {
      sonnerToast.error(`이미지 업로드 동기화 실패: ${sceneId}. 새로고침 후 다시 시도해 주세요.`);
      return;
    }
    try {
      if (images.storyboard) {
        const url = await saveImage(images.storyboard, targetSheet, sceneId, 'storyboard');
        await window.electronAPI?.supabaseUpdateSceneField?.(uuid, 'storyboardUrl', url);
        useDataStore.getState().updateSceneByUuid(uuid, { storyboardUrl: url });
      }
      if (images.guide) {
        const url = await saveImage(images.guide, targetSheet, sceneId, 'guide');
        await window.electronAPI?.supabaseUpdateSceneField?.(uuid, 'guideUrl', url);
        useDataStore.getState().updateSceneByUuid(uuid, { guideUrl: url });
      }
    } catch (err) {
      console.error('[씬 추가 이미지 업로드 실패]', err);
      sonnerToast.error(`이미지 업로드 실패: ${sceneId}`);
    }
  })();
}
```

- [ ] **Step 3: 타입체크**

Run: `npm run typecheck`
Expected: 오류 없음.

- [ ] **Step 4: 빌드 점검**

Run: `npm run build:vite`
Expected: 빌드 성공.

- [ ] **Step 5: 커밋**

```bash
git add src/views/ScenesView.tsx
git commit -m "fix(ScenesView): 씬 추가 시 이미지 업로드 UUID race 수정 — 폴링 후 직접 IPC"
```

### Task 1.4: 수동 검증 (Electron dev)

- [ ] **Step 1: 개발 모드 실행**

Run: `npm run electron:dev`
Expected: 앱 실행.

- [ ] **Step 2: 시나리오 5종 수행** (체크박스로 추적)

- [ ] 통합 모드 + 스토리보드만 붙여넣고 씬 추가 → 토스트 1개("씬 추가 완료"), 이미지 정상
- [ ] 통합 모드 + 가이드만 붙여넣고 씬 추가 → 동일
- [ ] 통합 모드 + 둘 다 첨부 → 동일
- [ ] BG 단독 모드 + 첨부 → 동일
- [ ] 액팅 단독 모드 + 첨부 → 동일

문제 발생 시 console.error 확인 후 fix.

---

## Chunk 2: 버그 2 — 벌크 삭제 카운트 표기

### Task 2.1: `countSelectedScenes` 단위 테스트

**Files:**
- Create: `tests/countSelectedScenes.test.ts`

- [ ] **Step 1: 테스트 작성 (실패하는 상태)**

```ts
import test from 'node:test';
import assert from 'node:assert/strict';
import { countSelectedScenes } from '../src/utils/bulkOperations.ts';
import type { MergedScene, Part } from '../src/types';

function makeMerged(mergedKey: string, hasBg: boolean, hasAct: boolean): MergedScene {
  return {
    sceneId: mergedKey.split('|')[1] ?? mergedKey,
    mergedKey,
    bgScene: hasBg ? { id: `bg-${mergedKey}`, sceneId: mergedKey.split('|')[1] ?? mergedKey, no: 1, memo: '', storyboardUrl: '', guideUrl: '', assignee: '', layoutId: '', lo: false, done: false, review: false, png: false } : undefined,
    actScene: hasAct ? { id: `act-${mergedKey}`, sceneId: mergedKey.split('|')[1] ?? mergedKey, no: 1, memo: '', storyboardUrl: '', guideUrl: '', assignee: '', layoutId: '', lo: false, done: false, review: false, png: false } : undefined,
    bgSceneIndex: hasBg ? 0 : -1,
    actSceneIndex: hasAct ? 0 : -1,
  } as MergedScene;
}

test('통합 모드 — 카드 1장 (bg + act 둘 다 선택) = 1개', () => {
  const merged = [makeMerged('A|a001', true, true)];
  const selected = new Set(['bg:A|a001', 'act:A|a001']);
  assert.equal(countSelectedScenes(selected, merged, null), 1);
});

test('통합 모드 — 카드 3장 (각 머지드 키별 bg+act) = 3개', () => {
  const merged = [
    makeMerged('A|a001', true, true),
    makeMerged('A|a002', true, true),
    makeMerged('A|a003', true, true),
  ];
  const selected = new Set([
    'bg:A|a001', 'act:A|a001',
    'bg:A|a002', 'act:A|a002',
    'bg:A|a003', 'act:A|a003',
  ]);
  assert.equal(countSelectedScenes(selected, merged, null), 3);
});

test('통합 모드 — bg 만 가진 머지드 카드 1장 = 1개', () => {
  const merged = [makeMerged('A|a001', true, false)];
  const selected = new Set(['bg:A|a001']);
  assert.equal(countSelectedScenes(selected, merged, null), 1);
});

test('개별 모드 — plain sceneId 3개 = 3개', () => {
  const part: Part = { partId: 'A', department: 'bg', sheetName: 'EP01_A_BG', scenes: [
    { id: 'u1', sceneId: 'a001', no: 1, memo: '', storyboardUrl: '', guideUrl: '', assignee: '', layoutId: '', lo: false, done: false, review: false, png: false },
    { id: 'u2', sceneId: 'a002', no: 2, memo: '', storyboardUrl: '', guideUrl: '', assignee: '', layoutId: '', lo: false, done: false, review: false, png: false },
    { id: 'u3', sceneId: 'a003', no: 3, memo: '', storyboardUrl: '', guideUrl: '', assignee: '', layoutId: '', lo: false, done: false, review: false, png: false },
  ] };
  const selected = new Set(['a001', 'a002', 'a003']);
  assert.equal(countSelectedScenes(selected, [], part), 3);
});

test('빈 selection = 0', () => {
  assert.equal(countSelectedScenes(new Set(), [], null), 0);
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `node --test tests/countSelectedScenes.test.ts`
Expected: FAIL — `countSelectedScenes` export 없음.

### Task 2.2: `countSelectedScenes` 구현

**Files:**
- Modify: `src/utils/bulkOperations.ts`

- [ ] **Step 1: 함수 추가**

`src/utils/bulkOperations.ts`에 `resolveSelectedUuids` 위 또는 아래에 추가:

```ts
/**
 * 사용자 눈에 보이는 단위(머지드 카드)로 선택 항목 카운트.
 * 통합 모드에서 한 카드(머지드 키)는 1로 센다 — bg:KEY 와 act:KEY 둘 다 들어있어도 1.
 * 개별 모드(plain sceneId)는 unique sceneId 카운트.
 *
 * 사용처: ConfirmDialog 문구의 N (예: "씬 N개를 삭제하시겠습니까?")
 */
export function countSelectedScenes(
  selectedIds: Set<string> | Iterable<string>,
  allMergedScenes: MergedScene[],
  fallbackPart?: Part | null,
): number {
  const mergedKeys = new Set<string>();
  const plainSceneIds = new Set<string>();
  for (const id of selectedIds) {
    if (id.startsWith(MERGED_KEY_PREFIX.bg)) {
      mergedKeys.add(id.slice(MERGED_KEY_PREFIX.bg.length));
    } else if (id.startsWith(MERGED_KEY_PREFIX.act)) {
      mergedKeys.add(id.slice(MERGED_KEY_PREFIX.act.length));
    } else {
      plainSceneIds.add(id);
    }
  }
  return mergedKeys.size + plainSceneIds.size;
}
```

- [ ] **Step 2: 테스트 실행 (통과 확인)**

Run: `node --test tests/countSelectedScenes.test.ts`
Expected: PASS (5/5)

- [ ] **Step 3: 타입체크**

Run: `npm run typecheck`
Expected: 오류 없음.

- [ ] **Step 4: 커밋**

```bash
git add tests/countSelectedScenes.test.ts src/utils/bulkOperations.ts
git commit -m "feat(bulkOperations): countSelectedScenes — 머지드 단위 카운트 헬퍼"
```

### Task 2.3: `handleBulkDelete` 문구 갱신

**Files:**
- Modify: `src/views/ScenesView.tsx:2730-2748`

- [ ] **Step 1: import 추가**

`src/views/ScenesView.tsx` 상단의 bulkOperations import에 추가:
```ts
import { ..., countSelectedScenes } from '@/utils/bulkOperations';
```

(이미 `resolveSelectedUuids`, `resolveSelectedScenes` 가 import 되어 있으므로 같은 줄에 추가)

- [ ] **Step 2: handleBulkDelete 수정**

기존:
```ts
const handleBulkDelete = async () => {
  const uuids = resolveSelectedUuids(selectedSceneIds, allMergedScenes, currentPart);
  if (uuids.length === 0) return;

  const ok = await ConfirmDialog.show({
    message: `${uuids.length}개의 씬을 삭제하시겠습니까?`,
    confirmLabel: '삭제',
    tone: 'danger',
  });
  ...
};
```

를 다음으로 교체:
```ts
const handleBulkDelete = async () => {
  const uuids = resolveSelectedUuids(selectedSceneIds, allMergedScenes, currentPart);
  if (uuids.length === 0) return;

  const sceneCount = countSelectedScenes(selectedSceneIds, allMergedScenes, currentPart);
  const isUnified = selectedDepartment === 'all';
  const suffix = isUnified ? ' (BG·액팅 양쪽 적용)' : '';

  const ok = await ConfirmDialog.show({
    message: `씬 ${sceneCount}개를 삭제하시겠습니까?${suffix}`,
    confirmLabel: '삭제',
    tone: 'danger',
  });
  if (!ok) return;

  await runBulkOp(
    'delete',
    uuids,
    (list) => bulkDeleteScenes(list, currentUser?.id ?? ''),
  );

  clearSelectedScenes();
};
```

- [ ] **Step 3: 타입체크 + 빌드**

Run: `npm run typecheck && npm run build:vite`
Expected: 성공.

- [ ] **Step 4: 커밋**

```bash
git add src/views/ScenesView.tsx
git commit -m "fix(ScenesView): 벌크 삭제 confirm 카운트 — 머지드 단위로 정정"
```

### Task 2.4: 수동 검증

- [ ] **Step 1: dev 실행 후 시나리오 5종**

- [ ] 통합 뷰 카드 1장 선택 → "씬 1개를 삭제하시겠습니까? (BG·액팅 양쪽 적용)"
- [ ] 통합 뷰 카드 3장 선택 → "씬 3개를 삭제하시겠습니까? (BG·액팅 양쪽 적용)"
- [ ] BG 단독 뷰 카드 2장 → "씬 2개를 삭제하시겠습니까?"
- [ ] 액팅 단독 뷰 카드 1장 → "씬 1개를 삭제하시겠습니까?"
- [ ] 통합 뷰 + 한쪽만 있는 머지드 1장 → "씬 1개를 삭제하시겠습니까? (BG·액팅 양쪽 적용)" — 실제 동작은 1 row만 삭제

---

## Chunk 3: 마무리 — 버전 bump + update-notes + PR

### Task 3.1: 버전 / update-notes 갱신

**Files:**
- Modify: `package.json` (version)
- Modify: `DEVLOG/update-notes.json` (새 항목)

- [ ] **Step 1: package.json 버전 1.25.11 → 1.25.12**

`package.json`의 `"version": "1.25.11"` → `"version": "1.25.12"`.

- [ ] **Step 2: update-notes.json 항목 추가**

`DEVLOG/update-notes.json` 최상단(최신순)에 추가:

```json
{
  "version": "1.25.12",
  "date": "2026-05-15",
  "category": "bugfix",
  "summary": "씬 추가할 때 이미지 같이 넣으면 가끔 빈 칸으로 들어가던 문제를 잡았어요. 그리고 통합 화면에서 씬 1개 지울 때 '2개 지운다'고 잘못 뜨던 안내문도 정리했어요.",
  "description": "스토리보드나 가이드 이미지를 붙여넣고 씬을 추가하면 잠깐 사이에 이미지가 빈 채로 들어가는 일이 있었는데, 이제 이미지 업로드가 끝날 때까지 안정적으로 기다렸다가 한 번에 깔끔하게 들어갑니다. 또 통합 뷰에서 씬 카드 1장을 골라 지우려고 했을 때 안내창이 '2개 지운다'고 잘못 뜨던 부분도 사용자가 보는 단위(1개)로 정확하게 보여드리도록 바꿨어요. 내부적으로는 여전히 BG와 액팅 양쪽이 함께 정리됩니다."
}
```

(기존 v1.25.11 항목 위에. 비개발자 톤 — 식별자/파일경로/기술용어 노출 금지.)

- [ ] **Step 3: 커밋**

```bash
git add package.json DEVLOG/update-notes.json
git commit -m "[v1.25.12] 씬 추가 이미지 race + 벌크 삭제 카운트 표기 — update-notes 갱신"
```

### Task 3.2: 전체 빌드 검증

- [ ] **Step 1: typecheck + 모든 테스트 + build:vite**

Run: `npm run typecheck && npm run test:auto-update && node --test tests/waitForSceneUuid.test.ts tests/countSelectedScenes.test.ts && npm run build:vite`
Expected: 모두 통과.

- [ ] **Step 2: 최종 dev 모드 + 양쪽 버그 회귀 시나리오 5+5종 재확인**

(Task 1.4, 2.4의 체크리스트 다시 한 번 실행)

### Task 3.3: PR 생성 (한솔 명시 시에만)

- [ ] **Step 1: 사용자 확인 후 진행**

한솔이 PR 생성을 요청하면 superpowers:pr-creator 또는 anthropic-skills:pr-creator 스킬 사용.
**한솔 명시 전엔 PR 생성 X** (memory 규칙 `feedback_pr_merge_gate.md`).

---

## 완료 기준

- [x] (가짜 체크 — 위 모든 task 완료 시 self-check)
- 양쪽 버그 시나리오 100% 통과
- `npm run typecheck` 통과
- `node --test tests/waitForSceneUuid.test.ts tests/countSelectedScenes.test.ts` 통과
- `npm run build:vite` 통과
- update-notes.json 톤이 비개발자 친화적 (CLAUDE.md 룰 + memory `feedback_update_notes_tone.md` 준수)
- 자동 G드라이브 배포 X (한솔 명시 시에만 — memory `feedback_deploy_gate.md`)
- 자동 PR 머지 X (한솔 명시 시에만)
